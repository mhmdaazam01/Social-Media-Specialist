<!DOCTYPE html>
<html lang="id" data-theme="dark" >
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Creatorlytics — Social Media Analytics Dashboard</title>
<meta name="description" content="Track performa konten TikTok & Instagram, analisa engagement rate, buat laporan profesional, dan kelola content calendar dalam satu dashboard.">
<meta name="theme-color" content="#0b0e17">
<meta property="og:title" content="Creatorlytics — Social Media Analytics">
<meta property="og:description" content="Dashboard analytics untuk content creator & social media manager. Track reach, ER, goals, dan laporan PDF.">
<meta property="og:type" content="website">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="Creatorlytics">
<meta name="twitter:description" content="Social media analytics dashboard untuk kreator konten Indonesia.">
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 32 32'><rect width='32' height='32' rx='8' fill='%230b0e17'/><path d='M6 22l5-6 4 3 5-7 6 5' stroke='%233dc7b7' stroke-width='2.5' stroke-linecap='round' stroke-linejoin='round' fill='none'/></svg>">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script>
/* ── Supabase sync stub (standalone/local mode) ──
   File ini menggantikan supabase-sync.js agar app jalan
   tanpa server. Semua data tersimpan di localStorage saja. */
window.initSupabaseSync = null;
window.saveToSupabase   = null;
window.supabaseReady    = false;
try{console.log('[Creatorlytics] Local mode — localStorage only.');}catch(e){}
</script>
<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Sora:wght@700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
<style>
:root{
  --font:'Manrope',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
  --font-display:'Sora',-apple-system,sans-serif;
  --mono:'JetBrains Mono',monospace;
  --nav-w:260px;
  --topbar-h:56px;
  --radius:8px;
  --radius-sm:6px;
  --radius-lg:16px;
  --radius-xl:20px;
  --radius-full:9999px;
  --accent:#0e9f8f;
  --accent-2:#ff7a59;
  --accent-dim:rgba(14,159,143,0.14);
  --accent-hover:#0bbdaa;
  /* Light mode base — active when data-theme is NOT dark */
  --bg:#f0f4f8;
  --bg-page:#e8edf4;
  --bg-hover:rgba(0,0,0,0.045);
  --bg-active:rgba(14,159,143,0.10);
  --surf:#ffffff;
  --surf2:#f4f7fb;
  --b:#dde3ef;
  --b2:rgba(60,80,120,0.18);
  --t:#111827;
  --t2:#374151;
  --t3:#6b7280;
  --t4:#9ca3af;
  --green:#15a854;--green-bg:rgba(21,168,84,0.10);--green-t:#0d7a3f;
  --amber:#c97c1a;--amber-bg:rgba(201,124,26,0.10);--amber-t:#9a5e0a;
  --red:#dc2626;--red-bg:rgba(220,38,38,0.10);--red-t:#b91c1c;
  --blue:#2563eb;--blue-bg:rgba(37,99,235,0.10);--blue-t:#1d4ed8;
  --purple:#7c3aed;--purple-bg:rgba(124,58,237,0.10);--purple-t:#6d28d9;
  --tt:#0d8a7d;--tt-bg:rgba(13,138,125,0.10);--tt-light:rgba(13,138,125,0.16);
  --ig:#c96b10;--ig-bg:rgba(201,107,16,0.10);--ig-light:rgba(201,107,16,0.16);
  --shadow-sm:0 1px 4px rgba(0,0,0,0.08);
  --shadow:0 4px 16px rgba(0,0,0,0.10);
  --shadow-md:0 8px 24px rgba(0,0,0,0.12);
  --shadow-lg:0 16px 48px rgba(0,0,0,0.15);
  --sidebar-bg:#ffffff;
  --sidebar-t:rgba(55,65,81,0.75);
  --sidebar-t-act:#111827;
  --sidebar-hover:rgba(14,159,143,0.07);
  --sidebar-active:rgba(14,159,143,0.12);
  --sidebar-b:#e5e9f0;
  --sidebar-accent:#0e9f8f;
  --nav-accent:#0e9f8f;
  --nav-accent-bg:rgba(14,159,143,0.12);
  --btn-primary:#0e9f8f;
  --btn-primary-hover:#0bbdaa;
  --sp-black:#e8edf4;
  --sp-card:#ffffff;
  --sp-elevated:#f4f7fb;
  --sp-highlight:#eaf0f8;
}

[data-theme="dark"]{
  --bg:#0d1117;
  --bg-page:#0a0d16;
  --bg-hover:rgba(255,255,255,0.055);
  --bg-active:rgba(61,199,183,0.12);
  --surf:rgba(17,21,35,0.92);
  --surf2:#141927;
  --b:#252d42;
  --b2:rgba(148,163,184,0.30);
  --t:#eef0ff;
  --t2:#9ea8c8;
  --t3:#6b7594;
  --t4:#3d4663;
  --green:#27d17f;--green-bg:rgba(39,209,127,0.12);--green-t:#7de8b1;
  --amber:#ffb06b;--amber-bg:rgba(255,176,107,0.14);--amber-t:#ffd09a;
  --red:#ff6b6b;--red-bg:rgba(255,107,107,0.14);--red-t:#ffaaaa;
  --blue:#6c8cff;--blue-bg:rgba(108,140,255,0.14);--blue-t:#aebfff;
  --purple:#a07cdf;--purple-bg:rgba(160,124,223,0.14);--purple-t:#ccaaff;
  --tt:#3dc7b7;--tt-bg:rgba(61,199,183,0.14);--tt-light:rgba(61,199,183,0.22);
  --ig:#ffb06b;--ig-bg:rgba(255,176,107,0.14);--ig-light:rgba(255,176,107,0.22);
  --shadow-sm:0 2px 8px rgba(0,5,20,0.55);
  --shadow:0 8px 28px rgba(0,5,20,0.65);
  --shadow-md:0 12px 36px rgba(0,5,20,0.70);
  --shadow-lg:0 24px 72px rgba(0,5,20,0.80);
  --sidebar-bg:rgba(9,12,22,0.97);
  --sidebar-t:rgba(158,168,200,0.68);
  --sidebar-t-act:#eef0ff;
  --sidebar-hover:rgba(61,199,183,0.07);
  --sidebar-active:rgba(61,199,183,0.14);
  --sidebar-b:#1e2438;
  --sidebar-accent:#3dc7b7;
  --nav-accent:#3dc7b7;
  --nav-accent-bg:rgba(61,199,183,0.14);
  --btn-primary:#0e9f8f;
  --btn-primary-hover:#0bbdaa;
  --sp-black:#0a0d16;
  --sp-card:rgba(17,21,35,0.92);
  --sp-elevated:#161c2e;
  --sp-highlight:#1e2540;
}

/* ─ BASE ─ */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html,body{height:100%;overflow:hidden;}
body{font-family:var(--font);font-size:14px;color:var(--t);background:var(--bg-page);display:flex;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;letter-spacing:-.01em;position:relative;}
/* ─ BLOB BACKGROUNDS (on body so visible behind sidebar too) ─ */
html{
  background:var(--bg-page);
}
html::before{
  content:'';position:fixed;width:600px;height:600px;
  background:radial-gradient(circle,rgba(61,199,183,0.38),transparent 65%);
  right:-120px;top:-160px;z-index:0;border-radius:50%;filter:blur(45px);pointer-events:none;
}
html::after{
  content:'';position:fixed;width:680px;height:680px;
  background:radial-gradient(circle,rgba(255,176,107,0.32),transparent 65%);
  left:-140px;bottom:-180px;z-index:0;border-radius:50%;filter:blur(50px);pointer-events:none;
}
[data-theme="light"] html::before{background:radial-gradient(circle,rgba(14,159,143,0.14),transparent 65%);}
[data-theme="light"] html::after{background:radial-gradient(circle,rgba(255,122,89,0.10),transparent 65%);}


.app{position:relative;z-index:1;}
::-webkit-scrollbar{width:4px;height:4px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:rgba(61,199,183,0.35);border-radius:8px;}
::-webkit-scrollbar-thumb:hover{background:var(--accent);}
input,select,button{font-family:var(--font);font-size:max(16px,1em);}
input,textarea,select{font-size:max(16px,1em)!important;}
button,[role="button"]{min-height:44px;}
select{background:var(--bg);color:var(--t);color-scheme:light;}
select option{background:#FFFFFF;color:#121212;}
[data-theme="dark"] select{background:#1C1C2E;color:#F0F0FF;color-scheme:dark;}
[data-theme="dark"] select option{background:#1C1C2E;color:#F0F0FF;}
[data-theme="dark"] select optgroup{background:#161624;color:#9090B0;}
input[type=number]::-webkit-inner-spin-button{opacity:0;}
button{cursor:pointer;}
a{color:var(--accent);text-decoration:none;}

/* ─ APP SHELL ─ */
.app{display:flex;height:100%;width:100%;overflow:hidden;}

/* ─ NAV SIDEBAR ─ */
.nav-sidebar{width:var(--nav-w);flex-shrink:0;background:var(--sidebar-bg);backdrop-filter:blur(12px);display:flex;flex-direction:column;overflow:hidden;border-right:1px solid var(--sidebar-b);}
.nav-brand{padding:20px 16px 16px;display:flex;align-items:center;gap:11px;flex-shrink:0;border-bottom:1px solid var(--sidebar-b);margin-bottom:6px;}
.brand-mark{width:36px;height:36px;border-radius:12px;flex-shrink:0;background:linear-gradient(140deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;box-shadow:0 4px 12px rgba(14,159,143,0.35);}
.brand-text{font-family:var(--font-display);font-size:15px;font-weight:900;color:var(--sidebar-t-act);letter-spacing:-.04em;line-height:1;}
.brand-sub{font-size:11px;color:var(--sidebar-t);font-weight:700;letter-spacing:.12em;text-transform:uppercase;margin-top:4px;}
.nav-section{padding:12px 14px 4px;font-size:11px;font-weight:800;color:var(--sidebar-t);letter-spacing:.14em;text-transform:uppercase;}
/* ── Workspace Switcher ── */
.ws-switcher{margin:8px 8px 4px;padding:9px 12px;border-radius:10px;border:1px solid var(--sidebar-b);background:rgba(255,255,255,0.03);display:flex;align-items:center;gap:8px;cursor:pointer;transition:background .15s;}
.ws-switcher:hover{background:rgba(255,255,255,0.06);}


.ws-dot{width:8px;height:8px;border-radius:50%;background:var(--accent);flex-shrink:0;box-shadow:0 0 6px var(--accent);}
/* ── Kanban Card ── */
.kanban-card{background:var(--surf);border:1px solid var(--b);border-radius:10px;padding:12px 13px;cursor:pointer;transition:border-color .15s,transform .15s,box-shadow .15s;}
.kanban-card:hover{border-color:var(--accent);transform:translateY(-2px);box-shadow:0 6px 20px rgba(0,0,0,.15);}

/* ── Mobile fixes for new modals ── */
@media(max-width:600px){
  .ws-switcher{margin:6px 6px 2px;}
  #m-csv .modal,#m-rpt-cfg .modal,#m-competitor .modal,#m-quickadd .modal{max-width:100vw!important;margin:0;border-radius:16px 16px 0 0;position:fixed;bottom:0;left:0;right:0;max-height:90vh;overflow-y:auto;}
  #m-quickadd .modal > div[style*="grid-template-columns"]{grid-template-columns:1fr!important;}
  #m-competitor #comp-manual-data > div{grid-template-columns:1fr!important;}
  .kanban-card{padding:10px 11px;}
}
.ws-name{flex:1;font-size:12px;font-weight:700;color:var(--sidebar-t-act);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.ws-badge{font-size:11px;font-weight:800;padding:2px 6px;border-radius:20px;background:rgba(255,176,107,0.14);color:#ffb06b;letter-spacing:.04em;flex-shrink:0;}
/* ── CSV Import Modal ── */
.csv-drop-area{border:2px dashed var(--b2);border-radius:12px;padding:32px 20px;text-align:center;transition:border-color .15s,background .15s;cursor:pointer;}
.csv-drop-area:hover,.csv-drop-area.drag-over{border-color:var(--accent);background:var(--accent-dim);}
.csv-drop-icon{font-size:32px;margin-bottom:8px;opacity:.5;}
.csv-tab{padding:6px 14px;border-radius:8px;border:none;background:none;color:var(--t3);font-size:12px;font-weight:700;cursor:pointer;transition:all .15s;}
.csv-tab.act{background:var(--bg-active);color:var(--accent);}
.csv-preview-table{width:100%;border-collapse:collapse;font-size:11px;}
.csv-preview-table th{background:var(--surf2);padding:6px 8px;text-align:left;font-weight:700;color:var(--t3);border-bottom:1px solid var(--b);}
.csv-preview-table td{padding:5px 8px;border-bottom:1px solid rgba(255,255,255,.04);color:var(--t2);}

/* ── Report Config Modal ── */
.rpt-cfg-metric{display:flex;align-items:center;gap:10px;padding:10px 12px;border-radius:9px;border:1px solid var(--b);background:var(--surf);margin-bottom:6px;cursor:pointer;transition:all .15s;}
.rpt-cfg-metric:hover{border-color:var(--accent);background:var(--bg-active);}
.rpt-cfg-metric.selected{border-color:var(--accent);background:var(--accent-dim);}
.rpt-cfg-check{width:16px;height:16px;border-radius:4px;border:1.5px solid var(--b2);flex-shrink:0;display:flex;align-items:center;justify-content:center;transition:all .15s;}
.rpt-cfg-metric.selected .rpt-cfg-check{background:var(--accent);border-color:var(--accent);}
/* ── Competitor view ── */
.comp-card{background:var(--surf);border:1px solid var(--b);border-radius:var(--radius-lg);padding:16px 18px;margin-bottom:10px;}
.comp-card-header{display:flex;align-items:center;gap:10px;margin-bottom:12px;}
.comp-vs-badge{font-size:11px;font-weight:800;padding:2px 8px;border-radius:20px;background:var(--red-bg);color:var(--red);}
/* ── FLW+ toggle ── */
.flw-toggle{font-size:10px;font-weight:700;padding:3px 9px;border-radius:6px;border:1px solid var(--b);background:none;color:var(--t3);cursor:pointer;transition:all .15s;}
.flw-toggle:hover{background:var(--bg-hover);color:var(--t2);}
.nav-body{flex:1;overflow-y:auto;padding:4px 8px 8px;}
.nav-item{display:flex;align-items:center;gap:9px;padding:9px 11px;border-radius:9px;border:none;background:none;color:var(--sidebar-t);font-size:13px;font-weight:600;width:100%;text-align:left;transition:background .15s,color .15s;cursor:pointer;margin-bottom:2px;position:relative;letter-spacing:-.01em;}
.nav-item:hover{background:var(--sidebar-hover);color:#FFFFFF;}
.nav-item.act{background:var(--nav-accent-bg);color:var(--nav-accent);font-weight:700;}
.nav-item.act::before{content:'';position:absolute;left:0;top:50%;transform:translateY(-50%);width:3px;height:55%;border-radius:0 3px 3px 0;background:var(--nav-accent);box-shadow:0 0 10px rgba(61,199,183,0.5);}
.nav-item-ico{width:18px;height:18px;flex-shrink:0;display:flex;align-items:center;justify-content:center;}
.nav-item-ico svg{display:block;}
.nav-item-badge{margin-left:auto;font-size:10px;font-family:var(--mono);font-weight:700;background:rgba(255,255,255,.08);color:rgba(255,255,255,.4);padding:2px 7px;border-radius:20px;}
.nav-divider{height:1px;background:rgba(255,255,255,0.05);margin:8px 6px;}
/* User Footer */
.nav-user-footer{flex-shrink:0;padding:10px 8px 12px;border-top:1px solid rgba(255,255,255,0.06);}
.nav-user-card{display:flex;align-items:center;gap:9px;padding:7px 8px;border-radius:9px;margin-bottom:7px;}
.nav-user-avatar{width:30px;height:30px;border-radius:50%;flex-shrink:0;background:linear-gradient(140deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:800;color:#fff;}
.nav-user-info{flex:1;min-width:0;}
.nav-user-name{font-size:12px;font-weight:700;color:var(--sidebar-t-act);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;line-height:1.2;}
.nav-user-role{font-size:10px;color:var(--sidebar-accent);font-weight:700;margin-top:1px;}
.nav-user-settings{flex-shrink:0;width:26px;height:26px;border-radius:7px;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.08);color:var(--sidebar-t);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .15s,color .15s;}
.nav-user-settings:hover{background:rgba(255,255,255,0.1);color:#fff;}
.nav-verified-badge{display:flex;align-items:center;gap:5px;font-size:10px;font-weight:700;color:var(--accent);padding:4px 10px;background:var(--accent-dim);border-radius:6px;margin-bottom:8px;}
.nav-footer-tools{display:flex;gap:4px;}
.nav-tool-btn{flex:1;height:28px;border-radius:7px;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);color:var(--sidebar-t);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .15s,color .15s;}
.nav-tool-btn:hover{background:rgba(255,255,255,0.09);color:#fff;}
/* keep old classes for compat */
.nav-footer{padding:8px 8px 14px;border-top:1px solid rgba(255,255,255,0.05);flex-shrink:0;}
.nav-footer-btn{display:flex;align-items:center;gap:9px;width:100%;padding:8px 11px;border-radius:8px;border:none;background:none;color:var(--sidebar-t);font-size:12px;font-weight:600;transition:background .15s,color .15s;text-align:left;cursor:pointer;}
.nav-footer-btn:hover{background:rgba(255,255,255,0.05);color:#fff;}
.nav-footer-btn span{display:flex;align-items:center;justify-content:center;width:15px;height:15px;flex-shrink:0;}
.nav-footer-btn span svg{display:block;}
/* Platform delete button in sidebar */
.nav-plt-row{position:relative;}
.nav-plt-del{
  display:none;width:18px;height:18px;border-radius:4px;border:none;
  background:none;color:rgba(255,255,255,.3);font-size:10px;
  align-items:center;justify-content:center;flex-shrink:0;
  transition:background .1s,color .1s;cursor:pointer;line-height:1;padding:0;
}
.nav-plt-row:hover .nav-plt-del{display:flex;}
.nav-plt-del:hover{background:rgba(255,77,77,.2);color:#FF8080 !important;}

/* ─ MAIN AREA ─ */
.main-area{flex:1;display:flex;flex-direction:column;overflow:hidden;min-width:0;background:radial-gradient(circle at 72% -8%,rgba(14,159,143,0.16),transparent 42%),radial-gradient(circle at 12% 0%,rgba(255,122,89,0.10),transparent 36%),var(--bg-page);}

/* ─ TOPBAR ─ */
.topbar{
  height:var(--topbar-h);background:transparent;
  border-bottom:1px solid var(--b);
  display:flex;align-items:center;padding:0 24px;gap:10px;flex-shrink:0;
}
.topbar-title{font-size:22px;font-weight:900;color:var(--t);flex:1;letter-spacing:-.04em;}
.topbar-sub{font-size:12px;color:var(--t3);}
.save-badge{
  font-size:11px;font-weight:700;color:var(--accent);
  background:var(--green-bg);padding:4px 12px;border-radius:var(--radius-full);
  opacity:0;transition:opacity .3s;letter-spacing:.01em;
}
.save-badge.show{opacity:1;}
.topbar-btn{
  width:36px;height:36px;border-radius:var(--radius-full);border:none;
  background:var(--surf2);color:var(--t2);font-size:16px;
  display:flex;align-items:center;justify-content:center;
  transition:background .12s,transform .1s;
}
.topbar-btn:hover{background:var(--b2);transform:scale(1.05);}

/* ─ VIEWS ─ */
.view{flex:1;overflow-y:auto;display:none;padding:24px 28px 80px;background:transparent;}
.view.active{display:block;}
.view.active{animation:fadeSlideIn .18s cubic-bezier(.16,1,.3,1);}
@keyframes fadeSlideIn{from{opacity:0;transform:translateY(6px);}to{opacity:1;transform:translateY(0);}}

/* ─ SECTION HEADERS ─ */
.section-hdr{
  display:flex;align-items:center;gap:8px;
  margin-bottom:14px;
}
.section-ttl{
  font-size:11px;font-weight:700;color:var(--t3);
  text-transform:uppercase;letter-spacing:.09em;flex:1;
}
.section-action{
  font-size:12px;color:var(--accent);border:none;background:none;
  cursor:pointer;font-weight:600;letter-spacing:-.01em;
  transition:opacity .12s;
}
.section-action:hover{opacity:.7;}

/* ─ STAT CARDS ─ */
.kpi-grid{
  display:grid;grid-template-columns:repeat(auto-fill,minmax(168px,1fr));
  gap:12px;margin-bottom:28px;
}
.kpi-card{
  position:relative;background:var(--surf);border:1px solid var(--b);
  border-radius:var(--radius-lg);padding:20px 20px 18px;
  transition:background .15s,transform .15s,box-shadow .2s;
  overflow:hidden;cursor:default;backdrop-filter:blur(8px);
}

.kpi-card::before{
  content:'';position:absolute;left:0;top:0;bottom:0;width:4px;
  background:var(--kpi-accent,var(--t4));
  border-radius:var(--radius-lg) 0 0 var(--radius-lg);
}
.kpi-card:hover{background:var(--surf2);box-shadow:var(--shadow);transform:translateY(-1px);}

.kpi-card.is-manage{padding-right:38px;}

/* ─ WEEKLY MOVEMENT PILLS ─ */
.wm-row{display:flex;gap:10px;flex-wrap:wrap;}
.wm-pill{
  display:flex;align-items:center;gap:12px;
  background:var(--surf);
  border:1px solid var(--b);
  border-radius:var(--radius-lg);padding:16px 18px;flex:1;min-width:140px;
  transition:transform .15s,box-shadow .15s;backdrop-filter:blur(8px);
}
.wm-pill:hover{transform:translateY(-2px);box-shadow:var(--shadow);}

.wm-pill-emoji{font-size:20px;flex-shrink:0;}
.wm-pill-label{font-size:10px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:3px;}
.wm-pill-val{font-size:15px;font-weight:800;font-family:var(--mono);color:var(--t);letter-spacing:-.03em;}
.wm-up{color:var(--green);font-size:11px;font-weight:700;}
.wm-down{color:var(--red);font-size:11px;font-weight:700;}
.dash-insight-empty{
  background:var(--surf);border-radius:var(--radius-lg);
  padding:32px;text-align:center;border:1px solid var(--b);backdrop-filter:blur(8px);
}

.kpi-remove{
  position:absolute;top:10px;right:10px;width:22px;height:22px;
  border:none;border-radius:var(--radius-full);background:transparent;color:var(--t4);
  display:flex;align-items:center;justify-content:center;
  font-size:13px;line-height:1;cursor:pointer;opacity:0;
  transition:opacity .12s,background .12s,color .12s;
}
.kpi-card:hover .kpi-remove{opacity:1;}
.kpi-remove:hover{background:var(--red-bg);color:var(--red);}
.kpi-icon{font-size:22px;margin-bottom:14px;display:block;}
.kpi-label{
  font-size:10px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.12em;margin-bottom:6px;
}
.kpi-value{
  font-size:30px;font-weight:900;font-family:var(--mono);
  color:var(--t);letter-spacing:-.05em;line-height:1;
}
.kpi-sub{font-size:11px;color:var(--t3);margin-top:8px;font-weight:500;}
.kpi-platform{display:flex;gap:5px;margin-top:10px;flex-wrap:wrap;}
.kpi-plt-item{
  font-size:10px;font-family:var(--mono);font-weight:700;
  padding:3px 8px;border-radius:var(--radius-full);
}
.kpi-plt-tt{background:var(--tt-bg);color:var(--tt);}
.kpi-plt-ig{background:var(--ig-bg);color:var(--ig);}

/* ─ SECTION HEADERS ─ */
.section-hdr{display:flex;align-items:center;gap:8px;margin-bottom:16px;}
.section-ttl{font-size:11px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.12em;flex:1;}
.section-action{font-size:12px;color:var(--accent);border:none;background:none;cursor:pointer;font-weight:800;transition:opacity .12s;}
.section-action:hover{opacity:.7;}

/* ─ PLATFORM COMPARISON BAR ─ */
.plt-compare{
  background:var(--surf);border:1px solid var(--b);
  border-radius:var(--radius-lg);padding:18px 20px;margin-bottom:20px;backdrop-filter:blur(8px);
}

.plt-compare-row{display:flex;align-items:center;gap:10px;margin-bottom:10px;}
.plt-compare-row:last-child{margin-bottom:0;}
.plt-compare-label{font-size:12px;font-weight:700;color:var(--t2);width:80px;flex-shrink:0;}
.plt-compare-bar-wrap{flex:1;height:5px;background:var(--sp-highlight);border-radius:var(--radius-full);overflow:hidden;}

.plt-compare-bar-fill{height:100%;border-radius:var(--radius-full);transition:width .7s cubic-bezier(.4,0,.2,1);}
.plt-compare-val{font-size:11px;font-family:var(--mono);font-weight:600;color:var(--t2);min-width:52px;text-align:right;flex-shrink:0;}

/* ─ TOP CONTENT ─ */
.top-post-list{
  background:var(--surf);border:1px solid var(--b);
  border-radius:var(--radius-lg);overflow:hidden;margin-bottom:20px;backdrop-filter:blur(8px);
}

.top-post-item{
  display:flex;align-items:center;gap:12px;
  padding:12px 18px;border-bottom:1px solid var(--b);
  transition:background .1s;cursor:default;
}
.top-post-item:last-child{border-bottom:none;}
.top-post-item:hover{background:var(--bg-hover);}

.rank-badge{
  width:28px;height:28px;border-radius:var(--radius-sm);flex-shrink:0;
  display:flex;align-items:center;justify-content:center;
  font-size:12px;font-weight:900;font-family:var(--mono);
}
.rank-1{background:var(--accent);color:#1B1405;box-shadow:0 2px 10px rgba(246,166,35,.35);}
.rank-2{background:var(--sp-highlight);color:var(--t2);}
.rank-3{background:var(--sp-elevated);color:var(--t3);}
.rank-n{background:transparent;color:var(--t3);}
.top-post-info{flex:1;min-width:0;}
.top-post-platform{
  font-size:10px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.08em;margin-bottom:2px;
  display:flex;align-items:center;gap:6px;flex-wrap:wrap;
}
.top-post-meta{font-size:11px;color:var(--t3);font-weight:500;}
.top-post-metrics{display:flex;align-items:center;gap:6px;flex-shrink:0;flex-wrap:wrap;justify-content:flex-end;}
.metric-pill{
  font-size:10.5px;font-family:var(--mono);font-weight:700;
  padding:3px 9px;border-radius:var(--radius-full);white-space:nowrap;
}
.mp-eng{background:var(--green-bg);color:var(--green);}
.mp-reach{background:var(--blue-bg);color:var(--blue);}
.mp-er{background:var(--amber-bg);color:var(--amber);}
.mp-impr{background:var(--purple-bg);color:var(--purple);}
.mp-flwg{background:var(--green-bg);color:var(--green-t);}
.mp-overall{background:var(--accent-dim);color:var(--accent);}
.mp-active{font-weight:900;}

/* ─ PILLAR MANAGEMENT ─ */
.pillar-mgmt-row{display:flex;align-items:center;gap:8px;padding:9px 4px;border-bottom:1px solid var(--b);}
.pillar-mgmt-row:last-child{border-bottom:none;}
.pillar-mgmt-emoji{font-size:17px;width:26px;text-align:center;flex-shrink:0;}
.pillar-mgmt-name{flex:1;font-size:13px;color:var(--t);font-weight:500;}
.pillar-mgmt-clr{width:11px;height:11px;border-radius:50%;flex-shrink:0;}
.pillar-mgmt-btn{width:26px;height:26px;border-radius:5px;border:none;background:none;color:var(--t3);font-size:12px;display:flex;align-items:center;justify-content:center;transition:background .1s,color .1s;flex-shrink:0;cursor:pointer;}
.pillar-mgmt-btn:hover{background:var(--bg-hover);color:var(--t);}
.pillar-mgmt-btn.del:hover{background:var(--red-bg);color:var(--red);}
.swatch{width:22px;height:22px;border-radius:50%;cursor:pointer;border:2px solid transparent;transition:border-color .12s,transform .12s;flex-shrink:0;}
.swatch:hover{transform:scale(1.12);}
.swatch.sel{border-color:var(--t);}
.pillar-manage-btn{
  display:inline-flex;align-items:center;gap:5px;
  padding:4px 10px;border-radius:6px;border:1px solid var(--b2);
  background:none;color:var(--t2);font-size:12px;font-weight:500;
  transition:background .12s,color .12s;cursor:pointer;
}
.pillar-manage-btn:hover{background:var(--bg-hover);color:var(--t);}

/* ─ PILLAR TAGS ─ */
.pillar-chip{
  display:inline-flex;align-items:center;gap:4px;
  font-size:10px;font-weight:600;letter-spacing:.02em;
  padding:2px 7px;border-radius:20px;cursor:default;
}
.pillar-dot{width:5px;height:5px;border-radius:50%;flex-shrink:0;}

/* ─ PILLAR BARS ─ */
.pillar-grid{
  display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:20px;
}
.pillar-card{
  background:var(--bg);border:1px solid var(--b);
  border-radius:var(--radius-lg);padding:14px 16px;
  transition:box-shadow .2s;
}
.pillar-card:hover{box-shadow:var(--shadow);}
.pillar-card-top{display:flex;align-items:center;gap:8px;margin-bottom:10px;}
.pillar-card-emoji{font-size:18px;}
.pillar-card-name{font-size:13px;font-weight:600;color:var(--t);}
.pillar-card-count{font-size:11px;color:var(--t3);margin-left:auto;}
.pillar-stat-row{display:flex;gap:12px;margin-bottom:8px;}
.pillar-stat{flex:1;}
.pillar-stat-lbl{font-size:10px;font-weight:600;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:2px;}
.pillar-stat-val{font-size:14px;font-weight:700;font-family:var(--mono);color:var(--t);}
.pillar-bar-wrap{height:5px;background:var(--surf2);border-radius:3px;overflow:hidden;}
.pillar-bar-fill{height:100%;border-radius:3px;transition:width .5s ease;}

/* ─ GOAL CARDS ─ */
.goal-grid{display:none;}
.goal-card{
  background:var(--bg);border:1px solid var(--b);
  border-radius:var(--radius-lg);padding:16px 18px;
  transition:box-shadow .2s,border-color .2s;
}
.goal-card:hover{box-shadow:var(--shadow);}
.goal-card-top{display:flex;align-items:flex-start;gap:8px;margin-bottom:10px;}
.goal-card-ico{font-size:20px;flex-shrink:0;}
.goal-card-name{font-size:13px;font-weight:600;color:var(--t);flex:1;}
.goal-card-edit{
  border:none;background:none;color:var(--t3);font-size:11px;
  cursor:pointer;padding:2px 4px;border-radius:4px;
  transition:background .1s,color .1s;
}
.goal-card-edit:hover{background:var(--bg-hover);color:var(--t);}
.goal-progress-bar{
  height:8px;background:var(--surf2);border-radius:4px;overflow:hidden;margin-bottom:8px;
}
.goal-progress-fill{height:100%;border-radius:4px;transition:width .6s ease;}
.goal-numbers{display:flex;align-items:baseline;gap:4px;}
.goal-current{font-size:18px;font-weight:700;font-family:var(--mono);color:var(--t);}
.goal-sep{font-size:12px;color:var(--t3);}
.goal-target{font-size:12px;font-family:var(--mono);color:var(--t3);}
.goal-pct{font-size:11px;font-weight:600;margin-left:auto;padding:2px 6px;border-radius:4px;}
.goal-pct-good{background:var(--green-bg);color:var(--green);}
.goal-pct-mid{background:var(--amber-bg);color:var(--amber);}
.goal-pct-low{background:var(--surf2);color:var(--t3);}
.goal-empty{
  background:var(--bg);border:1px dashed var(--b2);
  border-radius:var(--radius-lg);padding:18px;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:6px;cursor:pointer;transition:border-color .15s,background .15s;
  min-height:100px;
}
.goal-empty:hover{border-color:var(--blue);background:var(--blue-bg);}
.goal-empty-ico{font-size:22px;opacity:.4;}
.goal-empty-txt{font-size:12px;color:var(--t3);font-weight:500;}

/* ─ DASHBOARD TWO COLUMNS ─ */
.dash-cols{display:grid;grid-template-columns:1fr 320px;gap:16px;align-items:start;}
.dash-col-main{min-width:0;}
.dash-col-side{min-width:0;}

/* ─ CONTENT VIEW (3 cols) ─ */
.content-view-inner{
  display:grid;grid-template-columns:300px minmax(0,1fr);grid-template-rows:auto minmax(0,1fr);
  flex:1;overflow:hidden;
  background:var(--bg-page);
}
.content-months{
  grid-column:1;grid-row:1;width:auto;min-height:0;
  background:var(--bg);border-right:1px solid var(--b);border-bottom:1px solid var(--b);
  display:flex;flex-direction:column;overflow:hidden;
}
.content-months-hdr{
  padding:14px 18px 8px;
  font-size:10px;font-weight:600;color:var(--t3);
  text-transform:uppercase;letter-spacing:.08em;flex-shrink:0;
}
.content-months-body{overflow:visible;padding:0 16px 10px;}
.content-months-foot{padding:0 16px 14px;flex-shrink:0;}
.content-accs{
  grid-column:1;grid-row:2;width:auto;min-height:0;
  background:var(--bg);border-right:1px solid var(--b);
  display:flex;flex-direction:column;overflow:hidden;
}
.content-accs-hdr{
  padding:14px 18px 8px;font-size:10px;font-weight:600;color:var(--t3);
  text-transform:uppercase;letter-spacing:.07em;
  flex-shrink:0;
  display:flex;align-items:center;
}
.content-accs-body{flex:1;overflow-y:auto;padding:0 16px 16px;}
.content-accs-foot{padding:8px 16px 16px;flex-shrink:0;}
.content-editor{
  grid-column:2;grid-row:1 / span 2;display:flex;flex-direction:column;overflow:hidden;min-width:0;
  background:var(--bg);
}
.editor-hdr{
  padding:12px 20px;border-bottom:1px solid var(--b);
  display:flex;align-items:center;gap:10px;flex-shrink:0;
  flex-wrap:wrap;
}
.editor-title{font-size:15px;font-weight:600;color:var(--t);letter-spacing:-.01em;}
.editor-sub{font-size:11px;color:var(--t3);}
.editor-body{flex:1;overflow-y:auto;padding:16px 20px 60px;}

.period-select-wrap{position:relative;margin-bottom:22px;}
.period-select{
  width:100%;height:40px;border:1px solid var(--b);border-radius:8px;
  background:var(--surf);color:var(--t);font-size:13px;font-weight:700;
  font-family:var(--font);padding:0 38px 0 12px;outline:none;
  box-shadow:var(--shadow-sm);appearance:none;cursor:pointer;
}
.period-select:focus{border-color:var(--b2);box-shadow:0 0 0 3px var(--bg-active);}
.period-caret{
  position:absolute;right:12px;top:50%;transform:translateY(-50%);
  color:var(--t2);font-size:12px;pointer-events:none;
}
.period-actions{display:flex;gap:6px;margin-top:8px;}
.period-action{
  height:28px;border:1px solid var(--b);border-radius:7px;background:var(--bg);
  color:var(--t2);font-size:11px;font-weight:600;font-family:var(--font);
  padding:0 9px;cursor:pointer;transition:background .12s,border-color .12s,color .12s;
}
.period-action:hover{background:var(--bg-hover);border-color:var(--b2);color:var(--t);}
.period-action.danger:hover{background:var(--red-bg);border-color:transparent;color:var(--red);}
.week-tabs{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-bottom:8px;}
.week-tab{
  height:38px;border:none;border-radius:var(--radius-full);background:var(--sp-elevated);
  color:var(--t2);font-size:13px;font-weight:700;font-family:var(--font);
  cursor:pointer;transition:all .12s;
}

.week-tab:hover{background:var(--sp-highlight);color:var(--t);}

.week-tab.act{
  background:var(--accent);color:#000;font-weight:900;
  box-shadow:0 4px 12px rgba(29,185,84,.3);
}

.week-tools{display:flex;gap:10px;align-items:center;}
.week-tool{
  border:none;background:none;color:var(--t3);font-size:11px;font-weight:600;
  font-family:var(--font);padding:5px 0;cursor:pointer;transition:color .12s;
}
.week-tool:hover{color:var(--t);}
.week-tool.danger:hover{color:var(--red);}
.period-empty{padding:10px 0 2px;font-size:12px;line-height:1.5;color:var(--t3);}

/* ─ TREE ROWS ─ */
.tree-row{
  display:flex;align-items:center;gap:3px;
  padding:4px 6px;border-radius:6px;cursor:pointer;
  user-select:none;transition:background .1s;min-height:28px;
  color:var(--t2);font-size:12px;font-weight:500;
}
.tree-row:hover{background:var(--bg-hover);}
.tree-row.act{background:var(--bg-active);color:var(--t);}
.tree-toggle{
  width:16px;height:16px;border-radius:3px;
  display:flex;align-items:center;justify-content:center;
  flex-shrink:0;font-size:8px;color:var(--t3);
  transition:transform .16s;
}
.tree-toggle.open{transform:rotate(90deg);}
.tree-icon{font-size:13px;flex-shrink:0;width:16px;text-align:center;}
.tree-label{flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.tree-del{
  width:16px;height:16px;border-radius:3px;border:none;
  background:none;color:transparent;font-size:10px;
  display:flex;align-items:center;justify-content:center;flex-shrink:0;
  transition:all .1s;
}
.tree-row:hover .tree-del{color:var(--t3);}
.tree-del:hover{background:var(--bg-active);color:var(--red) !important;}
.tree-children{padding-left:12px;}
.tree-add{
  display:flex;align-items:center;gap:4px;
  padding:3px 6px;border-radius:6px;border:none;
  background:none;color:var(--t3);font-size:11px;font-weight:500;
  transition:background .1s,color .1s;width:100%;text-align:left;
  margin-top:2px;
}
.tree-add:hover{background:var(--bg-hover);color:var(--t2);}

/* ─ ACCOUNT LIST ─ */
.acc-item{
  position:relative;display:flex;align-items:center;padding:14px 44px 14px 16px;
  cursor:pointer;transition:background .12s;gap:10px;
  border-radius:var(--radius);margin:0 0 4px;border:none;
  background:none;min-height:60px;
}
.acc-item:hover{background:var(--bg-hover);}
.acc-item.act{background:var(--bg-active);}



.acc-item:hover .acc-acts{opacity:1;}
.acc-info{flex:1;min-width:0;}
.acc-name{
  font-size:13.5px;font-weight:700;color:var(--t);
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
}
.acc-item.act .acc-name{color:var(--accent);}
.acc-er{font-size:11px;color:var(--t3);font-family:var(--font);margin-top:2px;font-weight:500;}
.acc-acts{
  position:absolute;right:10px;top:10px;display:flex;gap:2px;opacity:0;
  transition:opacity .12s;flex-shrink:0;
}
.acc-act{
  width:22px;height:22px;border-radius:var(--radius-full);border:none;
  background:none;color:var(--t3);font-size:11px;
  display:flex;align-items:center;justify-content:center;
  transition:background .1s,color .1s;
}
.acc-act:hover{background:var(--bg-active);color:var(--t);}
.acc-act.d:hover{color:var(--red);}
.acc-count{display:none;}

/* ─ SMALL BUTTONS ─ */
.add-btn{
  display:flex;align-items:center;gap:5px;width:100%;
  padding:6px 8px;border-radius:6px;border:none;
  background:none;color:var(--t3);font-size:11px;font-weight:500;
  transition:background .1s,color .1s;text-align:left;
}
.add-btn:hover{background:var(--bg-hover);color:var(--t2);}
.add-btn-ico{font-size:14px;color:var(--t4);}

/* ─ PLATFORM SEGMENTED CONTROL ─ */
.seg{
  display:flex;gap:1px;padding:2px;
  background:var(--bg-hover);border-radius:8px;
}
.seg-btn{
  padding:4px 10px;border-radius:6px;border:none;
  background:none;color:var(--t2);font-size:11px;font-weight:500;
  transition:background .12s,color .12s;white-space:nowrap;
}
.seg-btn:hover{color:var(--t);}
.seg-btn.tt-act{background:var(--bg);color:var(--tt);box-shadow:var(--shadow-sm);}
.seg-btn.ig-act{background:var(--bg);color:var(--ig);box-shadow:var(--shadow-sm);}
.seg-btn.act{background:var(--bg);color:var(--t);box-shadow:var(--shadow-sm);}

/* ─ WEEKLY STATS ─ */
.weekly-stats-row{
  display:flex;align-items:center;gap:16px;flex-wrap:wrap;
  padding:10px 0;border-bottom:1px solid var(--b);margin-bottom:14px;
}
.ws-field{display:flex;align-items:center;gap:8px;}
.ws-label{font-size:11px;font-weight:500;color:var(--t3);}
.ws-inp{
  background:none;border:none;border-bottom:1.5px solid var(--b);
  font-size:13px;font-family:var(--mono);color:var(--t);
  outline:none;padding:2px 4px;width:110px;transition:border-color .15s;
}
.ws-inp:focus{border-bottom-color:var(--tt);}
.ws-hint{font-size:10px;color:var(--t4);}

/* ─ SUMMARY PROP TABLE ─ */
.prop-table{
  border:1px solid var(--b);border-radius:var(--radius);
  overflow:hidden;margin-bottom:14px;
}
.prop-row{
  display:flex;align-items:center;
  border-bottom:1px solid var(--b);min-height:30px;
}
.prop-row:last-child{border-bottom:none;}
.prop-key{
  width:140px;flex-shrink:0;padding:5px 12px;
  font-size:11px;font-weight:500;color:var(--t3);
  background:var(--surf2);border-right:1px solid var(--b);
}
.prop-val{flex:1;padding:5px 12px;font-size:12px;font-family:var(--mono);color:var(--t);}

/* ─ POST CARDS ─ */
/* ── CONTENT TABLE ── */
.content-table-wrap{
  background:var(--surf);border-radius:var(--radius);
  border:1px solid var(--b);overflow:hidden;box-shadow:var(--shadow-sm);
  margin-bottom:16px;
}
.ct-plat-bar{
  display:flex;align-items:center;justify-content:space-between;
  padding:10px 14px;border-bottom:1px solid var(--b);gap:10px;flex-wrap:wrap;
}
.ct-plat-tabs{display:flex;gap:4px;}
.ct-plat-tab{
  padding:6px 18px;border-radius:var(--radius-full);
  border:none;background:var(--sp-elevated);
  color:var(--t2);font-size:13px;font-weight:700;
  cursor:pointer;transition:all .12s;font-family:var(--font);
}

.ct-plat-tab:hover{background:var(--sp-highlight);color:var(--t);}

.ct-plat-tab.act{background:var(--accent);color:#000;font-weight:800;box-shadow:0 4px 14px rgba(29,185,84,.35);}
.ct-plat-tab.act.tt{background:var(--accent);color:#000;}
.ct-plat-tab.act.ig{background:var(--red);color:#fff;}
.ct-add-row-btn{
  display:flex;align-items:center;gap:6px;padding:8px 20px;
  border-radius:var(--radius-full);background:var(--accent);color:#000;
  border:none;font-size:13px;font-weight:800;cursor:pointer;
  font-family:var(--font);transition:all .12s;flex-shrink:0;
  box-shadow:0 4px 14px rgba(29,185,84,.35);
}
.ct-add-row-btn:hover{background:var(--accent-hover);transform:scale(1.03);}
.ct-add-row-btn:active{transform:scale(.97);}
.content-table-scroll{
  overflow-x:auto;-webkit-overflow-scrolling:touch;
  background:
    linear-gradient(to right,var(--surf) 0,transparent 16px) left/22px 100% no-repeat,
    linear-gradient(to left,var(--surf) 0,transparent 16px) right/22px 100% no-repeat,
    linear-gradient(to right,rgba(0,0,0,.05) 0,transparent 16px) left/22px 100% no-repeat,
    linear-gradient(to left,rgba(0,0,0,.05) 0,transparent 16px) right/22px 100% no-repeat;
  background-attachment:local,local,scroll,scroll;
}
.content-table{width:100%;border-collapse:collapse;font-size:13px;}
.content-table thead th{
  padding:8px 10px;text-align:left;
  font-size:10px;font-weight:600;color:var(--t3);
  text-transform:uppercase;letter-spacing:.07em;
  border-bottom:1px solid var(--b);
  background:var(--surf2);white-space:nowrap;
}
.content-table thead th.num{text-align:right;}
.content-row{border-bottom:1px solid var(--b);transition:background .1s;}
.content-row:last-child{border-bottom:none;}
.content-row:hover{background:var(--bg-hover);}
.ct-cell{padding:5px 8px;vertical-align:middle;}
.ct-cell.num{text-align:right;}
.ct-inp{
  background:none;border:none;outline:none;
  font-size:13px;font-family:var(--font);color:var(--t);
  border-radius:4px;padding:3px 5px;transition:background .12s;
}
.ct-inp:focus{background:var(--bg-active);}
.ct-inp::placeholder{color:var(--t4);}
.ct-inp-date{width:106px;}
.ct-inp-name{min-width:120px;width:100%;}
.ct-inp-num{
  width:68px;text-align:right;font-family:var(--mono);
}
.ct-inp-num::-webkit-inner-spin-button{opacity:0;}
.ct-pillar-sel{
  border:none;outline:none;background:var(--bg);
  font-size:13px;font-weight:600;font-family:var(--font);
  cursor:pointer;padding:3px 5px;border-radius:4px;
  transition:background .12s;min-width:90px;
}
.ct-pillar-sel:focus{background:var(--bg-active);}
.ct-er-val{
  font-size:12px;font-weight:600;font-family:var(--mono);
  color:var(--amber);white-space:nowrap;padding:3px 5px;
}
.ct-link-btn{
  display:inline-flex;align-items:center;gap:3px;
  font-size:11px;padding:3px 7px;border-radius:4px;
  border:none;cursor:pointer;font-family:var(--font);
  transition:all .12s;text-decoration:none;
}
.ct-link-btn.has{background:var(--blue-bg);color:var(--blue);}
.ct-link-btn.has:hover{opacity:.8;}
.ct-link-btn.add{background:none;color:var(--t3);}
.ct-link-btn.add:hover{background:var(--bg-active);color:var(--t2);}
.ct-link-wrap{display:flex;align-items:center;gap:4px;white-space:nowrap;}
.ct-link-edit{background:none;color:var(--t3);padding:3px 6px;}
.ct-link-edit:hover{background:var(--bg-active);color:var(--t);}
.ct-action-btn{
  width:22px;height:22px;border-radius:4px;border:none;
  background:none;color:var(--t3);font-size:11px;
  cursor:pointer;display:flex;align-items:center;justify-content:center;
  transition:background .1s,color .1s;flex-shrink:0;
}
.ct-action-btn:hover{background:var(--bg-active);color:var(--t);}
.ct-action-btn.del:hover{background:var(--red-bg);color:var(--red);}
.ct-add-bottom{
  display:flex;align-items:center;gap:6px;padding:9px 14px;
  border:none;border-top:1px solid var(--b);background:none;
  width:100%;color:var(--t3);font-size:13px;font-family:var(--font);
  cursor:pointer;transition:background .1s,color .1s;text-align:left;
}
.ct-add-bottom:hover{background:var(--bg-hover);color:var(--t2);}
.ct-empty{
  padding:40px 20px;text-align:center;
  color:var(--t3);font-size:13px;
}

/* ── CONTENT TABLE – sticky first col ── */
.content-table tbody td:first-child,
.content-table thead th:first-child{
  position:sticky;left:0;z-index:2;
  background:var(--surf);
}
.content-table thead th:first-child{
  z-index:3;background:var(--surf2);
}
.content-row:hover td:first-child{background:var(--bg-hover);}

/* ── OLD CARD RESET (table replaces card layout) ── */
.post-card{
  border:1px solid var(--b);border-radius:var(--radius);
  margin-bottom:8px;overflow:hidden;
  background:var(--bg);transition:border-color .15s;
}
.post-card:hover{border-color:var(--b2);}
.post-card-hdr{
  display:flex;align-items:center;padding:9px 14px;
  cursor:pointer;gap:8px;background:var(--surf2);
  border-bottom:1px solid var(--b);
}
.post-card-num{font-size:12px;font-weight:600;color:var(--t);flex-shrink:0;}
.post-card-date{font-size:11px;color:var(--t3);}
.post-card-url{
  font-size:10px;color:var(--blue);
  max-width:140px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;
}
.post-card-gap{flex:1;}
.er-tag{
  font-size:10px;font-family:var(--mono);font-weight:600;
  color:var(--amber);padding:2px 7px;border-radius:5px;
  background:var(--amber-bg);flex-shrink:0;
}
.eng-tag{
  font-size:10px;font-family:var(--mono);font-weight:600;
  color:var(--green);padding:2px 7px;border-radius:5px;
  background:var(--green-bg);flex-shrink:0;
}
.del-btn{
  padding:2px 5px;border-radius:4px;border:none;
  background:none;color:var(--t3);font-size:10px;
  transition:background .1s,color .1s;
}
.del-btn:hover{background:var(--bg-hover);color:var(--red);}
.dup-btn{
  padding:2px 5px;border-radius:4px;border:none;
  background:none;color:var(--t3);font-size:10px;
  transition:background .1s,color .1s;
}
.dup-btn:hover{background:var(--bg-hover);color:var(--blue);}
.chev{font-size:11px;color:var(--t3);transition:transform .16s;flex-shrink:0;}
.chev.open{transform:rotate(180deg);}
.post-body{padding:0;}
.post-body.hidden{display:none;}
.post-meta-row{
  display:flex;align-items:center;gap:12px;
  padding:9px 14px;border-bottom:1px solid var(--b);flex-wrap:wrap;
}
.meta-field{display:flex;align-items:center;gap:8px;flex:1;min-width:150px;}
.meta-key{font-size:11px;font-weight:500;color:var(--t3);min-width:46px;}
.meta-inp{
  background:none;border:none;border-bottom:1.5px solid var(--b);
  font-size:12px;font-family:var(--font);color:var(--t);
  outline:none;padding:2px 0;flex:1;transition:border-color .15s;
}
.meta-inp:focus{border-bottom-color:var(--tt);}
.meta-inp.ig:focus{border-bottom-color:var(--ig);}
.meta-inp::placeholder{color:var(--t4);}
.link-open{
  font-size:10px;color:var(--blue);border:none;
  background:none;padding:2px 5px;border-radius:3px;transition:background .1s;
}
.link-open:hover{background:var(--blue-bg);}
/* Pillar selector inside post */
.pillar-row{
  padding:8px 14px;border-bottom:1px solid var(--b);
  display:flex;align-items:center;gap:8px;flex-wrap:wrap;
}
.pillar-row-lbl{font-size:11px;font-weight:500;color:var(--t3);white-space:nowrap;}
.pillar-opt{
  padding:3px 10px;border-radius:20px;border:1px solid transparent;
  font-size:11px;font-weight:600;cursor:pointer;
  background:var(--surf2);color:var(--t2);
  transition:all .12s;letter-spacing:.01em;
}
.pillar-opt:hover{border-color:var(--b2);}
.pillar-opt.sel{border-color:transparent;}
.pp-hdr{
  padding:8px 14px 4px;
  font-size:10px;font-weight:600;color:var(--t3);
  text-transform:uppercase;letter-spacing:.07em;
}
.pp-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;padding:6px 14px 10px;}
.pp-field{display:flex;flex-direction:column;gap:2px;}
.pp-label{font-size:10px;font-weight:500;color:var(--t3);}
.pp-inp{
  background:none;border:none;border-bottom:1.5px solid var(--b);
  font-size:13px;font-family:var(--mono);color:var(--t);
  outline:none;padding:2px 0;width:100%;transition:border-color .15s;
}
.pp-inp:focus{border-bottom-color:var(--tt);}
.pp-inp.ig:focus{border-bottom-color:var(--ig);}
.pp-inp::placeholder{color:var(--t4);}
.er-result-row{
  display:flex;align-items:center;gap:12px;padding:8px 14px;background:var(--surf2);
}
.er-result-label{font-size:11px;font-weight:500;color:var(--t3);}
.er-result-val{font-size:13px;font-weight:700;font-family:var(--mono);color:var(--amber);}
.er-result-formula{font-size:10px;color:var(--t3);}

/* ─ ADD POST BUTTON ─ */
.add-post-row{
  display:flex;align-items:center;gap:8px;
  padding:10px 14px;border-radius:var(--radius);border:1.5px dashed var(--b2);
  background:none;color:var(--t3);font-size:12px;font-weight:500;
  transition:border-color .15s,background .15s,color .15s;width:100%;text-align:left;
  margin-top:8px;
}
.add-post-row:hover{
  border-color:var(--tt);color:var(--tt);background:var(--tt-bg);
}
.add-post-row span{font-size:16px;}

/* ─ ANALYTICS FILTERS ─ */
.analytics-filter{
  display:flex;align-items:center;gap:8px;flex-wrap:wrap;
  padding:12px 16px;background:var(--bg);border:1px solid var(--b);
  border-radius:var(--radius);margin-bottom:16px;
}
.overview-card-tools{
  display:flex;align-items:center;justify-content:flex-end;gap:8px;
  margin:-4px 0 14px;flex-wrap:wrap;
}
.af-label{font-size:11px;font-weight:500;color:var(--t3);}
.af-sel{
  background:var(--bg);border:1px solid var(--b2);color:var(--t);
  border-radius:6px;padding:4px 8px;font-size:11px;
  font-family:var(--font);outline:none;cursor:pointer;
}
.af-sel:focus{border-color:var(--tt);}
.date-inp{
  background:var(--bg);border:1px solid var(--b2);color:var(--t);
  border-radius:6px;padding:4px 8px;font-size:11px;
  font-family:var(--font);outline:none;cursor:pointer;
}
.date-inp:focus{border-color:var(--tt);}

/* ─ CHART CARDS ─ */
.chart-card{
  background:linear-gradient(145deg,#161624,#12121E);
  border:1px solid rgba(255,255,255,.07);
  border-radius:16px;padding:22px;margin-bottom:16px;
}

.chart-title{font-size:13px;font-weight:800;color:var(--t);margin-bottom:4px;letter-spacing:-.02em;}
.chart-sub{font-size:11px;color:var(--t3);margin-bottom:14px;font-weight:500;}
.chart-legend{display:flex;gap:12px;align-items:center;flex-wrap:wrap;margin-bottom:12px;}
.leg{display:flex;align-items:center;gap:5px;font-size:11px;color:var(--t2);font-weight:700;}
.leg-d{width:12px;height:3px;border-radius:var(--radius-full);}

/* Comparison tables */
.komp-table{width:100%;border-collapse:collapse;font-size:12.5px;}
.komp-table th{
  padding:9px 14px;text-align:left;font-size:11px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.12em;border-bottom:1px solid var(--b);
  background:var(--sp-elevated);
}

.komp-table td{padding:9px 14px;border-bottom:1px solid var(--b);font-family:var(--mono);font-weight:600;}
.komp-table tr:last-child td{border-bottom:none;}
.komp-table .metric-lbl{font-family:var(--font);color:var(--t2);font-size:12px;font-weight:700;}
.delta-pos{color:var(--green);}
.delta-neg{color:var(--red);}
.delta-neu{color:var(--t3);}

/* ─ LEADERBOARD ─ */
.leaderboard{
  background:var(--sp-card);border:none;
  border-radius:var(--radius-lg);overflow:hidden;
}

.lb-row{
  display:flex;align-items:center;gap:10px;
  padding:11px 16px;border-bottom:1px solid var(--b);transition:background .1s;
}
.lb-row:hover{background:var(--sp-elevated);}

.lb-row:last-child{border-bottom:none;}
.lb-rank{font-size:12px;font-family:var(--mono);font-weight:700;color:var(--t3);width:20px;flex-shrink:0;}
.lb-name{flex:1;font-size:13px;font-weight:700;color:var(--t2);}
.lb-val{font-size:12px;font-family:var(--mono);font-weight:600;color:var(--t);}
.lb-bar{width:80px;height:4px;background:var(--sp-highlight);border-radius:var(--radius-full);overflow:hidden;}

.lb-bar-fill{height:100%;border-radius:var(--radius-full);}

/* ─ PLACEHOLDER ─ */
.placeholder{
  display:flex;flex-direction:column;align-items:center;
  justify-content:center;height:100%;gap:10px;
  color:var(--t3);text-align:center;padding:40px;
}
.ph-ico{font-size:40px;opacity:.15;margin-bottom:6px;}
.ph-title{font-size:15px;font-weight:800;color:var(--t2);}
.ph-sub{font-size:13px;color:var(--t3);line-height:1.6;font-weight:500;}

/* ── Empty states v2 (F4) ── */
.empty-state-v2{display:flex;flex-direction:column;align-items:center;padding:60px 24px;text-align:center;gap:12px;}
.es-emoji{font-size:48px;}
.es-title{font-size:18px;font-weight:800;color:var(--t);letter-spacing:-.02em;}
.es-desc{font-size:14px;color:var(--t3);max-width:280px;line-height:1.6;}
.empty-state-v2 .btn-ok{margin-top:4px;}

/* ─ NOTIFICATION BANNER ─ */
.empty-banner{
  background:var(--sp-elevated);border:1px solid var(--b);
  border-radius:var(--radius);padding:16px 20px;margin-bottom:20px;
  display:flex;align-items:center;gap:14px;
}

.empty-banner-ico{font-size:22px;flex-shrink:0;}
.empty-banner-text{font-size:13px;color:var(--t2);line-height:1.5;font-weight:500;}

.empty-banner-btn{
  margin-left:auto;padding:8px 18px;border-radius:var(--radius-full);border:none;
  background:var(--accent);color:#000;font-size:12px;font-weight:800;
  cursor:pointer;flex-shrink:0;transition:background .12s;
}
.empty-banner-btn:hover{background:var(--accent-hover);}

/* ─ BADGES / TAGS ─ */
.tag{display:inline-flex;align-items:center;gap:4px;font-size:10px;font-weight:700;padding:3px 8px;border-radius:var(--radius-full);}
.tag-tt{background:var(--tt-bg);color:var(--tt);}
.tag-ig{background:var(--ig-bg);color:var(--ig);}

/* ─ ANALYTICS LAYOUT ─ */
.analytics-cols{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
.analytics-cols.wide{grid-template-columns:1fr;}

/* ─ GOALS / PILLAR / COMPARISON ─ */
.goal-period-bar{display:flex;align-items:center;gap:8px;margin-bottom:18px;flex-wrap:wrap;}
.komp-filter-row{
  display:flex;align-items:center;gap:8px;flex-wrap:wrap;
  padding:12px 16px;background:var(--sp-card);border:none;
  border-radius:var(--radius);margin-bottom:14px;
}


/* ─ PILLAR CARDS ─ */
.pillar-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:20px;}
.pillar-card{
  background:var(--sp-card);border:none;
  border-radius:var(--radius-lg);padding:16px 18px;transition:background .12s,transform .12s;
}

.pillar-card:hover{background:var(--sp-elevated);transform:translateY(-1px);}

.pillar-card-top{display:flex;align-items:center;gap:8px;margin-bottom:12px;}
.pillar-card-emoji{font-size:20px;}
.pillar-card-name{font-size:13px;font-weight:800;color:var(--t);}
.pillar-card-count{font-size:11px;color:var(--t3);margin-left:auto;font-weight:600;}
.pillar-stat-row{display:flex;gap:12px;margin-bottom:10px;}
.pillar-stat{flex:1;}
.pillar-stat-lbl{font-size:11px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.1em;margin-bottom:3px;}
.pillar-stat-val{font-size:16px;font-weight:900;font-family:var(--mono);color:var(--t);}
.pillar-bar-wrap{height:4px;background:var(--sp-highlight);border-radius:var(--radius-full);overflow:hidden;}

.pillar-bar-fill{height:100%;border-radius:var(--radius-full);transition:width .6s cubic-bezier(.4,0,.2,1);}

/* ─ GOAL CARDS ─ */
.goal-grid{display:none;}
.goal-card{background:var(--sp-card);border:none;border-radius:var(--radius-lg);padding:18px 20px;transition:background .12s;}

.goal-card:hover{background:var(--sp-elevated);}

.goal-card-top{display:flex;align-items:flex-start;gap:10px;margin-bottom:12px;}
.goal-card-ico{font-size:22px;flex-shrink:0;}
.goal-card-name{font-size:13px;font-weight:800;color:var(--t);flex:1;}
.goal-card-edit{border:none;background:none;color:var(--t3);font-size:11px;cursor:pointer;padding:3px 6px;border-radius:var(--radius-full);transition:background .1s,color .1s;}
.goal-card-edit:hover{background:var(--bg-hover);color:var(--t);}
.goal-progress-bar{height:6px;background:var(--sp-highlight);border-radius:var(--radius-full);overflow:hidden;margin-bottom:10px;}

.goal-progress-fill{height:100%;border-radius:var(--radius-full);transition:width .7s cubic-bezier(.4,0,.2,1);}
.goal-numbers{display:flex;align-items:baseline;gap:4px;}
.goal-current{font-size:20px;font-weight:900;font-family:var(--mono);color:var(--t);}
.goal-sep{font-size:12px;color:var(--t3);}
.goal-target{font-size:12px;font-family:var(--mono);color:var(--t3);font-weight:600;}
.goal-pct{font-size:11px;font-weight:800;margin-left:auto;padding:3px 8px;border-radius:var(--radius-full);}
.goal-pct-good{background:var(--green-bg);color:var(--green);}
.goal-pct-mid{background:var(--amber-bg);color:var(--amber);}
.goal-pct-low{background:var(--sp-highlight);color:var(--t3);}

.goal-empty{
  background:var(--sp-card);border:1px dashed var(--b2);
  border-radius:var(--radius-lg);padding:20px;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:8px;cursor:pointer;transition:all .15s;min-height:110px;
}

.goal-empty:hover{border-color:var(--accent);background:var(--green-bg);}
.goal-empty-ico{font-size:24px;opacity:.3;}
.goal-empty-txt{font-size:12px;color:var(--t3);font-weight:700;}

/* ─ MOBILE NAV ─ */
.mob-nav{
  display:none;position:fixed;bottom:0;left:0;right:0;
  background:var(--sp-card);border-top:1px solid var(--b);
  z-index:100;
  padding-bottom:max(env(safe-area-inset-bottom),8px);
  box-shadow:0 -8px 32px rgba(0,0,0,0.4);
}

.mob-nav-inner{display:grid;grid-template-columns:repeat(4,1fr);height:62px;}
.mnav{
  display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;
  border:none;background:none;color:var(--t3);font-size:8.5px;
  font-family:var(--font);padding:5px 2px;
  transition:color .15s;cursor:pointer;font-weight:700;
  letter-spacing:.01em;position:relative;min-height:58px;
}
.mnav-ico{font-size:19px;line-height:1;transition:transform .15s;}
.mnav.act{color:var(--accent);}
.mnav.act .mnav-ico{transform:scale(1.1);}
.mnav.act::after{
  content:'';position:absolute;bottom:0;left:30%;right:30%;height:2px;
  background:var(--accent);border-radius:var(--radius-full) var(--radius-full) 0 0;
}
/* ─ MODAL ─ */
.modal-ov{
  position:fixed;inset:0;background:rgba(0,0,0,0.65);
  z-index:200;display:flex;align-items:center;justify-content:center;
  backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px);
  padding:16px;
}
.modal-ov.hidden{display:none;}

/* ── Settings Side Panel ── */
#m-settings{align-items:stretch;justify-content:flex-end;padding:0;}
.settings-panel{
  width:380px;max-width:100vw;height:100vh;background:var(--surf);
  border-left:1px solid var(--b);display:flex;flex-direction:column;
  overflow:hidden;animation:slideInRight .22s cubic-bezier(.4,0,.2,1);
}
@keyframes slideInRight{from{transform:translateX(100%)}to{transform:translateX(0)}}
.settings-header{
  display:flex;align-items:center;justify-content:space-between;
  padding:20px 20px 16px;border-bottom:1px solid var(--b);flex-shrink:0;
}
.settings-header-title{font-size:15px;font-weight:800;color:var(--t);}
.settings-close{width:30px;height:30px;border-radius:8px;border:1px solid var(--b);background:none;color:var(--t3);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .15s;}
.settings-close:hover{background:var(--bg-hover);color:var(--t);}
.settings-body{flex:1;overflow-y:auto;padding:4px 0 24px;}
.settings-section{padding:16px 20px 4px;}
.settings-section-label{font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t4);margin-bottom:12px;}
.settings-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.04);}
.settings-row:last-child{border-bottom:none;}
.settings-row-left{flex:1;min-width:0;}
.settings-row-label{font-size:13px;font-weight:600;color:var(--t);line-height:1.3;}
.settings-row-sub{font-size:11px;color:var(--t3);margin-top:2px;line-height:1.4;}
.settings-inp{width:100%;padding:8px 11px;border-radius:9px;border:1.5px solid var(--b);background:var(--bg-hover);color:var(--t);font-size:13px;font-weight:500;outline:none;transition:border .15s;margin-top:6px;}
.settings-inp:focus{border-color:var(--accent);}
.settings-select{width:100%;padding:8px 11px;border-radius:9px;border:1.5px solid var(--b);background:var(--bg-hover);color:var(--t);font-size:13px;font-weight:500;outline:none;margin-top:6px;cursor:pointer;}
.settings-toggle{position:relative;width:40px;height:22px;flex-shrink:0;}
.settings-toggle input{opacity:0;width:0;height:0;position:absolute;}
.settings-toggle-track{position:absolute;inset:0;border-radius:11px;background:var(--b2);cursor:pointer;transition:background .2s;}
.settings-toggle input:checked + .settings-toggle-track{background:var(--accent);}
.settings-toggle-track::after{content:'';position:absolute;width:16px;height:16px;border-radius:50%;background:#fff;top:3px;left:3px;transition:transform .2s;}
.settings-toggle input:checked + .settings-toggle-track::after{transform:translateX(18px);}
.settings-divider{height:1px;background:var(--b);margin:8px 20px;}
.settings-btn-row{display:flex;gap:8px;flex-wrap:wrap;padding:4px 0;}
.settings-action-btn{flex:1;min-width:120px;padding:9px 14px;border-radius:10px;border:1.5px solid var(--b);background:var(--bg-hover);color:var(--t2);font-size:12px;font-weight:700;cursor:pointer;transition:all .15s;text-align:center;}
.settings-action-btn:hover{border-color:var(--accent);color:var(--accent);background:var(--accent-dim);}
.settings-action-btn.danger{border-color:rgba(239,68,68,.3);color:var(--red);}
.settings-action-btn.danger:hover{background:rgba(239,68,68,.08);border-color:var(--red);}
.settings-save-row{padding:16px 20px 0;flex-shrink:0;border-top:1px solid var(--b);}
@media(max-width:768px){.settings-panel{width:100vw;border-left:none;}}

@media(max-width:768px){
  .mob-nav{display:block;}
  body{padding-bottom:calc(62px + max(env(safe-area-inset-bottom),8px));}
  .nav-sidebar{display:none;}
  .app{flex-direction:column;}
  .view{padding:16px 16px 20px;}
  .topbar{padding:0 16px;}
  .topbar-title{font-size:18px;}
  .kpi-grid{grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px;}
  .kpi-value{font-size:22px;}
  .kpi-icon{font-size:18px;margin-bottom:10px;}
  .kpi-card{padding:14px 14px 12px;}
  .kpi-remove{opacity:.7;}
  .dash-cols{grid-template-columns:1fr;}
  .pillar-grid{grid-template-columns:1fr;}
  .analytics-cols{grid-template-columns:1fr;}
  .goal-grid{grid-template-columns:1fr 1fr;}
  .content-view-inner{display:block;}
  .content-months,.content-accs,.content-editor{display:none;}
  .content-months.mob-show,.content-accs.mob-show,.content-editor.mob-show{display:flex !important;}
  .content-months,.content-accs{width:100%;height:calc(100dvh - var(--topbar-h) - 62px - max(env(safe-area-inset-bottom),8px));border-right:none;}
  .content-editor{height:calc(100dvh - var(--topbar-h) - 62px - max(env(safe-area-inset-bottom),8px));}
  .week-tabs{grid-template-columns:repeat(4,minmax(56px,1fr));gap:6px;}
  .week-tab{height:44px;font-size:13px;}
  .mob-content-nav{display:flex;border-bottom:1px solid var(--b);background:var(--sp-card);flex-shrink:0;}
  
  .mob-content-tab{
    flex:1;padding:12px 0;border:none;background:none;
    font-size:11px;font-weight:700;color:var(--t3);font-family:var(--font);
    cursor:pointer;transition:color .15s;border-bottom:2px solid transparent;
    display:flex;flex-direction:column;align-items:center;gap:3px;min-height:56px;
  }
  .mob-content-tab.act{color:var(--accent);border-bottom-color:var(--accent);}
  .mob-content-tab-ico{font-size:20px;}
  .content-table-scroll{overflow-x:auto;-webkit-overflow-scrolling:touch;}
  .content-table{min-width:720px;}
  .modal-ov{align-items:flex-end;padding:0;}
  .modal{
    width:100%;max-width:100%;border-radius:var(--radius-xl) var(--radius-xl) 0 0;
    padding:28px 24px max(env(safe-area-inset-bottom),28px);
    animation:sheetIn .28s cubic-bezier(.16,1,.3,1);
    border-left:none;border-right:none;border-bottom:none;
  }
  @keyframes sheetIn{from{transform:translateY(100%);}to{transform:translateY(0);}}
  .top-post-metrics{gap:4px;}
  .metric-pill{font-size:11px;padding:2px 6px;}
  .top-post-item{padding:10px 14px;gap:8px;}
  .seg{overflow-x:auto;flex-wrap:nowrap;}
  .seg-btn{white-space:nowrap;flex-shrink:0;}
}

@media(max-width:768px){
  .mob-content-nav{display:none;}
  .view{display:none;}
  .view.active{display:block;}
  .view.content-view-wrap{display:none;padding:0;}
  .view.content-view-wrap.active{
    display:block;
    overflow-y:auto;padding:24px 28px 80px;background:var(--bg-page);
  }
  
  .content-table-scroll{overflow-x:auto;}
  .content-table{min-width:820px;}
}

/* ── Mobile stack form at 480px (M4) ── */
@media(max-width:480px){
  .qc-grid-2,.rpt-kpi-row,.kpi-grid{grid-template-columns:1fr!important;}
  .modal-acts{flex-direction:column-reverse;}
  .modal-acts button{width:100%;justify-content:center;}
  .dash-cols{grid-template-columns:1fr!important;}
  .analytics-cols:not(.wide){grid-template-columns:1fr!important;}
  .pillar-grid{grid-template-columns:1fr!important;}
}

/* ════════════════════════════════════════════
   ✨ PREMIUM DESIGN UPGRADE LAYER
   ════════════════════════════════════════════ */

/* ── Background texture ── */
[data-theme="dark"] body::before{
  content:'';position:fixed;inset:0;pointer-events:none;z-index:0;
  background-image:radial-gradient(circle at 1px 1px,rgba(255,255,255,.04) 1px,transparent 0);
  background-size:28px 28px;
}

.app{position:relative;z-index:1;}

/* ── Sidebar glow & gradient ── */
.nav-sidebar{
  background:var(--sidebar-bg);
  border-right:1px solid var(--sidebar-b);
  box-shadow:2px 0 24px rgba(0,0,0,.18);
  backdrop-filter:blur(14px);
}


/* ── Brand mark with teal-coral gradient ── */
.brand-mark{
  background:linear-gradient(140deg,var(--accent) 0%,var(--accent-2) 100%);
  box-shadow:0 4px 16px rgba(14,159,143,.4);
  border-radius:12px;
}

/* ── Nav items - smoother active indicator ── */
.nav-item{
  transition:background .18s,color .18s,transform .12s;
  border-radius:10px;
}
.nav-item:hover{transform:translateX(2px);}
.nav-item.act{
  background:var(--nav-accent-bg) !important;
  color:var(--nav-accent);
}
.nav-item.act::before{
  width:3px;height:55%;
  background:linear-gradient(180deg,var(--accent),var(--accent-2));
  box-shadow:0 0 8px rgba(14,159,143,.5);
  border-radius:0 3px 3px 0;
}

/* ── Topbar frosted glass ── */
.topbar{
  background:rgba(13,16,32,.80);
  backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);
  border-bottom:1px solid var(--b);
  box-shadow:0 1px 0 rgba(255,255,255,.03),0 4px 20px rgba(0,0,0,.2);
}


/* ── KPI Cards - glass depth ── */
.kpi-card{
  background:var(--surf);
  border:1px solid var(--b);
  box-shadow:0 2px 8px rgba(0,0,0,.18);
  backdrop-filter:blur(8px);
  transition:background .2s,transform .2s,box-shadow .2s;
}

.kpi-card:hover{
  background:var(--surf2);
  transform:translateY(-3px);
  box-shadow:var(--shadow-md);
}

/* Left accent glow */
.kpi-card::before{
  width:3px;
  box-shadow:0 0 10px var(--kpi-accent,var(--accent));
}
/* Subtle shine top edge */
.kpi-card::after{
  content:'';position:absolute;top:0;left:12px;right:12px;height:1px;
  background:linear-gradient(90deg,transparent,rgba(61,199,183,.18),transparent);
  border-radius:2px;
}

.kpi-value{
  background:linear-gradient(135deg,var(--t) 0%,var(--t2) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}
[data-theme="dark"] .kpi-value{
  background:linear-gradient(135deg,#fff 0%,#c0d0e0 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}

/* ── Chart cards - glass presence ── */
.chart-card{
  background:var(--surf);
  border:1px solid var(--b);
  box-shadow:0 4px 16px rgba(0,0,0,.18);
  border-radius:var(--radius-lg);
  backdrop-filter:blur(8px);
  position:relative;overflow:hidden;
}

.chart-card::before{
  content:'';position:absolute;top:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,transparent,rgba(61,199,183,.2),transparent);
}


/* ── Pillar cards ── */
.pillar-card{
  background:var(--surf) !important;
  border:1px solid var(--b) !important;
  box-shadow:0 2px 12px rgba(0,0,0,.15);
  border-radius:var(--radius-lg);
  backdrop-filter:blur(6px);
  position:relative;overflow:hidden;
}

.pillar-card:hover{
  transform:translateY(-2px);
  box-shadow:var(--shadow);
}

/* Gradient fill on pillar bars */
.pillar-bar-fill{
  background:linear-gradient(90deg,var(--accent),var(--accent-2)) !important;
  box-shadow:0 0 6px rgba(14,159,143,.35);
}

/* ── Goal cards ── */
.goal-card{
  background:var(--surf) !important;
  border:1px solid var(--b) !important;
  box-shadow:0 2px 10px rgba(0,0,0,.15);
  border-radius:var(--radius-lg) !important;
  backdrop-filter:blur(6px);
  transition:transform .2s,box-shadow .2s;
}

.goal-card:hover{
  transform:translateY(-2px);
  box-shadow:var(--shadow);
}

.goal-progress-fill{
  background:linear-gradient(90deg,var(--accent),var(--accent-2)) !important;
  box-shadow:0 0 8px rgba(14,159,143,.35);
}
.goal-progress-fill[style*="background:var(--amber)"]{
  background:linear-gradient(90deg,var(--accent-2),#ff9a70) !important;
  box-shadow:0 0 8px rgba(255,122,89,.35);
}
.goal-progress-fill[style*="background:var(--blue)"]{
  background:linear-gradient(90deg,#4D79FF,#2D46B9) !important;
  box-shadow:0 0 8px rgba(77,121,255,.35);
}
.goal-empty{
  background:var(--surf) !important;
  border:1px dashed var(--b2) !important;
  border-radius:var(--radius-lg) !important;
}

.goal-empty:hover{
  border-color:rgba(14,159,143,.5) !important;
  background:linear-gradient(145deg,rgba(29,185,84,.07) 0%,rgba(29,185,84,.03) 100%) !important;
  box-shadow:0 0 20px rgba(29,185,84,.08);
}

/* ── Top post list ── */
.top-post-list{
  background:var(--surf) !important;
  border:1px solid var(--b) !important;
  box-shadow:0 4px 16px rgba(0,0,0,.15);
  border-radius:var(--radius-lg) !important;
  backdrop-filter:blur(8px);
}

.top-post-item:hover{
  background:var(--bg-hover) !important;
}

.rank-1{
  background:linear-gradient(135deg,var(--accent),var(--accent-2));
  box-shadow:0 4px 12px rgba(14,159,143,.4);
}

/* ── Platform compare bars ── */
.plt-compare{
  background:var(--surf) !important;
  border:1px solid var(--b) !important;
  box-shadow:0 2px 12px rgba(0,0,0,.15);
  border-radius:var(--radius-lg) !important;
  backdrop-filter:blur(8px);
}


/* ── Segmented controls ── */
.seg{
  background:rgba(255,255,255,.05);
  border:1px solid var(--b);
  border-radius:10px;
  padding:3px;
  gap:2px;
}

.seg-btn{
  border-radius:8px;
  font-weight:600;
  transition:all .15s;
}
.seg-btn.act,.seg-btn.tt-act,.seg-btn.ig-act{
  background:var(--bg) !important;
  box-shadow:0 2px 8px rgba(0,0,0,.2) !important;
}


/* ── Week tabs ── */
.week-tab{
  border-radius:10px;
  font-size:12px;
  transition:all .15s;
}
.week-tab.act{
  background:linear-gradient(140deg,var(--accent),var(--accent-2)) !important;
  box-shadow:0 4px 16px rgba(14,159,143,.35) !important;
}
.week-tab:hover:not(.act){
  background:rgba(255,255,255,.08) !important;
}


/* ── Metric pills ── */
.metric-pill{
  border-radius:8px;
  font-size:10px;
  letter-spacing:.01em;
}
.mp-eng{box-shadow:0 0 8px rgba(61,199,183,.2);}
.mp-reach{box-shadow:0 0 8px rgba(77,121,255,.2);}
.mp-er{box-shadow:0 0 8px rgba(255,122,89,.2);}

/* ── Primary action button (add row btn) ── */
.ct-add-row-btn{
  background:linear-gradient(140deg,var(--accent),var(--accent-2)) !important;
  box-shadow:0 4px 16px rgba(14,159,143,.35) !important;
}
.ct-add-row-btn:hover{
  background:linear-gradient(140deg,var(--accent-hover),#ff9070) !important;
  box-shadow:0 6px 20px rgba(14,159,143,.45) !important;
  transform:translateY(-1px) scale(1.02) !important;
}

/* ── Platform tabs in content ── */
.ct-plat-tab.act{
  background:linear-gradient(140deg,var(--accent),var(--accent-2)) !important;
  box-shadow:0 4px 14px rgba(14,159,143,.35) !important;
}
.ct-plat-tab.act.ig{
  background:linear-gradient(135deg,var(--accent-2),#ff9a70) !important;
  box-shadow:0 4px 14px rgba(255,122,89,.35) !important;
}

/* ── Topbar buttons ── */
.topbar-btn{
  background:rgba(255,255,255,.07);
  border:1px solid rgba(255,255,255,.08);
  box-shadow:0 2px 6px rgba(0,0,0,.25);
  transition:all .15s;
}

.topbar-btn:hover{
  background:rgba(255,255,255,.12) !important;
  border-color:rgba(255,255,255,.14);
  box-shadow:0 4px 12px rgba(0,0,0,.35);
  transform:translateY(-1px) scale(1.05) !important;
}


/* ── Save badge with glow ── */
.save-badge{
  box-shadow:0 0 12px rgba(29,185,84,.3);
}

/* ── Leaderboard ── */
.leaderboard{
  background:var(--surf) !important;
  border:1px solid rgba(255,255,255,.07) !important;
  box-shadow:0 2px 12px rgba(0,0,0,.3);
  border-radius:14px !important;
}


/* ── Comparison table ── */
.komp-table th{
  background:rgba(255,255,255,.04) !important;
}

.delta-pos{
  color:var(--green);
  text-shadow:0 0 8px rgba(29,185,84,.3);
}
.delta-neg{
  color:var(--red);
  text-shadow:0 0 8px rgba(255,77,77,.25);
}

/* ── Filter row ── */
.komp-filter-row{
  background:rgba(255,255,255,.03) !important;
  border:1px solid rgba(255,255,255,.08) !important;
  border-radius:10px !important;
}


/* ── Analytics filter ── */
.analytics-filter{
  background:rgba(255,255,255,.03) !important;
  border:1px solid rgba(255,255,255,.08) !important;
  border-radius:10px !important;
}


/* ── Inputs and selects ── */
.af-sel,.date-inp{
  background:rgba(255,255,255,.06) !important;
  border:1px solid rgba(255,255,255,.12) !important;
  border-radius:8px !important;
  padding:5px 10px !important;
  transition:border-color .15s,box-shadow .15s;
}

.af-sel:focus,.date-inp:focus{
  border-color:rgba(29,185,84,.5) !important;
  box-shadow:0 0 0 3px rgba(29,185,84,.1) !important;
}

/* ── Empty banner ── */
.empty-banner{
  border-radius:12px !important;
  border:1px solid rgba(255,164,43,.2) !important;
  background:linear-gradient(135deg,rgba(255,164,43,.08) 0%,rgba(255,164,43,.03) 100%) !important;
  box-shadow:0 0 20px rgba(255,164,43,.06);
}

/* ── Section title ── */
.section-hdr{
  margin-bottom:16px;
}
.section-ttl{
  font-size:10px;
  letter-spacing:.15em;
  position:relative;
}

/* ── Tab strip ── */
.an-tab-strip{
  background:rgba(255,255,255,.03) !important;
  border:1px solid rgba(255,255,255,.07) !important;
  border-radius:12px !important;
  padding:3px !important;
}


/* ── Scrollbar ── */
::-webkit-scrollbar-thumb{
  background:rgba(255,255,255,.15);
}


/* ── Nav footer buttons ── */
.nav-footer-btn{
  border-radius:8px;
  transition:all .15s;
}
.nav-footer-btn:hover{
  background:rgba(255,255,255,.08) !important;
  transform:translateX(2px);
}

/* ── Content view wrap ── */
[data-theme="dark"] .view.content-view-wrap.active{
  background:var(--bg-page) !important;
}
.content-table-wrap{
  border:1px solid rgba(255,255,255,.07) !important;
  border-radius:12px !important;
  overflow:hidden;
  box-shadow:0 2px 12px rgba(0,0,0,.3);
}

.content-table thead th{
  background:rgba(255,255,255,.04) !important;
}


/* ── Modal ── */
.modal-ov{
  background:rgba(0,0,0,.75) !important;
}

/* ── Prop table ── */
.prop-table{
  border:1px solid rgba(255,255,255,.07) !important;
  border-radius:10px !important;
  overflow:hidden;
}

.prop-key{
  background:rgba(255,255,255,.03) !important;
  border-right:1px solid rgba(255,255,255,.07) !important;
}


/* ── Mobile nav glass ── */
.mob-nav{
  background:rgba(18,18,18,.9) !important;
  backdrop-filter:blur(20px) !important;
  -webkit-backdrop-filter:blur(20px) !important;
  border-top:1px solid rgba(255,255,255,.08) !important;
  box-shadow:0 -8px 40px rgba(0,0,0,.5) !important;
}


/* ── Animations ── */
@keyframes fadeUp{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);}}
.kpi-card{animation:fadeUp .35s ease both;}
.kpi-card:nth-child(1){animation-delay:.04s;}
.kpi-card:nth-child(2){animation-delay:.08s;}
.kpi-card:nth-child(3){animation-delay:.12s;}
.kpi-card:nth-child(4){animation-delay:.16s;}
.kpi-card:nth-child(5){animation-delay:.20s;}
.kpi-card:nth-child(6){animation-delay:.24s;}

/* ════════════════════════════
   ✨ MODAL & BUTTON STYLES
   ════════════════════════════ */

/* Desktop modal card */
@media(min-width:769px){
  .modal{
    background:var(--surf2);
    border:1px solid rgba(255,255,255,.1);
    border-radius:20px;
    padding:28px 28px 24px;
    min-width:340px;max-width:520px;width:100%;
    box-shadow:0 24px 80px rgba(0,0,0,.7),0 1px 0 rgba(255,255,255,.06) inset;
    animation:modalIn .22s cubic-bezier(.16,1,.3,1);
    position:relative;
  }
  
  @keyframes modalIn{
    from{opacity:0;transform:scale(.95) translateY(8px);}
    to{opacity:1;transform:scale(1) translateY(0);}
  }
  /* shine on desktop modal */
  .modal::before{
    content:'';position:absolute;top:0;left:24px;right:24px;height:1px;
    background:linear-gradient(90deg,transparent,rgba(255,255,255,.14),transparent);
    border-radius:2px;
  }
  
}

.modal-title{
  font-size:16px;font-weight:800;color:var(--t);
  letter-spacing:-.03em;margin-bottom:20px;
}

.modal-fld{
  display:flex;flex-direction:column;gap:6px;
  margin-bottom:16px;
}
.modal-fld label{
  font-size:11px;font-weight:700;color:var(--t3);
  text-transform:uppercase;letter-spacing:.1em;
}

.modal-inp{
  width:100%;padding:10px 12px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.12);
  border-radius:10px;
  color:var(--t);font-size:14px;font-family:var(--font);
  outline:none;
  transition:border-color .15s,box-shadow .15s,background .15s;
  color-scheme:dark;
}

.modal-inp:focus{
  border-color:rgba(29,185,84,.5);
  box-shadow:0 0 0 3px rgba(29,185,84,.1);
  background:rgba(29,185,84,.04);
}
.modal-inp::placeholder{color:var(--t4);}

.modal-acts{
  display:flex;gap:8px;justify-content:flex-end;
  margin-top:24px;padding-top:16px;
  border-top:1px solid rgba(255,255,255,.07);
}


/* Cancel button */
.btn-c{
  padding:9px 18px;border-radius:10px;border:1px solid rgba(255,255,255,.1);
  background:rgba(255,255,255,.05);color:var(--t2);
  font-size:13px;font-weight:700;font-family:var(--font);
  cursor:pointer;transition:all .15s;
}

.btn-c:hover{background:rgba(255,255,255,.1);color:var(--t);}


/* OK/primary button */
.btn-ok{
  padding:9px 20px;border-radius:10px;border:none;
  background:linear-gradient(140deg,var(--accent),var(--accent-2));
  color:#000;font-size:13px;font-weight:800;font-family:var(--font);
  cursor:pointer;
  box-shadow:0 4px 14px rgba(29,185,84,.35),0 1px 0 rgba(255,255,255,.2) inset;
  transition:all .15s;
}
.btn-ok:hover{
  background:linear-gradient(140deg,var(--accent-hover),var(--accent-2));
  box-shadow:0 6px 20px rgba(29,185,84,.45),0 1px 0 rgba(255,255,255,.2) inset;
  transform:translateY(-1px);
}
.btn-ok:active{transform:translateY(0);box-shadow:0 2px 8px rgba(29,185,84,.3);}

/* Danger button */
.btn-danger{
  padding:9px 20px;border-radius:10px;border:none;
  background:linear-gradient(135deg,#FF4D4D 0%,#cc2b2b 100%);
  color:#fff;font-size:13px;font-weight:800;font-family:var(--font);
  cursor:pointer;
  box-shadow:0 4px 14px rgba(255,77,77,.35),0 1px 0 rgba(255,255,255,.15) inset;
  transition:all .15s;
}
.btn-danger:hover{
  background:linear-gradient(135deg,#ff6b6b 0%,#FF4D4D 100%);
  box-shadow:0 6px 20px rgba(255,77,77,.45);
  transform:translateY(-1px);
}

/* ── Topbar title with gradient ── */
.topbar-title{
  background:linear-gradient(135deg,var(--t) 0%,var(--t2) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}
[data-theme="dark"] .topbar-title{
  background:linear-gradient(135deg,#fff 40%,#9a9a9a 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}

/* ── Tree rows enhanced ── */
.tree-row.act{
  background:linear-gradient(90deg,rgba(29,185,84,.15) 0%,rgba(29,185,84,.05) 100%);
  color:var(--accent);
  font-weight:700;
}

/* ── Account items enhanced ── */
.acc-item.act{
  background:linear-gradient(90deg,rgba(29,185,84,.12) 0%,rgba(29,185,84,.04) 100%);
  border-radius:8px;
}

/* ── Section title accent ── */
.section-ttl{
  background:linear-gradient(90deg,var(--t3),var(--t4));
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}

/* ── Overview card tools dropdown ── */
.overview-card-tools select,
.overview-card-tools .seg{
  border-radius:8px;
}

/* ── Content table sticky col shine ── */
.content-table thead th:first-child{
  background:rgba(255,255,255,.05) !important;
}

/* ── Page view padding refinement ── */
@media(min-width:769px){
  .view{padding:24px 28px 80px;}
}

/* ── Pillar chips improved ── */
.pillar-chip{
  border-radius:6px;
  font-size:11px;
  font-weight:700;
}

/* ── Empty state prettier ── */
.placeholder{
  background:linear-gradient(145deg,rgba(255,255,255,.02) 0%,transparent 100%);
  border-radius:16px;
}
.ph-ico{font-size:44px;opacity:.12;}

/* ── LB rank badge ── */
.lb-bar-fill{
  background:linear-gradient(90deg,var(--accent),#0ea84a);
  box-shadow:0 0 6px rgba(29,185,84,.3);
}

/* ── SVG icon alignment fixes ── */
.nav-item-ico{
  display:flex;align-items:center;justify-content:center;
  width:18px;height:18px;flex-shrink:0;
}
.nav-item-ico svg{display:block;}
.nav-footer-btn span{
  display:flex;align-items:center;justify-content:center;
  width:16px;height:16px;flex-shrink:0;
}
.nav-footer-btn span svg{display:block;}

/* ── Save badge flex row ── */
.save-badge{
  display:flex !important;align-items:center;gap:5px;
}

/* ── Theme toggle SVG icon state ── */
[data-theme="dark"] .topbar-btn svg path,
[data-theme="dark"] .topbar-btn svg circle{
  stroke:rgba(255,255,255,.7);
}


/* ── Mobile nav active dot ── */
.mnav.act::after{
  background:linear-gradient(90deg,var(--accent),#0ea84a);
  box-shadow:0 0 6px rgba(29,185,84,.4);
}

/* ── Content months panel ── */
.content-months{
  background:linear-gradient(180deg,#141414 0%,#111 100%) !important;
  border-right:1px solid rgba(255,255,255,.06) !important;
}

.content-months-hdr{
  border-bottom:1px solid rgba(255,255,255,.06) !important;
  font-size:10px;letter-spacing:.12em;
  background:rgba(255,255,255,.02);
}

.content-months-foot{
  border-top:1px solid rgba(255,255,255,.06) !important;
  padding:10px;
}


/* ── Account panel ── */
.content-accs{
  border-right:1px solid rgba(255,255,255,.06) !important;
}

.content-accs-hdr{
  border-bottom:1px solid rgba(255,255,255,.06) !important;
  font-size:10px;letter-spacing:.08em;font-weight:700;color:var(--t3);
  background:rgba(255,255,255,.02);
}


/* ── Add button ── */
.add-btn{
  background:linear-gradient(135deg,rgba(29,185,84,.15),rgba(29,185,84,.06)) !important;
  border:1px solid rgba(29,185,84,.25) !important;
  color:var(--accent) !important;
  border-radius:8px !important;
  font-weight:700;
  transition:all .15s !important;
}
.add-btn:hover{
  background:linear-gradient(135deg,rgba(29,185,84,.25),rgba(29,185,84,.12)) !important;
  border-color:rgba(29,185,84,.4) !important;
  box-shadow:0 4px 12px rgba(29,185,84,.2) !important;
  transform:translateY(-1px) !important;
}

/* ── Tree month row ── */
.tree-month{
  font-weight:700;letter-spacing:.01em;
}
.tree-month:hover{
  background:rgba(255,255,255,.05) !important;
}


/* ── Week tabs grid ── */
.week-tabs{gap:6px;}
.week-tab{
  border:1px solid rgba(255,255,255,.08) !important;
}

.week-tab:not(.act){
  background:rgba(255,255,255,.03) !important;
}


/* ── Komp table row hover ── */
.komp-row:hover td{
  background:rgba(255,255,255,.03) !important;
}


/* ── Content row hover ── */
.content-row:hover td{
  background:rgba(255,255,255,.03) !important;
}


/* ── Nav sidebar scrollbar ── */
.nav-body::-webkit-scrollbar{width:4px;}
.nav-body::-webkit-scrollbar-track{background:transparent;}
.nav-body::-webkit-scrollbar-thumb{
  background:rgba(255,255,255,.08);border-radius:4px;
}
.nav-body::-webkit-scrollbar-thumb:hover{
  background:rgba(255,255,255,.15);
}

/* ── Global smooth scroll ── */
*{scroll-behavior:smooth;}

/* ── Focus visible ring ── */
button:focus-visible,input:focus-visible,select:focus-visible{
  outline:2px solid rgba(29,185,84,.5);
  outline-offset:2px;
}

/* ══ SPRINT 2: REPORTS VIEW CSS ══ */

/* ── Report filter bar ── */
.rpt-filter-bar{
  display:flex;align-items:center;gap:10px;flex-wrap:wrap;
  padding:14px 18px;
  background:rgba(255,255,255,.03);
  border:1px solid rgba(255,255,255,.08);
  border-radius:12px;margin-bottom:20px;
}

.rpt-filter-label{
  font-size:10px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.12em;
}

/* ── Report body (exportable) ── */
#report-body{
  background:var(--bg-page);
  padding:28px;
  border-radius:16px;
  border:1px solid rgba(255,255,255,.06);
}


/* ── Report brand header ── */
.rpt-exec-summary{
  background:var(--sp-card);border-radius:var(--radius-lg);
  padding:24px 28px;margin-bottom:20px;
  border-left:4px solid var(--accent);
}

.rpt-exec-badge{
  font-size:10px;font-weight:800;color:var(--accent);
  text-transform:uppercase;letter-spacing:.12em;margin-bottom:12px;
}
.rpt-exec-text{
  font-size:14px;color:var(--t);line-height:1.7;font-weight:400;
}
.rpt-recs-list{display:flex;flex-direction:column;gap:10px;}
.rpt-rec-item{
  display:flex;align-items:flex-start;gap:12px;
  padding:14px 16px;border-radius:var(--radius);
  background:var(--bg-hover);
}

.rpt-rec-ico{font-size:18px;flex-shrink:0;margin-top:1px;}
.rpt-rec-text{font-size:13px;color:var(--t);line-height:1.6;}
.rpt-header{
  display:flex;align-items:center;justify-content:space-between;
  margin-bottom:28px;
  padding-bottom:20px;
  border-bottom:1px solid var(--b);
}
.rpt-brand{
  display:flex;align-items:center;gap:12px;
}
.rpt-brand-mark{
  width:36px;height:36px;border-radius:9px;
  background:linear-gradient(140deg,var(--accent),var(--accent-2));
  display:flex;align-items:center;justify-content:center;
  font-size:18px;flex-shrink:0;
  box-shadow:0 4px 12px rgba(29,185,84,.3);
}
.rpt-brand-name{
  font-size:18px;font-weight:900;color:var(--t);
  letter-spacing:-.04em;
}
.rpt-brand-sub{font-size:11px;color:var(--t3);font-weight:600;margin-top:2px;}
.rpt-period-badge{
  font-size:12px;font-weight:700;
  padding:6px 14px;border-radius:20px;
  background:var(--accent-dim);color:var(--accent);
  border:1px solid rgba(29,185,84,.2);
}

/* ── Report section ── */
.rpt-section{margin-bottom:28px;}
.rpt-section-title{
  font-size:11px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.15em;
  margin-bottom:14px;
  display:flex;align-items:center;gap:8px;
}
.rpt-section-title::after{
  content:'';flex:1;height:1px;background:var(--b);
}

/* ── Key Metrics KPI grid ── */
.rpt-kpi-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(150px,1fr));
  gap:10px;
}
.rpt-kpi-card{
  background:linear-gradient(145deg,rgba(30,30,30,.8),rgba(22,22,22,.9));
  border:1px solid rgba(255,255,255,.07);
  border-radius:12px;padding:16px 16px 14px;
  position:relative;overflow:hidden;
}

.rpt-kpi-card::before{
  content:'';position:absolute;left:0;top:0;bottom:0;width:3px;
  background:var(--rpt-accent,var(--accent));
  border-radius:12px 0 0 12px;
  box-shadow:0 0 8px var(--rpt-accent,var(--accent));
}
.rpt-kpi-label{
  font-size:11px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.1em;margin-bottom:6px;
}
.rpt-kpi-value{
  font-size:24px;font-weight:900;font-family:var(--mono);
  color:var(--t);letter-spacing:-.04em;line-height:1;
  margin-bottom:6px;
}
.rpt-kpi-delta{
  font-size:10.5px;font-weight:700;font-family:var(--mono);
  display:inline-flex;align-items:center;gap:3px;
  padding:2px 7px;border-radius:20px;
}
.rpt-delta-pos{background:rgba(29,185,84,.12);color:var(--green);}
.rpt-delta-neg{background:rgba(255,77,77,.12);color:var(--red);}
.rpt-delta-neu{background:rgba(255,255,255,.06);color:var(--t3);}


/* ── Platform performance cards ── */
.rpt-plt-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(200px,1fr));
  gap:12px;
}
.rpt-plt-card{
  background:linear-gradient(145deg,rgba(28,28,28,.9),rgba(20,20,20,.95));
  border:1px solid rgba(255,255,255,.08);
  border-radius:14px;padding:18px;
  position:relative;overflow:hidden;
}

.rpt-plt-card-top{
  display:flex;align-items:center;gap:10px;margin-bottom:14px;
  padding-bottom:12px;border-bottom:1px solid var(--b);
}
.rpt-plt-icon{font-size:22px;}
.rpt-plt-name{font-size:14px;font-weight:800;color:var(--t);}
.rpt-plt-post-count{
  margin-left:auto;font-size:11px;font-family:var(--mono);font-weight:700;
  padding:2px 8px;border-radius:20px;
  background:var(--accent-dim);color:var(--accent);
}
.rpt-plt-stats{display:flex;flex-direction:column;gap:7px;}
.rpt-plt-stat-row{display:flex;align-items:center;justify-content:space-between;}
.rpt-plt-stat-lbl{font-size:11px;color:var(--t3);font-weight:600;}
.rpt-plt-stat-val{font-size:12px;font-family:var(--mono);font-weight:700;color:var(--t);}
.rpt-plt-er-badge{
  display:inline-flex;align-items:center;gap:5px;
  font-size:11px;font-weight:700;
  padding:3px 9px;border-radius:20px;margin-top:10px;
}

/* ── Top posts in report ── */
.rpt-post-list{display:flex;flex-direction:column;gap:8px;}
.rpt-post-row{
  display:flex;align-items:center;gap:12px;
  padding:12px 16px;
  background:rgba(255,255,255,.03);
  border:1px solid rgba(255,255,255,.06);
  border-radius:10px;
  transition:background .12s;
}

.rpt-post-row:hover{background:rgba(255,255,255,.06);}

.rpt-post-rank{
  width:26px;height:26px;border-radius:7px;flex-shrink:0;
  display:flex;align-items:center;justify-content:center;
  font-size:11px;font-weight:900;font-family:var(--mono);
}
.rpt-rank-1{background:var(--accent);color:#000;box-shadow:0 2px 8px rgba(29,185,84,.4);}
.rpt-rank-2{background:rgba(255,255,255,.12);color:var(--t2);}
.rpt-rank-3{background:rgba(255,255,255,.07);color:var(--t3);}
.rpt-rank-n{background:transparent;color:var(--t4);font-size:12px;}


.rpt-post-info{flex:1;min-width:0;}
.rpt-post-name{
  font-size:12.5px;font-weight:700;color:var(--t);
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  margin-bottom:2px;
}
.rpt-post-meta{
  font-size:10px;color:var(--t3);font-weight:600;
  display:flex;align-items:center;gap:6px;
}
.rpt-post-metrics{
  display:flex;align-items:center;gap:6px;flex-shrink:0;flex-wrap:wrap;justify-content:flex-end;
}

/* ── Pillar breakdown in report ── */
.rpt-pillar-list{display:flex;flex-direction:column;gap:8px;}
.rpt-pillar-row{display:flex;align-items:center;gap:10px;}
.rpt-pillar-label{
  width:100px;flex-shrink:0;
  font-size:12px;font-weight:700;color:var(--t2);
  display:flex;align-items:center;gap:5px;
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
}
.rpt-pillar-bar-wrap{
  flex:1;height:6px;background:rgba(255,255,255,.07);
  border-radius:20px;overflow:hidden;
}

.rpt-pillar-bar-fill{height:100%;border-radius:20px;transition:width .7s cubic-bezier(.4,0,.2,1);}
.rpt-pillar-val{
  width:60px;flex-shrink:0;text-align:right;
  font-size:11px;font-family:var(--mono);font-weight:700;color:var(--t2);
}
.rpt-pillar-count{
  width:36px;flex-shrink:0;text-align:right;
  font-size:10px;color:var(--t3);font-weight:600;
}

/* ── Key highlights ── */
.rpt-highlights-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(200px,1fr));
  gap:10px;
}
.rpt-highlight-card{
  background:rgba(255,255,255,.03);
  border:1px solid rgba(255,255,255,.07);
  border-radius:12px;padding:14px 16px;
}

.rpt-hl-badge{
  font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.1em;
  color:var(--t3);margin-bottom:6px;
  display:flex;align-items:center;gap:5px;
}
.rpt-hl-value{
  font-size:18px;font-weight:900;font-family:var(--mono);color:var(--t);
  margin-bottom:4px;letter-spacing:-.03em;
}
.rpt-hl-sub{font-size:10.5px;color:var(--t3);font-weight:600;}

/* ── Content Performance ── */
.rpt-cp-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(200px,1fr));
  gap:12px;
}
.rpt-cp-stat{
  background:rgba(255,255,255,.03);
  border:1px solid rgba(255,255,255,.07);
  border-radius:14px;padding:16px;
}

.rpt-cp-lbl{
  font-size:10px;font-weight:700;text-transform:uppercase;
  letter-spacing:.1em;color:var(--t3);margin-bottom:8px;
}
.rpt-cp-val{
  font-size:24px;font-weight:900;font-family:var(--mono);
  color:var(--t);letter-spacing:-.04em;margin-bottom:4px;
}
.rpt-cp-sub{font-size:11px;color:var(--t3);}
.rpt-cp-score-row{
  display:flex;gap:8px;margin-top:4px;
}
.rpt-score-badge{
  flex:1;display:flex;flex-direction:column;align-items:center;
  gap:2px;padding:8px 4px;border-radius:10px;font-size:11px;font-weight:700;
}

/* ── Comparison selectors ── */
.rpt-comp-selectors{
  display:grid;grid-template-columns:1fr auto 1fr;
  align-items:center;gap:12px;margin-bottom:14px;
}
.rpt-comp-sel-group{display:flex;flex-direction:column;gap:6px;}
.rpt-comp-sel-label{
  font-size:10px;font-weight:800;text-transform:uppercase;
  letter-spacing:.12em;
}
.rpt-comp-vs{
  font-size:13px;font-weight:900;color:var(--t3);
  text-align:center;letter-spacing:.1em;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.1);
  border-radius:8px;padding:6px 10px;
}


/* ── Comparison table ── */
.comp-wrap{overflow-x:auto;border-radius:12px;border:1px solid rgba(255,255,255,.07);}

.comp-table{width:100%;border-collapse:collapse;font-size:12px;}
.comp-th{
  padding:10px 14px;text-align:left;
  font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;
  color:var(--t3);background:rgba(255,255,255,.04);
  border-bottom:1px solid rgba(255,255,255,.07);
}

.comp-row:not(:last-child) td{border-bottom:1px solid rgba(255,255,255,.04);}

.comp-row:hover td{background:rgba(255,255,255,.03);}

.comp-metric{
  padding:10px 14px;color:var(--t2);font-size:12px;font-weight:600;
}
.comp-val{
  padding:10px 14px;color:var(--t);font-size:12px;font-weight:700;
}
.comp-val-win{color:var(--accent)!important;font-weight:900!important;}
.comp-delta{
  padding:10px 14px;font-size:11px;font-weight:700;
  font-family:var(--mono);
}
.comp-delta-pos{color:var(--green);}
.comp-delta-neg{color:var(--red);}
.comp-delta-neu{color:var(--t3);}

/* ── No-export sections (hidden in PDF) ── */
.rpt-section-noexport{position:relative;}

/* ── Report: No data state ── */
.rpt-export-bar{
  display:flex;align-items:center;gap:8px;
  margin-bottom:20px;flex-wrap:wrap;
}
.rpt-export-btn{
  display:flex;align-items:center;gap:7px;
  padding:9px 18px;border-radius:20px;border:none;
  font-size:13px;font-weight:800;font-family:var(--font);
  cursor:pointer;transition:all .15s;
}
.rpt-export-btn-primary{
  background:var(--accent);color:#000;
  box-shadow:0 4px 14px rgba(29,185,84,.35);
}
.rpt-export-btn-primary:hover{background:var(--accent-hover);transform:translateY(-1px);}
.rpt-export-btn-sec{
  background:rgba(255,255,255,.08);color:var(--t2);
}

.rpt-export-btn-sec:hover{background:rgba(255,255,255,.14);color:var(--t);}

.rpt-export-spinner{
  display:none;width:14px;height:14px;
  border:2px solid rgba(0,0,0,.2);border-top-color:#000;
  border-radius:50%;animation:spin .6s linear infinite;
}
@keyframes spin{to{transform:rotate(360deg);}}

/* ── Report: No data state ── */
.rpt-empty{
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  padding:60px 20px;gap:10px;text-align:center;
}
.rpt-empty-ico{font-size:48px;opacity:.12;}
.rpt-empty-title{font-size:15px;font-weight:800;color:var(--t2);}
.rpt-empty-sub{font-size:13px;color:var(--t3);}

/* ══ SPRINT 4: posted_at CSS ══ */
.ct-inp-time{
  width:90px;
  padding:4px 6px;
  font-family:var(--mono);
  font-size:11px;
  background:rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.1);
  border-radius:6px;
  color:var(--t);
  cursor:pointer;
}

.ct-inp-time:focus{
  border-color:rgba(0,232,122,.4);
  outline:none;
  box-shadow:0 0 0 2px rgba(0,232,122,.1);
}
.ct-time-cell{
  display:flex;flex-direction:column;gap:3px;
  align-items:flex-start;
}
.ct-time-badge{
  font-size:11px;font-weight:700;
  color:var(--accent);
  background:rgba(0,232,122,.1);
  border:1px solid rgba(0,232,122,.2);
  padding:1px 5px;border-radius:4px;
  white-space:nowrap;
  letter-spacing:.02em;
}
.ct-time-empty{
  font-size:10px;color:var(--t4);
  padding:1px 5px;
}

/* ── posted_at info banner ── */
.posted-at-banner{
  display:flex;align-items:center;gap:8px;
  padding:8px 12px;margin-bottom:10px;
  background:rgba(0,232,122,.05);
  border:1px solid rgba(0,232,122,.12);
  border-radius:8px;font-size:11px;color:var(--t2);
}
.posted-at-banner b{color:var(--accent);}

.ins-layout{display:flex;flex-direction:column;gap:20px;}
.ins-section{
  background:linear-gradient(145deg,rgba(22,22,34,.9),rgba(14,14,22,.95));
  border:1px solid rgba(255,255,255,.07);border-radius:16px;padding:20px;
}

.ins-section-head{margin-bottom:14px;}
.ins-section-title{font-family:'Syne',var(--font);font-size:14px;font-weight:700;letter-spacing:-.02em;color:var(--t);margin-bottom:3px;}
.ins-section-sub{font-size:11px;color:var(--t3);}
.ins-two-col{display:grid;grid-template-columns:1fr 1fr;gap:20px;}
@media(max-width:768px){.ins-two-col{grid-template-columns:1fr;}}
.ins-pattern-card{background:rgba(0,232,122,.04);border:1px solid rgba(0,232,122,.12);border-radius:14px;padding:16px;}

.ins-pattern-head{display:flex;align-items:flex-start;gap:10px;margin-bottom:14px;}
.ins-pattern-icon{font-size:24px;flex-shrink:0;}
.ins-pattern-title{font-size:14px;font-weight:800;color:var(--t);letter-spacing:-.02em;}
.ins-pattern-sub{font-size:11px;color:var(--t3);margin-top:2px;}
.ins-pattern-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(160px,1fr));gap:10px;margin-bottom:14px;}
.ins-pattern-item{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.07);border-radius:10px;padding:11px 12px;}

.ins-pi-label{font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);margin-bottom:5px;}
.ins-pi-val{font-size:15px;font-weight:700;color:var(--t);margin-bottom:3px;letter-spacing:-.02em;}
.ins-pi-sub{font-size:10px;color:var(--t3);margin-bottom:5px;}
.ins-pi-bar{height:3px;background:rgba(255,255,255,.06);border-radius:2px;overflow:hidden;}

.ins-pattern-summary{font-size:12px;color:var(--t2);line-height:1.6;padding:10px 14px;background:rgba(255,255,255,.03);border-radius:8px;border-left:3px solid var(--accent);}

.ins-empty{text-align:center;padding:28px;color:var(--t3);font-size:12px;}
.ins-day-table{display:flex;flex-direction:column;gap:4px;}
.ins-day-row{display:grid;grid-template-columns:24px 80px 50px 1fr 90px;align-items:center;gap:8px;padding:8px 10px;border-radius:8px;transition:background .12s;}
.ins-day-row:hover{background:rgba(255,255,255,.04);}

.ins-day-top{background:rgba(0,232,122,.06)!important;border:1px solid rgba(0,232,122,.12);}

.ins-day-rank{font-size:11px;font-weight:700;color:var(--t3);text-align:center;}
.ins-day-name{font-size:12px;font-weight:700;color:var(--t);}
.ins-day-posts{font-size:11px;color:var(--t3);font-family:var(--mono);}
.ins-day-er{font-size:12px;font-weight:700;font-family:var(--mono);}
.ins-day-badge{font-size:11px;font-weight:700;padding:2px 7px;border-radius:20px;text-align:center;}
.ins-time-list{display:flex;flex-direction:column;gap:6px;}
.ins-time-row{display:grid;grid-template-columns:48px 60px 1fr 100px auto;align-items:center;gap:8px;padding:8px 10px;border-radius:8px;}
.ins-time-row:hover{background:rgba(255,255,255,.04);}

.ins-time-top{background:rgba(0,232,122,.06)!important;border:1px solid rgba(0,232,122,.12);}

.ins-time-hour{font-family:var(--mono);font-size:13px;font-weight:700;color:var(--t);}
.ins-time-period{font-size:10px;color:var(--t3);font-weight:600;}
.ins-time-bar-wrap{height:4px;background:rgba(255,255,255,.06);border-radius:2px;overflow:hidden;}

.ins-time-bar{height:4px;border-radius:2px;transition:width .3s ease;}
.ins-time-meta{font-size:11px;text-align:right;}
.ins-time-badge{font-size:11px;font-weight:800;padding:2px 7px;border-radius:20px;background:rgba(0,232,122,.12);color:var(--accent);white-space:nowrap;}
.ins-streak-row{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;}
@media(max-width:600px){.ins-streak-row{grid-template-columns:repeat(2,1fr);}}
.ins-streak-item{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:14px 12px;text-align:center;}

.ins-streak-fire{background:rgba(0,232,122,.08)!important;border-color:rgba(0,232,122,.2)!important;}
.ins-streak-ico{font-size:20px;margin-bottom:6px;}
.ins-streak-val{font-family:var(--mono);font-size:22px;font-weight:700;color:var(--t);letter-spacing:-.04em;}
.ins-streak-lbl{font-size:10px;color:var(--t3);margin-top:3px;font-weight:600;}
.tab-content{animation:fadeUp .3s cubic-bezier(.16,1,.3,1) both;}

/* ══ GEMINI REDESIGN: NEW COMPONENTS ══ */

/* ── Content Status Badges ── */
.content-status-badge{
  display:inline-flex;align-items:center;gap:4px;
  font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;
  padding:3px 9px;border-radius:20px;
}
.timeline-card-status{padding:0 0 8px 0;}
.status-viral{
  background:rgba(34,197,94,0.12);
  color:#22C55E;
  border:1px solid rgba(34,197,94,0.2);
}
.status-normal{
  background:rgba(99,102,241,0.12);
  color:#818CF8;
  border:1px solid rgba(99,102,241,0.2);
}
.status-attention{
  background:rgba(239,68,68,0.12);
  color:#F87171;
  border:1px solid rgba(239,68,68,0.2);
}

/* ── Goal Status Badges ── */
.goal-status-badge{
  font-size:11px;font-weight:800;letter-spacing:.07em;text-transform:uppercase;
  padding:3px 9px;border-radius:20px;margin-left:auto;
  white-space:nowrap;flex-shrink:0;
}
.goal-status-done{
  background:rgba(34,197,94,0.12);
  color:#22C55E;
  border:1px solid rgba(34,197,94,0.2);
}
.goal-status-progress{
  background:rgba(99,102,241,0.12);
  color:#818CF8;
  border:1px solid rgba(99,102,241,0.2);
}
.goal-status-behind{
  background:rgba(239,68,68,0.12);
  color:#EF4444;
  border:1px solid rgba(239,68,68,0.2);
}

/* ── Topbar: Live Analytics badge ── */
.topbar-live-badge{
  display:inline-flex;align-items:center;gap:5px;
  font-size:10px;font-weight:700;
  padding:3px 10px;border-radius:20px;
  background:rgba(34,197,94,0.12);
  color:#22C55E;
  border:1px solid rgba(34,197,94,0.2);
}
.topbar-live-dot{
  width:6px;height:6px;border-radius:50%;
  background:#22C55E;
  animation:livePulse 2s ease-in-out infinite;
}
@keyframes livePulse{0%,100%{opacity:1;transform:scale(1);}50%{opacity:.4;transform:scale(.7);}}
.topbar-live-sub{
  font-size:10px;color:var(--t3);margin-left:4px;
}

/* ── Dashboard "Total Pengikut Gabungan" header stat ── */
.dash-total-followers{
  display:flex;flex-direction:column;align-items:flex-end;
  padding:0 2px;
}
.dash-total-followers-label{
  font-size:11px;font-weight:700;text-transform:uppercase;
  letter-spacing:.1em;color:var(--t3);
}
.dash-total-followers-value{
  font-family:var(--mono);font-size:22px;font-weight:700;
  color:var(--t);letter-spacing:-.04em;line-height:1.1;
}

/* ── Goal card top row fix ── */
.goal-card-top{
  display:flex;align-items:center;gap:10px;
  margin-bottom:8px;flex-wrap:wrap;
}

/* ── Nav plt row updated dot style ── */
.nav-plt-dot{
  width:7px;height:7px;border-radius:50%;flex-shrink:0;
}

/* ── Override btn-ok for indigo ── */
.btn-ok{
  background:linear-gradient(140deg,var(--accent),var(--accent-2)) !important;
  color:#fff !important;
  box-shadow:0 4px 16px rgba(99,102,241,0.35),0 1px 0 rgba(255,255,255,0.15) inset !important;
}
.btn-ok:hover{
  background:linear-gradient(140deg,var(--accent-hover),var(--accent-2)) !important;
  box-shadow:0 8px 24px rgba(99,102,241,0.45) !important;
  transform:translateY(-2px) !important;
}

/* ── topbar title updated gradient ── */
.topbar-title{
  font-size:14px !important;
  font-weight:600 !important;
  color:var(--t2) !important;
  letter-spacing:-.01em !important;
  background:none !important;
  -webkit-background-clip:unset !important;
  -webkit-text-fill-color:unset !important;
  background-clip:unset !important;
}

/* ── rpt-slide top bar → indigo ── */
.rpt-slide::before{
  background:linear-gradient(90deg,var(--accent),var(--accent-2)) !important;
}
.rpt-slide-num{
  background:rgba(99,102,241,0.12) !important;
  color:#818CF8 !important;
  border:1px solid rgba(99,102,241,0.2) !important;
}




/* ══ SPRINT 7: MOBILE POST CARDS ══ */
function renderMobilePostCards(posts, plt, wk, acc, flw) {
  if (!posts.length) return `
    <div class="ct-empty" style="padding:40px 20px;">
      <div style="font-size:32px;margin-bottom:12px;opacity:.3;">📱</div>
      <div style="font-size:14px;color:var(--t3);margin-bottom:8px;">Belum ada post</div>
      <div style="font-size:12px;color:var(--t4);">Ketuk tombol ＋ untuk input post baru</div>
    </div>`;

  return '<div class="mob-post-cards">' + posts.map((p, i) => {
    const er = calcER(p, sel.erMode, flw);
    const lvl = erLevel(er);
    const pil = pillarOf(p.pillar || '');
    const score = computeContentScore(p, getAllPosts());
    const displayName = p.name || p.link?.replace(/^https?:\/\//, '').slice(0,40) || `Post ${i+1}`;
    const hasAnyData = (p.reach||p.impression||p.like||p.comment||p.share||p.save) > 0;
    let statusColor = 'var(--t4)', statusLabel = '';
    if (score >= 80 || er >= 5) { statusColor = 'var(--green)'; statusLabel = '🔥 Viral'; }
    else if (score >= 60 || er >= 3) { statusColor = 'var(--accent)'; statusLabel = '✅ Normal'; }
    else if (hasAnyData) { statusColor = 'var(--amber)'; statusLabel = '⚠️ Perhatian'; }

    return `<div class="mob-post-card" data-pid="${p.id}" data-plt="${plt}">
      <div class="mob-post-card-inner">
      <div class="mob-post-card-top">
        <div class="mob-post-card-meta" style="min-width:0;flex:1;">
          <div class="mob-post-card-name">${displayName}</div>
          <div class="mob-post-card-date" style="display:flex;align-items:center;gap:4px;flex-wrap:wrap;">
            <span>${p.date || '—'}</span>
            ${pil.id !== 'other' ? `<span style="color:var(--t4);">·</span><span style="color:${pil.color};font-size:10px;font-weight:700;">${pil.emoji} ${pil.label}</span>` : ''}
            ${statusLabel ? `<span style="color:var(--t4);">·</span><span style="color:${statusColor};font-size:10px;font-weight:800;">${statusLabel}</span>` : ''}
          </div>
        </div>
        <div style="display:flex;align-items:center;gap:6px;flex-shrink:0;">
          ${er > 0 ? `<span style="font-family:var(--mono);font-size:12px;font-weight:800;color:${lvl.color};background:${lvl.bg};padding:3px 8px;border-radius:8px;">${erStr(er)}</span>` : ''}
          ${p.link ? `<a href="${p.link}" target="_blank" style="font-size:11px;color:var(--blue);padding:4px 8px;border-radius:6px;background:var(--blue-bg);text-decoration:none;font-weight:700;">↗</a>` : ''}
        </div>
      </div>
      <div class="mob-post-card-metrics" style="grid-template-columns:repeat(5,1fr);gap:6px;">
        <div class="mob-metric-cell">
          <span class="mob-metric-val">${fmt(p.like||0)}</span>
          <span class="mob-metric-lbl">❤️</span>
        </div>
        <div class="mob-metric-cell">
          <span class="mob-metric-val">${fmt(p.comment||0)}</span>
          <span class="mob-metric-lbl">💬</span>
        </div>
        <div class="mob-metric-cell">
          <span class="mob-metric-val">${fmt(p.share||0)}</span>
          <span class="mob-metric-lbl">🔁</span>
        </div>
        <div class="mob-metric-cell">
          <span class="mob-metric-val">${fmt(p.save||0)}</span>
          <span class="mob-metric-lbl">🔖</span>
        </div>
        <div class="mob-metric-cell">
          <span class="mob-metric-val">${fmt(p.reach||p.impression||0)}</span>
          <span class="mob-metric-lbl">👁</span>
        </div>
      </div>
      <div class="mob-post-card-actions">
        <button class="mob-post-card-del" onclick="deletePost('${p.id}','${plt}')">🗑</button>
        <button style="flex:1;padding:7px 10px;border-radius:8px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:12px;font-weight:600;cursor:pointer;"
          onclick="editPostMobile('${p.id}','${plt}')">✏ Edit Angka</button>
        ${!hasAnyData ? `<button style="flex:1;padding:7px 10px;border-radius:8px;border:1px solid var(--accent);background:var(--accent-dim);color:var(--accent);font-size:12px;font-weight:700;cursor:pointer;"
          onclick="editPostMobile('${p.id}','${plt}')">+ Input Data</button>` : ''}
      </div>
      </div>
      <div class="mob-swipe-delete" onclick="deletePost('${p.id}','${plt}')">
        <span style="font-size:20px;">🗑</span>
        <span>Hapus</span>
      </div>
    </div>`;
  }).join('') + '</div>';
  setTimeout(initSwipeToDelete, 20);
}

function deletePost(pid, plt) {
  showConfirm('Hapus Post Ini?', 'Data engagement dan metrik post ini akan hilang permanen.', () => {
  const stashed = _stashPost(pid);
  const wk = getW(sel.mid, sel.wid);
  if (!wk) return;
  const d = aD(wk, sel.acc);
  const key = plt + 'Posts';
  if (!d[key]) return;
  d[key] = d[key].filter(p => p.id !== pid);
  scheduleSave(300);
  if(view==='content')renderContentTimeline();
  if (view === 'dash') renderDashboard();
  _qcToastUndo('🗑 Post dihapus.', () => {
    if (stashed) { _restorePost(stashed); if(view==='content')renderContentTimeline(); if(view==='dash')renderDashboard(); }
  });
  }, true);
}

function editPostMobile(pid, plt) {
  const wk = getW(sel.mid, sel.wid);
  if (!wk) return;
  const d = aD(wk, sel.acc);
  const key = plt + 'Posts';
  const p = (d[key]||[]).find(x => x.id === pid);
  if (!p) return;

  // Open QC modal in EDIT mode
  openQCModal();
  // Store edit context BEFORE setTimeout so it's set immediately
  window._qcEditId  = pid;
  window._qcEditPlt = plt;

  setTimeout(() => {
    // Pre-fill all fields from existing post
    const set = (id, v) => { const el=document.getElementById(id); if(el) el.value=v??''; };
    set('qc-name',      p.name);
    set('qc-date',      p.date);
    set('qc-like',      p.like||0);
    set('qc-comment',   p.comment||0);
    set('qc-share',     p.share||0);
    set('qc-save',      p.save||0);
    set('qc-reach',     p.reach||0);
    set('qc-impression',p.impression||0);
    set('qc-format',    p.format||'');
    const pillarSel = document.getElementById('qc-pillar');
    if (pillarSel) pillarSel.value = p.pillar || pillars[0]?.id || '';
    // Set platform button
    const pltBtns = document.querySelectorAll('.qc-plt-btn');
    pltBtns.forEach(btn => btn.classList.toggle('act', btn.dataset?.plt === plt || btn.getAttribute('onclick')?.includes("'"+plt+"'")));
    _qcPlt = plt;
    // Update modal title to show edit mode
    const title = document.querySelector('#m-qc .modal-title, #m-qc .qc-title');
    if (title) title.textContent = '✏️ Edit Konten';
  }, 80);
}

/* ══ SPRINT 7: ONBOARDING ══ */
.onboarding-ov{
  position:fixed;inset:0;z-index:9999;
  background:rgba(0,0,0,0.75);
  backdrop-filter:blur(8px);
  display:flex;align-items:center;justify-content:center;
  padding:20px;
  animation:fadeIn .25s ease;
}
.onboarding-ov.hidden{display:none;}
.onboarding-card{
  background:var(--sp-card);
  border:1px solid rgba(255,255,255,0.1);
  border-radius:20px;
  padding:36px 40px;
  max-width:480px;width:100%;
  position:relative;
  animation:slideUp .3s cubic-bezier(.16,1,.3,1);
}

.ob-logo{
  display:flex;align-items:center;gap:10px;margin-bottom:28px;
}
.ob-logo-mark{
  width:40px;height:40px;background:var(--accent);border-radius:10px;
  display:flex;align-items:center;justify-content:center;font-size:22px;
}
.ob-logo-text{font-size:18px;font-weight:900;color:var(--t);letter-spacing:-.04em;}
.ob-step-dots{display:flex;gap:6px;margin-bottom:24px;}
.ob-dot{width:28px;height:3px;border-radius:2px;background:rgba(255,255,255,.12);transition:background .2s;}

.ob-dot.act{background:var(--accent);}
.ob-heading{font-size:22px;font-weight:900;color:var(--t);letter-spacing:-.03em;margin-bottom:8px;line-height:1.25;}
.ob-sub{font-size:14px;color:var(--t2);line-height:1.6;margin-bottom:24px;}
.ob-step{display:none;}
.ob-step.act{display:block;}
.ob-plt-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:20px;}
.ob-plt-btn{
  display:flex;align-items:center;gap:8px;
  padding:12px 16px;border-radius:12px;
  border:2px solid rgba(255,255,255,.08);
  background:rgba(255,255,255,.03);
  color:var(--t2);font-size:13px;font-weight:700;
  cursor:pointer;transition:all .15s;
}

.ob-plt-btn:hover{border-color:var(--accent);color:var(--t);}
.ob-plt-btn.sel{border-color:var(--accent);background:rgba(29,185,84,.12);color:var(--t);}
.ob-plt-btn .ob-plt-ico{font-size:20px;flex-shrink:0;}
.ob-acc-input{
  width:100%;padding:12px 14px;border-radius:10px;
  border:2px solid rgba(255,255,255,.1);
  background:rgba(255,255,255,.04);
  color:var(--t);font-size:14px;font-family:var(--font);
  outline:none;transition:border-color .15s;margin-bottom:8px;
}

.ob-acc-input:focus{border-color:var(--accent);}
.ob-acc-input::placeholder{color:var(--t4);}
.ob-hint{font-size:11px;color:var(--t3);margin-bottom:20px;line-height:1.5;}
.ob-cta{
  width:100%;padding:14px;border-radius:12px;
  background:var(--accent);color:#000;
  border:none;font-size:15px;font-weight:800;
  cursor:pointer;transition:opacity .12s;letter-spacing:-.01em;
}
.ob-cta:hover{opacity:.88;}
.ob-cta:disabled{opacity:.4;cursor:not-allowed;}
.ob-skip{
  display:block;text-align:center;margin-top:12px;
  font-size:12px;color:var(--t3);cursor:pointer;
  transition:color .12s;
}
.ob-skip:hover{color:var(--t2);}
.ob-success-ico{font-size:48px;margin-bottom:16px;display:block;}
.ob-checklist{list-style:none;margin-bottom:24px;}
.ob-checklist li{display:flex;align-items:center;gap:10px;padding:8px 0;font-size:13px;color:var(--t2);border-bottom:1px solid var(--b);}
.ob-checklist li:last-child{border-bottom:none;}
.ob-checklist li::before{content:"✓";color:var(--accent);font-weight:800;font-size:14px;flex-shrink:0;}
@keyframes slideUp{from{opacity:0;transform:translateY(20px);}to{opacity:1;transform:translateY(0);}}
@keyframes fadeIn{from{opacity:0;}to{opacity:1;}}

/* ══ SPRINT 7: IMPROVED QC MODAL ══ */
.qc-modal-v2{max-width:500px;width:100%;}
.qc-fields{display:flex;flex-direction:column;gap:10px;margin-bottom:16px;}
.qc-field-row{display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.qc-field label{font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.07em;display:block;margin-bottom:5px;}
.qc-field input,.qc-field select{
  width:100%;padding:9px 12px;border-radius:8px;
  border:1.5px solid rgba(255,255,255,.1);
  background:rgba(255,255,255,.04);
  color:var(--t);font-size:13px;font-family:var(--font);
  outline:none;transition:border-color .15s;
  color-scheme:dark;
}

.qc-field input:focus,.qc-field select:focus{border-color:var(--accent);}
.qc-field input::placeholder{color:var(--t4);}

/* ─── .qc-inp / .qc-lbl ─ used in idea, brief, cal-event modals ─── */
.qc-lbl{font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.09em;color:var(--t3);margin-bottom:5px;display:block;}
.qc-inp{
  width:100%;padding:9px 12px;border-radius:9px;
  border:1px solid rgba(255,255,255,.1);
  background:rgba(255,255,255,.05);
  color:var(--t);font-size:13px;font-family:var(--font);
  outline:none;transition:border-color .15s,background .15s;
  color-scheme:dark;
  box-sizing:border-box;
}
.qc-inp:focus{border-color:rgba(99,102,241,.55);background:rgba(99,102,241,.05);}
.qc-inp::placeholder{color:var(--t4);}
textarea.qc-inp{resize:vertical;min-height:60px;line-height:1.5;}
select.qc-inp{cursor:pointer;}




/* ─── Dark mode: ALL inputs/selects inside .modal ─── */
[data-theme="dark"] .modal select,
[data-theme="dark"] .modal .qc-inp{
  background:#1C1C2E;color:#F0F0FF;
  border-color:rgba(255,255,255,.12);color-scheme:dark;
}
[data-theme="dark"] .modal select option,
[data-theme="dark"] .modal select optgroup{background:#1C1C2E;color:#F0F0FF;}
[data-theme="dark"] .modal input[type="text"],
[data-theme="dark"] .modal input[type="date"],
[data-theme="dark"] .modal input[type="time"],
[data-theme="dark"] .modal textarea{
  background:rgba(255,255,255,.05);color:#F0F0FF;
  border-color:rgba(255,255,255,.12);color-scheme:dark;
}
[data-theme="dark"] .modal input[type="date"]::-webkit-calendar-picker-indicator,
[data-theme="dark"] .modal input[type="time"]::-webkit-calendar-picker-indicator{
  filter:invert(1) opacity(.5);
}
/* qc-field select needs dark color-scheme too */
[data-theme="dark"] .qc-field select{color-scheme:dark;}

/* Calendar event card status select */
.calevent-status-sel{color-scheme:light;}
[data-theme="dark"] .calevent-status-sel{
  background:var(--surf2) !important;color:var(--t) !important;
  color-scheme:dark;border-color:rgba(255,255,255,.15) !important;
}
[data-theme="dark"] .calevent-status-sel option{background:#1C1C2E;color:#F0F0FF;}
.qc-section-label{font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.1em;padding:4px 0;border-bottom:1px solid var(--b);margin-bottom:8px;}
.qc-collapsible{overflow:hidden;max-height:0;transition:max-height .3s ease;}
.qc-collapsible.open{max-height:300px;}
.qc-toggle-link{
  font-size:12px;color:var(--accent);cursor:pointer;
  display:flex;align-items:center;gap:4px;margin-bottom:12px;
  font-weight:700;
}
.qc-toggle-link:hover{opacity:.8;}

/* Mobile content cards */
@media(max-width:768px){
  .content-table-wrap .content-table-scroll{overflow-x:auto;}
  .mob-post-cards{display:flex;flex-direction:column;gap:8px;padding:0 4px;}
  .mob-post-card{
    background:var(--sp-card);border-radius:12px;padding:14px 16px;
    border:1px solid rgba(255,255,255,.06);
  }
  
  .mob-post-card-top{display:flex;align-items:center;gap:8px;margin-bottom:10px;}
  .mob-post-card-meta{flex:1;min-width:0;}
  .mob-post-card-name{font-size:13px;font-weight:600;color:var(--t);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
  .mob-post-card-date{font-size:11px;color:var(--t3);margin-top:2px;}
  .mob-post-card-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;}
  .mob-metric-cell{background:rgba(255,255,255,.04);border-radius:8px;padding:7px 10px;text-align:center;}
  
  .mob-metric-val{font-family:var(--mono);font-size:13px;font-weight:700;color:var(--t);display:block;}
  .mob-metric-lbl{font-size:11px;color:var(--t3);font-weight:600;text-transform:uppercase;letter-spacing:.06em;}
  .mob-post-card-actions{display:flex;gap:6px;margin-top:10px;}
  .mob-post-card-del{padding:6px 14px;border-radius:8px;border:1px solid rgba(255,77,77,.3);background:none;color:var(--red);font-size:12px;font-weight:700;cursor:pointer;flex:1;}
  .mob-post-card-del:hover{background:var(--red-bg);}
  /* ── Swipe-to-delete (M3) ── */
  .mob-post-card{overflow:hidden;position:relative;}
  .mob-post-card-inner{transition:transform .2s ease;position:relative;z-index:1;}
  .mob-post-card.swipe-reveal .mob-post-card-inner{transform:translateX(-80px);}
  .mob-swipe-delete{position:absolute;right:0;top:0;bottom:0;width:80px;background:var(--red);color:#fff;display:flex;flex-direction:column;align-items:center;justify-content:center;border-radius:0 12px 12px 0;font-size:11px;font-weight:800;gap:3px;cursor:pointer;z-index:0;letter-spacing:.03em;}
}




/* ══ REVISI 1: Content Timeline View ══ */
.timeline-wrap{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(300px,1fr));
  gap:16px;padding:4px 0 80px;
  align-items:start;
}
.timeline-filter-bar{display:none;}
.timeline-add-btn{
  margin-left:auto;padding:7px 16px;border-radius:9px;
  border:none;background:linear-gradient(140deg,var(--accent),var(--accent-2));color:#fff;
  font-size:12px;font-weight:800;cursor:pointer;
  display:flex;align-items:center;gap:5px;
  box-shadow:0 3px 10px rgba(99,102,241,.35);
}
.timeline-add-btn:hover{opacity:.88;}
.timeline-group-label{
  grid-column:1/-1;
  font-size:11px;font-weight:800;color:var(--t3);
  text-transform:uppercase;letter-spacing:.1em;
  padding:8px 0;border-bottom:1px solid var(--b);
  margin-bottom:4px;
}
.timeline-card{
  display:flex;flex-direction:column;
  background:var(--surf);border:1px solid var(--b);
  border-radius:14px;overflow:hidden;
  transition:background .15s,transform .15s,box-shadow .15s;
}
[data-theme="dark"] .timeline-card{
  background:linear-gradient(145deg,#161624,#12121E);
  border-color:rgba(255,255,255,.07);
}
.timeline-card:hover{transform:translateY(-2px);box-shadow:0 8px 28px rgba(0,0,0,.3);}
.timeline-card-main{display:flex;align-items:flex-start;gap:12px;padding:16px 16px 10px;flex:1;}
.timeline-card-plt{font-size:20px;flex-shrink:0;margin-top:1px;}
.timeline-card-body{flex:1;min-width:0;width:100%;}
.timeline-card-meta{
  display:flex;align-items:center;gap:6px;flex-wrap:wrap;
  font-size:11px;color:var(--t3);margin-bottom:4px;
}
.timeline-card-meta-sep{color:var(--t4);}
.timeline-card-name{
  font-size:13px;font-weight:600;color:var(--t);
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;
  max-width:100%;margin-bottom:8px;
}
.timeline-card-metrics{display:flex;gap:6px;flex-wrap:wrap;}
.timeline-metric-pill{
  display:inline-flex;align-items:center;gap:3px;
  padding:3px 9px;border-radius:20px;
  background:rgba(255,255,255,.06);
  font-size:11px;font-weight:700;color:var(--t2);
  font-family:var(--mono);
}

.timeline-card-actions{
  display:flex;align-items:center;gap:6px;padding:10px 16px 12px;
  border-top:1px solid rgba(255,255,255,.05);margin-top:auto;
}
.timeline-btn-edit{
  padding:5px 14px;border-radius:7px;border:1px solid var(--b2);
  background:none;color:var(--t2);font-size:11px;font-weight:700;cursor:pointer;
}
.timeline-btn-edit:hover{border-color:var(--accent);color:var(--accent);}
.timeline-btn-del{
  padding:5px 14px;border-radius:7px;border:1px solid rgba(255,77,77,.2);
  background:none;color:var(--red);font-size:11px;font-weight:700;cursor:pointer;
}
.timeline-btn-del:hover{background:var(--red-bg);}
.timeline-empty{
  display:flex;flex-direction:column;align-items:center;
  padding:60px 20px;text-align:center;
}
.timeline-load-more{
  width:100%;padding:12px;border:1px solid var(--b2);
  background:none;color:var(--t2);font-size:13px;font-weight:600;
  cursor:pointer;border-radius:10px;margin:16px 0;
}
.timeline-load-more:hover{background:var(--bg-hover);}


/* ══ REVISI 2: First Insight Modal ══ */
.fi-modal-ov{
  position:fixed;inset:0;z-index:9998;
  background:rgba(0,0,0,0.7);backdrop-filter:blur(6px);
  display:flex;align-items:center;justify-content:center;padding:20px;
  animation:fadeIn .2s ease;
}
.fi-modal-ov.hidden{display:none;}
.fi-modal-card{
  background:var(--sp-card);border:1px solid rgba(255,255,255,.1);
  border-radius:20px;padding:32px 36px;max-width:440px;width:100%;
  animation:slideUp .3s cubic-bezier(.16,1,.3,1);
}

.fi-emoji{font-size:44px;margin-bottom:16px;display:block;}
.fi-heading{font-size:20px;font-weight:900;color:var(--t);letter-spacing:-.03em;margin-bottom:8px;}
.fi-sub{font-size:13px;color:var(--t3);margin-bottom:20px;}
.fi-tip-card{
  background:var(--bg-hover);border-radius:12px;padding:16px 18px;
  border-left:3px solid var(--accent);margin-bottom:20px;
}

.fi-tip-text{font-size:13px;color:var(--t2);line-height:1.65;}
.fi-progress{font-size:12px;color:var(--t3);margin-bottom:20px;}
.fi-progress strong{color:var(--accent);}
.fi-actions{display:flex;gap:8px;}
.fi-btn-main{flex:2;padding:12px;border-radius:10px;border:none;background:var(--accent);color:#000;font-size:13px;font-weight:800;cursor:pointer;transition:opacity .12s;}
.fi-btn-main:hover{opacity:.88;}
.fi-btn-sec{flex:1;padding:12px;border-radius:10px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:13px;font-weight:600;cursor:pointer;}
.fi-btn-sec:hover{background:var(--bg-hover);}

/* ══ REVISI 5: Retention Banner ══ */
.retention-banner{
  display:flex;align-items:center;gap:12px;
  background:rgba(251,191,36,0.1);border:1px solid rgba(251,191,36,0.25);
  border-radius:var(--radius-lg);padding:14px 16px;margin-bottom:20px;
}

.retention-banner-text{flex:1;font-size:13px;color:var(--t2);line-height:1.5;}
.retention-banner-btn{
  flex-shrink:0;padding:7px 14px;border-radius:8px;
  background:var(--amber);color:#000;border:none;
  font-size:12px;font-weight:800;cursor:pointer;transition:opacity .12s;
}
.retention-banner-btn:hover{opacity:.85;}
.retention-banner-x{
  flex-shrink:0;width:24px;height:24px;border-radius:50%;
  background:none;border:none;color:var(--t3);
  font-size:16px;cursor:pointer;display:flex;align-items:center;justify-content:center;
}
.retention-banner-x:hover{color:var(--t);background:var(--bg-hover);}


/* ══ PRODUCT CLEANUP: Analytics Tabs ══ */
.analytics-tabs{
  display:flex;gap:0;margin-bottom:20px;
  border-bottom:2px solid var(--b);
}
.analytics-tab-btn{
  padding:9px 18px;font-size:12px;font-weight:700;
  background:none;border:none;color:var(--t3);cursor:pointer;
  border-bottom:2px solid transparent;margin-bottom:-2px;
  transition:color .15s,border-color .15s;letter-spacing:.01em;
}
.analytics-tab-btn:hover{color:var(--t2);}
.analytics-tab-btn.act{color:var(--t);border-bottom-color:var(--accent);}

/* ══ PRODUCT CLEANUP: Weekly Intelligence Banner ══ */
.intelligence-banner{
  display:flex;align-items:center;gap:14px;
  padding:16px 20px;border-radius:14px;
  background:linear-gradient(135deg,rgba(29,185,84,.08) 0%,rgba(29,185,84,.03) 100%);
  border:1px solid rgba(29,185,84,.18);
  margin-bottom:24px;
}

.intelligence-ico{font-size:22px;flex-shrink:0;}
.intelligence-text{
  flex:1;font-size:13px;color:var(--t);line-height:1.55;
}
.intelligence-more{
  flex-shrink:0;padding:6px 14px;border-radius:8px;
  border:1px solid rgba(29,185,84,.3);background:none;
  color:var(--accent);font-size:12px;font-weight:700;cursor:pointer;
  transition:background .12s;white-space:nowrap;
}
.intelligence-more:hover{background:rgba(29,185,84,.1);}

/* ══ PRODUCT CLEANUP: Visual density reduction ══ */
.kpi-card{border-radius:12px;padding:18px 18px 16px;}
.kpi-card::before{width:3px;}
.chart-card{border-radius:12px;padding:18px;}
.section-hdr{margin-bottom:12px;}
.insight-card{border-radius:12px;}
/* Remove excessive shadows */
.modal{box-shadow:0 8px 32px rgba(0,0,0,.35);}

/* Cleaner nav */
.nav-item{border-radius:8px;margin:1px 0;}
/* Breathing room */
.view{padding:24px 28px;}
@media(max-width:768px){.view{padding:16px;}}

/* ── Print/export overrides ── */
@media print{
  .rpt-filter-bar,.rpt-export-bar,.nav-sidebar,.topbar,.mob-nav{display:none!important;}
  .view{padding:0!important;}
  #report-body{border:none!important;padding:0!important;box-shadow:none!important;}
  body{background:#fff!important;color:#000!important;}
}

/* ══ SPRINT 1: INTELLIGENCE LAYER CSS ══ */

/* ── Insight Grid ── */
.insight-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(260px,1fr));
  gap:10px;
  margin-bottom:20px;
}
.insight-card{
  background:linear-gradient(145deg,rgba(22,22,34,0.9),rgba(14,14,22,0.95));
  border:1px solid rgba(255,255,255,0.07);
  border-left:3px solid var(--accent);
  border-radius:12px;
  padding:12px 14px;
  transition:transform .18s,box-shadow .18s;
}

.insight-share-btn{
  margin-left:auto;
  flex-shrink:0;
  display:flex;align-items:center;justify-content:center;
  width:26px;height:26px;
  background:transparent;
  border:1px solid rgba(255,255,255,0.1);
  border-radius:7px;
  color:var(--t3);
  cursor:pointer;
  opacity:0;
  transition:opacity .15s, background .15s, color .15s;
}

.insight-card:hover .insight-share-btn{opacity:1;}
.insight-share-btn:hover{
  background:rgba(0,232,122,0.12);
  border-color:rgba(0,232,122,0.25);
  color:var(--accent);
}

/* ── Report Benchmark Summary ── */
.rpt-benchmark-grid{
  display:grid;
  grid-template-columns:repeat(auto-fill,minmax(180px,1fr));
  gap:12px;
  margin-top:14px;
}
.rpt-bm-card{
  background:rgba(255,255,255,.03);
  border:1px solid rgba(255,255,255,.07);
  border-radius:12px;
  padding:14px;
}

.rpt-bm-label{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.09em;color:var(--t3);margin-bottom:6px;}
.rpt-bm-yours{font-size:18px;font-weight:800;font-family:var(--mono);letter-spacing:-.04em;margin-bottom:2px;}
.rpt-bm-industry{font-size:10px;color:var(--t3);}
.rpt-bm-delta{font-size:11px;font-weight:700;font-family:var(--mono);margin-top:4px;}
.rpt-bm-delta.pos{color:var(--green);}
.rpt-bm-delta.neg{color:var(--red);}
.rpt-bm-delta.neu{color:var(--t3);}

/* ══ 5-SLIDE REPORT CSS ══ */
.rpt-slide{background:linear-gradient(145deg,rgba(22,22,34,.9),rgba(14,14,22,.95));border:1px solid rgba(255,255,255,.08);border-radius:18px;padding:24px;margin-bottom:20px;position:relative;overflow:hidden;}

.rpt-slide::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:linear-gradient(90deg,var(--accent),var(--blue));border-radius:18px 18px 0 0;}
.rpt-slide-num{display:inline-block;font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;padding:3px 10px;border-radius:20px;margin-bottom:10px;background:rgba(0,232,122,.12);color:var(--accent);border:1px solid rgba(0,232,122,.2);}
.rpt-slide-desc{font-size:12px;color:var(--t3);margin-bottom:16px;line-height:1.5;}
.rpt-slide-preview{margin-top:10px;padding:10px 14px;border-left:3px solid var(--accent);background:rgba(0,232,122,.04);border-radius:0 8px 8px 0;font-size:12.5px;color:var(--t2);line-height:1.6;}

.rpt-identity-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
@media(max-width:600px){.rpt-identity-grid{grid-template-columns:1fr;}}
.rpt-identity-field{display:flex;flex-direction:column;gap:5px;}
.rpt-field-label{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);}
.rpt-field-inp{padding:9px 12px;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.1);border-radius:9px;color:var(--t);font-family:var(--font);font-size:13px;outline:none;transition:border-color .15s,box-shadow .15s;width:100%;box-sizing:border-box;}

.rpt-field-inp:focus,.rpt-field-ta:focus,.rpt-tgt-inp:focus{border-color:rgba(0,232,122,.45)!important;box-shadow:0 0 0 3px rgba(0,232,122,.08)!important;}
.rpt-field-ta{padding:9px 12px;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.1);border-radius:9px;color:var(--t);font-family:var(--font);font-size:13px;line-height:1.55;resize:vertical;outline:none;transition:border-color .15s,box-shadow .15s;width:100%;box-sizing:border-box;}

.rpt-field-ta::placeholder,.rpt-field-inp::placeholder{color:var(--t4);font-style:italic;}
.rpt-exec-input-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px;}
@media(max-width:700px){.rpt-exec-input-grid{grid-template-columns:1fr;}}
.rpt-exec-field{display:flex;flex-direction:column;gap:6px;}
.rpt-tgt-th{padding:9px 12px;text-align:left;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t3);border-bottom:1px solid rgba(255,255,255,.07);white-space:nowrap;}

.rpt-tgt-row:not(:last-child) td{border-bottom:1px solid rgba(255,255,255,.04);}

.rpt-tgt-row:hover td{background:rgba(255,255,255,.025);}

.rpt-tgt-label{padding:10px 12px;font-size:12px;font-weight:600;color:var(--t2);white-space:nowrap;}
.rpt-tgt-actual{padding:10px 12px;font-size:12px;font-weight:700;color:var(--t);}
.rpt-tgt-target{padding:10px 12px;font-size:12px;color:var(--t3);}
.rpt-tgt-pct{padding:10px 12px;font-size:12px;}
.rpt-tgt-prev{padding:10px 12px;font-size:11px;}
.rpt-tgt-edit-row{display:flex;align-items:flex-end;gap:10px;flex-wrap:wrap;padding:12px 14px;background:rgba(255,255,255,.02);border:1px dashed rgba(255,255,255,.08);border-radius:10px;margin-top:10px;}

.rpt-tgt-inp{padding:6px 10px;background:rgba(255,255,255,.07);border:1px solid rgba(255,255,255,.12);border-radius:8px;color:var(--t);font-family:var(--mono);font-size:12px;width:100%;box-sizing:border-box;outline:none;transition:border-color .15s,box-shadow .15s;}

.rpt-cc-section-title{font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);margin-bottom:10px;}
.rpt-content-eval-list{display:flex;flex-direction:column;gap:10px;}
.rpt-content-card{display:flex;align-items:flex-start;gap:12px;padding:14px;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.07);border-radius:12px;transition:background .15s;}

.rpt-content-card:hover{background:rgba(255,255,255,.05);}

.rpt-cc-top{border-left:3px solid var(--accent);}
.rpt-cc-low{border-left:3px solid var(--red);}
.rpt-cc-rank{flex-shrink:0;width:32px;height:32px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:800;font-family:var(--mono);}
.rpt-cc-body{flex:1;min-width:0;}
.rpt-cc-name{font-size:13px;font-weight:700;color:var(--t);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;margin-bottom:5px;}
.rpt-cc-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;font-size:11px;color:var(--t3);margin-bottom:7px;}
.rpt-cc-stats{display:flex;gap:12px;font-size:11px;font-family:var(--mono);color:var(--t2);flex-wrap:wrap;}
.rpt-cc-note{margin-top:8px;padding:7px 10px;background:rgba(255,85,102,.06);border-radius:7px;font-size:11px;color:var(--t3);border-left:2px solid var(--red);}
@media print{.rpt-tgt-edit-row,.rpt-exec-field textarea,.rpt-identity-field input{display:none!important;}.rpt-slide-preview{display:block!important;}.rpt-slide{break-inside:avoid;page-break-inside:avoid;}}

.insight-card-head{
  display:flex;align-items:center;gap:8px;margin-bottom:5px;
}
.insight-icon{font-size:15px;flex-shrink:0;}
.insight-title{
  font-size:12px;font-weight:700;color:var(--t);
  font-family:var(--font-display,'Syne',sans-serif);
  letter-spacing:-0.02em;
}
.insight-msg{
  font-size:11.5px;color:var(--t2);line-height:1.55;
  padding-left:23px;
}
.insight-action{
  display:flex;align-items:center;gap:6px;
  margin-top:9px;margin-left:23px;
  padding:6px 10px;border-radius:8px;
  background:rgba(0,232,122,0.07);
  border:1px solid rgba(0,232,122,0.18);
  font-size:11px;font-weight:700;color:var(--accent);
  line-height:1.45;
}

.insight-action-ico{font-size:12px;flex-shrink:0;}

/* ── Goal Projection ── */
.goal-proj{margin-top:10px;padding:8px 10px;border-radius:8px;background:rgba(0,232,122,0.06);border:1px solid rgba(0,232,122,0.15);font-size:11px;color:var(--t2);line-height:1.5;}

.goal-proj strong{color:var(--accent);}
.goal-proj-warn{background:rgba(251,146,60,0.08)!important;border-color:rgba(251,146,60,0.2)!important;}

.goal-proj-warn strong{color:var(--amber)!important;}

/* ── Pillar Advisor ── */
.advisor-card{background:linear-gradient(135deg,rgba(168,85,247,0.08),rgba(139,92,246,0.04));border:1px solid rgba(168,85,247,0.2);border-radius:14px;padding:16px 18px;margin-top:16px;}

.advisor-title{font-size:12px;font-weight:800;color:#A855F7;text-transform:uppercase;letter-spacing:.08em;margin-bottom:12px;display:flex;align-items:center;gap:6px;}
.advisor-row{display:flex;align-items:flex-start;gap:10px;padding:8px 0;border-bottom:1px solid rgba(255,255,255,0.05);}

.advisor-row:last-child{border-bottom:none;}
.advisor-tag{display:inline-block;padding:2px 8px;border-radius:20px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;flex-shrink:0;margin-top:1px;}
.advisor-tag-up{background:rgba(0,232,122,0.15);color:var(--accent);}
.advisor-tag-down{background:rgba(248,113,113,0.15);color:#F87171;}
.advisor-tag-ok{background:rgba(250,204,21,0.15);color:#FACC15;}
.advisor-rec{font-size:12px;color:var(--t2);line-height:1.55;flex:1;}
.advisor-rec strong{color:var(--t);}

/* ── Format Tracker ── */
.fmt-badges{display:flex;gap:6px;flex-wrap:wrap;margin-top:4px;}
.fmt-badge{padding:4px 10px;border-radius:6px;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);font-size:11px;font-weight:700;color:var(--t2);cursor:pointer;transition:all .15s;}

.fmt-badge:hover{background:rgba(255,255,255,0.1);}
.fmt-badge.act{background:var(--accent);color:#000;border-color:transparent;}
.fmt-stat-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(140px,1fr));gap:8px;margin-top:12px;}
.fmt-stat-card{background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:10px;padding:10px 12px;text-align:center;}

.fmt-stat-val{font-family:var(--mono);font-size:18px;font-weight:700;color:var(--t);}
.fmt-stat-lbl{font-size:10px;color:var(--t3);margin-top:2px;font-weight:600;}

/* ── Benchmark mode ── */
.benchmark-banner{display:flex;align-items:center;gap:10px;padding:10px 14px;border-radius:10px;background:rgba(59,130,246,0.08);border:1px solid rgba(59,130,246,0.2);margin-bottom:14px;font-size:12px;color:var(--t2);}

.benchmark-banner strong{color:#60A5FA;}
.bm-toggle{margin-left:auto;padding:4px 12px;border-radius:6px;border:1px solid rgba(96,165,250,0.4);background:rgba(59,130,246,0.1);color:#60A5FA;font-size:11px;font-weight:700;cursor:pointer;transition:all .15s;}
.bm-toggle:hover{background:rgba(59,130,246,0.18);}
.bm-toggle.act{background:#3B82F6;color:#fff;border-color:#3B82F6;}
.bm-row{display:flex;align-items:center;gap:8px;padding:6px 10px;border-radius:8px;}
.bm-row:nth-child(odd){background:rgba(255,255,255,0.025);}

.bm-metric{flex:1;font-size:12px;color:var(--t2);}
.bm-yours{font-family:var(--mono);font-size:12px;font-weight:700;min-width:60px;text-align:right;}
.bm-industry{font-family:var(--mono);font-size:12px;color:var(--t3);min-width:60px;text-align:right;}
.bm-delta{font-size:11px;font-weight:700;min-width:50px;text-align:right;}
.bm-delta.pos{color:var(--accent);}
.bm-delta.neg{color:#F87171;}
.bm-delta.neu{color:var(--t3);}

/* ── Notification reminder banner ── */
.notif-bar{display:flex;align-items:center;gap:10px;padding:10px 16px;border-radius:12px;background:linear-gradient(135deg,rgba(0,232,122,0.1),rgba(0,200,100,0.06));border:1px solid rgba(0,232,122,0.2);margin-bottom:16px;font-size:12.5px;}

.notif-bar-ico{font-size:18px;flex-shrink:0;}
.notif-bar-txt{flex:1;color:var(--t2);line-height:1.5;}
.notif-bar-txt strong{color:var(--t);}
.notif-bar-btn{padding:6px 14px;border-radius:8px;background:var(--accent);color:#000;font-size:12px;font-weight:800;border:none;cursor:pointer;flex-shrink:0;transition:opacity .12s;}
.notif-bar-btn:hover{opacity:.85;}
.notif-bar-dismiss{padding:4px 8px;border-radius:6px;border:none;background:none;color:var(--t3);font-size:18px;cursor:pointer;flex-shrink:0;line-height:1;}

/* ── Quick Capture FAB ── */
.qc-fab{
  position:fixed;bottom:32px;right:28px;
  width:52px;height:52px;border-radius:50%;
  background:var(--accent);color:#000;
  border:none;cursor:pointer;
  display:flex;align-items:center;justify-content:center;
  font-size:24px;font-weight:700;line-height:1;
  box-shadow:0 4px 20px rgba(0,232,122,0.45);
  transition:transform .18s,box-shadow .18s;
  z-index:200;
}
.qc-fab:hover{transform:scale(1.1);box-shadow:0 6px 28px rgba(0,232,122,0.6);}
.qc-fab:active{transform:scale(0.96);}
@media(max-width:768px){
  .qc-fab{bottom:calc(62px + max(env(safe-area-inset-bottom),12px) + 16px);right:16px;}
}

/* ── Quick Capture Modal ── */
.qc-modal{max-width:480px;width:100%;}
.qc-step{display:none;}
.qc-step.active{display:block;}
.qc-plt-row{display:flex;gap:6px;flex-wrap:wrap;margin-top:4px;}
.qc-plt-btn{
  padding:6px 14px;border-radius:8px;border:1px solid rgba(255,255,255,0.1);
  background:rgba(255,255,255,0.04);color:var(--t2);
  font-size:12px;font-weight:700;cursor:pointer;transition:all .15s;
}

.qc-plt-btn:hover{background:rgba(255,255,255,0.08);}
.qc-plt-btn.act{background:var(--accent);color:#000;border-color:transparent;box-shadow:0 3px 10px rgba(0,232,122,0.3);}
.qc-num-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.qc-progress{display:flex;gap:4px;margin-bottom:16px;}
.qc-prog-dot{height:3px;flex:1;border-radius:2px;background:rgba(255,255,255,0.1);transition:background .2s;}

.qc-prog-dot.done{background:var(--accent);}
.qc-step-label{font-size:10px;color:var(--t3);font-weight:700;text-transform:uppercase;letter-spacing:.1em;margin-bottom:14px;}
.qc-save-later{font-size:11px;color:var(--t3);text-align:center;margin-top:12px;cursor:pointer;transition:color .12s;}
.qc-save-later:hover{color:var(--t2);}
.qc-acts-row{display:flex;gap:8px;margin-top:16px;}
.qc-acts-row .btn-ok,.qc-acts-row .btn-c{flex:1;}

/* ── Key Highlights ── */
.highlights-scroll{
  display:flex;gap:10px;overflow-x:auto;padding-bottom:8px;
  margin-bottom:20px;
  scrollbar-width:none;
}
.highlights-scroll::-webkit-scrollbar{display:none;}
.highlight-card{
  flex-shrink:0;
  width:220px;
  background:linear-gradient(145deg,rgba(22,22,34,0.9),rgba(14,14,22,0.95));
  border:1px solid rgba(255,255,255,0.07);
  border-radius:14px;
  padding:12px 14px;
  transition:transform .18s,box-shadow .18s;
  cursor:default;
}

.highlight-card:hover{
  transform:translateY(-3px);
  box-shadow:0 10px 30px rgba(0,0,0,0.4);
  border-color:rgba(0,232,122,0.2);
}
.highlight-badge{
  display:flex;align-items:center;gap:5px;margin-bottom:7px;
}
.highlight-icon{font-size:13px;}
.highlight-label{
  font-size:11px;font-weight:700;
  text-transform:uppercase;letter-spacing:.12em;
  color:var(--t3);
}
.highlight-post-name{
  font-size:12px;font-weight:600;color:var(--t);
  line-height:1.4;margin-bottom:7px;
  overflow:hidden;
  display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;
}
.highlight-meta{
  display:flex;align-items:center;gap:6px;flex-wrap:wrap;
  font-size:10px;color:var(--t3);
}



/* ── Page View Headers (Gemini-style) ── */
.view-header{
  display:flex;align-items:flex-start;justify-content:space-between;
  gap:16px;margin-bottom:28px;flex-wrap:wrap;
}
.view-header-left{flex:1;min-width:0;}
.view-header-meta{
  display:flex;align-items:center;gap:8px;margin-bottom:8px;flex-wrap:wrap;
}
.view-header-badge{
  display:inline-flex;align-items:center;gap:5px;
  font-size:10px;font-weight:800;padding:3px 10px;border-radius:20px;
  background:rgba(34,197,94,0.12);color:#22C55E;
  border:1px solid rgba(34,197,94,0.2);
  text-transform:uppercase;letter-spacing:.06em;
}
.view-header-badge-dot{
  width:6px;height:6px;border-radius:50%;background:#22C55E;
  animation:livePulse 2s ease-in-out infinite;
}
.view-header-sync{font-size:11px;color:var(--t3);}
.view-title{
  font-size:28px;font-weight:900;color:var(--t);
  letter-spacing:-.04em;line-height:1.1;margin-bottom:6px;
}
[data-theme="dark"] .view-title{color:#F0F0FF;}
.view-subtitle{font-size:13px;color:var(--t3);line-height:1.5;}
.view-header-right{
  display:flex;align-items:center;gap:12px;flex-shrink:0;padding-top:4px;
}
.view-header-stat{text-align:right;}
.view-header-stat-label{font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);margin-bottom:2px;}
.view-header-stat-value{font-family:var(--mono);font-size:22px;font-weight:700;color:var(--t);letter-spacing:-.04em;line-height:1;}
.view-header-avatar{
  width:36px;height:36px;border-radius:50%;
  background:linear-gradient(140deg,var(--accent),var(--accent-2));
  display:flex;align-items:center;justify-content:center;
  font-size:13px;font-weight:800;color:#fff;flex-shrink:0;
}
.view-header-btn{
  display:inline-flex;align-items:center;gap:6px;
  padding:9px 18px;border-radius:12px;border:none;
  background:linear-gradient(140deg,var(--accent),var(--accent-2));
  color:#fff;font-size:13px;font-weight:800;cursor:pointer;
  box-shadow:0 4px 14px rgba(14,159,143,.35);
  transition:all .15s;font-family:var(--font);
}
.view-header-btn:hover{
  background:linear-gradient(140deg,var(--accent-hover),#ff9070);
  transform:translateY(-1px);box-shadow:0 6px 22px rgba(14,159,143,.45);
}

/* ── KPI cards v2 — icon top-right style ── */
.kpi-card-v2{
  position:relative;background:var(--surf);border:1px solid var(--b);
  border-radius:14px;padding:20px 20px 16px;
  transition:background .15s,transform .15s,box-shadow .15s;
  overflow:hidden;cursor:default;
}
[data-theme="dark"] .kpi-card-v2{
  background:linear-gradient(145deg,#161624 0%,#12121E 100%);
  border-color:rgba(255,255,255,.07);
}
.kpi-card-v2:hover{transform:translateY(-2px);box-shadow:0 8px 28px rgba(0,0,0,.3);}
.kpi-v2-top{display:flex;align-items:flex-start;justify-content:space-between;gap:8px;margin-bottom:14px;}
.kpi-v2-label{font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t3);}
.kpi-v2-icon{
  width:32px;height:32px;border-radius:9px;flex-shrink:0;
  display:flex;align-items:center;justify-content:center;font-size:15px;
}
.kpi-v2-value{font-size:30px;font-weight:900;font-family:var(--mono);color:var(--t);letter-spacing:-.05em;line-height:1;margin-bottom:6px;}
[data-theme="dark"] .kpi-v2-value{color:#F0F0FF;}
.kpi-v2-sub{font-size:11px;color:var(--t3);font-weight:500;}
.kpi-v2-tag{display:inline-block;font-size:10px;font-weight:700;padding:2px 8px;border-radius:20px;margin-top:6px;}

/* ── Goals list-row style (matches screenshot) ── */
.goal-row{
  background:var(--surf);border:1px solid var(--b);
  border-radius:14px;padding:20px 24px;margin-bottom:12px;
  display:flex;align-items:flex-start;gap:16px;
  transition:background .15s;
}
[data-theme="dark"] .goal-row{
  background:linear-gradient(145deg,#161624,#12121E);
  border-color:rgba(255,255,255,.07);
}
.goal-row-ico{
  width:38px;height:38px;border-radius:10px;flex-shrink:0;
  background:rgba(99,102,241,.12);
  display:flex;align-items:center;justify-content:center;font-size:18px;
}
.goal-row-body{flex:1;min-width:0;}
.goal-row-title{font-size:15px;font-weight:700;color:var(--t);letter-spacing:-.02em;margin-bottom:3px;}
.goal-row-sub{font-size:12px;color:var(--t3);margin-bottom:10px;}
.goal-row-bar-wrap{margin-bottom:6px;}
.goal-row-progress{height:5px;border-radius:3px;background:var(--surf2);overflow:hidden;}
.goal-row-fill{height:100%;border-radius:3px;transition:width .4s ease;}
.goal-row-pct-row{display:flex;align-items:center;justify-content:space-between;margin-top:5px;font-size:11px;color:var(--t3);}
.goal-row-nums{font-family:var(--mono);font-size:12px;color:var(--t2);}
.goal-row-right{display:flex;flex-direction:column;align-items:flex-end;gap:8px;flex-shrink:0;}
.goal-status-badge{
  display:inline-flex;align-items:center;gap:4px;
  font-size:11px;font-weight:700;padding:4px 12px;border-radius:20px;
  white-space:nowrap;
}
.goal-status-done{background:rgba(34,197,94,.12);color:#22C55E;}
.goal-status-progress{background:rgba(99,102,241,.12);color:#818CF8;}
.goal-status-behind{background:rgba(239,68,68,.12);color:#F87171;}

/* ── Content page header ── */
.content-filter-chips{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:0;}
.content-chip{
  padding:6px 14px;border-radius:20px;border:1px solid var(--b);
  background:none;color:var(--t3);font-size:12px;font-weight:700;
  cursor:pointer;transition:all .12s;font-family:var(--font);
}
.content-chip.act{
  background:linear-gradient(135deg,#6258FF,#7B6BFF);
  color:#fff;border-color:transparent;
  box-shadow:0 3px 12px rgba(111,99,255,.35);
}
.content-chip:hover:not(.act){background:var(--bg-hover);color:var(--t);}
.content-search-wrap{
  position:relative;flex:1;min-width:180px;max-width:280px;
}
.content-search-ico{
  position:absolute;left:10px;top:50%;transform:translateY(-50%);
  color:var(--t3);pointer-events:none;
}
.content-search-input{
  width:100%;padding:7px 12px 7px 32px;
  border-radius:9px;border:1px solid var(--b);
  background:var(--surf2);color:var(--t);font-size:12px;
  font-family:var(--font);outline:none;
  transition:border-color .12s;
}
.content-search-input:focus{border-color:rgba(111,99,255,.55);}
.content-search-input::placeholder{color:var(--t3);}

/* -- Final polish: teal/coral palette -- */
[data-theme="dark"]{
  --bg-page:#0d1020;
  --surf:rgba(21,25,38,0.86);
  --surf2:#1a1f30;
  --sp-card:rgba(21,25,38,0.86);
  --sp-elevated:#1e2438;
  --sp-highlight:#252c42;
  --accent:#0e9f8f;
  --accent-hover:#0bbdaa;
  --accent-dim:rgba(14,159,143,0.12);
  --t2:#9ea8c8;
  --t3:#6b7594;
  --shadow-sm:0 1px 5px rgba(4,10,22,0.28);
  --shadow:0 5px 18px rgba(4,10,22,0.34);
  --shadow-md:0 9px 24px rgba(4,10,22,0.4);
  --shadow-lg:0 16px 44px rgba(4,10,22,0.48);
}



.main-area{
  background:
    radial-gradient(ellipse at 70% -10%, rgba(61,199,183,0.18) 0%, transparent 42%),
    radial-gradient(ellipse at 15% 5%, rgba(255,122,89,0.12) 0%, transparent 38%),
    radial-gradient(ellipse at 50% 110%, rgba(135,167,255,0.10) 0%, transparent 45%),
    var(--bg-page) !important;
}


.nav-item.act::before{box-shadow:none;}
.kpi-card:hover,.goal-card:hover,.pillar-card:hover,.timeline-card:hover{box-shadow:var(--shadow)!important;transform:translateY(-1px);}
.wm-pill:hover{box-shadow:var(--shadow)!important;transform:translateY(-1px);}
.view-subtitle,.topbar-sub,.kpi-sub,.goal-row-sub,.content-search-input::placeholder{color:var(--t3)!important;}

.view-header-btn,.timeline-add-btn,.ct-add-row-btn,.btn-ok,.fi-btn-main,.notif-bar-btn{
  box-shadow:0 4px 12px rgba(14,159,143,0.25)!important;
}

.section-action{font-weight:700;}

@media (min-width: 961px){
  .topbar-btn{display:none;}
  .qc-fab{display:none!important;}
}

/* -- Premium typography + spacing system -- */
:root{
  --space-1:4px;
  --space-2:8px;
  --space-3:12px;
  --space-4:16px;
  --space-5:20px;
  --space-6:24px;
  --space-7:32px;
  --space-8:40px;
}

body{
  font-size:14px;
  line-height:1.62;
  letter-spacing:-0.004em;
}

.topbar-title,.view-title,.brand-text,h1,h2,h3{
  font-family:var(--font-display)!important;
  letter-spacing:-0.018em!important;
  font-weight:700!important;
}

.view-title{font-size:44px!important;line-height:1.14!important;}
.topbar-title{font-size:36px!important;line-height:1.16!important;}
.view-subtitle,.topbar-sub{font-size:14px!important;line-height:1.65!important;}

.section-ttl{
  font-size:11px!important;
  letter-spacing:.11em!important;
  font-weight:700!important;
}

.kpi-val,.kpi-v2-value,.goal-current,.view-header-stat-value{
  font-weight:700!important;
  letter-spacing:-0.015em!important;
}

.nav-item,.content-chip,.seg-btn,.week-tab,.af-sel,.period-action,.btn-ok,.btn-c{
  font-weight:600!important;
}

.view{padding:32px 36px 92px!important;}
.section-hdr{margin-bottom:var(--space-5)!important;}
.kpi-grid{gap:var(--space-4)!important;margin-bottom:var(--space-7)!important;}
.kpi-card,.goal-card,.goal-row,.timeline-card,.rpt-kpi-card{padding:22px 22px 20px!important;}

.kpi-sub,.goal-row-sub,.top-post-meta,.wm-pill-label,.period-empty{
  line-height:1.6!important;
}

/* Palette: teal + coral + neutral */
.mp-eng,.mp-flwg,.goal-pct-good{background:var(--tt-bg)!important;color:var(--tt)!important;box-shadow:none!important;}
.mp-er,.goal-pct-mid{background:var(--amber-bg)!important;color:var(--amber)!important;box-shadow:none!important;}
.mp-reach,.mp-impr,.goal-pct-low{background:rgba(148,163,184,.16)!important;color:var(--t2)!important;box-shadow:none!important;}

/* Subtle transitions */
*{transition-duration:.18s!important;transition-timing-function:cubic-bezier(.2,.7,.2,1)!important;}
.kpi-card:hover,.goal-card:hover,.pillar-card:hover,.timeline-card:hover,.wm-pill:hover{transform:translateY(-1px)!important;}

/* -- Override accent vars to teal/coral palette -- */
:root{
  --accent:#0e9f8f;
  --accent-hover:#0bbdaa;
  --accent-dim:rgba(14,159,143,0.1);
}

[data-theme="dark"]{
  --bg-page:#0d1020;
  --surf:rgba(21,25,38,0.86);
  --surf2:#1a1f30;
  --sp-card:rgba(21,25,38,0.86);
  --sp-elevated:#1e2438;
  --nav-accent:#3dc7b7;
  --nav-accent-bg:rgba(61,199,183,0.14);
  --shadow-sm:0 1px 4px rgba(0,0,0,0.22);
  --shadow:0 6px 16px rgba(0,0,0,0.24);
  --shadow-md:0 10px 22px rgba(0,0,0,0.28);
  --shadow-lg:0 16px 36px rgba(0,0,0,0.32);
}

.main-area{background:radial-gradient(circle at 70% -10%,rgba(14,159,143,0.10),transparent 38%),radial-gradient(circle at 15% 0%,rgba(255,122,89,0.06),transparent 32%),var(--bg-page)!important;}
*{text-shadow:none!important;}

.brand-mark,.nav-item.act::before,.rank-1,.rpt-rank-1,.week-tab.act,
.ct-plat-tab.act,.ct-add-row-btn,.empty-banner-btn,.btn-ok,.rpt-export-btn-primary,
.view-header-btn,.timeline-add-btn,.content-chip.act,.qc-fab,.qc-plt-btn.act,
.notif-bar-btn,.fi-btn-main{
  box-shadow:none!important;
}

.kpi-card:hover,.goal-card:hover,.pillar-card:hover,.timeline-card:hover,.wm-pill:hover,
.topbar-btn:hover,.view-header-btn:hover,.btn-ok:hover,.ct-add-row-btn:hover{
  box-shadow:var(--shadow)!important;
}

.content-chip.act,.view-header-btn,.timeline-add-btn{
  background:linear-gradient(140deg,var(--accent),var(--accent-2))!important;
}

.btn-ok,.ct-add-row-btn,.rpt-export-btn-primary,.notif-bar-btn,.fi-btn-main,.empty-banner-btn{
  background:linear-gradient(140deg,var(--accent),var(--accent-2))!important;
  color:#fff!important;
}

/* ════════════════════════════════════
   CONTENT PLANNER & CALENDAR CSS
   ════════════════════════════════════ */

/* ── Idea Board ── */
.planner-kanban{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;align-items:start;}
@media(max-width:1100px){.planner-kanban{grid-template-columns:repeat(2,1fr);}}
@media(max-width:600px){.planner-kanban{grid-template-columns:1fr;}}
.kanban-col{background:rgba(255,255,255,.03);border:1px solid var(--b);border-radius:14px;padding:14px;min-height:200px;}

.kanban-col-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;}
.kanban-col-title{font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);}
.kanban-col-count{font-size:10px;font-weight:700;padding:2px 8px;border-radius:10px;background:var(--surf);color:var(--t3);}
.idea-card{
  background:var(--surf);border:1px solid var(--b);
  border-radius:12px;padding:14px;margin-bottom:8px;
  cursor:pointer;transition:transform .12s,box-shadow .12s;
  position:relative;
}
[data-theme="dark"] .idea-card{background:linear-gradient(145deg,#161624,#12121E);border-color:rgba(255,255,255,.07);}
.idea-card:hover{transform:translateY(-2px);box-shadow:0 6px 20px rgba(0,0,0,.25);}
.idea-card-title{font-size:13px;font-weight:700;color:var(--t);margin-bottom:6px;line-height:1.35;}
.idea-card-meta{display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin-bottom:8px;}
.idea-card-tag{font-size:11px;font-weight:700;padding:2px 7px;border-radius:10px;}
.idea-card-actions{display:flex;gap:4px;margin-top:8px;}
.idea-card-btn{padding:4px 10px;border-radius:7px;border:1px solid var(--b);background:none;color:var(--t3);font-size:10px;font-weight:700;cursor:pointer;font-family:var(--font);transition:all .1s;}
.idea-card-btn:hover{background:var(--bg-hover);color:var(--t);}
.idea-card-btn.primary{border-color:rgba(99,102,241,.4);color:#818CF8;background:rgba(99,102,241,.08);}
.idea-card-btn.primary:hover{background:rgba(99,102,241,.15);}
.priority-high{background:rgba(239,68,68,.12);color:#F87171;}
.priority-med{background:rgba(245,158,11,.12);color:#F59E0B;}
.priority-low{background:rgba(34,197,94,.12);color:#22C55E;}
.status-idea-badge{background:rgba(99,102,241,.12);color:#818CF8;}
.status-brief-badge{background:rgba(245,158,11,.12);color:#F59E0B;}
.status-draft-badge{background:rgba(139,92,246,.12);color:#A78BFA;}
.status-ready-badge{background:rgba(34,197,94,.12);color:#22C55E;}

/* ── List view ── */
.idea-list-item{
  display:flex;align-items:flex-start;gap:12px;
  background:var(--surf);border:1px solid var(--b);
  border-radius:12px;padding:14px 16px;margin-bottom:8px;
  cursor:pointer;transition:transform .12s,box-shadow .12s;
}
[data-theme="dark"] .idea-list-item{background:linear-gradient(145deg,#161624,#12121E);border-color:rgba(255,255,255,.07);}
.idea-list-item:hover{transform:translateY(-1px);box-shadow:0 4px 16px rgba(0,0,0,.2);}

/* ── Content Calendar ── */
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:1px;background:var(--b);border-radius:12px;overflow:hidden;}
.cal-cell{
  background:var(--bg-page);padding:8px;min-height:90px;
  position:relative;transition:background .12s;
}
[data-theme="dark"] .cal-cell{background:#0D0D1A;}
.cal-cell.today{background:rgba(99,102,241,.06);}
.cal-cell.today::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--accent),var(--accent-2));}
.cal-cell.other-month{opacity:.4;}
.cal-cell-date{font-size:11px;font-weight:700;color:var(--t3);margin-bottom:4px;}
.cal-cell.today .cal-cell-date{color:#818CF8;font-weight:900;}
.cal-event{
  font-size:10px;font-weight:700;padding:2px 6px;border-radius:5px;
  margin-bottom:2px;cursor:pointer;line-height:1.4;
  overflow:hidden;white-space:nowrap;text-overflow:ellipsis;
  transition:opacity .1s;
}
.cal-event:hover{opacity:.8;}
.cal-event-idea{background:rgba(99,102,241,.18);color:#818CF8;}
.cal-event-in_progress{background:rgba(245,158,11,.18);color:#F59E0B;}
.cal-event-scheduled{background:rgba(139,92,246,.18);color:#A78BFA;}
.cal-event-posted{background:rgba(34,197,94,.15);color:#22C55E;}
.cal-header-day{
  background:var(--surf);padding:8px;text-align:center;
  font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.1em;color:var(--t3);
}
.cal-add-btn{
  width:100%;margin-top:4px;padding:2px;border-radius:5px;border:1px dashed rgba(255,255,255,.1);
  background:none;color:var(--t4);font-size:10px;cursor:pointer;
  transition:all .1s;
}
.cal-add-btn:hover{border-color:rgba(99,102,241,.4);color:#818CF8;background:rgba(99,102,241,.06);}

/* ── Planner filter bar ── */
.planner-filter-bar{
  display:flex;align-items:center;gap:8px;flex-wrap:wrap;
  margin-bottom:16px;
}
.planner-seg{display:flex;gap:2px;background:var(--surf2);border-radius:9px;padding:3px;}
.planner-seg-btn{
  padding:5px 12px;border-radius:7px;border:none;
  background:none;color:var(--t3);font-size:12px;font-weight:700;
  cursor:pointer;transition:all .12s;font-family:var(--font);
}
.planner-seg-btn.act{background:var(--surf);color:var(--t);box-shadow:0 1px 4px rgba(0,0,0,.2);}

/* ── Idea tab panel ── */
#itab-info-panel, #itab-brief-panel{animation:fadeUp .2s ease both;}

/* ════════════════════════════════════════════════════
   LIGHT MODE MASTER FIX
   Override every hardcoded dark rgba/hex color.
   All rules scoped to :root:not([data-theme="dark"])
   so dark mode is completely unaffected.
   ════════════════════════════════════════════════════ */
:root:not([data-theme="dark"]) {
  /* ensure light vars win over any [data-theme="dark"] block */
  --bg:#f0f4f8;
  --bg-page:#e8edf4;
  --bg-hover:rgba(0,0,0,0.045);
  --surf:#ffffff;
  --surf2:#f4f7fb;
  --b:#dde3ef;
  --b2:rgba(60,80,120,0.18);
  --t:#111827;
  --t2:#374151;
  --t3:#6b7280;
  --t4:#9ca3af;
  --sidebar-bg:#ffffff;
  --sidebar-b:#e5e9f0;
  --sidebar-t:rgba(55,65,81,0.75);
  --sidebar-t-act:#111827;
  --sidebar-hover:rgba(14,159,143,0.07);
  --sp-card:#ffffff;
  --sp-elevated:#f4f7fb;
  --sp-highlight:#eaf0f8;
  --shadow-sm:0 1px 4px rgba(0,0,0,0.08);
  --shadow:0 4px 16px rgba(0,0,0,0.10);
  --shadow-md:0 8px 24px rgba(0,0,0,0.12);
  --shadow-lg:0 16px 48px rgba(0,0,0,0.15);
}

/* ── Topbar ── */
:root:not([data-theme="dark"]) .topbar {
  background: rgba(240,244,248,.9) !important;
  backdrop-filter: blur(16px) !important;
  -webkit-backdrop-filter: blur(16px) !important;
  box-shadow: 0 1px 0 rgba(0,0,0,.06) !important;
}

/* ── Nav sidebar elements ── */
:root:not([data-theme="dark"]) .nav-divider { background: var(--sidebar-b) !important; }
:root:not([data-theme="dark"]) .nav-user-footer { border-top-color: var(--sidebar-b) !important; }
:root:not([data-theme="dark"]) .nav-footer { border-top-color: var(--sidebar-b) !important; }
:root:not([data-theme="dark"]) .nav-user-settings {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
  color: var(--t3) !important;
}
:root:not([data-theme="dark"]) .nav-user-settings:hover { background: var(--b) !important; color: var(--t) !important; }
:root:not([data-theme="dark"]) .nav-tool-btn {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
  color: var(--t3) !important;
}
:root:not([data-theme="dark"]) .nav-tool-btn:hover { background: var(--b) !important; color: var(--t) !important; }
:root:not([data-theme="dark"]) .nav-footer-btn:hover { background: var(--sidebar-hover) !important; color: var(--t) !important; }
:root:not([data-theme="dark"]) .nav-item:hover { color: var(--t) !important; }
:root:not([data-theme="dark"]) .nav-plt-del { color: var(--t4) !important; }
:root:not([data-theme="dark"]) .nav-item-badge {
  background: var(--accent-dim) !important;
  color: var(--accent) !important;
}

/* ── Topbar buttons ── */
:root:not([data-theme="dark"]) .topbar-btn {
  background: var(--surf2) !important;
  border: 1px solid var(--b) !important;
  box-shadow: var(--shadow-sm) !important;
}
:root:not([data-theme="dark"]) .topbar-btn:hover {
  background: var(--surf) !important;
  border-color: var(--b2) !important;
  box-shadow: var(--shadow) !important;
}

/* ── Insight engine cards ── */
:root:not([data-theme="dark"]) .insight-card {
  background: var(--surf) !important;
  border: 1px solid var(--b) !important;
}

/* ── Chart cards ── */
:root:not([data-theme="dark"]) .chart-card {
  background: var(--surf) !important;
  border-color: var(--b) !important;
}

/* ── Segmented controls ── */
:root:not([data-theme="dark"]) .seg { background: var(--surf2) !important; }

/* ── Modal ── */
:root:not([data-theme="dark"]) .modal {
  border-color: var(--b) !important;
  box-shadow: var(--shadow-lg) !important;
}
:root:not([data-theme="dark"]) .modal::before { display: none !important; }
:root:not([data-theme="dark"]) .modal-acts { border-top-color: var(--b) !important; }
:root:not([data-theme="dark"]) .modal-inp {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
  color-scheme: light !important;
}
:root:not([data-theme="dark"]) .btn-c {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
  color: var(--t2) !important;
}
:root:not([data-theme="dark"]) .btn-c:hover { background: var(--b) !important; color: var(--t) !important; }

/* ── Inputs & selects ── */
:root:not([data-theme="dark"]) .qc-inp,
:root:not([data-theme="dark"]) .qc-field input,
:root:not([data-theme="dark"]) .qc-field select,
:root:not([data-theme="dark"]) .ct-inp-time,
:root:not([data-theme="dark"]) .af-sel,
:root:not([data-theme="dark"]) .date-inp,
:root:not([data-theme="dark"]) .rpt-field-inp,
:root:not([data-theme="dark"]) .rpt-field-ta,
:root:not([data-theme="dark"]) .rpt-tgt-inp,
:root:not([data-theme="dark"]) .ob-acc-input {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
  color: var(--t) !important;
  color-scheme: light !important;
}

/* ── Report slides & cards ── */
:root:not([data-theme="dark"]) .rpt-slide,
:root:not([data-theme="dark"]) .rpt-bm-card,
:root:not([data-theme="dark"]) .rpt-kpi-card,
:root:not([data-theme="dark"]) .rpt-plt-card,
:root:not([data-theme="dark"]) .rpt-content-card,
:root:not([data-theme="dark"]) .rpt-highlight-card,
:root:not([data-theme="dark"]) .rpt-cp-stat,
:root:not([data-theme="dark"]) .rpt-post-row,
:root:not([data-theme="dark"]) .rpt-filter-bar,
:root:not([data-theme="dark"]) #report-body {
  background: var(--surf) !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .rpt-tgt-edit-row {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .rpt-delta-neu { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .rpt-rank-2 { background: var(--surf2) !important; color: var(--t2) !important; }
:root:not([data-theme="dark"]) .rpt-rank-3 { background: var(--b) !important; color: var(--t3) !important; }
:root:not([data-theme="dark"]) .rpt-pillar-bar-wrap { background: var(--b) !important; }
:root:not([data-theme="dark"]) .rpt-comp-vs {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .comp-wrap { border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .rpt-export-btn-sec {
  background: var(--surf2) !important;
  border: 1px solid var(--b) !important;
  color: var(--t2) !important;
}
:root:not([data-theme="dark"]) .rpt-export-btn-sec:hover { background: var(--b) !important; }

/* ── Tables ── */
:root:not([data-theme="dark"]) .content-table-wrap { border-color: var(--b) !important; box-shadow: var(--shadow-sm) !important; }
:root:not([data-theme="dark"]) .content-table thead th,
:root:not([data-theme="dark"]) .content-table thead th:first-child { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .content-row:hover td,
:root:not([data-theme="dark"]) .komp-row:hover td,
:root:not([data-theme="dark"]) .comp-row:hover td { background: var(--bg-hover) !important; }
:root:not([data-theme="dark"]) .komp-table th { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .prop-table { border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .prop-key {
  background: var(--surf2) !important;
  border-right-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .csv-preview-table td { border-bottom-color: var(--b) !important; }
:root:not([data-theme="dark"]) .leaderboard { border-color: var(--b) !important; box-shadow: var(--shadow-sm) !important; }

/* ── Filter bars ── */
:root:not([data-theme="dark"]) .komp-filter-row,
:root:not([data-theme="dark"]) .analytics-filter {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .an-tab-strip {
  background: var(--surf2) !important;
  border-color: var(--b) !important;
}

/* ── Scrollbar ── */
:root:not([data-theme="dark"]) ::-webkit-scrollbar-thumb { background: rgba(14,159,143,.3) !important; }
:root:not([data-theme="dark"]) .nav-body::-webkit-scrollbar-thumb { background: rgba(14,159,143,.25) !important; }

/* ── Settings ── */
:root:not([data-theme="dark"]) .settings-row { border-bottom-color: var(--b) !important; }

/* ── Mobile nav ── */
:root:not([data-theme="dark"]) .mob-nav {
  background: rgba(240,244,248,.95) !important;
  border-top-color: var(--b) !important;
  box-shadow: 0 -4px 20px rgba(0,0,0,.08) !important;
}
:root:not([data-theme="dark"]) .mob-metric-cell { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .mob-post-card {
  background: var(--surf) !important;
  border-color: var(--b) !important;
}

/* ── Insight engine (analytics) ── */
:root:not([data-theme="dark"]) .ins-section {
  background: var(--surf) !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .ins-pattern-item { background: var(--surf2) !important; border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .ins-pi-bar { background: var(--b) !important; }
:root:not([data-theme="dark"]) .ins-pattern-summary { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .ins-time-bar-wrap { background: var(--b) !important; }
:root:not([data-theme="dark"]) .ins-streak-item { background: var(--surf2) !important; border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .ins-day-row:hover,
:root:not([data-theme="dark"]) .ins-time-row:hover { background: var(--bg-hover) !important; }

/* ── Content planner ── */
:root:not([data-theme="dark"]) .content-months {
  background: var(--surf2) !important;
  border-right-color: var(--b) !important;
}
:root:not([data-theme="dark"]) .content-months-hdr { background: var(--surf) !important; border-bottom-color: var(--b) !important; }
:root:not([data-theme="dark"]) .content-months-foot { border-top-color: var(--b) !important; }
:root:not([data-theme="dark"]) .content-accs { border-right-color: var(--b) !important; }
:root:not([data-theme="dark"]) .content-accs-hdr { background: var(--surf) !important; border-bottom-color: var(--b) !important; }
:root:not([data-theme="dark"]) .tree-month:hover { background: var(--bg-hover) !important; }
:root:not([data-theme="dark"]) .week-tab:not(.act) { background: var(--surf2) !important; border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .week-tab:hover:not(.act) { background: var(--b) !important; }

/* ── Kanban & Calendar ── */
:root:not([data-theme="dark"]) .kanban-col { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .idea-card { background: var(--surf) !important; border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .idea-list-item { background: var(--surf) !important; border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .cal-cell { background: var(--bg-page) !important; }
:root:not([data-theme="dark"]) .cal-add-btn { border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .highlight-card { background: var(--surf) !important; border-color: var(--b) !important; }

/* ── Onboarding ── */
:root:not([data-theme="dark"]) .onboarding-ov { background: rgba(0,0,0,0.4) !important; }
:root:not([data-theme="dark"]) .onboarding-card { border-color: var(--b) !important; }
:root:not([data-theme="dark"]) .ob-dot { background: var(--b) !important; }
:root:not([data-theme="dark"]) .ob-plt-btn { background: var(--surf2) !important; border-color: var(--b) !important; }

/* ── Timeline ── */
:root:not([data-theme="dark"]) .timeline-metric-pill { background: var(--surf2) !important; }
:root:not([data-theme="dark"]) .timeline-card-actions { border-top-color: var(--b) !important; }
:root:not([data-theme="dark"]) .fi-modal-card { border-color: var(--b) !important; }

/* ── Placeholder empty state ── */
:root:not([data-theme="dark"]) .placeholder { background: var(--surf2) !important; }

/* ── Select element ── */
:root:not([data-theme="dark"]) select {
  background: var(--surf2) !important;
  color: var(--t) !important;
  color-scheme: light !important;
  border-color: var(--b) !important;
}
:root:not([data-theme="dark"]) select option { background: #fff !important; color: var(--t) !important; }

/* ═══════════════════════════════════════════════════════
   IMPROVEMENT LAYER: Mobile UX, Skeleton, Transitions
   ═══════════════════════════════════════════════════════ */

/* ── P1-A: Prevent iOS auto-zoom on inputs (must be ≥16px) ── */
@media(max-width:768px){
  input[type="text"],
  input[type="number"],
  input[type="date"],
  input[type="time"],
  input[type="url"],
  input[type="email"],
  select,
  textarea {
    font-size: 16px !important;
  }
  /* Larger tap targets for mobile */
  .btn-ok, .btn-c, .btn-danger {
    min-height: 44px;
    padding: 11px 20px;
  }
  .seg-btn { min-height: 36px; }
  .nav-item { min-height: 44px; }
  /* Stack filter bars vertically */
  .analytics-filter { flex-wrap: wrap; gap: 8px; }
  /* Prevent text overflow in modals */
  .modal { word-break: break-word; }
  /* Compact KPI grid on very small screens */
  .kpi-grid { grid-template-columns: 1fr 1fr !important; }
  /* Better readability for table on mobile */
  .top-post-item { flex-direction: column; align-items: flex-start; gap: 8px; }
  .top-post-metrics { flex-wrap: wrap; }
  /* Wm-row stacks on mobile */
  .wm-row { grid-template-columns: 1fr 1fr !important; gap: 8px !important; }
  /* View header stacks */
  .view-header { flex-direction: column; align-items: flex-start; gap: 8px; }
  .view-header-right { align-self: flex-end; }
}

/* ── Haptic-like visual pulse on action buttons ── */
@keyframes tapPulse {
  0% { transform: scale(1); }
  50% { transform: scale(0.95); }
  100% { transform: scale(1); }
}
.tap-pulse { animation: tapPulse 0.15s ease-out; }

/* ── P2: View slide transition ── */
@keyframes viewFadeIn {
  from { opacity: 0; transform: translateY(6px); }
  to   { opacity: 1; transform: translateY(0); }
}
.view.active { animation: viewFadeIn 0.22s ease-out; }

/* ── P1-C: Skeleton / shimmer loading state ── */
@keyframes shimmer {
  0%   { background-position: -400px 0; }
  100% { background-position: 400px 0; }
}
.skeleton {
  background: linear-gradient(90deg, var(--surf2) 25%, var(--bg-hover) 50%, var(--surf2) 75%);
  background-size: 800px 100%;
  animation: shimmer 1.4s infinite linear;
  border-radius: var(--radius);
}
.skeleton-text  { height: 14px; margin-bottom: 8px; border-radius: 4px; }
.skeleton-title { height: 22px; width: 60%; margin-bottom: 12px; border-radius: 6px; }
.skeleton-card  { height: 80px; border-radius: var(--radius-lg); margin-bottom: 12px; }

/* ── P2: Enhanced Toast variants ── */
#toast-container {
  position: fixed;
  bottom: 80px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 9999;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  pointer-events: none;
}
.toast-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 11px 18px;
  border-radius: 24px;
  font-size: 13px;
  font-weight: 700;
  font-family: var(--font);
  box-shadow: 0 8px 28px rgba(0,0,0,.28);
  backdrop-filter: blur(12px);
  pointer-events: auto;
  animation: toastIn .25s cubic-bezier(.16,1,.3,1);
  max-width: min(380px, 90vw);
  text-align: center;
  cursor: pointer;
}
@keyframes toastIn { from { opacity: 0; transform: translateY(12px) scale(.95); } to { opacity: 1; transform: none; } }
@keyframes toastOut { to { opacity: 0; transform: translateY(8px) scale(.95); } }
.toast-item.hiding { animation: toastOut .2s ease forwards; }
.toast-default { background: rgba(30,30,50,.92); color: #fff; border: 1px solid rgba(255,255,255,.12); }
.toast-success  { background: rgba(0,232,122,.92); color: #000; border: 1px solid rgba(0,200,100,.5); }
.toast-warn     { background: rgba(245,158,11,.92); color: #000; border: 1px solid rgba(245,158,11,.5); }
.toast-error    { background: rgba(239,68,68,.92);  color: #fff; border: 1px solid rgba(239,68,68,.5); }

/* ── P1-C: Undo snackbar ── */
.undo-snackbar {
  position: fixed;
  bottom: max(env(safe-area-inset-bottom),80px);
  left: 50%;
  transform: translateX(-50%);
  background: #1e1e32;
  border: 1px solid rgba(255,255,255,.15);
  color: #fff;
  padding: 12px 20px;
  border-radius: 24px;
  font-size: 13px;
  font-weight: 600;
  font-family: var(--font);
  display: flex;
  align-items: center;
  gap: 14px;
  z-index: 10000;
  box-shadow: 0 10px 30px rgba(0,0,0,.4);
  animation: toastIn .25s cubic-bezier(.16,1,.3,1);
  backdrop-filter: blur(12px);
  max-width: min(400px,92vw);
}
.undo-snackbar-btn {
  background: var(--accent);
  color: #000;
  border: none;
  border-radius: 16px;
  padding: 5px 14px;
  font-size: 12px;
  font-weight: 800;
  cursor: pointer;
  font-family: var(--font);
  flex-shrink: 0;
  transition: opacity .15s;
}
.undo-snackbar-btn:hover { opacity: .85; }
.undo-snackbar-progress {
  position: absolute;
  bottom: 0; left: 0;
  height: 3px;
  background: var(--accent);
  border-radius: 0 0 24px 24px;
  transition: width linear;
}

/* ── P2: Keyboard Shortcut Help Overlay ── */
#kbd-help-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.65);
  backdrop-filter: blur(6px);
  z-index: 11000;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: fadeIn .2s ease;
}
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
.kbd-help-card {
  background: var(--surf);
  border: 1px solid var(--b2);
  border-radius: 20px;
  padding: 28px 32px;
  width: min(440px,92vw);
  box-shadow: 0 20px 60px rgba(0,0,0,.4);
}
.kbd-help-title {
  font-size: 16px;
  font-weight: 800;
  color: var(--t);
  margin-bottom: 20px;
  display: flex;
  align-items: center;
  gap: 8px;
}
.kbd-help-grid {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 10px 20px;
  align-items: center;
}
.kbd-key {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 28px;
  padding: 3px 8px;
  background: var(--surf2);
  border: 1px solid var(--b2);
  border-bottom-width: 2px;
  border-radius: 6px;
  font-family: var(--mono);
  font-size: 12px;
  font-weight: 700;
  color: var(--t2);
  white-space: nowrap;
}
.kbd-desc { font-size: 13px; color: var(--t2); font-weight: 500; }
.kbd-help-close {
  margin-top: 20px;
  width: 100%;
  padding: 10px;
  border-radius: 10px;
  border: 1px solid var(--b);
  background: var(--bg-hover);
  color: var(--t2);
  font-size: 13px;
  font-weight: 700;
  cursor: pointer;
  font-family: var(--font);
  transition: background .15s;
}
.kbd-help-close:hover { background: var(--surf2); }

/* ── P2: Help FAB button ── */
#help-fab {
  position: fixed;
  bottom: max(env(safe-area-inset-bottom),80px);
  right: 20px;
  width: 38px;
  height: 38px;
  border-radius: 50%;
  background: var(--surf2);
  border: 1px solid var(--b2);
  color: var(--t3);
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  z-index: 800;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all .2s;
  box-shadow: 0 2px 10px rgba(0,0,0,.2);
  font-family: var(--font);
}
#help-fab:hover { background: var(--surf); color: var(--accent); border-color: var(--accent); transform: scale(1.08); }
@media(max-width:768px){ #help-fab { bottom: max(env(safe-area-inset-bottom),74px); right: 14px; } }

/* ── P2: Click-to-copy feedback ── */
.copy-chip {
  position: absolute;
  top: -24px;
  left: 50%;
  transform: translateX(-50%);
  background: var(--accent);
  color: #000;
  font-size: 10px;
  font-weight: 800;
  padding: 2px 8px;
  border-radius: 8px;
  pointer-events: none;
  animation: copyPop .8s ease forwards;
  white-space: nowrap;
  z-index: 9999;
}
@keyframes copyPop {
  0%   { opacity: 0; transform: translateX(-50%) translateY(4px); }
  20%  { opacity: 1; transform: translateX(-50%) translateY(0); }
  80%  { opacity: 1; transform: translateX(-50%) translateY(0); }
  100% { opacity: 0; transform: translateX(-50%) translateY(-6px); }
}
[data-copyable] { cursor: pointer; position: relative; }
[data-copyable]:hover { opacity: .8; }

</style>
</head>
<body>

<div class="app">

  <!-- ── NAV SIDEBAR ── -->
  <nav class="nav-sidebar">
    <div class="nav-brand">
      <div class="brand-mark" style="display:flex;align-items:center;justify-content:center;">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="2" y="10" width="3" height="8" rx="1" fill="white" fill-opacity="0.9"/>
          <rect x="8" y="6" width="3" height="12" rx="1" fill="white"/>
          <rect x="14" y="2" width="3" height="16" rx="1" fill="white" fill-opacity="0.75"/>
          <path d="M3.5 9.5L9.5 5.5L15.5 1.5" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-opacity="0.5"/>
        </svg>
      </div>
      <div>
        <div class="brand-text">Creatorlytics</div>
        <div class="brand-sub">Social Analytics</div>
      </div>
    </div>
    <!-- Workspace: tampilkan nama akun aktif -->
    <div class="ws-switcher" onclick="openModal('m-acc')" title="Kelola akun & workspace">
      <div class="ws-dot" style="background:var(--accent);box-shadow:0 0 0 3px rgba(14,159,143,.2);"></div>
      <div class="ws-name" id="ws-active-name" style="flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">Workspace</div>
      <svg width="12" height="12" viewBox="0 0 12 12" fill="none" style="flex-shrink:0;opacity:.5"><path d="M3 4.5L6 7.5L9 4.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </div>
    <div class="nav-body">
      <div class="nav-section">Menu</div>
      <button class="nav-item act" id="nav-dash" onclick="switchView('dash')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="1" y="1" width="6" height="6" rx="1.5" fill="currentColor" opacity=".9"/><rect x="9" y="1" width="6" height="6" rx="1.5" fill="currentColor" opacity=".5"/><rect x="1" y="9" width="6" height="6" rx="1.5" fill="currentColor" opacity=".5"/><rect x="9" y="9" width="6" height="6" rx="1.5" fill="currentColor" opacity=".3"/></svg></span>Dashboard
      </button>
      <button class="nav-item" id="nav-content" onclick="switchView('content')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="1.5" y="2" width="13" height="1.5" rx=".75" fill="currentColor"/><rect x="1.5" y="5.5" width="9" height="1.5" rx=".75" fill="currentColor" opacity=".6"/><rect x="1.5" y="9" width="11" height="1.5" rx=".75" fill="currentColor" opacity=".6"/><rect x="1.5" y="12.5" width="7" height="1.5" rx=".75" fill="currentColor" opacity=".4"/></svg></span>Input Data
      </button>
      <button class="nav-item" id="nav-analytics" onclick="switchView('analytics')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M1.5 12.5L5 8l3 2.5L11 6l3.5 3" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/><circle cx="14.5" cy="3.5" r="1.5" fill="currentColor" opacity=".7"/></svg></span>Analytics
      </button>
      <button class="nav-item" id="nav-goals" onclick="switchView('goals')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="8" cy="8" r="6.5" stroke="currentColor" stroke-width="1.4" opacity=".5"/><circle cx="8" cy="8" r="3.5" stroke="currentColor" stroke-width="1.4" opacity=".75"/><circle cx="8" cy="8" r="1.2" fill="currentColor"/></svg></span>Goals
      </button>
      <button class="nav-item" id="nav-competitor" onclick="switchView('competitor')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="5.5" cy="6" r="2.5" stroke="currentColor" stroke-width="1.4"/><circle cx="10.5" cy="6" r="2.5" stroke="currentColor" stroke-width="1.4" opacity=".6"/><path d="M1 13c0-2 2-3.5 4.5-3.5S10 11 10 13" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/><path d="M10.5 9.5c1.5 0 3.5 1 3.5 3.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" opacity=".6"/></svg></span>Kompetitor
      </button>
      <button class="nav-item" id="nav-report" onclick="switchView('report')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="2.5" y="1.5" width="11" height="13" rx="1.5" stroke="currentColor" stroke-width="1.4"/><path d="M5 5.5h6M5 8h6M5 10.5h3.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg></span>Laporan
      </button>
      <div class="nav-divider"></div>
      <div class="nav-section">Perencanaan</div>
      <button class="nav-item" id="nav-planner" onclick="switchView('planner')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="1" y="1" width="7" height="7" rx="1.2" stroke="currentColor" stroke-width="1.3"/><rect x="8" y="1" width="7" height="7" rx="1.2" stroke="currentColor" stroke-width="1.3" opacity=".5"/><rect x="1" y="8" width="7" height="7" rx="1.2" stroke="currentColor" stroke-width="1.3" opacity=".5"/><rect x="8" y="8" width="7" height="7" rx="1.2" stroke="currentColor" stroke-width="1.3" opacity=".3"/></svg></span>Content Planner
        <span style="margin-left:auto;font-size:11px;font-weight:800;padding:2px 6px;border-radius:10px;background:rgba(99,102,241,.15);color:#818CF8;" id="nav-planner-badge"></span>
      </button>
      <button class="nav-item" id="nav-calendar" onclick="switchView('calendar')">
        <span class="nav-item-ico"><svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="1.5" y="2.5" width="13" height="12" rx="1.5" stroke="currentColor" stroke-width="1.4"/><path d="M1.5 6.5h13M5 1.5v2M11 1.5v2" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/><circle cx="5.5" cy="10" r="1" fill="currentColor"/><circle cx="8" cy="10" r="1" fill="currentColor"/><circle cx="10.5" cy="10" r="1" fill="currentColor"/></svg></span>Kalender Konten
      </button>
      <div class="nav-divider"></div>
      <div class="nav-section">Platform Terhubung</div>
      <div id="nav-platforms"></div>
    </div>

    <!-- User Profile Footer (Gemini style) -->
    <div class="nav-user-footer">
      <div class="nav-user-card">
        <div class="nav-user-avatar" id="nav-user-avatar">CR</div>
        <div class="nav-user-info">
          <div class="nav-user-name" id="nav-user-name">Creator</div>
          <div class="nav-user-role" id="nav-user-email" style="cursor:pointer;font-size:10px;color:var(--t3);font-weight:500;margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;" onclick="openSettings()">Tambah email →</div>
        </div>
        <button class="nav-user-settings" onclick="openSettings()" title="Pengaturan">
          <svg width="15" height="15" viewBox="0 0 15 15" fill="none"><path d="M7.5 9.5a2 2 0 1 0 0-4 2 2 0 0 0 0 4z" stroke="currentColor" stroke-width="1.3"/><path d="M12.2 7.5a4.9 4.9 0 0 0-.1-.9l1.4-1.1-1.3-2.2-1.7.6a5 5 0 0 0-1.5-.9L8.7 1.5h-2.4L6 3a5 5 0 0 0-1.5.9L2.8 3.3 1.5 5.5l1.4 1.1a5 5 0 0 0 0 1.8L1.5 9.5l1.3 2.2 1.7-.6c.4.3.9.6 1.5.9l.3 1.5h2.4l.3-1.5c.6-.3 1.1-.6 1.5-.9l1.7.6 1.3-2.2-1.4-1.1c0-.3.1-.6.1-.9z" stroke="currentColor" stroke-width="1.3"/></svg>
        </button>
      </div>
      <div class="nav-verified-badge">
        <svg width="11" height="11" viewBox="0 0 11 11" fill="none"><circle cx="5.5" cy="5.5" r="4" stroke="#22C55E" stroke-width="1.2"/><path d="M3.5 5.5L5 7L7.5 4" stroke="#22C55E" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Data tersimpan lokal
      </div>
      <div class="nav-footer-tools">
        <button class="nav-tool-btn" id="theme-btn-nav" onclick="toggleTheme()" title="Toggle tema">
          <span id="theme-ico-nav"><svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M12.5 8.5A5.5 5.5 0 0 1 5.5 1.5a5.5 5.5 0 1 0 7 7z" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg></span>
        </button>
        <button class="nav-tool-btn" onclick="openModal('m-plt')" title="Tambah Platform">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>
        </button>
        <button class="nav-tool-btn" onclick="openModal('m-pillar')" title="Kelola Pilar">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><circle cx="4" cy="4" r="2.2" stroke="currentColor" stroke-width="1.2"/><circle cx="10" cy="4" r="2.2" stroke="currentColor" stroke-width="1.2"/><circle cx="4" cy="10" r="2.2" stroke="currentColor" stroke-width="1.2"/><circle cx="10" cy="10" r="2.2" stroke="currentColor" stroke-width="1.2"/></svg>
        </button>
      </div>
    </div>
  </nav>

  <!-- ── MAIN AREA ── -->
  <div class="main-area">

    <!-- Topbar -->
    <div class="topbar">
      <div>
        <div class="topbar-title" id="topbar-title">Dashboard</div>
      </div>
      <div style="flex:1;"></div>
      <div id="save-badge" class="save-badge">
        <svg width="11" height="11" viewBox="0 0 11 11" fill="none" style="flex-shrink:0"><path d="M2 5.5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Tersimpan
      </div>
      <!-- Quick Add Post button in topbar -->
      <button class="topbar-btn" onclick="openQCModal()" title="Input Post Baru" style="background:linear-gradient(135deg,rgba(14,159,143,.2),rgba(14,159,143,.08));border:1px solid rgba(14,159,143,.3);color:var(--accent);font-weight:800;font-size:12px;padding:0 12px;gap:5px;display:flex;align-items:center;">
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M6.5 1.5v10M1.5 6.5h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
        Post
      </button>
      <button class="topbar-btn" onclick="refreshView()" title="Refresh">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M12.5 2.5A6 6 0 1 0 13 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M12.5 2.5V5.5h-3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>
      <button class="topbar-btn" onclick="toggleTheme()" id="theme-btn" title="Toggle tema">
        <svg id="theme-icon-svg" width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 1v1M7 12v1M1 7H0m13 0h1M2.6 2.6l.7.7M10.7 10.7l.7.7M2.6 11.4l.7-.7M10.7 3.3l.7-.7" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/><circle cx="7" cy="7" r="2.8" stroke="currentColor" stroke-width="1.2"/></svg>
      </button>
    </div>

    <!-- ── DASHBOARD ── -->
    <div id="v-dash" class="view active">
      <div id="dash-body"></div>
    </div>

    <!-- ── CONTENT ── -->
    <div id="v-content" class="view content-view-wrap">
      <!-- Timeline View (Revisi 1) — rendered by renderContentTimeline() -->
    </div>

    <!-- ── ANALYTICS ── -->
    <div id="v-analytics" class="view">
      <div id="analytics-body"></div>
    </div>

    <!-- ── GOALS ── -->
    <div id="v-goals" class="view">
      <div id="goals-body"></div>
    </div>

    <!-- ── CONTENT PLANNER (Bank Konten) — merged to Calendar ── -->
    <div id="v-planner" class="view">
      <div id="planner-body"></div>
    </div>

    <!-- ── COMPETITOR TRACKER ── -->
    <div id="v-competitor" class="view">
      <div id="competitor-body"></div>
    </div>

    <!-- ── CONTENT CALENDAR ── -->
    <div id="v-calendar" class="view">
      <div id="calendar-body"></div>
    </div>

    <!-- ── LAPORAN ── -->
    <div id="v-report" class="view">
      <div id="report-view-inner"></div>
    </div>

  </div><!-- /main-area -->
</div><!-- /app -->

<!-- ── MOBILE NAV ── -->
<div class="mob-nav">
  <div class="mob-nav-inner" style="grid-template-columns:repeat(7,1fr);">
    <button class="mnav act" id="mn-dash" onclick="switchView('dash')">
      <span class="mnav-ico">⊞</span>Dash
    </button>
    <button class="mnav" id="mn-content" onclick="switchView('content')">
      <span class="mnav-ico">✏</span>Input
    </button>
    <button class="mnav" id="mn-analytics" onclick="switchView('analytics')">
      <span class="mnav-ico">↗</span>Analitik
    </button>
    <button class="mnav" id="mn-goals" onclick="switchView('goals')">
      <span class="mnav-ico">◎</span>Goals
    </button>
    <button class="mnav" id="mn-planner" onclick="switchView('planner')">
      <span class="mnav-ico">💡</span>Planner
    </button>
    <button class="mnav" id="mn-calendar" onclick="switchView('calendar')">
      <span class="mnav-ico">📅</span>Kalender
    </button>
    <button class="mnav" id="mn-report" onclick="switchView('report')">
      <span class="mnav-ico">📄</span>Laporan
    </button>
  </div>
</div>

<!-- ── MODALS ── -->
<!-- Tambah Bulan -->
<div class="modal-ov hidden" id="m-month" onclick="if(event.target===this)closeModal('m-month')">
  <div class="modal">
    <div class="modal-title">Tambah Bulan</div>
    <div class="modal-fld"><label>Tahun</label><select class="modal-inp" id="mi-year"></select></div>
    <div class="modal-fld">
      <label>Bulan</label>
      <select class="modal-inp" id="mi-month">
        <option value="0">Januari</option><option value="1">Februari</option><option value="2">Maret</option>
        <option value="3">April</option><option value="4">Mei</option><option value="5">Juni</option>
        <option value="6">Juli</option><option value="7">Agustus</option><option value="8">September</option>
        <option value="9">Oktober</option><option value="10">November</option><option value="11">Desember</option>
      </select>
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-month')">Batal</button>
      <button class="btn-ok" onclick="confirmAddMonth()">Tambah</button>
    </div>
  </div>
</div>

<!-- Tambah Akun -->
<div class="modal-ov hidden" id="m-acc" onclick="if(event.target===this)closeModal('m-acc')">
  <div class="modal">
    <div class="modal-title">Tambah Akun</div>
    <div class="modal-fld">
      <label>Nama Akun</label>
      <input class="modal-inp" id="mi-acc" type="text" placeholder="Contoh: Poppo Live" maxlength="40"
        onkeydown="if(event.key==='Enter')confirmAddAcc()">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-acc')">Batal</button>
      <button class="btn-ok" onclick="confirmAddAcc()">Tambah</button>
    </div>
  </div>
</div>

<!-- Rename Akun -->
<div class="modal-ov hidden" id="m-rename" onclick="if(event.target===this)closeModal('m-rename')">
  <div class="modal">
    <div class="modal-title">Rename Akun</div>
    <div class="modal-fld">
      <label>Nama Baru</label>
      <input class="modal-inp" id="mi-rename" type="text" maxlength="40"
        onkeydown="if(event.key==='Enter')confirmRename()">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-rename')">Batal</button>
      <button class="btn-ok" onclick="confirmRename()">Simpan</button>
    </div>
  </div>
</div>

<!-- Konfirmasi Hapus Platform -->
<div class="modal-ov hidden" id="m-del-plt" onclick="if(event.target===this)closeModal('m-del-plt')">
  <div class="modal" style="max-width:360px;">
    <div class="modal-title" style="color:var(--red);">🗑 Hapus Platform?</div>
    <div id="del-plt-body" style="font-size:13px;color:var(--t2);line-height:1.6;margin-bottom:4px;"></div>
    <div style="font-size:11px;color:var(--t3);margin-top:8px;">Semua data konten platform ini akan ikut terhapus.</div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-del-plt')">Batal</button>
      <button class="btn-danger" id="del-plt-confirm-btn">Ya, Hapus</button>
    </div>
  </div>
</div>

<!-- Tambah Platform -->
<div class="modal-ov hidden" id="m-plt" onclick="if(event.target===this)closeModal('m-plt')">
  <div class="modal">
    <div class="modal-title">Tambah Platform</div>
    <div class="modal-fld">
      <label>Nama Platform</label>
      <input class="modal-inp" id="mi-plt-name" type="text" placeholder="Contoh: YouTube" maxlength="20"
        onkeydown="if(event.key==='Enter')confirmAddPlatform()">
    </div>
    <div class="modal-fld">
      <label>Emoji / Ikon</label>
      <input class="modal-inp" id="mi-plt-emoji" type="text" placeholder="Contoh: ▶️" maxlength="4">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-plt')">Batal</button>
      <button class="btn-ok" onclick="confirmAddPlatform()">Tambah</button>
    </div>
  </div>
</div>

<!-- Edit Goal -->
<div class="modal-ov hidden" id="m-goal" onclick="if(event.target===this)closeModal('m-goal')">
  <div class="modal" style="max-width:420px;">
    <div class="modal-title" id="goal-modal-title">Tambah Goal Baru</div>
    <div class="modal-fld">
      <label>Nama Goal *</label>
      <input class="modal-inp" id="mi-goal-label" type="text" placeholder="Contoh: Target Reach Bulan Ini" maxlength="60">
    </div>
    <div class="modal-fld">
      <label>Platform</label>
      <select class="modal-inp" id="mi-goal-plt" style="width:100%;">
        <option value="all">🌐 Semua Platform</option>
      </select>
    </div>
    <div class="modal-fld">
      <label>Metrik</label>
      <select class="modal-inp" id="mi-goal-metric" style="width:100%;">
        <option value="posts">📝 Total Post</option>
        <option value="reach">👁 Reach</option>
        <option value="engagement">❤️ Engagement</option>
        <option value="followers">📈 Followers Gained</option>
        <option value="likes">👍 Likes</option>
        <option value="comments">💬 Komentar</option>
        <option value="shares">🔁 Share</option>
      </select>
    </div>
    <div class="modal-fld">
      <label>Target Angka *</label>
      <input class="modal-inp" id="mi-goal-val" type="number" min="1" placeholder="Contoh: 100000">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-goal')">Batal</button>
      <button class="btn-ok" onclick="confirmGoal()">Simpan Goal</button>
    </div>
  </div>
</div>

<!-- Kelola Pilar -->
<div class="modal-ov hidden" id="m-pillar" onclick="if(event.target===this)closeModal('m-pillar')">
  <div class="modal" style="min-width:420px;max-width:95vw;">
    <div class="modal-title">🎨 Kelola Content Pillar</div>
    <div id="pillar-list-ui" style="max-height:260px;overflow-y:auto;margin-bottom:4px;"></div>
    <div style="border-top:1px solid var(--b);padding-top:14px;margin-top:8px;">
      <div style="font-size:11px;font-weight:600;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px;">Tambah Pilar Baru</div>
      <div style="display:flex;gap:8px;margin-bottom:10px;">
        <input class="modal-inp" id="mi-pil-emoji" placeholder="Emoji" maxlength="4" style="width:60px;text-align:center;font-size:16px;">
        <input class="modal-inp" id="mi-pil-name" placeholder="Nama pilar..." style="flex:1;" onkeydown="if(event.key==='Enter')confirmAddPillar()">
      </div>
      <div style="font-size:11px;color:var(--t3);margin-bottom:7px;">Warna pilar:</div>
      <div id="pillar-clr-swatches" style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:2px;"></div>
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-pillar')">Tutup</button>
      <button class="btn-ok" onclick="confirmAddPillar()">+ Tambah Pilar</button>
    </div>
  </div>
</div>

<!-- Rename Pilar -->
<div class="modal-ov hidden" id="m-pillar-rename" onclick="if(event.target===this)closeModal('m-pillar-rename')">
  <div class="modal">
    <div class="modal-title">Rename Pilar</div>
    <div class="modal-fld">
      <label>Nama Baru</label>
      <input class="modal-inp" id="mi-pil-rename" maxlength="30" onkeydown="if(event.key==='Enter')confirmRenamePillar()">
    </div>
    <div class="modal-fld">
      <label>Emoji</label>
      <input class="modal-inp" id="mi-pil-rename-emoji" maxlength="4" style="font-size:18px;text-align:center;width:70px;">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-pillar-rename')">Batal</button>
      <button class="btn-ok" onclick="confirmRenamePillar()">Simpan</button>
    </div>
  </div>
</div>


<!-- ══ SPRINT 7: ONBOARDING OVERLAY ══ -->
<div class="onboarding-ov hidden" id="onboarding-ov">
  <div class="onboarding-card">
    <div class="ob-logo">
      <div class="ob-logo-mark">📊</div>
      <div class="ob-logo-text">Creatorlytics</div>
    </div>
    <div class="ob-step-dots">
      <div class="ob-dot act" id="ob-d1"></div>
      <div class="ob-dot" id="ob-d2"></div>
      <div class="ob-dot" id="ob-d3"></div>
    </div>

    <!-- Step 1: Nama brand/kreator -->
    <div class="ob-step act" id="ob-s1">
      <div class="ob-heading">Hei! Nama brand atau akun kamu?</div>
      <div class="ob-sub">Ini akan jadi nama workspace utama kamu. Bisa diubah kapan saja.</div>
      <div class="qc-field" style="margin-bottom:8px;">
        <input class="ob-acc-input" id="ob-acc-name" type="text" placeholder="Contoh: NutriBalance ID, @tokofashion, Studio Kreatif…" maxlength="40"
          onkeydown="if(event.key==='Enter')obNext(1)" style="font-size:15px;padding:12px 14px;">
      </div>
      <p class="ob-hint">💡 Kalau kamu manage banyak akun/brand, masukkan satu dulu — yang lain bisa ditambah nanti.</p>
      <button class="ob-cta" onclick="obNext(1)">Lanjut →</button>
      <span class="ob-skip" onclick="obSkip()">Lewati, langsung masuk</span>
    </div>

    <!-- Step 2: Platform -->
    <div class="ob-step" id="ob-s2">
      <div class="ob-heading">Platform yang aktif kamu pakai?</div>
      <div class="ob-sub">Pilih semua platform yang kamu track. Bisa ditambah atau diubah nanti.</div>
      <div class="ob-plt-grid">
        <button class="ob-plt-btn sel" id="ob-tt" onclick="obTogglePlt('tt',this)">
          <span class="ob-plt-ico">🎵</span><span>TikTok</span>
        </button>
        <button class="ob-plt-btn sel" id="ob-ig" onclick="obTogglePlt('ig',this)">
          <span class="ob-plt-ico">📸</span><span>Instagram</span>
        </button>
        <button class="ob-plt-btn" id="ob-yt" onclick="obTogglePlt('yt',this)">
          <span class="ob-plt-ico">▶️</span><span>YouTube</span>
        </button>
        <button class="ob-plt-btn" id="ob-other" onclick="obTogglePlt('other_plt',this);document.getElementById('ob-other-name-wrap').style.display=this.classList.contains('sel')?'block':'none'">
          <span class="ob-plt-ico">📌</span><span>Lainnya</span>
        </button>
        <div id="ob-other-name-wrap" style="display:none;width:100%;margin-top:4px;">
          <input id="ob-other-name" type="text" class="modal-inp" placeholder="Nama platform (misal: X, LinkedIn, Pinterest…)" maxlength="30" style="width:100%;font-size:13px;">
        </div>
      </div>
      <button class="ob-cta" id="ob-step2-cta" onclick="obNext(2)">Mulai Tracking →</button>
      <span class="ob-skip" onclick="obSkip()">Lewati</span>
    </div>

    <!-- Step 3: Selesai -->
    <div class="ob-step" id="ob-s3">
      <span class="ob-success-ico">🎉</span>
      <div class="ob-heading">Siap! Selamat datang.</div>
      <div class="ob-sub">3 langkah pertama yang disarankan:</div>
      <ul class="ob-checklist">
        <li>Ketuk tombol <strong>＋</strong> di pojok kanan bawah untuk input post pertama</li>
        <li>Dashboard akan tampilkan <strong>Insight otomatis</strong> setelah 3+ post masuk</li>
        <li>Tab <strong>Analytics</strong> untuk chart performa dan top content</li>
        <li>Tab <strong>Laporan</strong> untuk export ringkasan ke PDF</li>
      </ul>
      <button class="ob-cta" onclick="obFinish()">Masuk ke Dashboard →</button>
    </div>
  </div>
</div>

<!-- ── IDEA / BRIEF MODAL ── -->
<div class="modal-ov hidden" id="m-idea" onclick="if(event.target===this)closeModal('m-idea')">
  <div class="modal" style="width:680px;max-width:95vw;max-height:90vh;overflow-y:auto;">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;">
      <div>
        <div style="font-size:16px;font-weight:800;color:var(--t);" id="m-idea-title">Content Planner Baru</div>
        <div style="font-size:11px;color:var(--t3);margin-top:2px;">Simpan ide & link referensi konten</div>
      </div>
      <button onclick="closeModal('m-idea')" style="width:28px;height:28px;border-radius:8px;border:none;background:var(--bg-hover);color:var(--t3);cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;">✕</button>
    </div>
    <input type="hidden" id="m-idea-id">
    <div style="display:flex;gap:4px;margin-bottom:20px;background:var(--surf2);border-radius:10px;padding:4px;" id="idea-tabs-row">
      <button onclick="ideaTab('info')" id="itab-info" style="flex:1;padding:7px;border-radius:7px;border:none;background:var(--surf);color:var(--t);font-size:12px;font-weight:700;cursor:pointer;transition:all .15s;">📋 Info Konten</button>
      <button onclick="ideaTab('brief')" id="itab-brief" style="flex:1;padding:7px;border-radius:7px;border:none;background:none;color:var(--t3);font-size:12px;font-weight:600;cursor:pointer;transition:all .15s;">📋 Content Brief</button>
    </div>
    <div id="itab-info-panel">
      <div style="display:flex;flex-direction:column;gap:12px;">
        <div>
          <div class="qc-lbl">Judul Konten *</div>
          <input id="idea-inp-title" class="qc-inp" type="text" placeholder="Contoh: Review Setup WFH Budget 1jt">
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
          <div>
            <div class="qc-lbl">Format</div>
            <select id="idea-inp-format" class="qc-inp" style="width:100%;">
              <option value="">Pilih format...</option>
              <option value="video">🎬 Video</option>
              <option value="reels">📱 Reels/Short</option>
              <option value="carousel">🎠 Carousel</option>
              <option value="photo">📸 Photo</option>
              <option value="story">⭕ Story</option>
              <option value="live">🔴 Live</option>
            </select>
          </div>
          <div>
            <div class="qc-lbl">Pilar Konten</div>
            <select id="idea-inp-pillar" class="qc-inp" style="width:100%;"></select>
          </div>
        </div>
        <div>
          <div class="qc-lbl">Caption / Notes</div>
          <textarea id="idea-inp-desc" class="qc-inp" rows="3" placeholder="Caption, angle konten, notes produksi, atau ide awal..."></textarea>
        </div>
        <div>
          <div class="qc-lbl">🔗 Link Referensi <span style="font-weight:500;color:var(--t3);font-size:10px;">— video/konten yang jadi inspirasi</span></div>
          <div id="idea-reflinks-list" style="display:flex;flex-direction:column;gap:6px;margin-bottom:8px;"></div>
          <div style="display:flex;gap:6px;">
            <input id="idea-inp-reflink-new" class="qc-inp" type="url" placeholder="https://www.tiktok.com/@user/video/..." style="flex:1;font-size:12px;">
            <button type="button" onclick="addIdeaRefLink()" style="padding:8px 14px;border-radius:8px;border:1px solid var(--accent);background:var(--accent-dim);color:var(--accent);font-size:12px;font-weight:700;cursor:pointer;white-space:nowrap;transition:background .15s;" onmouseover="this.style.background='rgba(14,159,143,0.25)'" onmouseout="this.style.background='var(--accent-dim)'">+ Tambah Link</button>
          </div>
        </div>
        <!-- Hidden fields — masih ada di DOM untuk saveIdea() compatibility -->
        <input type="hidden" id="idea-inp-plt" value="">
        <input type="hidden" id="idea-inp-priority" value="med">
        <input type="hidden" id="idea-inp-status" value="idea">
        <input type="hidden" id="idea-inp-date" value="">
        <input type="hidden" id="idea-inp-tags" value="">
      </div>
    </div>
    <div id="itab-brief-panel" style="display:none;">
      <div style="display:flex;flex-direction:column;gap:12px;">
        <div><div class="qc-lbl">🎣 Hook (3-5 detik pertama)</div><textarea id="idea-inp-hook" class="qc-inp" rows="2" placeholder="Kalimat/visual pembuka yang menarik perhatian..."></textarea></div>
        <div><div class="qc-lbl">📝 Script / Outline</div><textarea id="idea-inp-script" class="qc-inp" rows="6" placeholder="[0:00] Hook&#10;[0:05] Intro masalah&#10;[0:15] Solusi step by step&#10;[0:45] Outro + CTA"></textarea></div>
        <div><div class="qc-lbl">📣 Call to Action</div><textarea id="idea-inp-cta" class="qc-inp" rows="2" placeholder="Subscribe, follow, komen pendapat kalian..."></textarea></div>
        <div><div class="qc-lbl">🏷️ Referensi / Hashtag</div><textarea id="idea-inp-ref" class="qc-inp" rows="2" placeholder="#wfh #setup — atau link video referensi..."></textarea></div>
        <div><div class="qc-lbl">📌 Catatan Produksi</div><textarea id="idea-inp-notes" class="qc-inp" rows="2" placeholder="Peralatan, lokasi, talent, deadline editing..."></textarea></div>
      </div>
    </div>
    <div style="display:flex;gap:8px;margin-top:20px;padding-top:16px;border-top:1px solid var(--b);">
      <button onclick="saveIdea()" class="btn-ok" style="flex:1;">💾 Simpan</button>
      <button onclick="closeModal('m-idea')" class="btn-c">Batal</button>
    </div>
  </div>
</div>

<!-- ── CALENDAR EVENT MODAL ── -->
<div class="modal-ov hidden" id="m-calevent" onclick="if(event.target===this)closeModal('m-calevent')">
  <div class="modal" style="width:500px;max-width:95vw;max-height:90vh;overflow-y:auto;">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px;">
      <div>
        <div style="font-size:16px;font-weight:800;color:var(--t);" id="m-calevent-title">Jadwal Konten</div>
        <div style="font-size:11px;color:var(--t3);margin-top:2px;">Tambah ke content calendar</div>
      </div>
      <button onclick="closeModal('m-calevent')" style="width:28px;height:28px;border-radius:8px;border:none;background:var(--bg-hover);color:var(--t3);cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;">✕</button>
    </div>
    <input type="hidden" id="m-calevent-id">
    <input type="hidden" id="m-calevent-ideaid">
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
      <div style="grid-column:1/-1;"><div class="qc-lbl">Judul Konten *</div><input id="calevent-inp-title" class="qc-inp" type="text" placeholder="Judul postingan..."></div>
      <div><div class="qc-lbl">Platform</div><select id="calevent-inp-plt" class="qc-inp" style="width:100%;"></select></div>
      <div><div class="qc-lbl">Akun</div><select id="calevent-inp-acc" class="qc-inp" style="width:100%;"></select></div>
      <div><div class="qc-lbl">Tanggal Posting</div><input id="calevent-inp-date" class="qc-inp" type="date"></div>
      <div><div class="qc-lbl">Jam Posting</div><input id="calevent-inp-time" class="qc-inp" type="time" value="19:00"></div>
      <div><div class="qc-lbl">Format</div>
        <select id="calevent-inp-format" class="qc-inp" style="width:100%;">
          <option value="video">🎬 Video</option><option value="reels">📱 Reels/Short</option><option value="carousel">🎠 Carousel</option><option value="photo">📸 Photo</option><option value="story">⭕ Story</option><option value="live">🔴 Live</option>
        </select></div>
      <div><div class="qc-lbl">Status</div>
        <select id="calevent-inp-status" class="qc-inp" style="width:100%;">
          <option value="idea">💡 Ide</option><option value="in_progress">📋 Content Brief</option>
        </select></div>
      <div><div class="qc-lbl">Pilar</div><select id="calevent-inp-pillar" class="qc-inp" style="width:100%;"></select></div>
      <div style="grid-column:1/-1;"><div class="qc-lbl">Caption / Notes</div><textarea id="calevent-inp-notes" class="qc-inp" rows="2" placeholder="Caption, hashtag, notes produksi..."></textarea></div>
      <div style="grid-column:1/-1;">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:4px;">
          <div class="qc-lbl" style="margin:0;">📋 Brief Konten</div>
          <span style="font-size:10px;color:var(--t4);">Muncul di Jadwal Mendatang</span>
        </div>
        <textarea id="calevent-inp-brief" class="qc-inp" rows="2" placeholder="Apa angle utama konten ini? Apa yang ingin disampaikan? Apa tujuannya?"></textarea>
      </div>
      <div style="grid-column:1/-1;">
        <div class="qc-lbl">🪝 Hook / Kalimat Pembuka</div>
        <input id="calevent-inp-hook" class="qc-inp" type="text" placeholder="Contoh: '3 kesalahan yang bikin konten kamu ga viral...'" maxlength="200">
      </div>
    </div>
    <div style="display:flex;gap:8px;margin-top:20px;padding-top:16px;border-top:1px solid var(--b);">
      <button onclick="saveCalEvent()" class="btn-ok" style="flex:1;">💾 Simpan</button>
      <button onclick="closeModal('m-calevent')" class="btn-c">Batal</button>
    </div>
  </div>
</div>

<!-- ── Quick Capture FAB ── -->
<button class="qc-fab" onclick="openQCModal()" title="Tambah Post Baru" aria-label="Tambah Post Baru">＋</button>
<button id="help-fab" onclick="_showShortcutsToast()" title="Keyboard Shortcuts">?</button>


<!-- ══ REVISI 2: First Insight Modal ══ -->
<div class="fi-modal-ov hidden" id="fi-modal">
  <div class="fi-modal-card">
    <span class="fi-emoji">🎉</span>
    <div class="fi-heading">Post pertama tersimpan!</div>
    <div class="fi-sub">Berdasarkan niche kamu, ini yang biasanya bekerja:</div>
    <div class="fi-tip-card">
      <div class="fi-tip-text" id="fi-tip-text">—</div>
    </div>
    <div class="fi-progress">Tambah <strong id="fi-need-count">4</strong> post lagi untuk melihat insight personal kamu.</div>
    <div class="fi-actions">
      <button class="fi-btn-main" onclick="closeFiModal();openQCModal()">+ Tambah Post Lagi</button>
      <button class="fi-btn-sec" onclick="closeFiModal();switchView('dash')">Dashboard</button>
    </div>
  </div>
</div>

<!-- ── Quick Capture Modal v2 (Sprint 7 — single form) ── -->
<div class="modal-ov hidden" id="m-qc" onclick="if(event.target===this)closeModal('m-qc')">
  <div class="modal qc-modal-v2">
    <div class="modal-title">⚡ Input Post</div>

    <div class="qc-fields">
      <!-- Platform -->
      <div class="qc-field">
        <label>Platform</label>
        <div class="qc-plt-row" id="qc-plt-row"></div>
      </div>
      <!-- Akun + Tanggal -->
      <div class="qc-field-row">
        <div class="qc-field">
          <label>Akun</label>
          <select id="qc-acc"></select>
        </div>
        <div class="qc-field">
          <label>Tanggal</label>
          <input id="qc-date" type="date">
        </div>
      </div>
      <!-- Judul / Caption -->
      <div class="qc-field">
        <label>Judul / Caption <span style="font-weight:400;color:var(--t3);">(opsional)</span></label>
        <input id="qc-name" type="text" placeholder="Contoh: Tutorial makeup simpel..." maxlength="120"
          onkeydown="if(event.key==='Enter')qcSaveFull()">
      </div>
      <!-- Pilar + Format -->
      <div class="qc-field-row">
        <div class="qc-field">
          <label>Pilar Konten</label>
          <select id="qc-pillar"></select>
        </div>
        <div class="qc-field">
          <label>Format</label>
          <select id="qc-format">
            <option value="">— Format —</option>
            <option value="video">🎬 Video</option>
            <option value="reel">⚡ Reels/Short</option>
            <option value="carousel">🎠 Carousel</option>
            <option value="photo">🖼️ Foto</option>
            <option value="live">🔴 Live</option>
            <option value="story">⭕ Story</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Angka engagement — collapsible -->
    <div class="qc-section-label">
      <span>Angka Engagement</span>
    </div>
    <div class="qc-field-row" style="margin-bottom:8px;">
      <div class="qc-field"><label>❤️ Like</label><input id="qc-like" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
      <div class="qc-field"><label>💬 Komentar</label><input id="qc-comment" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
    </div>
    <div class="qc-field-row" style="margin-bottom:8px;">
      <div class="qc-field"><label>🔁 Share</label><input id="qc-share" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
      <div class="qc-field"><label>🔖 Save</label><input id="qc-save" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
    </div>
    <div class="qc-field-row" style="margin-bottom:16px;">
      <div class="qc-field"><label>👁 Reach</label><input id="qc-reach" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
      <div class="qc-field"><label>📺 Impresi</label><input id="qc-impression" type="number" min="0" placeholder="0" oninput="if(+this.value<0)this.value=0"></div>
    </div>

    <div class="qc-field" style="margin-bottom:12px;">
      <label>🔗 Link Konten <span style="font-weight:400;color:var(--t3);">(opsional)</span></label>
      <input id="qc-link" type="url" placeholder="https://www.tiktok.com/@user/video/...">
    </div>
    <div style="display:flex;gap:8px;">
      <button class="btn-c" style="flex:1;" onclick="closeModal('m-qc')">Batal</button>
      <button id="qc-save-btn" class="btn-ok" style="flex:2;" onclick="qcSaveFull()">💾 Simpan Post</button>
    </div>
    <div class="qc-save-later" onclick="qcSaveLater()" style="margin-top:10px;">Simpan tanpa angka (isi nanti di Content)</div>
  </div>
</div>

<script>
/* ══ CONSTANTS ══ */
/* ════════════════════════════════════════════════════
   MODULE: Constants & Configuration (C4)
   Exports: CONFIG, MN, KEY, DEFAULT_PILLARS, PILLAR_PALETTE
   Depends on: nothing
   ════════════════════════════════════════════════════ */
let _dirtyViews = new Set();
/* ════════════════════════════════════════════════════
   MODULE: Dirty-flag System (C2)
   Exports: markDirty(), isDirty(), clearDirty()
   Depends on: nothing
   ────────────────────────────────────────────────────
   Optimasi re-render — view cuma di-render ulang
   kalau ada perubahan data sejak terakhir dilihat.
   ════════════════════════════════════════════════════ */
let _renderedViews = new Set();
function markDirty(view) { _dirtyViews.add(view); }
function isDirty(view) { return _dirtyViews.has(view); }
function clearDirty(view) { _dirtyViews.delete(view); }

const CONFIG = {
  MIN_POSTS_FOR_INSIGHTS: 5,
  POSTING_GAP_WARNING_DAYS: 2,
  ER_GOOD_THRESHOLD: 3,
  ER_LOW_THRESHOLD: 1,
  BACKUP_REMINDER_DAYS: 30,
  MAX_COMPETITORS: 5,
  DEBOUNCE_SAVE_MS: 400,
};
const DEFAULTS=[]; // Users add their own accounts
const LEGACY_DEFAULTS=new Set(['BoloUp','Crush','Chamet ID','Chamet Official','Meto','Niki','Niki Agency','Olamet','Poppo ID','Poppo Agency','Yomet','Emma','Sela','Recho','Vone','World Agency']);
function stripLegacyAccounts(arr){ return (arr||[]).filter(a=>!LEGACY_DEFAULTS.has(a)); }
const MN=['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
const KEY='smanalytics:v7';
const BUILT_IN_PLTS=[]; // Users add their own platforms
const DEFAULT_PILLARS=[
  {id:'education',label:'Edukasi',emoji:'📚',color:'#2563EB',bg:'#EFF6FF'},
  {id:'entertainment',label:'Hiburan',emoji:'🎭',color:'#7C3AED',bg:'#F5F3FF'},
  {id:'recruitment',label:'Rekrutmen',emoji:'🎯',color:'#059669',bg:'#ECFDF5'},
  {id:'meme',label:'Meme',emoji:'😂',color:'#D97706',bg:'#FFFBEB'},
  {id:'storytelling',label:'Storytelling',emoji:'📖',color:'#DC2626',bg:'#FEF2F2'},
  {id:'promo',label:'Promo',emoji:'📣',color:'#0891B2',bg:'#E0F2FE'},
  {id:'other',label:'Lainnya',emoji:'✨',color:'#6B7280',bg:'#F9FAFB'},
];
let pillars=[...DEFAULT_PILLARS.map(p=>({...p}))];
const PILLAR_PALETTE=['#2563EB','#7C3AED','#059669','#D97706','#DC2626','#0891B2','#DB2777','#EA580C','#0D9488','#6B7280','#1D4ED8','#4F46E5'];
let selPillarColor=PILLAR_PALETTE[0];
let renamePillarTarget=null;

/* ════════════════════════════════════════════════════
   MODULE: State Variables
   Exports: accounts, months, platforms, customGoals, sel, view, etc.
   Depends on: Constants
   ════════════════════════════════════════════════════ */
/* ══ STATE ══ */
let accounts=[];
let months=[];
let platforms=[];
let customGoals=[];
let sel={mid:null,wid:null,acc:null,plt:'tt',erMode:'impression'};
let userProfile={email:'',displayName:'',reminderEnabled:true}; // persisted
let exp={};
let charts={};
let view='dash';
let renameTarget=null;
let goalEditKey=null;
let ovFilter='all',ovYear=new Date().getFullYear(),ovMonth=new Date().getMonth(),ovAcc='all';
let ovDateFrom='',ovDateTo='';
let kompMode='bulan';
let kompPlt='tt';
let kompA={monthKey:null,wid:null,acc:null};
let kompB={monthKey:null,wid:null,acc:null};
let kompDateFrom='',kompDateTo='';
let analyticsTab='trends'; // trends | top | pillar | comparison
let mobContentPanel='months'; // months | accounts | editor
let dashSortBy='engagement'; // engagement | er_rate | reach | impression | flw_gained | overall
let analyticsTopSortBy='engagement';
let overviewMetrics=['posts','impression','reach','engagement'];

/* ══ CONTENT PLANNING STATE ══ */
let contentIdeas = [];   // {id, title, desc, platform, pillar, format, status:'idea'|'brief', priority:'low'|'med'|'high', tags:[], createdAt, brief:{hook,script,cta,notes}}
let calendarPosts = [];  // {id, title, platform, account, pillar, format, scheduledDate, scheduledTime, status:'idea'|'in_progress'|'scheduled'|'posted', ideaId, notes}
let plannerView = 'board'; // 'board' | 'list'
let calendarDate = new Date(); // current calendar month view
let calendarViewMode = 'month'; // 'month' | 'week'
let ideaFilter = { platform: '', pillar: '', status: '', priority: '' };

/* ── SPRINT 2: REPORT STATE ── */
let reportPeriod='month'; // 'month' | 'all'
let reportYear=new Date().getFullYear();
let reportMonth=new Date().getMonth();
let reportPlt='all'; // 'all' | 'tt' | 'ig' | custom id
// Comparison state
let reportCompMonthA={year:new Date().getFullYear(),month:new Date().getMonth()};
let reportCompMonthB={year:new Date().getFullYear(),month:new Date().getMonth()>0?new Date().getMonth()-1:11};
let reportCompWeekA=null; // wid string
let reportCompWeekB=null; // wid string

// ── Report user-input data (per month, saved to localStorage) ──
let reportUserData = {
  brandName:'', brandFocus:'', kendala:'', solusi:'',
  targetFollowers:0, targetReach:0, targetEngagement:0, targetPosts:0,
  nextTarget:'', rencanaTaktis:'',
};

function _rudKey(){return 'rpt_ud_'+reportYear+'_'+reportMonth;}
function saveRUD(){try{localStorage.setItem(_rudKey(),JSON.stringify(reportUserData));}catch(e){}}
function loadRUD(){
  try{
    const d=localStorage.getItem(_rudKey());
    if(d) reportUserData={...reportUserData,...JSON.parse(d)};
    else reportUserData={brandName:'',brandFocus:'',kendala:'',solusi:'',targetFollowers:0,targetReach:0,targetEngagement:0,targetPosts:0,nextTarget:'',rencanaTaktis:''};
  }catch(e){}
}
function updateRUD(field,val){
  reportUserData[field]=val;
  saveRUD();
}

/* ── SPRINT 6: NEW STATE ── */
let benchmarkMode=false;          // toggle: compare vs industry benchmarks
let notifPermission='default';    // 'granted'|'denied'|'default'
let lastNotifCheck=0;             // timestamp of last reminder check

/* ════════════════════════════════════════════════════
   MODULE: Helpers & Utilities
   Exports: safeGet(), safeSet(), validateAppData(), showToast(),
            emptyStateHTML(), checkAutoBackupReminder(),
            getAllPostsFlat(), getPostsByPlatform(), makeCopyable(), dll.
   Depends on: State variables
   ════════════════════════════════════════════════════ */
/* ══ HELPERS ══ */
function safeGet(key, fallback = null) {
  try {
    const raw = localStorage.getItem(key);
    return raw ? JSON.parse(raw) : fallback;
  } catch(e) {
    console.warn('[Creatorlytics] Failed to read', key, e);
    return fallback;
  }
}

function safeSet(key, value) {
  try {
    localStorage.setItem(key, JSON.stringify(value));
    return true;
  } catch(e) {
    console.warn('[Creatorlytics] Failed to write', key, e);
    if (typeof showToast === 'function') {
      showToast('⚠️ Gagal menyimpan data. Storage mungkin penuh.', 'warn');
    }
    return false;
  }
}

function validateAppData(data) {
  if (!data || typeof data !== 'object') return false;
  if (!Array.isArray(data.months)) data.months = [];
  if (!Array.isArray(data.accounts)) data.accounts = [];
  if (!Array.isArray(data.platforms)) data.platforms = [];
  if (!Array.isArray(data.pillars)) data.pillars = [...DEFAULT_PILLARS];
  if (!Array.isArray(data.customGoals)) data.customGoals = [];
  if (!Array.isArray(data.contentIdeas)) data.contentIdeas = [];
  if (!Array.isArray(data.calendarPosts)) data.calendarPosts = [];
  if (!Array.isArray(data.competitors)) data.competitors = [];
  data.months.forEach(m => {
    if (!Array.isArray(m.weeks)) m.weeks = [];
    m.weeks.forEach(w => {
      if (!w.data || typeof w.data !== 'object') w.data = {};
    });
  });
  return true;
}

/* ── Unified Toast System ── */
function showToast(msg, type = 'success', duration = 3200) {
  let container = document.getElementById('toast-container');
  if (!container) {
    container = document.createElement('div');
    container.id = 'toast-container';
    document.body.appendChild(container);
  }
  const el = document.createElement('div');
  el.className = 'toast-item toast-' + type;
  const icons = { success:'✅', error:'❌', warn:'⚠️', info:'💡' };
  el.innerHTML = (icons[type] || '') + ' ' + msg;
  el.onclick = () => { el.classList.add('hiding'); setTimeout(() => el.remove(), 200); };
  container.appendChild(el);
  setTimeout(() => {
    if (el.isConnected) { el.classList.add('hiding'); setTimeout(() => el.remove(), 200); }
  }, duration);
}

function vibrate(pattern = [10]) {
  if ('vibrate' in navigator) navigator.vibrate(pattern);
}

/* ── Click-to-copy (PL6) ── */
function makeCopyable(el, value) {
  el.style.cursor = 'pointer';
  el.title = 'Klik untuk copy';
  el.addEventListener('click', e => {
    e.stopPropagation();
    navigator.clipboard.writeText(String(value)).then(() => {
      showToast('📋 Disalin!', 'info', 1500);
      vibrate([5]);
    }).catch(() => {});
  });
}

/* ── Empty State Helper (F4) ── */
function emptyStateHTML(view, extra = '') {
  const states = {
    dashboard: { emoji:'✨', title:'Belum ada konten', desc:'Catat post pertama kamu sekarang — cukup 30 detik.', cta:'+ Catat Post Pertama', action:"openQCModal()" },
    analytics: { emoji:'📊', title:'Belum cukup data untuk dianalisis', desc:'Minimal 5 post untuk mulai melihat insight.', cta:'Tambah Post Sekarang', action:"openQCModal()" },
    goals: { emoji:'🎯', title:'Belum ada goal', desc:'Set target bulanan untuk reach, followers, atau ER-mu.', cta:'Buat Goal Pertama', action:"openGoalModal()" },
    competitor: { emoji:'🔍', title:'Belum ada kompetitor', desc:'Tambah akun kompetitor untuk benchmark performa.', cta:'Tambah Kompetitor', action:"openAddCompetitorModal()" },
    planner: { emoji:'💡', title:'Belum ada ide konten', desc:'Simpan ide konten sebelum lupa.', cta:'Tambah Ide', action:"openIdeaModal()" },
    calendar: { emoji:'📅', title:'Belum ada jadwal konten', desc:'Buat dan jadwalkan konten dengan mudah.', cta:'Tambah Jadwal', action:"openCalEventModal()" },
    content: { emoji:'📱', title:'Belum ada data konten', desc:'Input post pertamamu untuk mulai tracking performa.', cta:'Input Post', action:"openQCModal()" },
    report: { emoji:'📄', title:'Belum ada laporan', desc:'Pilih periode dan platform untuk generate laporan.', cta:'Buat Laporan', action:"openReportConfigModal()" },
  };
  const s = states[view] || { emoji:'📌', title:'Belum ada data', desc:'Mulai dengan menambah data baru.', cta:'Tambah Data', action:'' };
  const extraHtml = extra ? `<div style="margin-top:4px;font-size:12px;color:var(--t4);line-height:1.5;">${extra}</div>` : '';
  return `<div class="empty-state-v2"><div class="es-emoji">${s.emoji}</div><div class="es-title">${s.title}</div><div class="es-desc">${s.desc}${extraHtml}</div>${s.action ? `<button class="btn-ok" onclick="${s.action}">${s.cta}</button>` : ''}</div>`;
}

/* ── Auto-backup reminder (P0-4) ── */
function checkAutoBackupReminder() {
  try {
    const allPosts = getAllPostsFlat();
    if (allPosts.length < 10) return;
    const lastBackup = localStorage.getItem('cl-last-backup');
    const daysSince = lastBackup ? Math.floor((Date.now() - new Date(lastBackup)) / 86400000) : 999;
    if (daysSince >= 30) {
      showBackupReminder(allPosts.length, daysSince);
    }
  } catch(e) {}
}
function showBackupReminder(postCount, days) {
  const existing = document.getElementById('backup-reminder');
  if (existing) existing.remove();
  const bar = document.createElement('div');
  bar.id = 'backup-reminder';
  bar.style.cssText = 'display:flex;align-items:center;gap:10px;padding:10px 16px;border-radius:12px;background:linear-gradient(135deg,rgba(255,176,107,0.12),rgba(255,122,89,0.06));border:1px solid rgba(255,176,107,0.2);margin-bottom:16px;font-size:12.5px;';
  bar.innerHTML = `
    <span style="font-size:18px;flex-shrink:0;">💾</span>
    <div style="flex:1;color:var(--t2);line-height:1.5;">Kamu punya <strong>${fmt(postCount)} post</strong> — sudah <strong>${days} hari</strong> sejak backup terakhir.</div>
    <button class="notif-bar-btn" onclick="exportJSONBackup();this.closest('#backup-reminder').remove();localStorage.setItem('cl-last-backup',new Date().toISOString())">Backup JSON</button>
    <button class="notif-bar-dismiss" onclick="this.closest('#backup-reminder').remove();localStorage.setItem('cl-last-backup',new Date().toISOString())" title="Sudah">×</button>
  `;
  const dashMain = document.querySelector('#view-dash .dash-col-main') || document.querySelector('#view-dash');
  if (dashMain) dashMain.prepend(bar);
}

/* ── Shared Data Utilities (C1) ── */
function getAllPostsFlat() {
  const res = [];
  (months || []).forEach(m => {
    (m.weeks || []).forEach(w => {
      const wData = w.data || {};
      Object.keys(wData).forEach(acc => {
        (platforms || []).forEach(p => {
          const posts = wData[acc][p.id + 'Posts'] || [];
          posts.forEach(post => {
            res.push({ ...post, _mid: m.id, _wid: w.id, _acc: acc, _plt: p.id, _m: m, _w: w });
          });
        });
      });
    });
  });
  return res;
}
function getPostsByPlatform(plt, filters = {}) {
  let posts = getAllPostsFlat().filter(p => p._plt === plt);
  if (filters.acc) posts = posts.filter(p => p._acc === filters.acc);
  if (filters.from) posts = posts.filter(p => p.date >= filters.from);
  if (filters.to) posts = posts.filter(p => p.date <= filters.to);
  if (filters.pillar) posts = posts.filter(p => p.pillar === filters.pillar);
  return posts;
}
function getPostsByDateRange(from, to) {
  return getAllPostsFlat().filter(p => p.date >= from && p.date <= to);
}
function getPostsWithER(posts) {
  return posts.map(p => ({
    ...p,
    er: calcER(p, sel.erMode, (function(){try{return getPltFlw(aD(getW(p._mid, p._wid), p._acc), p._plt);}catch(e){return 0;}})())
  }));
}

function uid(){return Date.now().toString(36)+Math.random().toString(36).slice(2,6);}
function fmt(n){
  if(n>=1000000)return (n/1000000).toFixed(1)+'M';
  if(n>=1000)return (n/1000).toFixed(1)+'K';
  return String(n);
}
function fmtFull(n){return(+n||0).toLocaleString('id-ID');}
const FORMAT_TYPES=[
  {id:'video',label:'Video',emoji:'🎬'},
  {id:'carousel',label:'Carousel',emoji:'🎠'},
  {id:'photo',label:'Foto',emoji:'🖼️'},
  {id:'reel',label:'Reels/Short',emoji:'⚡'},
  {id:'live',label:'Live',emoji:'🔴'},
  {id:'story',label:'Story',emoji:'⭕'},
];
function emptyPost(){
  const defPillar=pillars[0]?.id||'other';
  return{id:uid(),date:'',name:'',link:'',like:0,comment:0,share:0,save:0,repost:0,reach:0,impression:0,followersGained:0,pillar:defPillar,posted_at:'',format:'',captionLen:0};
}
function emptyAcc(){
  const b={ttPosts:[],igPosts:[],ttFlw:0,igFlw:0,ttPV:0,igPV:0};
  platforms.forEach(p=>{b[p.id+'Posts']=[];b[p.id+'Flw']=0;b[p.id+'PV']=0;});
  return b;
}
function allPlatforms(){
  const seen=new Set(['tt','ig']);
  return[...BUILT_IN_PLTS,...platforms.filter(p=>{if(seen.has(p.id))return false;seen.add(p.id);return true;})];
}
function pltInfo(pid){
  const p=allPlatforms().find(x=>x.id===pid);
  if(!p)return{id:pid,name:pid||'Platform',emoji:'📌'};
  return{...p,emoji:p.emoji||'📌',name:p.name||'Platform'};
}
function pltColorRaw(pid){
  const dark=document.documentElement.getAttribute('data-theme')==='dark';
  if(pid==='tt')return dark?'#22D3EE':'#0891B2';
  if(pid==='ig')return dark?'#E879F9':'#A21CAF';
  const idx=platforms.findIndex(p=>p.id===pid);
  return['#E67E22','#27AE60','#8E44AD','#2980B9','#C0392B','#1ABC9C'][idx%6];
}
function pillarOf(pid){return pillars.find(p=>p.id===pid)||pillars[pillars.length-1]||{id:'general',label:'General',emoji:'📌',color:'#6B7280',bg:'#F9FAFB'};}
function emptyWeek(n){const d={};accounts.forEach(a=>d[a]=emptyAcc());return{id:uid(),label:'Minggu '+n,data:d};}
function emptyMonth(y,m){return{id:uid(),year:y,month:m,weeks:[]};}
function getM(mid){return months.find(m=>m.id===mid);}
function getW(mid,wid){return getM(mid)?.weeks.find(w=>w.id===wid);}
function aD(w,a){if(!w || !w.data) return emptyAcc(); if(!w.data[a])w.data[a]=emptyAcc();return w.data[a];}
function esc(s){return String(s).replace(/'/g,"\\'").replace(/&/g,'&amp;').replace(/"/g,'&quot;');}
function getPltPosts(d,pid){return d[pid+'Posts']||[];}
function getPltFlw(d,pid){return d[pid+'Flw']||0;}
function getPltPV(d,pid){return d[pid+'PV']||0;}
function calcER(p,mode,flw){
  const eng=(p.like||0)+(p.comment||0)+(p.share||0)+(p.save||0)+(p.repost||0);
  const base=mode==='impression'?(p.impression||0):mode==='reach'?(p.reach||0):(flw||0);
  return base?eng/base*100:0;
}
function totalEng(p){return(p.like||0)+(p.comment||0)+(p.share||0)+(p.save||0)+(p.repost||0);}
function erStr(v){return v.toFixed(2)+'%';}
function sumP(arr,k){return arr.reduce((s,p)=>s+(p[k]||0),0);}
const OVERVIEW_METRIC_DEFS=[
  {key:'posts',label:'Post',value:s=>s.posts},
  {key:'impression',label:'Impressi',value:s=>s.impression},
  {key:'reach',label:'Reach',value:s=>s.reach},
  {key:'engagement',label:'Engagement',value:s=>s.engagement,color:'var(--green)'},
  {key:'like',label:'Like',value:s=>s.like},
  {key:'comment',label:'Comment',value:s=>s.comment},
  {key:'share',label:'Share',value:s=>s.share},
  {key:'save',label:'Save',value:s=>s.save},
  {key:'followersGained',label:'Flw Gained',value:s=>s.followersGained},
  {key:'totalPV',label:'Profile Visit',value:s=>s.totalPV},
  {key:'totalFlw',label:'Followers',value:s=>s.totalFlw,sub:'terkini'},
];
const TOP_SORT_LABELS={engagement:'Engagement',er_rate:'ER Rate',reach:'Reach',impression:'Impression',flw_gained:'Flw Gained',overall:'Overall Score'};
function overviewMetricDef(key){return OVERVIEW_METRIC_DEFS.find(m=>m.key===key);}
function normalizeOverviewMetrics(){
  const valid=new Set(OVERVIEW_METRIC_DEFS.map(m=>m.key));
  overviewMetrics=[...new Set((overviewMetrics||[]).filter(k=>valid.has(k)))];
  if(!overviewMetrics.length)overviewMetrics=['posts','impression','reach','engagement'];
}
function addOverviewMetric(key){
  if(!key||overviewMetrics.includes(key)||!overviewMetricDef(key))return;
  overviewMetrics.push(key);scheduleSave(100);destroyCharts();renderAnalytics();
}
function removeOverviewMetric(key){
  if(overviewMetrics.length<=1)return;
  overviewMetrics=overviewMetrics.filter(k=>k!==key);
  scheduleSave(100);destroyCharts();renderAnalytics();
}
function aTots(wk,a,plt){
  const d=wk ? aD(wk,a) : null;
  if(!d) return {impression:0,reach:0,like:0,comment:0,share:0,save:0,repost:0,followersGained:0,posts:0,er:0,engagement:0,flw:0,pv:0};
  const posts=getPltPosts(d,plt) || [];
  const flw=getPltFlw(d,plt) || 0;
  const pv=getPltPV(d,plt) || 0;
  const er=posts.length?posts.reduce((s,p)=>s+calcER(p,sel.erMode,flw),0)/posts.length:0;
  return{impression:sumP(posts,'impression'),reach:sumP(posts,'reach'),
    like:sumP(posts,'like'),comment:sumP(posts,'comment'),share:sumP(posts,'share'),
    save:sumP(posts,'save'),repost:sumP(posts,'repost'),
    followersGained:sumP(posts,'followersGained'),
    posts:posts.length,er,engagement:posts.reduce((s,p)=>s+totalEng(p),0),flw,pv};
}
function erLbl(){return sel.erMode==='impression'?'Impressi':sel.erMode==='reach'?'Reach':'Followers';}

/* ══ SPRINT 1: INTELLIGENCE LAYER ══ */

// ── ER Level System (official Creatorlytics benchmark) ──
function erLevel(er){
  if(er>=5)return{label:'Luar Biasa',color:'#00E87A',bg:'rgba(0,232,122,0.12)',tier:5,emoji:'🔥'};
  if(er>=4)return{label:'Sangat Bagus',color:'#4ADE80',bg:'rgba(74,222,128,0.12)',tier:4,emoji:'⭐'};
  if(er>=3)return{label:'Bagus',color:'#A3E635',bg:'rgba(163,230,53,0.12)',tier:3,emoji:'👍'};
  if(er>=2)return{label:'Rata-rata',color:'#FACC15',bg:'rgba(250,204,21,0.12)',tier:2,emoji:'📊'};
  if(er>=1)return{label:'Rendah',color:'#FB923C',bg:'rgba(251,146,60,0.12)',tier:1,emoji:'📉'};
  return{label:'Sangat Rendah',color:'#F87171',bg:'rgba(248,113,113,0.12)',tier:0,emoji:'⚠️'};
}

// ── Content Score (normalized, do NOT store) ──
function computeContentScore(post, allPosts){
  if(!allPosts||!allPosts.length)return 0;
  const flw=post._flw||post.followersGained||0;
  const postER=calcER(post,sel.erMode,flw);
  const maxER=Math.max(...allPosts.map(p=>calcER(p,sel.erMode,p._flw||0)),0.001);
  const maxReach=Math.max(...allPosts.map(p=>p.reach||0),1);
  const maxShare=Math.max(...allPosts.map(p=>p.share||0),1);
  const maxSave=Math.max(...allPosts.map(p=>p.save||0),1);
  const nER   =Math.min((postER/maxER)*100,100);
  const nReach=Math.min(((post.reach||0)/maxReach)*100,100);
  const nShare=Math.min(((post.share||0)/maxShare)*100,100);
  const nSave =Math.min(((post.save||0)/maxSave)*100,100);
  return Math.round(0.35*nER+0.30*nReach+0.20*nShare+0.15*nSave);
}

// ── Score Grade ──
function scoreGrade(score){
  if(score>=90)return{grade:'S',label:'Viral Potential',color:'#00E87A',bg:'rgba(0,232,122,0.12)'};
  if(score>=80)return{grade:'A',label:'Strong Engagement',color:'#4ADE80',bg:'rgba(74,222,128,0.12)'};
  if(score>=70)return{grade:'B',label:'High Reach',color:'#FACC15',bg:'rgba(250,204,21,0.12)'};
  return{grade:'C',label:'Weak Hook',color:'#F87171',bg:'rgba(248,113,113,0.12)'};
}

// ── New Creator Metrics (all computed, never stored) ──
function saveRate(post){
  return(post.reach||0)>0?(((post.save||0)/(post.reach))*100):0;
}
function shareRate(post){
  return(post.reach||0)>0?(((post.share||0)/(post.reach))*100):0;
}
function avgEngPerPost(posts){
  if(!posts.length)return 0;
  return Math.round(posts.reduce((s,p)=>s+totalEng(p),0)/posts.length);
}
function avgReachPerPost(posts){
  if(!posts.length)return 0;
  return Math.round(posts.reduce((s,p)=>s+(p.reach||0),0)/posts.length);
}

// ── Best Performing Day ──
const DAY_ID=['Minggu','Senin','Selasa','Rabu','Kamis','Jumat','Sabtu'];
function bestPerformingDay(posts){
  const dayMap={};
  posts.forEach(p=>{
    if(!p.date)return;
    const d=new Date(p.date+'T12:00:00');
    const day=DAY_ID[d.getDay()];
    if(!dayMap[day]){dayMap[day]={totalER:0,count:0,totalReach:0};}
    const flw=p._flw||0;
    dayMap[day].totalER+=calcER(p,sel.erMode,flw);
    dayMap[day].totalReach+=p.reach||0;
    dayMap[day].count++;
  });
  const arr=Object.entries(dayMap).map(([day,d])=>({
    day,
    avgER:d.count?d.totalER/d.count:0,
    avgReach:d.count?Math.round(d.totalReach/d.count):0,
    count:d.count
  })).sort((a,b)=>b.avgER-a.avgER);
  return arr;
}

// ── Posting Streak ──
function computePostingStreak(allPosts){
  const postedDates=new Set(allPosts.filter(p=>p.date).map(p=>p.date));
  let streak=0;
  const today=new Date();
  for(let i=0;i<90;i++){
    const d=new Date(today);d.setDate(d.getDate()-i);
    const key=d.toISOString().split('T')[0];
    if(postedDates.has(key)){streak++;}
    else if(i>0)break;
  }
  const last30=new Set();
  for(let i=0;i<30;i++){
    const d=new Date(today);d.setDate(d.getDate()-i);
    const key=d.toISOString().split('T')[0];
    if(postedDates.has(key))last30.add(key);
  }
  return{streak,daysLast30:last30.size,totalPosted:postedDates.size};
}

// ── Key Highlights ──
function computeKeyHighlights(allPosts){
  if(!allPosts.length)return[];
  const byEng=[...allPosts].sort((a,b)=>totalEng(b)-totalEng(a));
  const byER=[...allPosts].sort((a,b)=>(calcER(b,sel.erMode,b._flw||0))-(calcER(a,sel.erMode,a._flw||0)));
  const byReach=[...allPosts].sort((a,b)=>(b.reach||0)-(a.reach||0));
  const byShare=[...allPosts].sort((a,b)=>(b.share||0)-(a.share||0));
  const bySave=[...allPosts].sort((a,b)=>(b.save||0)-(a.save||0));
  const byScore=[...allPosts].sort((a,b)=>computeContentScore(b,allPosts)-computeContentScore(a,allPosts));
  return[
    {label:'Best Engagement',icon:'❤️',metric:'Eng: '+fmt(totalEng(byEng[0])),post:byEng[0]},
    {label:'Best ER',icon:'📊',metric:'ER: '+erStr(calcER(byER[0],sel.erMode,byER[0]._flw||0)),post:byER[0]},
    {label:'Best Reach',icon:'👁',metric:'Reach: '+fmt(byReach[0].reach||0),post:byReach[0]},
    {label:'Most Shared',icon:'🔁',metric:'Share: '+fmt(byShare[0].share||0),post:byShare[0]},
    {label:'Most Saved',icon:'🔖',metric:'Save: '+fmt(bySave[0].save||0),post:bySave[0]},
    {label:'Top Score',icon:'⭐',metric:'Score: '+computeContentScore(byScore[0],allPosts),post:byScore[0]},
  ].filter(h=>h.post);
}

// ── Insight Engine (rule-based, no AI) ──
function generateInsights(allPosts){
  const insights=[];
  // Seed insights for new users (< 5 posts) — always useful, never empty
  if(allPosts.length < 5) return getSeedInsights(allPosts.length);

  // ─ Build weekly buckets (last 2 weeks from most recent post date) ─
  const sortedByDate=[...allPosts].filter(p=>p.date).sort((a,b)=>b.date.localeCompare(a.date));
  const latestDate=sortedByDate[0]?.date;
  if(!latestDate)return insights;
  const latest=new Date(latestDate+'T12:00:00');
  const w1Start=new Date(latest);w1Start.setDate(w1Start.getDate()-6);
  const w2Start=new Date(latest);w2Start.setDate(w2Start.getDate()-13);
  const w2End=new Date(latest);w2End.setDate(w2End.getDate()-7);
  const currPosts=allPosts.filter(p=>p.date&&p.date>=w1Start.toISOString().split('T')[0]);
  const prevPosts=allPosts.filter(p=>{
    if(!p.date)return false;
    const d=p.date;
    return d>=w2Start.toISOString().split('T')[0]&&d<=w2End.toISOString().split('T')[0];
  });

  const cReach=currPosts.reduce((s,p)=>s+(p.reach||0),0);
  const pReach=prevPosts.reduce((s,p)=>s+(p.reach||0),0);
  const cEng=currPosts.reduce((s,p)=>s+totalEng(p),0);
  const pEng=prevPosts.reduce((s,p)=>s+totalEng(p),0);
  const cER=currPosts.length?currPosts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/currPosts.length:0;
  const pER=prevPosts.length?prevPosts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/prevPosts.length:0;

  // ─ Rule 1: Reach growth / drop ─
  if(pReach>0){
    const delta=((cReach-pReach)/pReach)*100;
    if(delta>20)insights.push({type:'growth',icon:'📈',priority:1,
      title:'Reach naik '+delta.toFixed(0)+'%',
      message:`Reach ${fmt(cReach)} minggu ini vs ${fmt(pReach)} minggu lalu. Konten kamu lagi on fire!`,
      action:'Pertahankan frekuensi dan format yang sama minggu ini. Coba replikasi topik dari post dengan reach tertinggi.'});
    else if(delta<-20)insights.push({type:'warning',icon:'⚠️',priority:1,
      title:'Reach turun '+Math.abs(delta).toFixed(0)+'%',
      message:`Reach minggu ini ${fmt(cReach)}, turun dari ${fmt(pReach)} minggu lalu.`,
      action:'Cek tab Top Content — identifikasi format yang terakhir perform, lalu ulangi pendekatan tersebut minggu ini.'});
  }

  // ─ Rule 2: Engagement growth / drop ─
  if(pEng>0){
    const delta=((cEng-pEng)/pEng)*100;
    if(delta>25)insights.push({type:'growth',icon:'❤️',priority:1,
      title:'Engagement naik '+delta.toFixed(0)+'%',
      message:`Total engagement ${fmt(cEng)} minggu ini, naik ${delta.toFixed(0)}% dari ${fmt(pEng)}.`,
      action:'Balas semua komentar di post minggu ini untuk menjaga momentum. Engagement tinggi = sinyal positif ke algoritma.'});
    else if(delta<-25)insights.push({type:'warning',icon:'💔',priority:2,
      title:'Engagement turun',
      message:`Engagement turun ${Math.abs(delta).toFixed(0)}% dibanding minggu lalu.`,
      action:'Coba sertakan call-to-action di caption (pertanyaan, poll, atau ajak save). Komentar balasan juga mendorong engagement balik.'});
  }

  // ─ Rule 3: ER level insight ─
  const lvl=erLevel(cER);
  if(cER>0){
    let erAction='';
    if(lvl.tier<=1)erAction='Fokus ke save & share — keduanya punya bobot algoritma tinggi. Coba format tutorial atau list yang mudah disimpan.';
    else if(lvl.tier===2)erAction='ER sudah oke, tapi masih bisa naik. Cek pilar mana yang paling tinggi ER-nya dan perbanyak konten di situ.';
    else if(lvl.tier>=3)erAction='Pertahankan! Lihat pola konten yang perform — jadikan itu formula utama minggu depan.';
    insights.push({type:'highlight',icon:lvl.emoji||'📊',priority:2,
      title:'ER minggu ini: '+lvl.label,
      message:`Rata-rata ER ${erStr(cER)} masuk kategori "${lvl.label}"${pER>0?' (sebelumnya '+erStr(pER)+')':''}.`,
      action:erAction});
  }

  // ─ Rule 4: Posting frequency ─
  if(currPosts.length===0)insights.push({type:'warning',icon:'📭',priority:1,
    title:'Belum ada posting minggu ini',
    message:'Konsistensi posting berpengaruh pada jangkauan.',
    action:'Gunakan tombol ＋ di kanan bawah untuk input post sekarang. Target minimal 3 konten minggu ini.'});
  else if(prevPosts.length>0&&currPosts.length<prevPosts.length*0.5)insights.push({type:'consistency',icon:'⏰',priority:2,
    title:'Frekuensi posting turun',
    message:`Minggu ini ${currPosts.length} post, padahal minggu lalu ${prevPosts.length} post.`,
    action:`Coba buat ${prevPosts.length-currPosts.length} konten lagi minggu ini. Gunakan tombol ＋ untuk input cepat setelah posting.`});
  else if(currPosts.length>=5)insights.push({type:'highlight',icon:'🔥',priority:3,
    title:`Konsisten! ${currPosts.length} post minggu ini`,
    message:'Kamu posting cukup aktif. Konsistensi adalah kunci algoritma.',
    action:'Kualitas tetap nomor satu — pastikan tiap post punya hook yang kuat di 3 detik pertama.'});

  // ─ Rule 5: Pillar comparison ─
  const pillarMap={};
  allPosts.forEach(p=>{
    const pid=p.pillar||'other';
    if(!pillarMap[pid]){pillarMap[pid]={posts:[],label:pillarOf(pid).label};}
    pillarMap[pid].posts.push(p);
  });
  const pillarStats=Object.entries(pillarMap)
    .filter(([,d])=>d.posts.length>=2)
    .map(([id,d])=>({
      id,label:d.label,count:d.posts.length,
      avgER:d.posts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/d.posts.length,
      avgReach:d.posts.reduce((s,p)=>s+(p.reach||0),0)/d.posts.length
    })).sort((a,b)=>b.avgER-a.avgER);
  if(pillarStats.length>=2){
    const best=pillarStats[0];
    const worst=pillarStats[pillarStats.length-1];
    if(best.avgER>worst.avgER*1.4)insights.push({type:'pillar',icon:'🎯',priority:2,
      title:`Pilar terbaik: ${best.label}`,
      message:`Konten "${best.label}" ER ${erStr(best.avgER)}, unggul dari "${worst.label}" (${erStr(worst.avgER)}).`,
      action:`Naikkan porsi konten "${best.label}" minggu ini. Kurangi "${worst.label}" atau evaluasi angle-nya.`});
  }

  // ─ Rule 5b: Best pillar reach vs ER mismatch ─
  if(pillarStats.length>=2){
    const byReach=[...pillarStats].sort((a,b)=>b.avgReach-a.avgReach);
    const topER=pillarStats[0];const topReach=byReach[0];
    if(topER.id!==topReach.id)insights.push({type:'pillar',icon:'🔀',priority:3,
      title:`Split sinyal: ${topReach.label} vs ${topER.label}`,
      message:`"${topReach.label}" menang di reach, tapi "${topER.label}" punya ER lebih tinggi.`,
      action:`Jika tujuanmu viral → fokus ke "${topReach.label}". Jika tujuanmu komunitas → fokus ke "${topER.label}".`});
  }

  // ─ Rule 6: Platform dominance ─
  const ttPosts=allPosts.filter(p=>p._plt==='tt');
  const igPosts=allPosts.filter(p=>p._plt==='ig');
  const ttReach=ttPosts.reduce((s,p)=>s+(p.reach||0),0);
  const igReach=igPosts.reduce((s,p)=>s+(p.reach||0),0);
  const totalPltReach=ttReach+igReach;
  if(totalPltReach>0){
    if(ttReach/totalPltReach>0.7)insights.push({type:'platform',icon:'🎵',priority:3,
      title:'TikTok dominasi reach',
      message:`TikTok menghasilkan ${Math.round(ttReach/totalPltReach*100)}% dari total reach.`,
      action:'TikTok performanya kuat. Pertimbangkan repurpose konten TikTok terbaik ke Instagram Reels untuk efisiensi.'});
    else if(igReach/totalPltReach>0.7)insights.push({type:'platform',icon:'📸',priority:3,
      title:'Instagram dominasi reach',
      message:`Instagram menghasilkan ${Math.round(igReach/totalPltReach*100)}% dari total reach.`,
      action:'Pertimbangkan naikkan frekuensi posting TikTok — platform ini masih punya potensi organic reach yang besar.'});
    else if(ttPosts.length>0&&igPosts.length>0)insights.push({type:'platform',icon:'⚖️',priority:3,
      title:'Distribusi platform seimbang',
      message:`TikTok ${fmt(ttReach)} vs Instagram ${fmt(igReach)} reach.`,
      action:'Cross-posting berjalan baik. Coba eksperimen format eksklusif di satu platform untuk lihat mana yang merespons lebih baik.'});
  }

  // ─ Rule 7: Best performing day ─
  const dayRanking=bestPerformingDay(allPosts);
  if(dayRanking.length>=3){
    const best=dayRanking[0];
    const worst=dayRanking[dayRanking.length-1];
    insights.push({type:'highlight',icon:'📅',priority:3,
      title:`Hari terbaik: ${best.day}`,
      message:`Posting di hari ${best.day} rata-rata ER ${erStr(best.avgER)}${worst?` — terendah hari ${worst.day} (${erStr(worst.avgER)})`:''}.`,
      action:`Jadwalkan konten terbaik kamu di hari ${best.day}. Hindari posting konten penting di hari ${worst?worst.day:'akhir pekan'}.`});
  }

  // ─ Rule 8: Goal progress ─
  const lowGoals=customGoals.filter(g=>{
    const curr=getGoalCurrentValue(g);
    const t=g.target||0;
    return t>0 && (curr/t)*100<40;
  });
  if(lowGoals.length>0){
    const g0=lowGoals[0];
    const curr0=getGoalCurrentValue(g0);
    const pct=Math.round((curr0/(g0.target||1))*100);
    const remaining=Math.max(0,(g0.target||0)-curr0);
    insights.push({type:'warning',icon:'🎯',priority:2,
      title:`${lowGoals.length} goal perlu dikejar`,
      message:`Goal "${g0.label}" baru ${pct}% tercapai.`,
      action:`Butuh tambahan ${fmt(remaining)} ${g0.label.toLowerCase()} lagi. Naikkan frekuensi posting atau fokus ke format yang tinggi reach-nya.`});
  }

  // ─ Rule 9: Save rate insight ─
  const postsWithReach=allPosts.filter(p=>(p.reach||0)>0);
  if(postsWithReach.length>=3){
    const avgSaveRate=postsWithReach.reduce((s,p)=>s+((p.save||0)/p.reach)*100,0)/postsWithReach.length;
    if(avgSaveRate<1)insights.push({type:'warning',icon:'🔖',priority:3,
      title:'Save rate rendah',
      message:`Rata-rata hanya ${avgSaveRate.toFixed(1)}% penonton menyimpan kontenmu.`,
      action:'Tambahkan nilai "simpan untuk nanti" di konten — tutorial, tips, checklist, atau informasi yang ingin ditonton ulang.'});
    else if(avgSaveRate>5)insights.push({type:'highlight',icon:'🔖',priority:3,
      title:`Save rate tinggi: ${avgSaveRate.toFixed(1)}%`,
      message:'Audiens banyak yang menyimpan kontenmu — pertanda konten bernilai tinggi.',
      action:'Perbanyak format yang bisa "disimpan untuk nanti": how-to, list, template, atau tips praktis.'});
  }

  // ─ Rule 10: Format diversity ─
  const formatMap={};
  allPosts.forEach(p=>{if(p.format){if(!formatMap[p.format])formatMap[p.format]={count:0,eng:0,reach:0};formatMap[p.format].count++;formatMap[p.format].eng+=totalEng(p);formatMap[p.format].reach+=(p.reach||0);}});
  const fmtEntries=Object.entries(formatMap).filter(([,d])=>d.count>=2).map(([id,d])=>({id,count:d.count,avgEng:d.eng/d.count,avgReach:d.reach/d.count})).sort((a,b)=>b.avgEng-a.avgEng);
  if(fmtEntries.length>=2){
    const bestFmt=fmtEntries[0];const worstFmt=fmtEntries[fmtEntries.length-1];
    const labels={video:'Video',carousel:'Carousel',photo:'Foto',reel:'Reels/Short',live:'Live',story:'Story'};
    if(bestFmt.avgEng>worstFmt.avgEng*1.5)insights.push({type:'highlight',icon:'🎬',priority:3,
      title:`Format terbaik: ${labels[bestFmt.id]||bestFmt.id}`,
      message:`${labels[bestFmt.id]||bestFmt.id} rata-rata ${fmt(Math.round(bestFmt.avgEng))} engagement/post vs ${labels[worstFmt.id]||worstFmt.id} hanya ${fmt(Math.round(worstFmt.avgEng))}.`,
      action:`Naikkan porsi ${labels[bestFmt.id]||bestFmt.id} di content mix kamu. Cek apakah topik tertentu juga berperan, bukan hanya formatnya.`});
  }else if(Object.keys(formatMap).length===0&&allPosts.length>=5){
    insights.push({type:'consistency',icon:'🎬',priority:3,
      title:'Format konten belum diisi',
      message:`${allPosts.length} post belum ada tag format (Video, Carousel, Reels, dll).`,
      action:'Isi kolom Format di Input Data untuk unlock analisis performa per format — insight ini sangat berguna untuk keputusan produksi konten.'});
  }

  // ─ Rule 11: Quick Win — top post formula ─
  const sortedByER=allPosts.filter(p=>calcER(p,sel.erMode,p._flw||0)>3).sort((a,b)=>calcER(b,sel.erMode,b._flw||0)-calcER(a,sel.erMode,a._flw||0));
  if(sortedByER.length>=2){
    const topPosts=sortedByER.slice(0,3);
    const commonPillar=topPosts.map(p=>p.pillar).filter(Boolean);
    const pillarCount={};commonPillar.forEach(p=>{pillarCount[p]=(pillarCount[p]||0)+1;});
    const dominantPillar=Object.entries(pillarCount).sort((a,b)=>b[1]-a[1])[0];
    if(dominantPillar&&dominantPillar[1]>=2){
      const pil=pillarOf(dominantPillar[0]);
      insights.push({type:'pillar',icon:'⚡',priority:2,
        title:`Quick win: dobel porsi "${pil.label}"`,
        message:`${dominantPillar[1]} dari ${topPosts.length} post ER-tertinggi kamu adalah pilar "${pil.label}".`,
        action:`Jadikan "${pil.label}" konten utama minggu ini. Buat minimal 2 konten di pilar ini sebelum eksperimen pilar lain.`});
    }
  }

  return insights.sort((a,b)=>a.priority-b.priority).slice(0,6);
}

// ── Insight Card HTML ──
function insightCardHTML(ins, idx=0){
  const typeColors={
    growth:'var(--green)',warning:'var(--amber)',highlight:'var(--blue)',
    pillar:'var(--purple,#A855F7)',platform:'var(--tt)',consistency:'var(--amber)'
  };
  const borderColor=typeColors[ins.type]||'var(--b2)';
  const actionHTML=ins.action
    ?`<div class="insight-action"><span class="insight-action-ico">💡</span>${ins.action}</div>`:'';
  return`<div class="insight-card" id="ins-card-${idx}" data-ins-title="${ins.title.replace(/"/g,'&quot;')}" data-ins-msg="${ins.message.replace(/"/g,'&quot;')}" data-ins-icon="${ins.icon}" style="border-left-color:${borderColor};">
    <div class="insight-card-head">
      <span class="insight-icon">${ins.icon}</span>
      <span class="insight-title">${ins.title}</span>
      <button class="insight-share-btn" title="Share sebagai gambar" onclick="shareInsightCard(this.closest('.insight-card'))">
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M9 4.5C9 3.12 7.88 2 6.5 2S4 3.12 4 4.5M2 8l1.5-3h6L11 8M4.5 8v3h4V8" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </button>
    </div>
    <div class="insight-msg">${ins.message}</div>
    ${actionHTML}
  </div>`;
}

// ── Share insight as PNG ──
async function shareInsightCard(cardEl) {
  if (!cardEl) return;
  // Load html2canvas lazily
  if (!window.html2canvas) {
    _qcToast('⏳ Menyiapkan...');
    await loadScript('https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js');
  }
  // Build a standalone share card
  const title = cardEl.dataset.insTitle || cardEl.querySelector('.insight-title')?.textContent || '';
  const msg   = cardEl.dataset.insMsg   || cardEl.querySelector('.insight-msg')?.textContent   || '';
  const icon  = cardEl.dataset.insIcon  || cardEl.querySelector('.insight-icon')?.textContent  || '💡';
  const isDark = document.documentElement.getAttribute('data-theme') !== 'light';

  const shareDiv = document.createElement('div');
  shareDiv.style.cssText = [
    'position:fixed','top:-9999px','left:-9999px',
    'width:420px','padding:32px 28px',
    'background:'+(isDark?'#0E0E16':'#FFFFFF'),
    'border-radius:20px',
    'border:1px solid '+(isDark?'rgba(255,255,255,0.1)':'rgba(0,0,0,0.08)'),
    'font-family:Outfit,system-ui,sans-serif',
    'color:'+(isDark?'#F0F0FF':'#0A0A14'),
  ].join(';');
  shareDiv.innerHTML = `
    <div style="display:flex;align-items:center;gap:10px;margin-bottom:20px;">
      <div style="width:36px;height:36px;background:#00E87A;border-radius:9px;display:flex;align-items:center;justify-content:center;font-size:18px;">📊</div>
      <span style="font-size:15px;font-weight:800;letter-spacing:-.03em;">Creatorlytics</span>
    </div>
    <div style="display:flex;align-items:flex-start;gap:12px;margin-bottom:16px;">
      <span style="font-size:28px;flex-shrink:0;">${icon}</span>
      <div>
        <div style="font-size:16px;font-weight:800;letter-spacing:-.02em;line-height:1.3;margin-bottom:6px;">${title}</div>
        <div style="font-size:13px;color:${isDark?'#8888AA':'#555577'};line-height:1.55;">${msg}</div>
      </div>
    </div>
    <div style="border-top:1px solid ${isDark?'rgba(255,255,255,0.07)':'rgba(0,0,0,0.07)'};padding-top:12px;font-size:10px;color:${isDark?'#4A4A6A':'#AAAACC'};display:flex;justify-content:space-between;">
      <span>creatorlytics.app</span>
      <span>${new Date().toLocaleDateString('id-ID',{day:'numeric',month:'long',year:'numeric'})}</span>
    </div>`;
  document.body.appendChild(shareDiv);
  try {
    const canvas = await html2canvas(shareDiv, { scale:2, useCORS:true, backgroundColor:null });
    shareDiv.remove();
    const link = document.createElement('a');
    link.download = 'insight-'+Date.now()+'.png';
    link.href = canvas.toDataURL('image/png');
    link.click();
    _qcToast('🖼 Insight disimpan sebagai PNG!');
  } catch(e) {
    shareDiv.remove();
    _qcToast('❌ Gagal export gambar.');
  }
}
function highlightCardHTML(h,allPosts){
  const p=h.post;
  const score=computeContentScore(p,allPosts);
  const sg=scoreGrade(score);
  const pltInf=pltInfo(p._plt||'tt');
  const truncName=p.name?p.name.slice(0,42)+(p.name.length>42?'…':''):'(Tanpa judul)';
  return`<div class="highlight-card">
    <div class="highlight-badge">
      <span class="highlight-icon">${h.icon}</span>
      <span class="highlight-label">${h.label}</span>
    </div>
    <div class="highlight-post-name">${truncName}</div>
    <div class="highlight-meta">
      <span style="color:${pltInf.color||'var(--t3)'};">${pltInf.emoji} ${pltInf.name}</span>
      <span style="font-family:var(--mono);font-size:11px;color:var(--t2);">${h.metric}</span>
      <span style="font-family:var(--mono);font-size:10px;padding:2px 6px;border-radius:4px;background:${sg.bg};color:${sg.color};font-weight:700;">${sg.grade} ${score}</span>
      </div>
      </div>
      <div class="mob-swipe-delete" onclick="deletePost('${p.id}','${plt}')">
        <span style="font-size:20px;">🗑</span>
        <span>Hapus</span>
      </div>
    </div>`;
}

/* ── ALL POSTS ── */
function getAllPosts(filterFn){
  const result=[];
  (months || []).forEach(m=>{
    if (!m) return;
    (m.weeks || []).forEach(w=>{
      if (!w) return;
      (accounts || []).forEach(a=>{
        allPlatforms().forEach(plt=>{
          const d=aD(w,a);
          if (!d) return;
          (getPltPosts(d,plt.id) || []).forEach(p=>{
            if (!p) return;
            const enriched={...p,_account:a,_plt:plt.id,_pltInfo:plt,_month:(MN[m.month] ? MN[m.month].slice(0,3) : '')+' '+m.year,_week:w.label,_flw:getPltFlw(d,plt.id)};
            if(!filterFn||filterFn(enriched,m))result.push(enriched);
          });
        });
      });
    });
  });
  return result;
}

function getCurrMonthStats(plt){
  const now=new Date();
  const posts=[];let totalPV=0;let latestFlwMap={};
  months.forEach(m=>{
    if(m.year!==now.getFullYear()||m.month!==now.getMonth())return;
    m.weeks.forEach(w=>{
      accounts.forEach(a=>{
        const d=aD(w,a);
        const wPosts=getPltPosts(d,plt);
        const flw=getPltFlw(d,plt);
        const pv=getPltPV(d,plt);
        if(flw>0)latestFlwMap[a]=flw;
        totalPV+=pv;
        wPosts.forEach(p=>posts.push(p));
      });
    });
  });
  const totalFlw=Object.values(latestFlwMap).reduce((s,v)=>s+v,0);
  return{posts:posts.length,reach:sumP(posts,'reach'),impression:sumP(posts,'impression'),
    engagement:posts.reduce((s,p)=>s+totalEng(p),0),
    followersGained:sumP(posts,'followersGained'),totalPV,totalFlw};
}

function getAllStats(plt){
  const posts=[];let totalPV=0;let latestFlwMap={};
  months.forEach(m=>{
    m.weeks.forEach(w=>{
      accounts.forEach(a=>{
        const d=aD(w,a);
        const wPosts=getPltPosts(d,plt);
        const flw=getPltFlw(d,plt);
        const pv=getPltPV(d,plt);
        if(flw>0)latestFlwMap[a]=flw;
        totalPV+=pv;
        wPosts.forEach(p=>posts.push(p));
      });
    });
  });
  const totalFlw=Object.values(latestFlwMap).reduce((s,v)=>s+v,0);
  return{posts:posts.length,reach:sumP(posts,'reach'),impression:sumP(posts,'impression'),
    engagement:posts.reduce((s,p)=>s+totalEng(p),0),
    followersGained:sumP(posts,'followersGained'),totalPV,totalFlw};
}

function getFilteredStats(plt,year,month){
  const posts=[];let totalPV=0;let latestFlwMap={};
  months.forEach(m=>{
    if(ovFilter==='month'&&(m.year!==year||m.month!==month))return;
    m.weeks.forEach(w=>{
      accounts.forEach(a=>{
        if(ovAcc!=='all'&&a!==ovAcc)return;
        const d=aD(w,a);
        const wPosts=getPltPosts(d,plt);
        const flw=getPltFlw(d,plt);
        const pv=getPltPV(d,plt);
        if(flw>0)latestFlwMap[a]=flw;
        totalPV+=pv;
        wPosts.forEach(p=>{
          if(ovFilter==='range'){
            if(!p.date)return;
            if(ovDateFrom&&p.date<ovDateFrom)return;
            if(ovDateTo&&p.date>ovDateTo)return;
          }
          posts.push(p);
        });
      });
    });
  });
  const totalFlw=Object.values(latestFlwMap).reduce((s,v)=>s+v,0);
  return{posts:posts.length,reach:sumP(posts,'reach'),impression:sumP(posts,'impression'),
    engagement:posts.reduce((s,p)=>s+totalEng(p),0),
    like:sumP(posts,'like'),comment:sumP(posts,'comment'),share:sumP(posts,'share'),
    save:sumP(posts,'save'),repost:sumP(posts,'repost'),
    followersGained:sumP(posts,'followersGained'),totalPV,totalFlw,_posts:posts};
}

function buildChartData(){
  const rel=ovFilter==='month'?months.filter(m=>m.year===ovYear&&m.month===ovMonth):months;
  return rel.map(m=>{
    const lbl=MN[m.month].slice(0,3)+' '+m.year;
    let ttI=0,igI=0,ttEng=0,igEng=0,ttER=0,igER=0,ttERc=0,igERc=0;
    // FIX: include custom platforms in total stats (merging ke tt+ig aggregate)
    const customPltData={};
    platforms.forEach(p=>{customPltData[p.id]={I:0,Eng:0,ER:0,ERc:0};});
    m.weeks.forEach(w=>{
      accounts.forEach(a=>{
        if(ovAcc!=='all'&&a!==ovAcc)return;
        const d=aD(w,a);
        allPlatforms().forEach(pltObj=>{
          const plt=pltObj.id;
          const wPosts=getPltPosts(d,plt);
          const flw=getPltFlw(d,plt);
          wPosts.forEach(p=>{
            if(ovFilter==='range'){if(!p.date)return;if(ovDateFrom&&p.date<ovDateFrom)return;if(ovDateTo&&p.date>ovDateTo)return;}
            const eng=totalEng(p);const er=calcER(p,sel.erMode,flw);
            if(plt==='tt'){ttI+=p.impression||0;ttEng+=eng;ttER+=er;ttERc++;}
            else if(plt==='ig'){igI+=p.impression||0;igEng+=eng;igER+=er;igERc++;}
            else if(customPltData[plt]){
              customPltData[plt].I+=p.impression||0;
              customPltData[plt].Eng+=eng;
              customPltData[plt].ER+=er;
              customPltData[plt].ERc++;
            }
          });
        });
      });
    });
    const result={lbl,ttI,igI,ttEng,igEng,ttER:ttERc?ttER/ttERc:0,igER:igERc?igER/igERc:0};
    // Attach custom platform data untuk stats yang lebih akurat
    platforms.forEach(p=>{
      const cd=customPltData[p.id]||{I:0,Eng:0,ER:0,ERc:0};
      result['plt_'+p.id+'_I']=cd.I;
      result['plt_'+p.id+'_Eng']=cd.Eng;
      result['plt_'+p.id+'_ER']=cd.ERc?cd.ER/cd.ERc:0;
    });
    return result;
  });
}

/* ══ PERSIST ══ */
const SYNC_META_KEY = KEY + '-meta';
let saveT=null;
function scheduleSave(d=700){clearTimeout(saveT);saveT=setTimeout(()=>{persist();markDirty('dash');markDirty('analytics');markDirty('report');markDirty('goals');markDirty('competitor');},d);}
async function persist(){
  try{
    const stateObj = {accounts,months,platforms,customGoals,pillars,overviewMetrics,ovAcc,analyticsTopSortBy,userNiche,contentIdeas,calendarPosts,competitors,userProfile,erMode:sel.erMode};
    const raw=JSON.stringify(stateObj);
    const now=new Date().toISOString();
    if(window.storage){
      await window.storage.set(KEY,raw);
      await window.storage.set(SYNC_META_KEY,JSON.stringify({updatedAt:now}));
    }else{
      safeSet(KEY, stateObj);
      safeSet(SYNC_META_KEY, {updatedAt:now});
    }
    
    // Sync to Supabase
    if(window.saveToSupabase){
      await window.saveToSupabase(JSON.parse(raw), now);
    }
  }catch(e){}
}
async function load(){
  let localData=null;
  let localMeta=null;
  try{
    if(window.storage){
      const r=await window.storage.get(KEY);localData=r?.value?JSON.parse(r.value):null;
      const m=await window.storage.get(SYNC_META_KEY);localMeta=m?.value?JSON.parse(m.value):null;
    }else{
      localData=safeGet(KEY);
      localMeta=safeGet(SYNC_META_KEY);
    }
  }catch(e){/* ignore */}

  // Try to load from Supabase first
  if(window.initSupabaseSync){
    try{
      const supabaseData = await window.initSupabaseSync();
      if(supabaseData){
        const serverUpdated = supabaseData.updated_at?new Date(supabaseData.updated_at).getTime():0;
        const localUpdated = localMeta?.updatedAt?new Date(localMeta.updatedAt).getTime():0;
        let sourceData = supabaseData.data || null;

        if(localData){
          if(localUpdated > serverUpdated){
            sourceData = localData;
            if(window.saveToSupabase){
              await window.saveToSupabase(localData, localMeta?.updatedAt||new Date().toISOString());
            }
          }
        }

        if(sourceData && validateAppData(sourceData)){
          accounts=stripLegacyAccounts(sourceData.accounts||[]);
          months=sourceData.months||[];
          platforms=(sourceData.platforms||[])
            .filter(p=>p.id!=='tt'&&p.id!=='ig'&&p.id!=='tiktok'&&p.id!=='instagram')
            .map(p=>({...p,emoji:p.emoji||'📌',name:p.name||p.id||'Platform'}));
          customGoals=Array.isArray(sourceData.customGoals)?sourceData.customGoals:(sourceData.goals?[]:[]);
          overviewMetrics=sourceData.overviewMetrics||overviewMetrics;
          ovAcc=sourceData.ovAcc||ovAcc;
          analyticsTopSortBy=sourceData.analyticsTopSortBy||analyticsTopSortBy;
          if(sourceData.userNiche&&NICHE_BENCHMARKS[sourceData.userNiche])userNiche=sourceData.userNiche;
          if(sourceData.pillars&&Array.isArray(sourceData.pillars)&&sourceData.pillars.length>0){
            pillars=sourceData.pillars;
            pillars.forEach(p=>{if(!p.bg&&p.color)p.bg=p.color+'20';});
          }else{pillars=[...DEFAULT_PILLARS.map(p=>({...p}))]}
          if(sourceData.contentIdeas&&Array.isArray(sourceData.contentIdeas))contentIdeas=sourceData.contentIdeas;
          if(sourceData.calendarPosts&&Array.isArray(sourceData.calendarPosts))calendarPosts=sourceData.calendarPosts;
          if(sourceData.competitors&&Array.isArray(sourceData.competitors))competitors=sourceData.competitors;
          if(sourceData.userProfile&&typeof sourceData.userProfile==='object')userProfile={...userProfile,...sourceData.userProfile};
          if(sourceData.erMode)sel.erMode=sourceData.erMode;
          normalizeOverviewMetrics();
          months.forEach(m=>{
            if(!m.weeks)m.weeks=[];
            m.weeks.forEach(w=>{
              if(!w.data)w.data={};
              accounts.forEach(a=>{
                if(!w.data[a])w.data[a]=emptyAcc();
                else{
                  if(!w.data[a].ttPosts)w.data[a].ttPosts=[];
                  if(!w.data[a].igPosts)w.data[a].igPosts=[];
                  if(!w.data[a].ttFlw)w.data[a].ttFlw=0;
                  if(!w.data[a].igFlw)w.data[a].igFlw=0;
                  if(w.data[a].ttPV===undefined)w.data[a].ttPV=0;
                  if(w.data[a].igPV===undefined)w.data[a].igPV=0;
                  platforms.forEach(plt=>{
                    if(!w.data[a][plt.id+'Posts'])w.data[a][plt.id+'Posts']=[];
                    if(!w.data[a][plt.id+'Flw'])w.data[a][plt.id+'Flw']=0;
                    if(!w.data[a][plt.id+'PV'])w.data[a][plt.id+'PV']=0;
                  });
                }
              });
            });
          });
          return;
        }
      }
    }catch(e){
      console.warn('Failed to load from Supabase, falling back to localStorage:', e);
    }
  }
  
  // Fallback to localStorage
  try{
    if(localData && validateAppData(localData)){
      accounts=stripLegacyAccounts(localData.accounts||[]);
      months=localData.months||[];
      platforms=(localData.platforms||[])
        .filter(p=>p.id!=='tt'&&p.id!=='ig'&&p.id!=='tiktok'&&p.id!=='instagram')
        .map(p=>({...p,emoji:p.emoji||'📌',name:p.name||p.id||'Platform'}));
      customGoals=Array.isArray(localData.customGoals)?localData.customGoals:[];
      overviewMetrics=localData.overviewMetrics||overviewMetrics;
      ovAcc=localData.ovAcc||ovAcc;
      analyticsTopSortBy=localData.analyticsTopSortBy||analyticsTopSortBy;
      if(localData.userNiche&&NICHE_BENCHMARKS[localData.userNiche])userNiche=localData.userNiche;
      if(localData.pillars&&Array.isArray(localData.pillars)&&localData.pillars.length>0){
        pillars=localData.pillars;
        pillars.forEach(p=>{if(!p.bg&&p.color)p.bg=p.color+'20';});
      }else{pillars=[...DEFAULT_PILLARS.map(p=>({...p}))]}
      if(localData.contentIdeas&&Array.isArray(localData.contentIdeas))contentIdeas=localData.contentIdeas;
      if(localData.calendarPosts&&Array.isArray(localData.calendarPosts))calendarPosts=localData.calendarPosts;
      if(localData.competitors&&Array.isArray(localData.competitors))competitors=localData.competitors;
      if(localData.userProfile&&typeof localData.userProfile==='object')userProfile={...userProfile,...localData.userProfile};
      if(localData.erMode)sel.erMode=localData.erMode;
    }
    const beforeStrip = accounts.slice();
    accounts = stripLegacyAccounts(accounts);
    // If legacy accounts were stripped, immediately re-save clean state
    if(beforeStrip.length !== accounts.length) scheduleSave(100);
    normalizeOverviewMetrics();
    months.forEach(m=>{
      if(!m.weeks)m.weeks=[];
      m.weeks.forEach(w=>{
        if(!w.data)w.data={};
        accounts.forEach(a=>{
          if(!w.data[a])w.data[a]=emptyAcc();
          else{
            if(!w.data[a].ttPosts)w.data[a].ttPosts=[];
            if(!w.data[a].igPosts)w.data[a].igPosts=[];
            if(!w.data[a].ttFlw)w.data[a].ttFlw=0;
            if(!w.data[a].igFlw)w.data[a].igFlw=0;
            if(w.data[a].ttPV===undefined)w.data[a].ttPV=0;
            if(w.data[a].igPV===undefined)w.data[a].igPV=0;
            w.data[a].ttPosts.forEach(p=>{
              if(p.followersGained===undefined)p.followersGained=0;
              if(p.name===undefined)p.name='';
              if(!p.pillar||!pillars.find(pl=>pl.id===p.pillar))p.pillar=pillars[pillars.length-1]?.id||'other';
            });
            w.data[a].igPosts.forEach(p=>{
              if(p.followersGained===undefined)p.followersGained=0;
              if(p.name===undefined)p.name='';
              if(!p.pillar||!pillars.find(pl=>pl.id===p.pillar))p.pillar=pillars[pillars.length-1]?.id||'other';
            });
            platforms.forEach(p=>{
              if(!w.data[a][p.id+'Posts'])w.data[a][p.id+'Posts']=[];
              if(w.data[a][p.id+'Flw']===undefined)w.data[a][p.id+'Flw']=0;
              if(w.data[a][p.id+'PV']===undefined)w.data[a][p.id+'PV']=0;
              w.data[a][p.id+'Posts'].forEach(post=>{
                if(!post.pillar||!pillars.find(pl=>pl.id===post.pillar))post.pillar=pillars[pillars.length-1]?.id||'other';
                if(post.name===undefined)post.name='';
              });
            });
          }
        });
      });
    });
  }catch(e){accounts=[];platforms=[];customGoals=[];}
}
function flash(){
  const el=document.getElementById('save-badge');
  if(!el)return;
  el.classList.add('show');
  setTimeout(()=>el.classList.remove('show'),2200);
}

/* ══ THEME ══ */
const SVG_MOON=`<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M12.5 8.5A5.5 5.5 0 0 1 5.5 1.5a5.5 5.5 0 1 0 7 7z" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>`;
const SVG_SUN=`<svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 1v1M7 12v1M1 7H0m13 0h1M2.6 2.6l.7.7M10.7 10.7l.7.7M2.6 11.4l.7-.7M10.7 3.3l.7-.7" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/><circle cx="7" cy="7" r="2.8" stroke="currentColor" stroke-width="1.2"/></svg>`;
const SVG_MOON_NAV=`<svg width="15" height="15" viewBox="0 0 15 15" fill="none"><path d="M13 9A5.5 5.5 0 0 1 6 2a5.5 5.5 0 1 0 7 7z" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>`;
const SVG_SUN_NAV=`<svg width="15" height="15" viewBox="0 0 15 15" fill="none"><path d="M7.5 1v1M7.5 13v1M1 7.5H0m14 0h1M3 3l.7.7M11.3 11.3l.7.7M3 12l.7-.7M11.3 3.7l.7-.7" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/><circle cx="7.5" cy="7.5" r="3" stroke="currentColor" stroke-width="1.3"/></svg>`;
function toggleTheme(){
  const cur=document.documentElement.getAttribute('data-theme');
  const next=cur==='dark'?'light':'dark';
  document.documentElement.setAttribute('data-theme',next);
  const btn=document.getElementById('theme-btn');
  const icoNav=document.getElementById('theme-ico-nav');
  if(btn)btn.innerHTML=next==='dark'?SVG_SUN:SVG_MOON;
  if(icoNav)icoNav.innerHTML=next==='dark'?SVG_SUN_NAV:SVG_MOON_NAV;
  const btnNav=document.getElementById('theme-btn-nav');
  if(btnNav){const t=btnNav.childNodes;t.forEach(n=>{if(n.nodeType===3)n.textContent=next==='dark'?' Light Mode':' Dark Mode';});}
  try{localStorage.setItem('sm-theme',next);}catch(e){}
  destroyCharts();
  if(view==='analytics')renderAnalytics();
  if(view==='dash')renderDashboard();
}
function initTheme(){
  try{
    const saved = localStorage.getItem('sm-theme');
    const theme = (saved === 'light') ? 'light' : 'dark'; // default dark if not set
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('sm-theme', theme);
    const isDark = theme === 'dark';
    const btn = document.getElementById('theme-btn');
    const icoNav = document.getElementById('theme-ico-nav');
    const btnNav = document.getElementById('theme-btn-nav');
    if(btn) btn.innerHTML = isDark ? SVG_SUN : SVG_MOON;
    if(icoNav) icoNav.innerHTML = isDark ? SVG_SUN_NAV : SVG_MOON_NAV;
    if(btnNav){ btnNav.childNodes.forEach(n=>{ if(n.nodeType===3) n.textContent = isDark ? ' Light Mode' : ' Dark Mode'; }); }
  }catch(e){}
}

/* ══ VIEW SWITCHING ══ */
const VIEW_LABELS={dash:'Dashboard',content:'Input Data',analytics:'Analytics',goals:'Goals',report:'Laporan',planner:'Content Planner',calendar:'Kalender',competitor:'Kompetitor'};
function switchView(v){
  // Planner (Bank Konten) jalan sebagai view mandiri
  view=v;
  ['dash','content','analytics','goals','report','planner','calendar','competitor'].forEach(id=>{
    const el=document.getElementById('v-'+id);
    const nav=document.getElementById('nav-'+id);
    const mob=document.getElementById('mn-'+id);
    if(el)el.classList.toggle('active',id===v);
    if(nav)nav.classList.toggle('act',id===v);
    if(mob)mob.classList.toggle('act',id===v);
  });
  const tl=document.getElementById('topbar-title');
  if(tl)tl.textContent=VIEW_LABELS[v]||v;
  destroyCharts();
  if(v==='dash'){if(isDirty('dash')||!_renderedViews.has('dash')){renderDashboard();clearDirty('dash');_renderedViews.add('dash');}}
  if(v==='analytics'){if(isDirty('analytics')||!_renderedViews.has('analytics')){renderAnalytics();clearDirty('analytics');_renderedViews.add('analytics');}}
  if(v==='goals'){renderGoals();clearDirty('goals');_renderedViews.add('goals');}
  if(v==='report'){if(isDirty('report')||!_renderedViews.has('report')){renderReport();clearDirty('report');_renderedViews.add('report');}}
  if(v==='content'){renderContentTimeline();_renderedViews.add('content');}
  if(v==='planner'){renderPlanner();_renderedViews.add('planner');}
  if(v==='calendar'){renderCalendar();_renderedViews.add('calendar');}
  if(v==='competitor'){if(isDirty('competitor')||!_renderedViews.has('competitor')){renderCompetitor();clearDirty('competitor');_renderedViews.add('competitor');}}
}
function mobContent(panel){
  mobContentPanel=panel;
  const isMob=window.innerWidth<=768;
  if(!isMob)return;
  ['cm-panel','ca-panel','ce-panel'].forEach(id=>{
    const el=document.getElementById(id);
    if(el)el.classList.remove('mob-show');
  });
  const map={months:'cm-panel',accounts:'ca-panel',editor:'ce-panel'};
  const el=document.getElementById(map[panel]);
  if(el)el.classList.add('mob-show');
  // Highlight mobile content tabs
  ['months','accounts','editor'].forEach(k=>{
    const t=document.getElementById('mct-'+k);
    if(t)t.classList.toggle('act',k===panel);
  });
}

/* ── Swipe-to-delete for mobile post cards (M3) ── */
function initSwipeToDelete() {
  document.querySelectorAll('.mob-post-card').forEach(card => {
    let startX = 0, currentX = 0, isDragging = false;
    const inner = card.querySelector('.mob-post-card-inner');
    if (!inner) return;

    const onTouchStart = e => {
      startX = e.touches[0].clientX;
      isDragging = true;
      card.classList.remove('swipe-reveal');
    };
    const onTouchMove = e => {
      if (!isDragging) return;
      currentX = e.touches[0].clientX;
      const diff = startX - currentX;
      if (diff > 0) {
        inner.style.transform = `translateX(${-Math.min(diff, 80)}px)`;
      }
    };
    const onTouchEnd = () => {
      if (!isDragging) return;
      isDragging = false;
      const diff = startX - currentX;
      inner.style.transform = '';
      if (diff > 60) {
        card.classList.add('swipe-reveal');
      }
    };

    card.addEventListener('touchstart', onTouchStart, { passive: true });
    card.addEventListener('touchmove', onTouchMove, { passive: true });
    card.addEventListener('touchend', onTouchEnd, { passive: true });
  });
}

/* ══ MODALS ══ */
function openModal(id){
  if(id==='m-month'){
    const cy=new Date().getFullYear();
    const ys=document.getElementById('mi-year');
    ys.innerHTML='';
    for(let y=cy-3;y<=cy+3;y++){const o=document.createElement('option');o.value=y;o.textContent=y;if(y===cy)o.selected=true;ys.appendChild(o);}
    document.getElementById('mi-month').value=new Date().getMonth();
  }
  if(id==='m-acc'){
    document.getElementById('mi-acc').value='';
    setTimeout(()=>document.getElementById('mi-acc').focus(),50);
  }
  if(id==='m-pillar'){renderPillarModal();}
  document.getElementById(id)?.classList.remove('hidden');
}
function closeModal(id){document.getElementById(id)?.classList.add('hidden');}

/* ══ QUICK CAPTURE ══ */
let _qcPlt='tt';
function qcSetPlt(pid,btn){
  _qcPlt=pid;
  document.querySelectorAll('.qc-plt-btn').forEach(b=>b.classList.remove('act'));
  btn.classList.add('act');
}
/* qcShowStep/qcNext/qcBack removed in Sprint 7 — single-form QC modal */
function _qcBuildPost_v1_DEPRECATED(saveFull){
  // Replaced by Sprint 7 _qcBuildPost above
  const name=document.getElementById('qc-name')?.value.trim()||'';
  const acc=document.getElementById('qc-acc')?.value||accounts[0]||'';
  const date=document.getElementById('qc-date')?.value||new Date().toISOString().split('T')[0];
  if(!acc)return null;
  // Find or create target month+week
  const d=new Date(date+'T12:00:00');
  const y=d.getFullYear();const m=d.getMonth();
  let mo=months.find(x=>x.year===y&&x.month===m);
  if(!mo){
    mo={id:uid(),year:y,month:m,weeks:[]};
    months.push(mo);months.sort((a,b)=>a.year!==b.year?a.year-b.year:a.month-b.month);
    exp[mo.id]=true;
  }
  // Use last week of that month, or create one
  if(!mo.weeks.length){
    const wk={id:uid(),label:'Minggu 1',data:{}};
    if(!wk.data[acc])wk.data[acc]=emptyAcc();
    mo.weeks.push(wk);
  }
  const wk=mo.weeks[mo.weeks.length-1];
  if(!wk.data[acc])wk.data[acc]=emptyAcc();
  const d2=wk.data[acc];
  const key=_qcPlt+'Posts';
  if(!d2[key])d2[key]=[];
  const p=emptyPost();
  p.date=date;p.name=name;
  p.pillar=pillars[0]?.id||'other';
  if(saveFull){
    p.like=parseInt(document.getElementById('qc-like')?.value)||0;
    p.comment=parseInt(document.getElementById('qc-comment')?.value)||0;
    p.share=parseInt(document.getElementById('qc-share')?.value)||0;
    p.save=parseInt(document.getElementById('qc-save')?.value)||0;
    p.reach=parseInt(document.getElementById('qc-reach')?.value)||0;
    p.impression=parseInt(document.getElementById('qc-impression')?.value)||0;
  }
  d2[key].push(p);
  return{mo,wk,acc,plt:_qcPlt};
}
function qcSaveFull(){
  // Validate: clamp all number fields to >= 0 and show error if somehow negative
  const numFields = ['qc-like','qc-comment','qc-share','qc-save','qc-reach','qc-impression'];
  let hasError = false;
  numFields.forEach(id => {
    const el = document.getElementById(id);
    if (!el) return;
    const v = parseInt(el.value);
    if (!isNaN(v) && v < 0) {
      el.value = 0;
      el.style.borderColor = 'var(--red)';
      hasError = true;
      setTimeout(() => { el.style.borderColor = ''; }, 1500);
    }
  });
  if (hasError) {
    _qcToast('⚠️ Angka tidak boleh negatif, sudah dikoreksi ke 0.');
    return;
  }
  const isEdit = !!window._qcEditId;
  const res = _qcBuildPost(true); if(!res) return;
  closeModal('m-qc'); scheduleSave(300);
  // Reset modal UI back to add-mode
  const titleEl = document.querySelector('#m-qc .modal-title');
  if(titleEl) titleEl.textContent = '+ Tambah Konten';
  const saveBtn = document.getElementById('qc-save-btn');
  if(saveBtn) saveBtn.textContent = '💾 Simpan Post';
  // Revisi 2: first insight modal (only on very first post)
  if(!isEdit) {
    const allNow = getAllPosts();
    if(allNow.length === 1) showFirstInsightModal(allNow[0]);
  }
  // Re-render current view
  if(view === 'content') renderContentTimeline();
  else if(view === 'dash') renderDashboard();
  else if(view === 'analytics') { destroyCharts(); renderAnalytics(); }
  // Update last-input timestamp (Revisi 5)
  try { localStorage.setItem('cl-last-input', new Date().toISOString()); } catch(e) {}
  // Toast
  if(isEdit) { _qcToast('✅ Post diperbarui!'); }
  else { smartSaveToast(res._savedPost, res); }
}
function qcSaveLater(){
  // Must be on step 2 to call this, but check name is filled
  const name=document.getElementById('qc-name')?.value.trim();
  if(!name){qcShowStep(1);document.getElementById('qc-name')?.focus();return;}
  const res=_qcBuildPost(false);if(!res)return;
  closeModal('m-qc');scheduleSave(300);
  if(view==='dash')renderDashboard();
  else if(view==='analytics'){destroyCharts();renderAnalytics();}
  _qcToast('📝 Disimpan! Lengkapi angka di tab Content.');
}
function _qcToast(msg){
  let t=document.getElementById('qc-toast');
  if(!t){
    t=document.createElement('div');t.id='qc-toast';
    t.style.cssText='position:fixed;bottom:100px;left:50%;transform:translateX(-50%) translateY(20px);background:rgba(30,30,45,0.97);color:#fff;padding:10px 20px;border-radius:12px;font-size:13px;font-weight:700;z-index:9999;transition:all .3s;opacity:0;pointer-events:none;border:1px solid rgba(255,255,255,0.12);box-shadow:0 8px 24px rgba(0,0,0,0.4);white-space:nowrap;';
    document.body.appendChild(t);
  }
  t.innerHTML='';
  t.style.pointerEvents='none';
  t.textContent=msg;
  t.style.opacity='1';t.style.transform='translateX(-50%) translateY(0)';
  clearTimeout(t._to);
  t._to=setTimeout(()=>{t.style.opacity='0';t.style.transform='translateX(-50%) translateY(10px)';},2400);
}

/* Toast dengan tombol Undo — untuk destructive actions */
let _undoStack = null; // {fn, timer}
function _qcToastUndo(msg, undoFn) {
  // Clear any previous undo
  if (_undoStack) { clearTimeout(_undoStack.timer); _undoStack = null; }

  let t = document.getElementById('qc-toast');
  if (!t) {
    t = document.createElement('div'); t.id = 'qc-toast';
    t.style.cssText = 'position:fixed;bottom:100px;left:50%;transform:translateX(-50%) translateY(20px);background:rgba(30,30,45,0.97);color:#fff;padding:10px 16px;border-radius:12px;font-size:13px;font-weight:700;z-index:9999;transition:all .3s;opacity:0;border:1px solid rgba(255,255,255,0.12);box-shadow:0 8px 24px rgba(0,0,0,0.4);display:flex;align-items:center;gap:12px;white-space:nowrap;';
    document.body.appendChild(t);
  }

  t.style.pointerEvents = 'auto';
  t.innerHTML = `<span>${msg}</span><button onclick="window._doUndo()" style="padding:4px 12px;border-radius:8px;border:1.5px solid rgba(61,199,183,0.6);background:transparent;color:#3dc7b7;font-size:12px;font-weight:800;cursor:pointer;flex-shrink:0;transition:background .15s;" onmouseover="this.style.background='rgba(61,199,183,0.15)'" onmouseout="this.style.background='transparent'">Undo</button>`;
  t.style.opacity = '1'; t.style.transform = 'translateX(-50%) translateY(0)';
  clearTimeout(t._to);

  // Commit deletion after 5 seconds
  const timer = setTimeout(() => {
    _undoStack = null;
    t.style.opacity = '0'; t.style.transform = 'translateX(-50%) translateY(10px)';
    t.style.pointerEvents = 'none';
  }, 5000);

  _undoStack = { fn: undoFn, timer };
  window._doUndo = () => {
    if (!_undoStack) return;
    clearTimeout(_undoStack.timer);
    _undoStack.fn();
    _undoStack = null;
    t.style.opacity = '0'; t.style.transform = 'translateX(-50%) translateY(10px)';
    t.style.pointerEvents = 'none';
    _qcToast('↩ Post dikembalikan!');
  };
}

/* ── Keyboard shortcuts cheatsheet ── */
function _showShortcutsToast() {
  let el = document.getElementById('kb-cheatsheet');
  if (el) { el.style.opacity='0'; el.style.transform='translateY(8px)'; setTimeout(()=>el.remove(),200); return; }
  el = document.createElement('div');
  el.id = 'kb-cheatsheet';
  el.style.cssText = 'position:fixed;bottom:24px;right:24px;background:rgba(13,17,23,0.97);border:1px solid rgba(255,255,255,0.12);border-radius:14px;padding:16px 20px;z-index:9998;font-size:12px;color:#eef0ff;box-shadow:0 8px 32px rgba(0,0,0,0.5);min-width:230px;backdrop-filter:blur(12px);opacity:0;transform:translateY(8px);transition:all .2s;cursor:pointer;';
  el.innerHTML = `
    <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.1em;color:rgba(61,199,183,0.85);margin-bottom:10px;">⌨️ Keyboard Shortcuts</div>
    ${[
      ['N',      'Post baru (Quick Add)'],
      ['/',      'Fokus ke pencarian konten'],
      ['Esc',    'Tutup modal'],
      ['G → D',  'Ke Dashboard'],
      ['G → A',  'Ke Analytics'],
      ['G → C',  'Ke Konten'],
      ['G → G',  'Ke Goals'],
      ['G → R',  'Ke Laporan'],
      ['G → P',  'Ke Planner'],
      ['G → K',  'Ke Kalender'],
      ['G → M',  'Ke Kompetitor'],
      ['?',      'Tampilkan/sembunyikan ini'],
    ].map(([k,v]) => `<div style="display:flex;align-items:center;justify-content:space-between;gap:14px;padding:4px 0;border-bottom:1px solid rgba(255,255,255,0.05);">
      <kbd style="background:rgba(255,255,255,0.1);border:1px solid rgba(255,255,255,0.18);border-radius:5px;padding:1px 8px;font-family:monospace;font-size:11px;white-space:nowrap;flex-shrink:0;">${k}</kbd>
      <span style="color:rgba(158,168,200,0.8);font-size:11px;text-align:right;">${v}</span>
    </div>`).join('')}
    <div style="margin-top:10px;font-size:10px;color:rgba(107,117,148,0.6);text-align:center;">Klik untuk tutup · hilang otomatis 8 detik</div>
  `;
  el.onclick = () => { el.style.opacity='0'; el.style.transform='translateY(8px)'; setTimeout(()=>el.remove(),200); };
  document.body.appendChild(el);
  requestAnimationFrame(() => { el.style.opacity='1'; el.style.transform='translateY(0)'; });
  setTimeout(() => { if(el.parentNode){ el.style.opacity='0'; el.style.transform='translateY(8px)'; setTimeout(()=>el.remove(),200); } }, 8000);
}

/* ══ MONTHS & WEEKS ══ */
function confirmAddMonth(){
  const y=parseInt(document.getElementById('mi-year').value);
  const m=parseInt(document.getElementById('mi-month').value);
  if(months.some(mo=>mo.year===y&&mo.month===m)){_qcToast(`⚠️ ${MN[m]} ${y} sudah ada.`);return;}
  const mo=emptyMonth(y,m);months.push(mo);
  months.sort((a,b)=>a.year!==b.year?a.year-b.year:a.month-b.month);
  exp[mo.id]=true;closeModal('m-month');scheduleSave(100);
  sel.mid=mo.id;sel.wid=null;sel.acc=null;
  if(view==='content')renderContentTimeline();
}
function togMonth(mid){
  exp[mid]=!exp[mid];sel.mid=mid;
  if(!exp[mid]){sel.wid=null;sel.acc=null;}
  if(view==='content')renderContentTimeline();
}
function selWeek(mid,wid){
  sel.mid=mid;sel.wid=wid;exp[mid]=true;
  if(!sel.acc&&accounts.length)sel.acc=accounts[0];
  if(view==='content')renderContentTimeline();
  if(window.innerWidth<=768)mobContent('accounts');
}
function addWeek(mid){
  const m=getM(mid);if(!m)return;
  const w=emptyWeek(m.weeks.length+1);m.weeks.push(w);
  scheduleSave(100);selWeek(mid,w.id);
}
function delWeek(mid,wid){
  const m=getM(mid);if(!m)return;
  showConfirm('Hapus Minggu Ini?','Semua data post di minggu ini akan hilang permanen.',()=>{
    m.weeks=m.weeks.filter(w=>w.id!==wid);
    if(sel.wid===wid){sel.wid=null;sel.acc=null;}
    scheduleSave(100);if(view==='content')renderContentTimeline();
  },true);
}
function delMonth(mid){
  const _mo=getM(mid);
  const _lbl=_mo?(MN[_mo.month]+' '+_mo.year):'bulan ini';
  showConfirm('Hapus '+_lbl+'?','Semua data konten bulan ini akan terhapus permanen.',()=>{
    months=months.filter(m=>m.id!==mid);
    if(sel.mid===mid){sel.mid=null;sel.wid=null;sel.acc=null;}
    delete exp[mid];scheduleSave(100);if(view==='content')renderContentTimeline();
  },true);
}

/* ══ ACCOUNTS ══ */
function confirmAddAcc(){
  const name=document.getElementById('mi-acc').value.trim();
  if(!name){_qcToast('⚠️ Nama tidak boleh kosong.');return;}
  if(accounts.includes(name)){_qcToast('⚠️ "'+name+'" sudah ada.');return;}
  accounts.push(name);
  months.forEach(m=>m.weeks.forEach(w=>{w.data[name]=emptyAcc();}));
  closeModal('m-acc');scheduleSave(100);renderAccList();
}
function openRename(name){
  renameTarget=name;
  document.getElementById('mi-rename').value=name;
  document.getElementById('m-rename').classList.remove('hidden');
  setTimeout(()=>{const i=document.getElementById('mi-rename');i.focus();i.select();},50);
}
function confirmRename(){
  const nw=document.getElementById('mi-rename').value.trim();
  if(!nw||nw===renameTarget){closeModal('m-rename');return;}
  if(accounts.includes(nw)){_qcToast('⚠️ \"' + nw + '\" sudah dipakai.');return;}
  const i=accounts.indexOf(renameTarget);if(i===-1)return;
  accounts[i]=nw;
  months.forEach(m=>m.weeks.forEach(w=>{
    if(w.data[renameTarget]!==undefined){w.data[nw]=w.data[renameTarget];delete w.data[renameTarget];}
  }));
  if(sel.acc===renameTarget)sel.acc=nw;
  closeModal('m-rename');renameTarget=null;scheduleSave(100);if(view==='content')renderContentTimeline();
}
function deleteAcc(name){
  showConfirm(`Hapus Akun "${name}"?`,'Semua data posting akun ini akan dihapus permanen.',()=>{
    accounts=accounts.filter(a=>a!==name);
    months.forEach(m=>m.weeks.forEach(w=>{delete w.data[name];}));
    if(sel.acc===name)sel.acc=accounts[0]||null;
    scheduleSave(100);if(view==='content')renderContentTimeline();
  },true);
}
function selAcc(a){
  sel.acc=a;if(view==='content')renderContentTimeline();
  if(window.innerWidth<=768)mobContent('editor');
}

/* ══ PLATFORMS ══ */
function confirmAddPlatform(){
  const name=document.getElementById('mi-plt-name').value.trim();
  const emoji=document.getElementById('mi-plt-emoji').value.trim()||'📌';
  if(!name){_qcToast('⚠️ Nama platform tidak boleh kosong.');return;}
  const id='plt_'+name.toLowerCase().replace(/[^a-z0-9]/g,'_')+'_'+uid().slice(0,4);
  if(platforms.some(p=>p.name.toLowerCase()===name.toLowerCase())){_qcToast('⚠️ Platform sudah ada.');return;}
  platforms.push({id,name,emoji});
  months.forEach(m=>m.weeks.forEach(w=>{
    accounts.forEach(a=>{
      if(!w.data[a])w.data[a]=emptyAcc();
      w.data[a][id+'Posts']=[];w.data[a][id+'Flw']=0;w.data[a][id+'PV']=0;
    });
  }));
  closeModal('m-plt');scheduleSave(100);renderNavPlatforms();renderEditorHeader();
}
function deletePlatform(id){
  const p=platforms.find(x=>x.id===id);
  if(!p)return;
  const bodyEl=document.getElementById('del-plt-body');
  const btnEl=document.getElementById('del-plt-confirm-btn');
  if(bodyEl)bodyEl.textContent=`Platform "${p.emoji||'📌'} ${p.name}" akan dihapus permanen.`;
  if(btnEl){
    btnEl.onclick=()=>{
      platforms=platforms.filter(x=>x.id!==id);
      if(sel.plt===id)sel.plt='tt';
      closeModal('m-del-plt');
      scheduleSave(100);renderNavPlatforms();if(view==='content')renderContentTimeline();
    };
  }
  document.getElementById('m-del-plt').classList.remove('hidden');
}

/* ══ PILLARS MANAGEMENT ══ */
function renderPillarModal(){
  const listEl=document.getElementById('pillar-list-ui');
  if(!listEl)return;
  listEl.innerHTML=pillars.map(p=>`
    <div class="pillar-mgmt-row">
      <span class="pillar-mgmt-emoji">${p.emoji}</span>
      <span class="pillar-mgmt-name">${p.label}</span>
      <span class="pillar-mgmt-clr" style="background:${p.color};"></span>
      <button class="pillar-mgmt-btn" title="Rename" onclick="openRenamePillar('${p.id}')">✏</button>
      ${pillars.length>1
        ?`<button class="pillar-mgmt-btn del" title="Hapus" onclick="confirmDeletePillar('${p.id}')">✕</button>`
        :'<span style="width:26px;display:inline-block;"></span>'}
    </div>`).join('');
  const sw=document.getElementById('pillar-clr-swatches');
  if(sw)sw.innerHTML=PILLAR_PALETTE.map(c=>`<div class="swatch${c===selPillarColor?' sel':''}" style="background:${c}" onclick="selPillarColor='${c}';renderPillarModal()"></div>`).join('');
}
function confirmAddPillar(){
  const emoji=(document.getElementById('mi-pil-emoji').value.trim()||'📌');
  const name=document.getElementById('mi-pil-name').value.trim();
  if(!name){_qcToast('⚠️ Nama pilar tidak boleh kosong.');return;}
  if(pillars.some(p=>p.label.toLowerCase()===name.toLowerCase())){_qcToast('⚠️ Pilar sudah ada.');return;}
  const id='pil_'+uid();
  pillars.push({id,label:name,emoji,color:selPillarColor,bg:selPillarColor+'20'});
  document.getElementById('mi-pil-emoji').value='';
  document.getElementById('mi-pil-name').value='';
  selPillarColor=PILLAR_PALETTE[0];
  scheduleSave(100);renderPillarModal();
  if(view==='dash')renderDashboard();
  if(view==='analytics')renderAnalytics();
}
function openRenamePillar(id){
  renamePillarTarget=id;
  const pil=pillars.find(p=>p.id===id);if(!pil)return;
  document.getElementById('mi-pil-rename').value=pil.label;
  document.getElementById('mi-pil-rename-emoji').value=pil.emoji;
  document.getElementById('m-pillar-rename').classList.remove('hidden');
  setTimeout(()=>document.getElementById('mi-pil-rename').focus(),50);
}
function confirmRenamePillar(){
  if(!renamePillarTarget)return;
  const newName=document.getElementById('mi-pil-rename').value.trim();
  const newEmoji=document.getElementById('mi-pil-rename-emoji').value.trim();
  if(!newName){_qcToast('⚠️ Nama tidak boleh kosong.');return;}
  const dupe=pillars.find(p=>p.id!==renamePillarTarget&&p.label.toLowerCase()===newName.toLowerCase());
  if(dupe){_qcToast('⚠️ Nama sudah digunakan pilar lain.');return;}
  const pil=pillars.find(p=>p.id===renamePillarTarget);if(!pil)return;
  pil.label=newName;if(newEmoji)pil.emoji=newEmoji;
  closeModal('m-pillar-rename');renamePillarTarget=null;
  scheduleSave(100);renderPillarModal();
  if(view==='dash')renderDashboard();
  if(view==='analytics')renderAnalytics();
  if(view==='content')renderContentTimeline();
}
function confirmDeletePillar(id){
  if(pillars.length<=1){_qcToast('⚠️ Minimal 1 pilar harus ada.');return;}
  const pil=pillars.find(p=>p.id===id);if(!pil)return;
  const fallback=pillars.find(p=>p.id!==id);
  const postCount=getAllPosts().filter(p=>p.pillar===id).length;
  const extra=postCount>0?`${postCount} konten dengan pilar ini akan dipindahkan ke "${fallback.label}". `:'';
  // showConfirm replaces native confirm() for cross-browser safety
  showConfirm(`Hapus Pilar "${pil.label}"?`,extra+'Tindakan ini tidak dapat dibatalkan.',()=>{
  months.forEach(m=>m.weeks.forEach(w=>accounts.forEach(a=>{
    allPlatforms().forEach(plt=>{getPltPosts(aD(w,a),plt.id).forEach(post=>{if(post.pillar===id)post.pillar=fallback.id;});});
  })));
  pillars=pillars.filter(p=>p.id!==id);
  scheduleSave(100);renderPillarModal();
  if(view==='dash')renderDashboard();
  if(view==='analytics'){destroyCharts();renderAnalytics();}
  if(view==='content')renderContentTimeline();
  },true); // end showConfirm callback
}

/* ══ GOALS ══ */
// ── Custom Goals System ──
// goals is now an array of goal objects: [{id, label, platform, metric, target, emoji}]

function allGoals() { return customGoals; }

function getGoalCurrentValue(g) {
  // metric: 'posts','reach','engagement','followers','likes','comments','shares'
  // platform: 'all' or a platform id
  const now = new Date();
  const plts = g.platform === 'all' ? allPlatforms().map(p => p.id) : [g.platform];
  if (g.metric === 'posts') {
    let n = 0;
    months.forEach(m => {
      if (m.year !== now.getFullYear() || m.month !== now.getMonth()) return;
      m.weeks.forEach(w => { accounts.forEach(a => {
        plts.forEach(pid => { n += getPltPosts(aD(w,a), pid).length; });
      }); });
    });
    return n;
  }
  let total = 0;
  plts.forEach(pid => {
    const s = getCurrMonthStats(pid);
    if (g.metric === 'reach')       total += s.reach;
    if (g.metric === 'engagement')  total += s.engagement;
    if (g.metric === 'followers')   total += s.followersGained;
    if (g.metric === 'likes')       total += s.likes || 0;
    if (g.metric === 'comments')    total += s.comments || 0;
    if (g.metric === 'shares')      total += s.shares || 0;
  });
  return total;
}

/* ── getGoalValueForMonth: same as getGoalCurrentValue but for any year/month ── */
function getGoalValueForMonth(g, targetYear, targetMonth) {
  const plts = g.platform === 'all' ? allPlatforms().map(p => p.id) : [g.platform];
  let total = 0;
  months.forEach(m => {
    if (m.year !== targetYear || m.month !== targetMonth) return;
    m.weeks.forEach(w => {
      accounts.forEach(a => {
        const d = aD(w, a);
        plts.forEach(pid => {
          const wPosts = getPltPosts(d, pid);
          wPosts.forEach(p => {
            if (g.metric === 'posts')       total += 1;
            if (g.metric === 'reach')       total += (p.reach || 0);
            if (g.metric === 'engagement')  total += totalEng(p);
            if (g.metric === 'followers')   total += (p.followersGained || 0);
            if (g.metric === 'likes')       total += (p.like || 0);
            if (g.metric === 'comments')    total += (p.comment || 0);
            if (g.metric === 'shares')      total += (p.share || 0);
            if (g.metric === 'saves')       total += (p.save || 0);
            if (g.metric === 'impression')  total += (p.impression || 0);
          });
        });
      });
    });
  });
  return total;
}

const METRIC_LABELS = {
  posts:'Total Post', reach:'Reach', engagement:'Engagement',
  followers:'Followers Gained', likes:'Likes', comments:'Komentar', shares:'Share'
};
const METRIC_EMOJIS = {
  posts:'📝', reach:'👁', engagement:'❤️',
  followers:'📈', likes:'👍', comments:'💬', shares:'🔁'
};

function openGoalModal(id) {
  goalEditKey = id || null;
  const g = id ? customGoals.find(x => x.id === id) : null;
  document.getElementById('goal-modal-title').textContent = g ? 'Edit Goal' : 'Tambah Goal Baru';

  // Populate platform options
  const pltSel = document.getElementById('mi-goal-plt');
  pltSel.innerHTML = '<option value="all">🌐 Semua Platform</option>' +
    allPlatforms().map(p => '<option value="' + p.id + '">' + (p.emoji||'📌') + ' ' + p.name + '</option>').join('');

  if (g) {
    document.getElementById('mi-goal-label').value = g.label || '';
    pltSel.value = g.platform || 'all';
    document.getElementById('mi-goal-metric').value = g.metric || 'reach';
    document.getElementById('mi-goal-val').value = g.target || 0;
  } else {
    document.getElementById('mi-goal-label').value = '';
    pltSel.value = 'all';
    document.getElementById('mi-goal-metric').value = 'reach';
    document.getElementById('mi-goal-val').value = '';
  }
  document.getElementById('m-goal').classList.remove('hidden');
  setTimeout(() => document.getElementById('mi-goal-label').focus(), 50);
}

function confirmGoal() {
  const label = document.getElementById('mi-goal-label').value.trim();
  const platform = document.getElementById('mi-goal-plt').value;
  const metric = document.getElementById('mi-goal-metric').value;
  const target = parseInt(document.getElementById('mi-goal-val').value) || 0;
  if (!label) { _qcToast('⚠️ Nama goal wajib diisi!'); return; }
  if (!target) { _qcToast('⚠️ Target wajib diisi!'); return; }
  const metric_emoji = METRIC_EMOJIS[metric] || '🎯';
  if (goalEditKey) {
    const g = customGoals.find(x => x.id === goalEditKey);
    if (g) { g.label = label; g.platform = platform; g.metric = metric; g.target = target; g.emoji = metric_emoji; }
  } else {
    customGoals.push({ id: 'g_' + Date.now(), label, platform, metric, target, emoji: metric_emoji });
  }
  closeModal('m-goal'); scheduleSave(200); renderGoals();
  if (view === 'dash') renderDashboard();
}

function deleteGoal(id) {
  showConfirm('Hapus Goal Ini?','Goal ini akan dihapus dari tracking bulan ini.',()=>{
    customGoals = customGoals.filter(g => g.id !== id);
    scheduleSave(200); renderGoals();
    if (view === 'dash') renderDashboard();
  },true);
}

/* ══ SPRINT 6: NEW FEATURE FUNCTIONS ══ */

// computeGoalProjection removed — logic inlined in renderGoals

// ── Niche Benchmark System (per-niche, not static "general creator") ──
const NICHE_BENCHMARKS = {
  general:  { label:'Kreator Umum',   er_tt:3.5, er_ig:2.5, save_rate:2.0, share_rate:1.5, reach_post:5000,  freq_week:5  },
  edukasi:  { label:'Edukasi',        er_tt:5.0, er_ig:3.5, save_rate:4.5, share_rate:2.0, reach_post:8000,  freq_week:4  },
  hiburan:  { label:'Hiburan/Komedi', er_tt:4.5, er_ig:3.0, save_rate:1.5, share_rate:3.5, reach_post:12000, freq_week:7  },
  lifestyle:{ label:'Lifestyle',      er_tt:3.5, er_ig:3.0, save_rate:3.0, share_rate:1.5, reach_post:6000,  freq_week:5  },
  gaming:   { label:'Gaming',         er_tt:4.0, er_ig:2.0, save_rate:1.0, share_rate:2.5, reach_post:7000,  freq_week:6  },
  beauty:   { label:'Beauty/Fashion', er_tt:4.0, er_ig:3.5, save_rate:3.5, share_rate:1.5, reach_post:7000,  freq_week:5  },
  kuliner:  { label:'Kuliner/Food',   er_tt:4.5, er_ig:3.5, save_rate:3.0, share_rate:2.0, reach_post:6000,  freq_week:5  },
  bisnis:   { label:'Bisnis/Finance', er_tt:4.0, er_ig:3.0, save_rate:5.0, share_rate:2.5, reach_post:4000,  freq_week:4  },
};
let userNiche = 'general'; // persisted in storage

function getNicheBenchmark() {
  return NICHE_BENCHMARKS[userNiche] || NICHE_BENCHMARKS.general;
}

function getBenchmarkComparison(allPosts) {
  const bm = getNicheBenchmark();
  const rows = [];
  const ttP = allPosts.filter(p=>p._plt==='tt'&&(p.impression||0)>0);
  const igP = allPosts.filter(p=>p._plt==='ig'&&(p.impression||0)>0);
  const myERtt = ttP.length ? ttP.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/ttP.length : null;
  const myERig = igP.length ? igP.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/igP.length : null;
  if (myERtt !== null) rows.push({ label:'ER TikTok', yours:myERtt.toFixed(2)+'%', industry:bm.er_tt+'%', delta:(myERtt-bm.er_tt), unit:'%' });
  if (myERig !== null) rows.push({ label:'ER Instagram', yours:myERig.toFixed(2)+'%', industry:bm.er_ig+'%', delta:(myERig-bm.er_ig), unit:'%' });
  const withReach = allPosts.filter(p=>(p.reach||0)>0);
  if (withReach.length) {
    const mySave  = withReach.reduce((s,p)=>s+((p.save||0)/p.reach)*100,0)/withReach.length;
    const myShare = withReach.reduce((s,p)=>s+((p.share||0)/p.reach)*100,0)/withReach.length;
    rows.push({ label:'Save Rate',  yours:mySave.toFixed(2)+'%',  industry:bm.save_rate+'%',  delta:(mySave-bm.save_rate),  unit:'%' });
    rows.push({ label:'Share Rate', yours:myShare.toFixed(2)+'%', industry:bm.share_rate+'%', delta:(myShare-bm.share_rate), unit:'%' });
  }
  if (allPosts.length) {
    const myRPP = allPosts.reduce((s,p)=>s+(p.reach||0),0)/allPosts.length;
    rows.push({ label:'Reach/Post', yours:fmt(myRPP), industry:fmt(bm.reach_post), delta:(myRPP-bm.reach_post), unit:'' });
  }
  return rows;
}

function benchmarkHTML(allPosts) {
  const bm = getNicheBenchmark();
  const rows = getBenchmarkComparison(allPosts);
  if (!rows.length) return '';
  const nicheOpts = Object.entries(NICHE_BENCHMARKS).map(([id,n])=>
    `<option value="${id}" ${userNiche===id?'selected':''}>${n.label}</option>`
  ).join('');
  const rowsHTML = rows.map(r => {
    const dCls = r.delta > 0.1 ? 'pos' : r.delta < -0.1 ? 'neg' : 'neu';
    const dStr = r.delta > 0.1 ? '▲ +'+(r.unit==='%'?r.delta.toFixed(2)+'%':fmt(r.delta))
               : r.delta < -0.1 ? '▼ '+(r.unit==='%'?Math.abs(r.delta).toFixed(2)+'%':fmt(Math.abs(r.delta)))
               : '≈ Setara';
    return `<div class="bm-row">
      <div class="bm-metric">${r.label}</div>
      <div class="bm-yours">${r.yours}</div>
      <div class="bm-industry">${r.industry}</div>
      <div class="bm-delta ${dCls}">${dStr}</div>
    </div>`;
  }).join('');
  return `<div style="margin-top:14px;">
    <div style="display:flex;align-items:center;gap:8px;margin-bottom:10px;flex-wrap:wrap;">
      <span style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;">Bandingkan vs niche:</span>
      <select class="af-sel" style="font-size:11px;padding:4px 8px;" onchange="userNiche=this.value;try{localStorage.setItem('cl-niche',this.value);}catch(e){}destroyCharts();renderAnalytics();">${nicheOpts}</select>
      <span style="font-size:10px;color:var(--t3);">Benchmark untuk <strong style="color:var(--t2);">${bm.label}</strong></span>
    </div>
    <div style="display:grid;grid-template-columns:1fr 70px 70px 55px;gap:4px;padding:0 10px 6px;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">
      <span>Metrik</span><span style="text-align:right;">Kamu</span><span style="text-align:right;">${bm.label}</span><span style="text-align:right;">Delta</span>
    </div>
    ${rowsHTML}
    <div style="font-size:10px;color:var(--t4);margin-top:8px;padding:0 10px;">* Benchmark estimasi per niche. Angka aktual bisa berbeda tergantung skala akun.</div>
  </div>`;
}

// ── Format Tracker Analytics ──
function formatAnalyticsHTML(allPosts) {
  if (!allPosts.length) return '';
  const fmtMap = {};
  FORMAT_TYPES.forEach(f => fmtMap[f.id] = { label:f.label, emoji:f.emoji, posts:[], totalEng:0, totalReach:0 });
  fmtMap['_unknown'] = { label:'Tidak Diisi', emoji:'❓', posts:[], totalEng:0, totalReach:0 };
  allPosts.forEach(p => {
    const key = FORMAT_TYPES.find(f=>f.id===p.format) ? p.format : '_unknown';
    fmtMap[key].posts.push(p);
    fmtMap[key].totalEng += totalEng(p);
    fmtMap[key].totalReach += (p.reach||0);
  });
  const active = Object.entries(fmtMap).filter(([,d])=>d.posts.length>0).sort((a,b)=>b[1].totalEng-a[1].totalEng);
  if (active.length === 0 || (active.length === 1 && active[0][0] === '_unknown')) {
    return `<div style="text-align:center;padding:20px;color:var(--t3);font-size:13px;">Belum ada data format. Isi kolom Format di tab Content untuk mulai tracking.</div>`;
  }
  const cards = active.filter(([k])=>k!=='_unknown').map(([id, d]) => {
    const avgER = d.posts.length ? d.posts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/d.posts.length : 0;
    const avgReach = d.posts.length ? Math.round(d.totalReach/d.posts.length) : 0;
    return `<div class="fmt-stat-card">
      <div style="font-size:20px;margin-bottom:4px;">${d.emoji}</div>
      <div style="font-size:11px;font-weight:700;color:var(--t);margin-bottom:8px;">${d.label}</div>
      <div class="fmt-stat-val">${d.posts.length}</div><div class="fmt-stat-lbl">Post</div>
      <div style="height:6px;"></div>
      <div class="fmt-stat-val" style="font-size:14px;">${erStr(avgER)}</div><div class="fmt-stat-lbl">Avg ER</div>
      <div style="height:4px;"></div>
      <div class="fmt-stat-val" style="font-size:14px;">${fmt(avgReach)}</div><div class="fmt-stat-lbl">Avg Reach</div>
    </div>`;
  }).join('');
  // Best format
  const best = active.filter(([k])=>k!=='_unknown')[0];
  const bestTip = best ? `<div class="insight-action" style="margin-left:0;margin-top:12px;"><span class="insight-action-ico">💡</span>Format <strong>${best[1].emoji} ${best[1].label}</strong> perform paling baik. Pertimbangkan porsi lebih besar minggu ini.</div>` : '';
  return `<div class="fmt-stat-grid">${cards}</div>${bestTip}`;
}

// ── Pillar Strategy Advisor ──
function pillarAdvisorHTML(filteredPosts) {
  const pillarMap = {};
  pillars.forEach(p => pillarMap[p.id] = { obj:p, posts:[], totalEng:0, totalReach:0 });
  filteredPosts.forEach(p => {
    const pid = p.pillar || pillars[pillars.length-1]?.id || 'other';
    if (!pillarMap[pid]) pillarMap[pid] = { obj:pillarOf(pid), posts:[], totalEng:0, totalReach:0 };
    pillarMap[pid].posts.push(p);
    pillarMap[pid].totalEng += totalEng(p);
    pillarMap[pid].totalReach += (p.reach||0);
  });
  const active = Object.values(pillarMap).filter(d=>d.posts.length>0)
    .map(d=>({
      ...d,
      avgER: d.posts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/d.posts.length,
      avgReach: d.totalReach/d.posts.length,
      freq: d.posts.length / Math.max(filteredPosts.length, 1),
    })).sort((a,b)=>b.avgER-a.avgER);
  if (active.length < 2) return '';

  const best = active[0];
  const worst = active[active.length-1];
  const totalFreq = active.reduce((s,d)=>s+d.posts.length,0);

  const rows = active.map(d => {
    const freqPct = Math.round((d.posts.length/totalFreq)*100);
    const isTop = d.obj.id === best.obj.id;
    const isWeak = d.obj.id === worst.obj.id && worst.avgER < best.avgER * 0.6;
    let tag = '<span class="advisor-tag advisor-tag-ok">Stabil</span>';
    let rec = `<span>ER rata-rata <strong>${erStr(d.avgER)}</strong>, frekuensi <strong>${freqPct}%</strong> dari total posting.`;
    if (isTop) {
      tag = '<span class="advisor-tag advisor-tag-up">Terbaik</span>';
      rec = `<span>ER tertinggi <strong>${erStr(d.avgER)}</strong>. Pertimbangkan naikkan porsi ke <strong>${Math.min(freqPct+10,50)}%</strong> dari total posting.</span>`;
    } else if (isWeak) {
      tag = '<span class="advisor-tag advisor-tag-down">Lemah</span>';
      rec = `<span>ER rendah <strong>${erStr(d.avgER)}</strong>. Coba angle baru atau kurangi frekuensi, alihkan ke pilar dengan ER lebih tinggi.</span>`;
    } else {
      rec = `<span>ER <strong>${erStr(d.avgER)}</strong>, reach rata-rata <strong>${fmt(d.avgReach)}</strong>. Frekuensi <strong>${freqPct}%</strong> — porsi seimbang.</span>`;
    }
    return `<div class="advisor-row">
      <div style="min-width:22px;text-align:center;font-size:16px;">${d.obj.emoji}</div>
      <div style="min-width:80px;font-size:12px;font-weight:700;color:var(--t);">${d.obj.label}</div>
      ${tag}
      <div class="advisor-rec">${rec}</div>
    </div>`;
  }).join('');

  // Overall strategy tip
  const overUsed = active.find(d=>d.avgER<best.avgER*0.6&&(d.posts.length/totalFreq)>0.3);
  let stratTip = overUsed
    ? `Kamu terlalu sering posting "${overUsed.obj.label}" padahal ER-nya rendah. Shift 20–30% frekuensi ke "${best.obj.label}".`
    : `Mix pilar sudah cukup baik! Fokus menjaga konsistensi "${best.obj.label}" sebagai anchor konten utama.`;

  return `<div class="advisor-card">
    <div class="advisor-title">🧠 Pillar Strategy Advisor</div>
    ${rows}
    <div class="insight-action" style="margin-left:0;margin-top:12px;"><span class="insight-action-ico">🎯</span>${stratTip}</div>
  </div>`;
}

// ── Notification / Reminder System ──
async function requestNotifPermission() {
  if (!('Notification' in window)) return;
  const perm = await Notification.requestPermission();
  notifPermission = perm;
  if (perm === 'granted') {
    checkReminders(true);
    renderDashboard(); // refresh to hide banner
  }
}
function checkReminders(force=false) {
  if (notifPermission !== 'granted') return;
  const now = Date.now();
  if (!force && now - lastNotifCheck < 3600000) return; // check once/hour
  lastNotifCheck = now;
  const allPosts = getAllPosts();
  if (!allPosts.length) return;
  const sorted = [...allPosts].filter(p=>p.date).sort((a,b)=>b.date.localeCompare(a.date));
  if (!sorted.length) return;
  const lastDate = new Date(sorted[0].date+'T12:00:00');
  const daysSince = Math.floor((Date.now()-lastDate.getTime())/(1000*60*60*24));
  if (daysSince >= 2) {
    try {
      new Notification('⏰ Creatorlytics Reminder', {
        body: `Sudah ${daysSince} hari belum ada post baru. Yuk posting hari ini!`,
        icon: 'data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><circle cx="16" cy="16" r="16" fill="%2300E87A"/><text x="16" y="22" text-anchor="middle" font-size="18">📊</text></svg>'
      });
    } catch(e) {}
  }
}
function reminderBannerHTML(allPosts) {
  if (notifPermission === 'granted') {
    // Maybe show a "last posted X days ago" nudge inline
    const sorted = [...allPosts].filter(p=>p.date).sort((a,b)=>b.date.localeCompare(a.date));
    if (!sorted.length) return '';
    const lastDate = new Date(sorted[0].date+'T12:00:00');
    const daysSince = Math.floor((Date.now()-lastDate.getTime())/(1000*60*60*24));
    if (daysSince < 2) return '';
    return `<div class="notif-bar">
      <span class="notif-bar-ico">⏰</span>
      <div class="notif-bar-txt">Sudah <strong>${daysSince} hari</strong> belum ada post baru. Jaga konsistensinya!</div>
      <button class="notif-bar-btn" onclick="openQCModal()">＋ Input Sekarang</button>
      <button class="notif-bar-dismiss" onclick="this.closest('.notif-bar').remove()" title="Tutup">×</button>
    </div>`;
  }
  if (notifPermission === 'denied') return '';
  // Default: ask for permission
  return `<div class="notif-bar">
    <span class="notif-bar-ico">🔔</span>
    <div class="notif-bar-txt"><strong>Aktifkan reminder posting</strong> — Creatorlytics akan mengingatkan kamu jika 2+ hari tidak posting.</div>
    <button class="notif-bar-btn" onclick="requestNotifPermission()">Aktifkan</button>
    <button class="notif-bar-dismiss" onclick="notifPermission='denied';this.closest('.notif-bar').remove()" title="Nanti">×</button>
  </div>`;
}

function refreshView(){
  destroyCharts();
  if(view==='dash')renderDashboard();
  else if(view==='analytics')renderAnalytics();
  else if(view==='goals')renderGoals();
  else if(view==='content'){renderContentTimeline();}
  flash();
}
function updateWorkspaceName(){
  const displayName = userProfile.displayName || accounts[0] || 'Creator';
  const email = userProfile.email || '';
  const wsEl = document.getElementById('ws-active-name');
  if(wsEl) wsEl.textContent = displayName;
  const unEl = document.getElementById('nav-user-name');
  if(unEl) unEl.textContent = displayName;
  const emailEl = document.getElementById('nav-user-email');
  if(emailEl) emailEl.textContent = email || 'Tambah email →';
  const avEl = document.getElementById('nav-user-avatar');
  if(avEl) avEl.textContent = displayName.slice(0,2).toUpperCase()||'CR';
}
function renderNavPlatforms(){
  const el=document.getElementById('nav-platforms');
  if(!el)return;
  const all=allPlatforms();
  const dotColor={tt:'#22C55E',ig:'#F59E0B'};
  el.innerHTML=all.map(p=>{
    const isBuiltIn=p.id==='tt'||p.id==='ig';
    const dot=dotColor[p.id]||'var(--accent)';
    return `<div class="nav-item nav-plt-row" style="font-size:12px;color:var(--sidebar-t);padding:7px 11px;cursor:default;">
      <span style="font-size:13px;flex-shrink:0;">${p.emoji||'📌'}</span>
      <span style="flex:1;">${p.name||'Platform'}</span>
      <span style="width:7px;height:7px;border-radius:50%;background:${dot};flex-shrink:0;box-shadow:0 0 6px ${dot};"></span>
      ${isBuiltIn?'':`<button class="nav-plt-del" onclick="event.stopPropagation();deletePlatform('${p.id}')" title="Hapus platform">✕</button>`}
    </div>`;
  }).join('');
  // Update user name from first account
  const nameEl=document.getElementById('nav-user-name');
  const avatarEl=document.getElementById('nav-user-avatar');
  const firstName=accounts[0]||'Creator';
  if(nameEl)nameEl.textContent=firstName;
  if(avatarEl)avatarEl.textContent=firstName.slice(0,2).toUpperCase();
}

/* ══ RENDER: MONTH TREE ══ */

/* ══ RENDER: ACCOUNT LIST ══ */
function renderMonthTree(){
  const el=document.getElementById('month-tree');
  if(!el)return;
  if(!months.length){
    el.innerHTML=`<div class="period-empty">Belum ada bulan.<br>Klik tambah bulan untuk mulai tracking.</div>`;
    return;
  }
  const active=getM(sel.mid)||months[0];
  const options=months.map(m=>{
    const lbl=MN[m.month]+' '+m.year;
    return `<option value="${m.id}" ${active.id===m.id?'selected':''}>${lbl}</option>`;
  }).join('');
  const weeks=active.weeks.map((w,wi)=>{
    const isActW=sel.mid===active.id&&sel.wid===w.id;
    const label=(w.label||'Minggu '+(wi+1)).replace(/^Minggu\s*/i,'W');
    return `<button class="week-tab ${isActW?'act':''}" onclick="selWeek('${active.id}','${w.id}')">${label}</button>`;
  }).join('');
  el.innerHTML=`
    <div class="period-select-wrap">
      <select class="period-select" onchange="setPeriod(this.value)">${options}</select>
      <span class="period-caret">v</span>
      <div class="period-actions">
        <button class="period-action danger" onclick="delMonth('${active.id}')">Hapus</button>
      </div>
    </div>
    <div class="week-tabs">
      ${weeks||`<div class="period-empty" style="grid-column:1/-1;">Belum ada minggu.</div>`}
    </div>
    <div class="week-tools">
      <button class="week-tool" onclick="addWeek('${active.id}')">+ Tambah minggu</button>
      ${sel.mid===active.id&&sel.wid?`<button class="week-tool danger" onclick="delWeek('${active.id}','${sel.wid}')">Hapus minggu</button>`:''}
    </div>`;
}

function setPeriod(mid){
  sel.mid=mid;exp[mid]=true;
  const m=getM(mid);
  if(!m||!m.weeks.some(w=>w.id===sel.wid)){sel.wid=null;sel.acc=null;}
  if(view==='content')renderContentTimeline();
}


/* ══ RENDER: EDITOR HEADER ══ */
function renderAccList(){
  const el=document.getElementById('acc-list-v2');
  if(!el)return;
  if(!sel.wid){
    el.innerHTML=`<div class="period-empty">Pilih minggu dulu untuk melihat akun.</div>`;
    return;
  }
  const wk=getW(sel.mid,sel.wid);if(!wk){el.innerHTML='';return;}
  el.innerHTML=accounts.map(a=>{
    const tt=aTots(wk,a,'tt'),ig=aTots(wk,a,'ig');
    let tot=tt.posts+ig.posts;
    platforms.forEach(p=>{tot+=aTots(wk,a,p.id).posts;});
    const isA=sel.acc===a;
    const meta=tot?`${tot} posts tracked this week`:'No data yet';
    return `<div class="acc-item ${isA?'act':''}" onclick="selAcc('${esc(a)}')">
      <div class="acc-info">
        <div class="acc-name">${a}</div>
        <div class="acc-er">${meta}</div>
      </div>
      <div class="acc-acts">
        <button class="acc-act" onclick="event.stopPropagation();openRename('${esc(a)}')" title="Rename">✎</button>
        <button class="acc-act d" onclick="event.stopPropagation();deleteAcc('${esc(a)}')" title="Hapus">×</button>
      </div>
      <span class="acc-count">${tot||'—'}</span>
    </div>`;
  }).join('');
}

function renderEditorHeader(){
  const titleEl=document.getElementById('content-title');
  const subEl=document.getElementById('content-sub');
  const segEl=document.getElementById('content-plt-seg');
  if(!sel.wid||!sel.acc){
    if(titleEl)titleEl.textContent='Content Input';
    if(subEl)subEl.textContent='Timeline semua konten';
    if(segEl)segEl.innerHTML='';
    return;
  }
  const wk=getW(sel.mid,sel.wid);
  const m=getM(sel.mid);
  if(titleEl)titleEl.textContent=sel.acc;
  if(subEl)subEl.textContent=(wk?.label||'')+ ' · '+(MN[m?.month]||'')+' '+(m?.year||'');
  if(segEl){
    const all=allPlatforms();
    segEl.innerHTML=`<div class="seg">
      ${all.map(p=>`<button class="seg-btn ${sel.plt===p.id?(p.id==='tt'?'tt-act':p.id==='ig'?'ig-act':'act'):''}" onclick="setPlt('${p.id}')">${p.emoji||'📌'} ${p.name||'Platform'}</button>`).join('')}
      <button class="seg-btn" onclick="openModal('m-plt')" style="color:var(--t3);" title="Tambah platform">+</button>
    </div>`;
  }
  // ER buttons
  ['impression','reach','followers'].map(m=>{
    const map={impression:'er-impr',reach:'er-reach',followers:'er-flw'};
    const el=document.getElementById(map[m]);
    if(el)el.classList.toggle('act',sel.erMode===m);
  });
}
function setPlt(p){sel.plt=p;renderEditorHeader();if(view==='content')renderContentTimeline();}
function setER(m){sel.erMode=m;renderEditorHeader();if(view==='content')renderContentTimeline();}

/* ══ RENDER: EDITOR BODY ══ */
function renderEditorBody(){
  renderEditorHeader();
  const body=document.getElementById('editor-body');
  if(!body)return;
  if(!sel.wid||!sel.acc){
    body.innerHTML=`<div class="placeholder"><div class="ph-ico">←</div><div class="ph-title">Pilih Minggu & Akun</div><div class="ph-sub">Buka bulan di panel kiri,<br>pilih minggu, lalu pilih akun.</div></div>`;
    return;
  }
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const plt=sel.plt;
  const posts=getPltPosts(d,plt);
  const flw=getPltFlw(d,plt);
  const pv=getPltPV(d,plt);
  const pi=pltInfo(plt);
  const avgEr=posts.length?posts.reduce((s,p)=>s+calcER(p,sel.erMode,flw),0)/posts.length:0;
  const totEng=posts.reduce((s,p)=>s+totalEng(p),0);
  const totImpr=posts.reduce((s,p)=>s+(p.impression||0),0);

  // Platform tabs
  const allPlts=allPlatforms();
  const platTabs=allPlts.map(p=>{
    const isTT=p.id==='tt',isIG=p.id==='ig';
    const actCls=sel.plt===p.id?` act${isTT?' tt':isIG?' ig':''}`:''
    return`<button class="ct-plat-tab${actCls}" onclick="setPlt('${p.id}')">${p.emoji||'📌'} ${p.name||'Platform'}</button>`;
  }).join('');

  // Weekly stats + summary bar
  const weeklyHTML=`
    <div class="weekly-stats-row" style="margin-bottom:12px;">
      <div class="ws-field">
        <span class="ws-label">${pi.emoji||'📌'} Followers</span>
        <input class="ws-inp" type="number" min="0" value="${flw||''}" placeholder="0" oninput="saveFlw('${plt}',this.value)">
        <span class="ws-hint">minggu ini</span>
      </div>
      <div class="ws-field">
        <span class="ws-label">Profile Visit</span>
        <input class="ws-inp" type="number" min="0" value="${pv||''}" placeholder="0" oninput="savePV('${plt}',this.value)">
        <span class="ws-hint">minggu ini</span>
      </div>
      ${posts.length?`
      <div class="ws-field" style="margin-left:auto;gap:16px;">
        <div class="ws-field"><span class="ws-label">Total Post</span><span style="font-family:var(--mono);font-size:13px;font-weight:600;color:var(--t)">${posts.length}</span></div>
        <div class="ws-field"><span class="ws-label">Total Eng</span><span style="font-family:var(--mono);font-size:13px;font-weight:600;color:var(--green)">${fmtFull(totEng)}</span></div>
        <div class="ws-field"><span class="ws-label">Impressi</span><span style="font-family:var(--mono);font-size:13px;font-weight:600;color:var(--purple)">${fmtFull(totImpr)}</span></div>
        <div class="ws-field"><span class="ws-label">ER avg</span><span style="font-family:var(--mono);font-size:13px;font-weight:600;color:var(--amber)">${erStr(avgEr)}</span></div>
      </div>`:''}
    </div>`;

  // Table rows
  const metrics=['impression','reach','like','comment','share','save','repost','followersGained'];
  const metricLabels={impression:'IMPR',reach:'REACH',like:'LIKES',comment:'CMT',share:'SHARE',save:'SAVE',repost:'REPOST',followersGained:'FLW+'};
  const showFlw=window._showFlwGained!==false;

  function scoreGrade_local(score){return scoreGrade(score);}
  function computeScore_local(post){return computeContentScore(post,getAllPosts());}

  const activeMetrics = showFlw ? metrics : metrics.filter(m => m !== 'followersGained');

  const rows=posts.map((p,idx)=>{
    const er=calcER(p,sel.erMode,flw);
    const pid=p.id;
    const pil=pillarOf(p.pillar||pillars[0]?.id||'other');
    const pilOpts=pillars.map(pl=>`<option value="${pl.id}" ${(p.pillar||pillars[0]?.id)===pl.id?'selected':''}>${pl.emoji} ${pl.label}</option>`).join('');
    const numCells=activeMetrics.map(k=>`
      <td class="ct-cell num">
        <input type="number" class="ct-inp ct-inp-num" value="${p[k]||''}" placeholder="—"
          min="0" oninput="savePF('${pid}','${k}',this.value)">
      </td>`).join('');
    return `<tr class="content-row">
      <td class="ct-cell">
        <input type="date" class="ct-inp ct-inp-date" value="${p.date||''}"
          oninput="savePF('${pid}','date',this.value)">
      </td>
      <td class="ct-cell" style="min-width:130px;">
        <div class="ct-time-cell">
          <input type="time" class="ct-inp ct-inp-time" value="${p.posted_at?new Date(p.posted_at).toTimeString().slice(0,5):''}"
            title="Jam posting (opsional — untuk analisis Best Posting Time)"
            onchange="(function(){
              const d='${p.date||new Date().toISOString().split('T')[0]}';
              const t=this.value;
              if(!t){savePF('${pid}','posted_at','');return;}
              savePF('${pid}','posted_at',d+'T'+t+':00');
            }).call(this)">
          ${p.posted_at?`<span class="ct-time-badge" title="${new Date(p.posted_at).toLocaleString('id-ID')}">${derivePostingTime(p)?.period||''}</span>`:'<span class="ct-time-empty" title="Opsional">—</span>'}
        </div>
      </td>
      <td class="ct-cell" style="min-width:150px;">
        <input type="text" class="ct-inp ct-inp-name"
          value="${(p.name||'').replace(/"/g,'&quot;')}"
          placeholder="Nama konten…"
          oninput="savePF('${pid}','name',this.value)">
      </td>
      <td class="ct-cell">
        <select class="ct-pillar-sel" id="psel-${pid}" style="color:${pil.color}"
          onchange="savePF('${pid}','pillar',this.value);const pl=pillarOf(this.value);this.style.color=pl.color">
          ${pilOpts}
        </select>
      </td>
      <td class="ct-cell" style="min-width:100px;">
        <select class="ct-inp" style="font-size:11px;padding:3px 4px;"
          onchange="savePF('${pid}','format',this.value)">
          <option value="">— Format —</option>
          ${FORMAT_TYPES.map(f=>`<option value="${f.id}" ${p.format===f.id?'selected':''}>${f.emoji} ${f.label}</option>`).join('')}
        </select>
      </td>
      <td class="ct-cell" id="lkcell-${pid}">${linkCellHTML(pid,p.link)}</td>
      ${numCells}
      <td class="ct-cell" style="white-space:nowrap;">
        <span class="ct-er-val" id="er-${pid}">${erStr(er)}</span>
      </td>
      <td class="ct-cell" style="white-space:nowrap;">
        ${(()=>{const allP=getAllPosts();const s=computeContentScore(p,allP);const g=scoreGrade(s);return `<span style="font-family:var(--mono);font-size:11px;padding:2px 7px;border-radius:5px;background:${g.bg};color:${g.color};font-weight:700;">${g.grade} <span style="opacity:.75">${s}</span></span>`;})()}
      </td>
      <td class="ct-cell">
        <div style="display:flex;gap:2px;">
          <button class="ct-action-btn" onclick="dupPost('${pid}')" title="Duplikat">⧉</button>
          <button class="ct-action-btn del" onclick="delPost('${pid}')" title="Hapus">✕</button>
        </div>
      </td>
    </tr>`;
  }).join('');

  const thead=`<thead><tr>
    <th>TGL</th>
    <th>JAM POST</th>
    <th>NAMA KONTEN</th>
    <th>PILAR</th>
    <th>FORMAT</th>
    <th>LINK</th>
    ${activeMetrics.map(k=>`<th class="num">${metricLabels[k]}</th>`).join('')}
    <th class="num">ER</th>
    <th class="num">Score</th>
    <th></th>
  </tr></thead>`;

  const hasPostedAtData=posts.some(p=>p.posted_at);
  const postedAtBanner=!hasPostedAtData?`
    <div class="posted-at-banner">
      ⏰ <b>Baru!</b> Kolom <b>JAM POST</b> — isi jam posting untuk analisis <b>Best Posting Time</b> di tab Analytics → Insights.
    </div>`:'';

  const tableHTML=`
    <div class="content-table-wrap">
      ${postedAtBanner}
      <div class="ct-plat-bar">
        <div class="ct-plat-tabs">${platTabs}</div>
        <div style="display:flex;gap:6px;align-items:center;">
          <button class="flw-toggle" onclick="window._showFlwGained=!${showFlw};renderEditorBody();" title="${showFlw?'Sembunyikan':'Tampilkan'} kolom Followers Gained">
            📈 FLW+ ${showFlw?'✓':'○'}
          </button>
          <button class="ct-add-row-btn" style="background:rgba(14,159,143,.12);border-color:rgba(14,159,143,.3);color:var(--accent);" onclick="openCSVImportModal()">
            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" style="display:inline-block;vertical-align:middle;margin-right:3px;"><path d="M6 1v7M3 5.5L6 8l3-2.5M1.5 10.5h9" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
            Import CSV
          </button>
          <button class="ct-add-row-btn" onclick="addPost()">+ Add New Row</button>
        </div>
      </div>
    <div class="content-table-scroll">
        <table class="content-table">
          ${thead}
          <tbody>${rows||`<tr><td colspan="15" class="ct-empty">Belum ada konten. Klik "+ Add New Row" untuk mulai.</td></tr>`}</tbody>
        </table>
      </div>
      <button class="ct-add-bottom" onclick="addPost()">
        <span style="font-size:15px;color:var(--t4)">+</span> Tambah baris baru
      </button>
    </div>`;

  body.innerHTML=`${weeklyHTML}${tableHTML}`;

  // Sprint 7: On mobile, replace table with card layout for readability
  if (window.innerWidth <= 768) {
    const mobileHTML = `
      <div class="content-table-wrap">
        <div class="ct-plat-bar">
          <div class="ct-plat-tabs">${platTabs}</div>
          <button class="ct-add-row-btn" onclick="openQCModal()">＋ Add Post</button>
        </div>
        ${renderMobilePostCards(posts, plt, wk, sel.acc, flw)}
      </div>`;
    body.innerHTML = `${weeklyHTML}${mobileHTML}`;
    setTimeout(initSwipeToDelete, 50);
  }
}

/* ── POST ACTIONS ── */
function addPost(){
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const p=emptyPost();
  p.date=new Date().toISOString().split('T')[0];
  // inherit pillar from last post
  const posts=getPltPosts(d,sel.plt);
  if(posts.length)p.pillar=posts[posts.length-1].pillar||pillars[0]?.id||'other';
  else p.pillar=pillars[0]?.id||'other';
  const key=sel.plt+'Posts';
  if(!d[key])d[key]=[];
  d[key].push(p);
  scheduleSave(300);if(view==='content')renderContentTimeline();
}
function dupPost(pid){
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const key=sel.plt+'Posts';
  const posts=d[key]||[];
  const src=posts.find(p=>p.id===pid);if(!src)return;
  const np={...src,id:uid(),date:new Date().toISOString().split('T')[0]};
  d[key].push(np);
  scheduleSave(300);if(view==='content')renderContentTimeline();
}
function delPost(pid){
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const key=sel.plt+'Posts';
  if(d[key])d[key]=d[key].filter(p=>p.id!==pid);
  scheduleSave(300);if(view==='content')renderContentTimeline();
}
function savePF(pid,k,v){
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const key=sel.plt+'Posts';
  const posts=d[key]||[];
  const p=posts.find(x=>x.id===pid);if(!p)return;
  if(k==='pillar'){
    p.pillar=v;
    // Update pillar select color in table
    const selEl=document.getElementById('psel-'+pid);
    if(selEl){const pil=pillarOf(v);selEl.style.color=pil.color;}
    scheduleSave(700);return;
  }
  p[k]=k==='date'||k==='link'||k==='name'||k==='posted_at'||k==='format'?v:(parseInt(v)||0);
  // If date changed, update the date part of posted_at to stay in sync
  if(k==='date'&&p.posted_at&&v){
    try{
      const existingTime=new Date(p.posted_at).toTimeString().slice(0,5);
      p.posted_at=v+'T'+existingTime+':00';
    }catch(e){}
  }
  const flw=getPltFlw(d,sel.plt);
  const el=document.getElementById('er-'+pid);
  if(el)el.textContent=erStr(calcER(p,sel.erMode,flw));
  scheduleSave(700);
}
function promptLink(pid){
  const wk=getW(sel.mid,sel.wid);if(!wk)return;
  const d=aD(wk,sel.acc);
  const posts=d[sel.plt+'Posts']||[];
  const p=posts.find(x=>x.id===pid);if(!p)return;
  const url=prompt('URL konten (kosongkan untuk hapus):',p.link||'https://');
  if(url===null)return;
  p.link=url.trim();
  scheduleSave(500);
  // Re-render only the link cell
  const cell=document.getElementById('lkcell-'+pid);
  if(cell)cell.innerHTML=linkCellHTML(pid,p.link);
}
function linkCellHTML(pid,link){
  if(link)return`<div class="ct-link-wrap">
    <a href="${link}" target="_blank" class="ct-link-btn has" title="${link}">Open</a>
    <button class="ct-link-btn ct-link-edit" onclick="promptLink('${pid}')" title="Edit link">Edit</button>
  </div>`;
  return`<button class="ct-link-btn add" onclick="promptLink('${pid}')">+ link</button>`;
}
function saveFlw(plt,v){const wk=getW(sel.mid,sel.wid);if(!wk)return;aD(wk,sel.acc)[plt+'Flw']=parseInt(v)||0;scheduleSave(700);}
function savePV(plt,v){const wk=getW(sel.mid,sel.wid);if(!wk)return;aD(wk,sel.acc)[plt+'PV']=parseInt(v)||0;scheduleSave(700);}

/* ══ CHARTS ══ */
function updateERModeHint(){
  const mode = sel.erMode||'impression';
  const hints = {
    impression:'÷ Total Tayangan — angka lebih rendah, lebih akurat',
    reach:'÷ Akun yang Menjangkau — lebih representatif dari impresi',
    followers:'÷ Total Followers — standar benchmark industri'
  };
  const el = document.getElementById('er-mode-hint');
  if(el) el.textContent = hints[mode]||'';
}
function destroyCharts(){Object.values(charts).forEach(c=>{try{c.destroy();}catch(e){}});charts={};}
function isDark(){return document.documentElement.getAttribute('data-theme')==='dark';}
function gc(){return isDark()?'rgba(255,255,255,.05)':'rgba(15,23,42,.06)';}
function tc(){return isDark()?'#475569':'#94A3B8';}
function makeChart(id,type,labels,datasets,opts={}){
  const el=document.getElementById(id);if(!el)return null;
  const base={
    responsive:true,maintainAspectRatio:false,
    plugins:{legend:{display:false},tooltip:{callbacks:{label:c=>`${c.dataset.label}: ${(+c.raw).toLocaleString('id-ID')}`}}},
    scales:{x:{grid:{color:gc()},ticks:{color:tc(),font:{size:10,family:'Sora'}}},
            y:{grid:{color:gc()},ticks:{color:tc(),font:{size:10,family:'Sora'},callback:v=>v>=1000?Math.round(v/1000)+'K':v}}}
  };
  return new Chart(el,{type,data:{labels,datasets},options:{...base,...opts}});
}

/* ══ RENDER: TOP POSTS (reusable, called by dashboard) ══ */
function sortedTopPosts(allPosts,sortBy,n){
  // Pre-compute sort values
  const withScore=allPosts.map(p=>{
    const eng=totalEng(p);
    const er=p._flw?calcER(p,sel.erMode,p._flw):0;
    const imp=p.impression||0;
    const reach=p.reach||0;
    const flwG=p.followersGained||0;
    // Overall Score: avg of normalised values (computed after we know maxes)
    return{...p,_eng:eng,_er:er,_imp:imp,_reach:reach,_flwG:flwG};
  });
  if(sortBy==='overall'){
    const maxEng=Math.max(...withScore.map(p=>p._eng),1);
    const maxImp=Math.max(...withScore.map(p=>p._imp),1);
    const maxReach=Math.max(...withScore.map(p=>p._reach),1);
    const maxFlwG=Math.max(...withScore.map(p=>p._flwG),1);
    withScore.forEach(p=>{
      p._overall=(p._eng/maxEng+p._imp/maxImp+p._reach/maxReach+p._flwG/maxFlwG)/4*100;
    });
    return withScore.sort((a,b)=>b._overall-a._overall).slice(0,n);
  }
  const keyMap={engagement:'_eng',er_rate:'_er',reach:'_reach',impression:'_imp',flw_gained:'_flwG'};
  const k=keyMap[sortBy]||'_eng';
  return withScore.sort((a,b)=>b[k]-a[k]).slice(0,n);
}

function topPostsHTML(posts,sortBy){
  if(!posts.length)return`<div style="padding:24px;text-align:center;color:var(--t3);font-size:12px;">Belum ada konten. Mulai tambahkan di tab Content.</div>`;
  const pillCls={engagement:'mp-eng',er_rate:'mp-er',reach:'mp-reach',impression:'mp-impr',flw_gained:'mp-flwg',overall:'mp-overall'};
  const activeCls=pillCls[sortBy]||'mp-eng';
  return posts.map((p,i)=>{
    const pi=pltInfo(p._plt);
    const pil=pillarOf(p.pillar||pillars[pillars.length-1]?.id||'other');
    const engPill=`<span class="metric-pill mp-eng${sortBy==='engagement'?' mp-active':''}" style="${sortBy==='engagement'?'font-weight:700;box-shadow:0 0 0 1.5px var(--green);':''}">❤ ${fmt(p._eng)}</span>`;
    const erPill=p._er?`<span class="metric-pill mp-er${sortBy==='er_rate'?' mp-active':''}" style="${sortBy==='er_rate'?'font-weight:700;box-shadow:0 0 0 1.5px var(--amber);':''}">📊 ${erStr(p._er)}</span>`:'';
    const reachPill=`<span class="metric-pill mp-reach${sortBy==='reach'?' mp-active':''}" style="${sortBy==='reach'?'font-weight:700;box-shadow:0 0 0 1.5px var(--blue);':''}">👁 ${fmt(p._reach)}</span>`;
    const impPill=p._imp?`<span class="metric-pill mp-impr${sortBy==='impression'?' mp-active':''}" style="background:var(--purple-bg);color:var(--purple);${sortBy==='impression'?'font-weight:700;box-shadow:0 0 0 1.5px var(--purple);':''}">📺 ${fmt(p._imp)}</span>`:'';
    const flwPill=p._flwG?`<span class="metric-pill mp-flwg${sortBy==='flw_gained'?' mp-active':''}" style="background:var(--green-bg);color:var(--green-t);${sortBy==='flw_gained'?'font-weight:700;box-shadow:0 0 0 1.5px var(--green);':''}">📈 +${fmt(p._flwG)}</span>`:'';
    const ovPill=sortBy==='overall'&&p._overall!=null?`<span class="metric-pill" style="background:linear-gradient(135deg,var(--tt-bg),var(--ig-bg));color:var(--t);font-weight:700;box-shadow:0 0 0 1.5px var(--tt);">★ ${p._overall.toFixed(0)}pt</span>`:'';
    const score=computeContentScore(p,posts);const sg=scoreGrade(score);
    const scorePill=`<span style="font-family:var(--mono);font-size:11px;padding:2px 6px;border-radius:4px;background:${sg.bg};color:${sg.color};font-weight:700;">${sg.grade}</span>`;
    return `<div class="top-post-item">
      <div class="rank-badge rank-${i<3?(i+1):'n'}">${i+1}</div>
      <div class="top-post-info">
        <div class="top-post-platform">
          <span>${pi.emoji||'📌'} ${pi.name||'Platform'}</span>
          <span>·</span><span>${p._account}</span>
          <span class="pillar-chip" style="background:${pil.bg};color:${pil.color};">
            <span class="pillar-dot" style="background:${pil.color};"></span>${pil.label}
          </span>
          ${scorePill}
        </div>
        <div class="top-post-meta">
          ${p.name?`<span style="font-weight:500;color:var(--t2);">${p.name}</span> · `:''}${p.date||'—'} · ${p._month}
        </div>
      </div>
      <div class="top-post-metrics">
        ${ovPill}${engPill}${erPill}${reachPill}${impPill}${flwPill}
      </div>
    </div>`;
  }).join('');
}

/* ══ RENDER: DASHBOARD (IA Rework — Smart Homepage) ══ */
function renderDashboard(){
  updateWorkspaceName();
  const body=document.getElementById('dash-body');if(!body)return;
  const allPosts=getAllPosts();
  const ttAll=getAllStats('tt');
  const igAll=getAllStats('ig');
  const now=new Date();
  const ttCurr=getCurrMonthStats('tt');
  const igCurr=getCurrMonthStats('ig');
  const totalPosts=allPosts.length;
  const totalReach=allPosts.reduce((s,p)=>s+(p.reach||0),0);

  // ── ER ──
  let erNumer=0,erDenom=0;
  allPosts.forEach(p=>{
    const base=sel.erMode==='impression'?(p.impression||0):sel.erMode==='reach'?(p.reach||0):(p._flw||0);
    if(base>0){erNumer+=totalEng(p)/base*100;erDenom++;}
  });
  const avgER=erDenom?erNumer/erDenom:0;
  const erLvl=erLevel(avgER);

  const insights=generateInsights(allPosts);
  const hasData=totalPosts>0;

  // ── Weekly movement: compare this month vs last month ──
  const currM={year:now.getFullYear(),month:now.getMonth()};
  const {year:py,month:pm}=prevMonthYear(currM.year,currM.month);
  const currStats=getReportStats('all',currM.year,currM.month,'month');
  const prevStats=getReportStats('all',py,pm,'month');

  function movementPill(label,curr,prev,fmtFn,emoji){
    if(!prev||prev===0)return`<div class="wm-pill"><span class="wm-pill-emoji">${emoji}</span><div><div class="wm-pill-label">${label}</div><div class="wm-pill-val">${fmtFn(curr)}</div></div></div>`;
    const delta=((curr-prev)/prev)*100;
    const isUp=delta>=0;
    const cls=isUp?'wm-up':'wm-down';
    const sign=isUp?'↑':'↓';
    return`<div class="wm-pill"><span class="wm-pill-emoji">${emoji}</span><div><div class="wm-pill-label">${label}</div><div class="wm-pill-val">${fmtFn(curr)} <span class="${cls}">${sign} ${Math.abs(delta).toFixed(0)}%</span></div></div></div>`;
  }

  const weeklyMovement=hasData?`
    <div style="margin-bottom:28px;">
      <div class="section-hdr">
        <div>
          <div class="section-ttl">📊 Pergerakan Bulan Ini vs Bulan Lalu</div>
          <div style="font-size:11px;color:var(--t3);margin-top:2px;font-weight:500;">Lacak peningkatan performa organik kamu secara berkala</div>
        </div>
        <div style="display:flex;gap:8px;align-items:center;font-size:10px;color:var(--t3);">
          <span style="display:flex;align-items:center;gap:4px;"><span style="width:8px;height:8px;border-radius:50%;background:var(--accent);display:inline-block;"></span>Bulan Ini</span>
          <span style="display:flex;align-items:center;gap:4px;"><span style="width:8px;height:8px;border-radius:50%;background:var(--t4);display:inline-block;"></span>Bulan Lalu</span>
        </div>
      </div>
      <div class="wm-row">
        ${movementPill('Reach',currStats.reach,prevStats.reach,fmt,'👁')}
        ${movementPill('Engagement',currStats.engagement,prevStats.engagement,fmt,'❤️')}
        ${movementPill('ER Rate',currStats.avgER,prevStats.avgER,v=>erStr(v),'📈')}
        ${movementPill('Posts',currStats.posts,prevStats.posts,fmtFull,'📝')}
      </div>
    </div>`:''

  // ── Insight Engine — HERO ──
  const insightEngineHTML=`
    <div style="margin-bottom:28px;">
      <div class="section-hdr">
        <div class="section-ttl">💡 Insight Engine</div>
        <span style="font-size:10px;color:var(--t3);font-style:italic;">Auto generated · rule-based</span>
      </div>
      ${insights.length?`<div class="insight-grid">${insights.slice(0,4).map((ins,i)=>insightCardHTML(ins,i)).join('')}</div>`:
        hasData?`<div class="dash-insight-empty"><div style="font-size:28px;opacity:.25;">💡</div><div style="font-size:13px;color:var(--t3);margin-top:8px;">Insight akan muncul otomatis setelah kamu punya lebih banyak data posting.</div></div>`:
        `<div class="dash-insight-empty"><div style="font-size:28px;opacity:.25;">💡</div><div style="font-size:13px;color:var(--t3);margin-top:8px;">Tambahkan konten pertamamu untuk mulai mendapatkan insight.</div><div style="margin-top:12px;"><button onclick="openQCModal()" style="padding:8px 18px;border-radius:8px;border:none;background:var(--accent);color:#000;font-size:12px;font-weight:800;cursor:pointer;">+ Input Post Pertama</button></div></div>`}
    </div>`;

  // ── Top 3 Content Only ──
  const top3=sortedTopPosts(allPosts,'engagement',3);
  const top3HTML=`
    <div style="margin-bottom:28px;">
      <div class="section-hdr">
        <div class="section-ttl">🏆 Best Performing Content</div>
        <button class="section-action" onclick="switchView('analytics');analyticsTab='top';renderAnalytics()">Lihat semua →</button>
      </div>
      <div class="top-post-list">${topPostsHTML(top3,'engagement')}</div>
    </div>`;

  // ── Goal Progress ──
  const activeGoals=customGoals.slice(0,3);
  const goalPrevHTML=activeGoals.length?activeGoals.map(g=>{
    const curr=getGoalCurrentValue(g);
    const target=g.target||0;
    const pct=target?Math.min(Math.round((curr/target)*100),100):0;
    const cls=pct>=80?'goal-pct-good':pct>=40?'goal-pct-mid':'goal-pct-low';
    const barColor=pct>=80?'var(--green)':pct>=40?'var(--amber)':'var(--t3)';
    const pltObj=g.platform==='all'?{emoji:'🌐'}:allPlatforms().find(p=>p.id===g.platform)||{emoji:'📌'};
    return `<div style="margin-bottom:14px;">
      <div style="display:flex;align-items:center;gap:6px;margin-bottom:6px;">
        <span style="font-size:14px;">${g.emoji||pltObj.emoji||'🎯'}</span>
        <span style="font-size:12px;font-weight:600;color:var(--t);">${g.label}</span>
        <span class="goal-pct ${cls}" style="margin-left:auto;">${pct}%</span>
      </div>
      <div class="goal-progress-bar"><div class="goal-progress-fill" style="width:${pct}%;background:${barColor};"></div></div>
      <div style="display:flex;justify-content:space-between;margin-top:4px;">
        <span style="font-size:11px;font-family:var(--mono);color:var(--t2);">${fmtFull(curr)}</span>
        <span style="font-size:11px;color:var(--t3);">dari ${fmtFull(target)}</span>
      </div>
    </div>`;
  }).join(''):
  `<div style="text-align:center;padding:20px 0;color:var(--t3);font-size:12px;">
    <div style="font-size:24px;margin-bottom:8px;opacity:.3;">🎯</div>
    Tambah goal untuk track progres.
    <div style="margin-top:10px;"><button onclick="switchView('goals')" style="padding:6px 16px;border-radius:8px;border:none;background:var(--accent);color:#000;font-size:12px;font-weight:800;cursor:pointer;">+ Tambah Goal →</button></div>
  </div>`;

  // Total followers
  const totalFollowers=allPosts.reduce((mx,p)=>Math.max(mx,p._flw||0),0);
  const totalFollDisplay=allPlatforms().reduce((s,plt)=>{
    const plPosts=allPosts.filter(p=>p._plt===plt.id);
    return s+(plPosts.length?Math.max(...plPosts.map(p=>p._flw||0)):0);
  },0);
  // Dynamic sync timestamp
  let lastSyncStr = 'Belum pernah disimpan';
  try {
    const _sm = localStorage.getItem(SYNC_META_KEY);
    if (_sm) {
      const _smp = JSON.parse(_sm);
      if (_smp && _smp.updatedAt) {
        const _diff = Math.floor((Date.now() - new Date(_smp.updatedAt).getTime()) / 1000);
        if (_diff < 60) lastSyncStr = 'Disimpan baru saja';
        else if (_diff < 3600) lastSyncStr = `Disimpan ${Math.floor(_diff/60)} mnt lalu`;
        else if (_diff < 86400) lastSyncStr = `Disimpan ${Math.floor(_diff/3600)} jam lalu`;
        else lastSyncStr = `Disimpan ${Math.floor(_diff/86400)} hari lalu`;
      }
    }
  } catch(e) {}

  body.innerHTML=`
    <!-- Page Header -->
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-header-meta">
          <span class="view-header-badge">
            <span class="view-header-badge-dot"></span>
            Live Analytics
          </span>
          <span class="view-header-sync">• ${lastSyncStr}</span>
        </div>
        <div class="view-title">Ringkasan Dashboard</div>
      </div>
      <div class="view-header-right">
        <div class="view-header-stat">
          <div class="view-header-stat-label">Total Pengikut Gabungan</div>
          <div class="view-header-stat-value">${fmt(totalFollDisplay||0)}</div>
        </div>
        <div class="view-header-avatar" id="dash-avatar">${(accounts[0]||'CR').slice(0,2).toUpperCase()}</div>
      </div>
    </div>

    ${reminderBannerHTML(allPosts)}
    ${!hasData?emptyStateHTML('dashboard'):''}
    ${hasData?(()=>{const narrative=buildWeeklyNarrative(allPosts,currStats,prevStats,insights);return narrative?`<div class="intelligence-banner"><span class="intelligence-ico">🧠</span><div class="intelligence-text">${narrative}</div><button class="intelligence-more" onclick="switchView('analytics');analyticsTab='trends';destroyCharts();renderAnalytics()">Analisa →</button></div>`:''})():''}

    <!-- 4 Core KPI Cards v2 -->
    <div class="kpi-grid" style="grid-template-columns:repeat(auto-fill,minmax(192px,1fr));margin-bottom:32px;">
      <div class="kpi-card-v2">
        <div class="kpi-v2-top">
          <div class="kpi-v2-label">Total Reach</div>
          <div class="kpi-v2-icon" style="background:rgba(99,102,241,.15);">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="8" cy="8" r="2.5" fill="#818CF8"/><path d="M2 8a6 6 0 0 1 12 0" stroke="#818CF8" stroke-width="1.4" stroke-linecap="round"/><circle cx="8" cy="8" r="6.5" stroke="#818CF8" stroke-width="1" opacity=".3"/></svg>
          </div>
        </div>
        <div class="kpi-v2-value" data-copyable="${fmt(totalReach)}">${fmt(totalReach)}</div>
        <div class="kpi-v2-sub">TT ${fmt(ttAll.reach)} · IG ${fmt(igAll.reach)}</div>
        ${(()=>{const d=prevStats.reach>0?((currStats.reach-prevStats.reach)/prevStats.reach*100):null;return d!==null?`<div style="font-size:10px;font-weight:700;margin-top:5px;color:${d>=0?'var(--green)':'var(--red)'};">${d>=0?'▲':'▼'} ${Math.abs(d).toFixed(0)}% vs bln lalu</div>`:'';})()}
      </div>
      <div class="kpi-card-v2">
        <div class="kpi-v2-top">
          <div class="kpi-v2-label">Avg ER Rate</div>
          <div class="kpi-v2-icon" style="background:${erLvl.bg};">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M2 12l3.5-4 3 2 3.5-5 2 2" stroke="${erLvl.color}" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
        </div>
        <div class="kpi-v2-value" style="color:${erLvl.color};" data-copyable="${avgER.toFixed(2)}%">${avgER.toFixed(2)}%</div>
        <div class="kpi-v2-sub"><span class="kpi-v2-tag" style="background:${erLvl.bg};color:${erLvl.color};">${erLvl.label}</span></div>
        ${(()=>{const d=prevStats.avgER>0?((currStats.avgER-prevStats.avgER)/prevStats.avgER*100):null;return d!==null?`<div style="font-size:10px;font-weight:700;margin-top:5px;color:${d>=0?'var(--green)':'var(--red)'};">${d>=0?'▲':'▼'} ${Math.abs(d).toFixed(0)}% vs bln lalu</div>`:'';})()}
      </div>
      <div class="kpi-card-v2">
        <div class="kpi-v2-top">
          <div class="kpi-v2-label">Total Konten</div>
          <div class="kpi-v2-icon" style="background:rgba(245,158,11,.12);">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="2.5" y="1.5" width="11" height="13" rx="1.5" stroke="#F59E0B" stroke-width="1.4"/><path d="M5 5.5h6M5 8h6M5 10.5h3.5" stroke="#F59E0B" stroke-width="1.3" stroke-linecap="round"/></svg>
          </div>
        </div>
        <div class="kpi-v2-value" data-copyable="${fmtFull(totalPosts)}">${fmtFull(totalPosts)}</div>
        <div class="kpi-v2-sub">Bulan ini terposting ${fmtFull(ttCurr.posts+igCurr.posts)} video</div>
        ${(()=>{const d=prevStats.posts>0?((currStats.posts-prevStats.posts)/prevStats.posts*100):null;return d!==null?`<div style="font-size:10px;font-weight:700;margin-top:5px;color:${d>=0?'var(--green)':'var(--red)'};">${d>=0?'▲':'▼'} ${Math.abs(d).toFixed(0)}% vs bln lalu</div>`:'';})()}
      </div>
      <div class="kpi-card-v2">
        <div class="kpi-v2-top">
          <div class="kpi-v2-label">Akun Aktif</div>
          <div class="kpi-v2-icon" style="background:rgba(34,197,94,.12);">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="6" cy="5" r="2.5" stroke="#22C55E" stroke-width="1.4"/><path d="M1 13c0-2.8 2.2-5 5-5h1" stroke="#22C55E" stroke-width="1.4" stroke-linecap="round"/><path d="M11 9v5M8.5 11.5h5" stroke="#22C55E" stroke-width="1.4" stroke-linecap="round"/></svg>
          </div>
        </div>
        <div class="kpi-v2-value" data-copyable="${accounts.length}">${accounts.length}</div>
        <div class="kpi-v2-sub">TikTok + Instagram terhubung</div>
      </div>
    </div>

    ${weeklyMovement}
    ${insightEngineHTML}
    ${hasData?top3HTML:''}

    <!-- Goal Progress -->
    <div class="section-hdr"><div class="section-ttl">🎯 Goal Progress — Bulan Ini</div><button class="section-action" onclick="switchView('goals')">Edit →</button></div>
    <div class="chart-card" style="margin-bottom:16px;padding:20px 22px;">${goalPrevHTML}</div>

    <!-- Competitor Mini-Card -->
    ${competitors.length>0?(()=>{
      const myStats=getReportStats('all',new Date().getFullYear(),new Date().getMonth(),'month');
      const myER=myStats.avgER||0;
      const myReach=myStats.posts>0?(myStats.reach/myStats.posts):0;
      const top=competitors.slice(0,3);
      const rows=top.map(c=>{
        const plt=allPlatforms().find(p=>p.id===c.platform)||{emoji:'📌'};
        const erDiff=myER-c.avgER;
        const reachDiff=myReach-c.avgReach;
        const erCls=erDiff>=0?'color:var(--green)':'color:var(--red)';
        const rCls=reachDiff>=0?'color:var(--green)':'color:var(--red)';
        return`<div style="display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid var(--b);">
          <div style="font-size:18px;flex-shrink:0;">${plt.emoji}</div>
          <div style="flex:1;min-width:0;">
            <div style="font-size:12px;font-weight:700;color:var(--t);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${c.name}</div>
            <div style="font-size:10px;color:var(--t3);">${fmt(c.followers)} followers</div>
          </div>
          <div style="text-align:right;">
            <div style="font-size:11px;font-weight:700;${erCls}">${erDiff>=0?'+':''}${erDiff.toFixed(2)}% ER</div>
            <div style="font-size:10px;color:var(--t4);">vs mereka</div>
          </div>
        </div>`;
      }).join('');
      return`<div class="section-hdr"><div class="section-ttl">⚔️ vs Kompetitor</div><button class="section-action" onclick="switchView('competitor')">Lihat semua →</button></div>
      <div class="chart-card" style="margin-bottom:16px;padding:16px 20px;">
        <div style="font-size:10px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:4px;">Kamu vs Kompetitor — Bulan Ini</div>
        ${rows}
        ${competitors.length>3?`<div style="font-size:11px;color:var(--t3);text-align:center;padding:8px 0 0;">+${competitors.length-3} kompetitor lain</div>`:''}
      </div>`;
    })():''}

    <!-- Quick Access: Planning Tools -->
    <div class="section-hdr"><div class="section-ttl">🛠️ Alat Perencanaan</div></div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:24px;">
      <div onclick="openQuickAdd()" style="background:linear-gradient(135deg,rgba(14,159,143,.12),rgba(14,159,143,.05));border:1px solid rgba(14,159,143,.25);border-radius:14px;padding:18px 20px;cursor:pointer;transition:transform .15s;" onmouseover="this.style.transform='translateY(-2px)'" onmouseout="this.style.transform=''">
        <div style="font-size:22px;margin-bottom:8px;">⚡</div>
        <div style="font-size:14px;font-weight:800;color:var(--t);margin-bottom:3px;">Quick Add Post</div>
        <div style="font-size:11px;color:var(--t3);">Input post tanpa navigasi · Langsung dari sini</div>
      </div>
      <div onclick="switchView('calendar')" style="background:linear-gradient(135deg,rgba(245,158,11,.1),rgba(34,197,94,.06));border:1px solid rgba(245,158,11,.2);border-radius:14px;padding:18px 20px;cursor:pointer;transition:transform .15s;" onmouseover="this.style.transform='translateY(-2px)'" onmouseout="this.style.transform=''">
        <div style="font-size:22px;margin-bottom:8px;">📅</div>
        <div style="font-size:14px;font-weight:800;color:var(--t);margin-bottom:3px;">Kalender Konten</div>
        <div style="font-size:11px;color:var(--t3);">${calendarPosts.filter(e=>e.status!=='posted').length} konten terjadwal · Termasuk brief & hook</div>
      </div>
    </div>
  `;
}

/* ══ SPRINT 3: ANALYTICS INSIGHTS TAB ══ */

// ── Winning Pattern Detector ──
function winningPatternDetector(allPosts){
  if(!allPosts.length) return null;

  // Only use top 30% performing posts by ER
  const sorted=[...allPosts]
    .filter(p=>calcER(p,sel.erMode,p._flw||0)>0)
    .sort((a,b)=>calcER(b,sel.erMode,b._flw||0)-calcER(a,sel.erMode,a._flw||0));
  const topN=Math.max(Math.ceil(sorted.length*0.3),1);
  const topPosts=sorted.slice(0,topN);
  if(!topPosts.length) return null;

  // ── Best Pillar ──
  const pillarCount={};
  topPosts.forEach(p=>{
    const pid=p.pillar||'other';
    pillarCount[pid]=(pillarCount[pid]||0)+1;
  });
  const bestPillarId=Object.entries(pillarCount).sort((a,b)=>b[1]-a[1])[0]?.[0];
  const bestPillarObj=pillars.find(p=>p.id===bestPillarId)||null;
  const bestPillarPct=bestPillarId?Math.round((pillarCount[bestPillarId]/topPosts.length)*100):0;

  // ── Best Day ──
  const dayCount={};
  topPosts.forEach(p=>{
    if(!p.date)return;
    const day=DAY_ID[new Date(p.date+'T12:00:00').getDay()];
    dayCount[day]=(dayCount[day]||0)+1;
  });
  const bestDay=Object.entries(dayCount).sort((a,b)=>b[1]-a[1])[0]||null;

  // ── Caption length pattern ──
  const shortCap=topPosts.filter(p=>(p.name||'').length<60).length;
  const longCap=topPosts.filter(p=>(p.name||'').length>=60).length;
  const captionPref=shortCap>longCap?'Pendek (<60 karakter)':longCap>shortCap?'Panjang (≥60 karakter)':null;

  // ── Best posting time from posted_at ──
  const hourCount={};
  topPosts.forEach(p=>{
    if(!p.posted_at)return;
    const h=new Date(p.posted_at).getHours();
    hourCount[h]=(hourCount[h]||0)+1;
  });
  const bestHour=Object.entries(hourCount).sort((a,b)=>b[1]-a[1])[0]||null;
  const period=bestHour?
    (+bestHour[0]<6?'Dini hari':+bestHour[0]<12?'Pagi':+bestHour[0]<17?'Siang':+bestHour[0]<21?'Malam':'Larut malam')
    :null;

  // ── Platform ──
  const pltCount={};
  topPosts.forEach(p=>{ pltCount[p._plt||'tt']=(pltCount[p._plt||'tt']||0)+1; });
  const bestPlt=Object.entries(pltCount).sort((a,b)=>b[1]-a[1])[0]||null;
  const bestPltObj=bestPlt?allPlatforms().find(p=>p.id===bestPlt[0]):null;
  const bestPltPct=bestPlt?Math.round((bestPlt[1]/topPosts.length)*100):0;

  return{
    topN,totalPosts:allPosts.length,
    pillar:{obj:bestPillarObj,pct:bestPillarPct,count:pillarCount[bestPillarId]||0},
    day:{name:bestDay?.[0],pct:bestDay?Math.round((bestDay[1]/topPosts.length)*100):0},
    caption:captionPref,
    hour:{h:bestHour?+bestHour[0]:null,period,pct:bestHour?Math.round((+bestHour[1]/topPosts.length)*100):0},
    platform:{obj:bestPltObj,pct:bestPltPct},
  };
}

// ── Derive time info from posted_at ISO string ──
function derivePostingTime(post){
  if(!post.posted_at)return null;
  const d=new Date(post.posted_at);
  if(isNaN(d.getTime()))return null;
  const h=d.getHours();
  return{
    hour:h,
    minute:d.getMinutes(),
    day:DAY_ID[d.getDay()],
    dayIndex:d.getDay(),
    isWeekend:[0,6].includes(d.getDay()),
    period:h<6?'Dini hari':h<12?'Pagi':h<17?'Siang':h<21?'Malam':'Larut malam',
    display:String(h).padStart(2,'0')+':'+String(d.getMinutes()).padStart(2,'0'),
  };
}

// ── Best posting time (from posted_at) ──
function getBestPostingTimeData(posts){
  const hourMap={};
  posts.forEach(p=>{
    if(!p.posted_at)return;
    const d=new Date(p.posted_at);
    const h=d.getHours();
    const er=calcER(p,sel.erMode,p._flw||0);
    if(!hourMap[h])hourMap[h]={totalER:0,count:0,posts:0};
    if(er>0){hourMap[h].totalER+=er;hourMap[h].count++;}
    hourMap[h].posts++;
  });
  return Object.entries(hourMap)
    .map(([h,d])=>({h:+h,avgER:d.count?d.totalER/d.count:0,posts:d.posts}))
    .sort((a,b)=>b.avgER-a.avgER);
}

// ── Insights Tab Renderer ──
function renderInsightsTab(body, allPosts){
  const insights=generateInsights(allPosts);
  const highlights=computeKeyHighlights(allPosts);
  const dayRanking=bestPerformingDay(allPosts);
  const streak=computePostingStreak(allPosts);
  const pattern=winningPatternDetector(allPosts);
  const hasPostedAt=allPosts.some(p=>p.posted_at);
  const timeData=hasPostedAt?getBestPostingTimeData(allPosts):[];

  // ── Winning Pattern card ──
  const patternHTML=pattern?`
    <div class="ins-pattern-card">
      <div class="ins-pattern-head">
        <span class="ins-pattern-icon">🧬</span>
        <div>
          <div class="ins-pattern-title">Winning Pattern</div>
          <div class="ins-pattern-sub">Dari top ${pattern.topN} post terbaik kamu (${pattern.totalPosts} total)</div>
        </div>
      </div>
      <div class="ins-pattern-grid">
        ${pattern.pillar.obj?`
        <div class="ins-pattern-item">
          <div class="ins-pi-label">🎨 Pilar Terkuat</div>
          <div class="ins-pi-val">${pattern.pillar.obj.emoji} ${pattern.pillar.obj.label}</div>
          <div class="ins-pi-sub">${pattern.pillar.pct}% dari top posts</div>
          <div class="ins-pi-bar"><div style="width:${pattern.pillar.pct}%;background:${pattern.pillar.obj.color||'var(--accent)'};height:3px;border-radius:2px;"></div></div>
        </div>`:''}
        ${pattern.day.name?`
        <div class="ins-pattern-item">
          <div class="ins-pi-label">📅 Hari Terbaik</div>
          <div class="ins-pi-val">${pattern.day.name}</div>
          <div class="ins-pi-sub">${pattern.day.pct}% dari top posts</div>
          <div class="ins-pi-bar"><div style="width:${pattern.day.pct}%;background:var(--blue);height:3px;border-radius:2px;"></div></div>
        </div>`:''}
        ${pattern.platform.obj?`
        <div class="ins-pattern-item">
          <div class="ins-pi-label">📱 Platform Terkuat</div>
          <div class="ins-pi-val">${pattern.platform.obj.emoji} ${pattern.platform.obj.name}</div>
          <div class="ins-pi-sub">${pattern.platform.pct}% dari top posts</div>
          <div class="ins-pi-bar"><div style="width:${pattern.platform.pct}%;background:${pltColorRaw(pattern.platform.obj.id)};height:3px;border-radius:2px;"></div></div>
        </div>`:''}
        ${pattern.caption?`
        <div class="ins-pattern-item">
          <div class="ins-pi-label">✍️ Panjang Caption</div>
          <div class="ins-pi-val" style="font-size:13px;">${pattern.caption}</div>
          <div class="ins-pi-sub">Lebih banyak di top posts</div>
        </div>`:''}
        ${pattern.hour.h!==null?`
        <div class="ins-pattern-item">
          <div class="ins-pi-label">⏰ Jam Terbaik</div>
          <div class="ins-pi-val">${String(pattern.hour.h).padStart(2,'0')}:00 · ${pattern.hour.period}</div>
          <div class="ins-pi-sub">${pattern.hour.pct}% dari top posts (dari data posted_at)</div>
        </div>`:`
        <div class="ins-pattern-item" style="opacity:.5;">
          <div class="ins-pi-label">⏰ Jam Terbaik</div>
          <div class="ins-pi-val" style="font-size:13px;color:var(--t3);">Belum ada data</div>
          <div class="ins-pi-sub">Tambahkan posted_at di konten</div>
        </div>`}
      </div>
      <div class="ins-pattern-summary">
        💡 <strong>Kesimpulan:</strong>
        Konten terbaik kamu cenderung berupa
        ${pattern.pillar.obj?`<span style="color:${pattern.pillar.obj.color||'var(--accent)'};">konten ${pattern.pillar.obj.label}</span>`:'konten tertentu'}${pattern.day.name?`, diposting di hari <strong>${pattern.day.name}</strong>`:''}${pattern.caption?`, dengan caption ${pattern.caption.toLowerCase()}`:''}${pattern.platform.obj?`, dan perform terbaik di ${pattern.platform.obj.name}`:''}.
      </div>
    </div>`:
    `<div class="ins-empty"><div style="font-size:32px;opacity:.3;">🧬</div><div style="margin-top:8px;font-size:13px;color:var(--t3);">Butuh lebih banyak data untuk mendeteksi pola.</div></div>`;

  // ── Day ranking table ──
  const dayTableHTML=dayRanking.length?`
    <div class="ins-day-table">
      ${dayRanking.map((d,i)=>{
        const lvl=erLevel(d.avgER);
        const isTop=i===0;
        return`<div class="ins-day-row ${isTop?'ins-day-top':''}">
          <div class="ins-day-rank">${i+1}</div>
          <div class="ins-day-name">${isTop?'🏆':''} ${d.day}</div>
          <div class="ins-day-posts">${d.count} post</div>
          <div class="ins-day-er" style="color:${lvl.color};">${erStr(d.avgER)}</div>
          <div class="ins-day-badge" style="background:${lvl.bg};color:${lvl.color};">${lvl.label}</div>
        </div>`;
      }).join('')}
    </div>`:'<div class="ins-empty">Belum ada data tanggal posting.</div>';

  // ── Best posting time breakdown ──
  const timeHTML=timeData.length?`
    <div class="ins-time-list">
      ${timeData.slice(0,6).map((t,i)=>{
        const period=t.h<6?'Dini hari':t.h<12?'Pagi':t.h<17?'Siang':t.h<21?'Malam':'Larut';
        const lvl=erLevel(t.avgER);
        const isTop=i===0;
        const maxPosts=timeData[0]?.posts||1;
        return`<div class="ins-time-row ${isTop?'ins-time-top':''}">
          <div class="ins-time-hour">${String(t.h).padStart(2,'0')}:00</div>
          <div class="ins-time-period">${period}</div>
          <div class="ins-time-bar-wrap"><div class="ins-time-bar" style="width:${Math.round((t.posts/maxPosts)*100)}%;background:${lvl.color};"></div></div>
          <div class="ins-time-meta">
            ${t.avgER>0?`<span style="color:${lvl.color};font-family:var(--mono);">${erStr(t.avgER)}</span> ER · `:''}
            <span style="color:var(--t3);">${t.posts} post</span>
          </div>
          ${isTop?`<div class="ins-time-badge">Best</div>`:''}
        </div>`;
      }).join('')}
    </div>`:`
    <div class="ins-empty">
      <div style="font-size:28px;opacity:.3;">⏰</div>
      <div style="margin-top:8px;font-size:13px;color:var(--t3);">Tambahkan field <strong>Posted At</strong> di tab Content untuk analisis jam posting.</div>
    </div>`;

  // ── Streak section ──
  const streakHTML=`
    <div class="ins-streak-row">
      <div class="ins-streak-item ${streak.streak>=7?'ins-streak-fire':''}">
        <div class="ins-streak-ico">${streak.streak>=7?'🔥':streak.streak>=3?'✅':'📭'}</div>
        <div class="ins-streak-val" style="color:${streak.streak>=3?'var(--accent)':'var(--t)'};">${streak.streak}</div>
        <div class="ins-streak-lbl">Hari Streak</div>
      </div>
      <div class="ins-streak-item">
        <div class="ins-streak-ico">📅</div>
        <div class="ins-streak-val">${streak.daysLast30}</div>
        <div class="ins-streak-lbl">Hari Aktif (30hr)</div>
      </div>
      <div class="ins-streak-item">
        <div class="ins-streak-ico">📝</div>
        <div class="ins-streak-val">${streak.totalPosted}</div>
        <div class="ins-streak-lbl">Total Hari Posting</div>
      </div>
      <div class="ins-streak-item">
        <div class="ins-streak-ico">📊</div>
        <div class="ins-streak-val">${streak.daysLast30?Math.round(streak.daysLast30/30*100):0}%</div>
        <div class="ins-streak-lbl">Konsistensi Bln Ini</div>
      </div>
    </div>`;

  // ── Full insight list ──
  const allInsights=generateInsights(allPosts);
  const insightsListHTML=allInsights.length?
    allInsights.map((ins,i)=>insightCardHTML(ins,i)).join(''):
    `<div class="ins-empty">Tambahkan lebih banyak konten untuk melihat insights otomatis.</div>`;

  // ── Key Highlights ──
  const hlHTML=highlights.length?`
    <div class="highlights-scroll">
      ${highlights.map(h=>highlightCardHTML(h,allPosts)).join('')}
    </div>`:'<div class="ins-empty">Belum ada data highlight.</div>';

  body.innerHTML=`
    <div class="ins-layout">

      <!-- Winning Pattern -->
      <div class="ins-section">
        <div class="ins-section-head">
          <div class="ins-section-title">🧬 Winning Pattern Detector</div>
          <div class="ins-section-sub">Pola konten terbaik kamu berdasarkan top ${Math.max(1,Math.ceil(allPosts.length*0.3))} post</div>
        </div>
        ${patternHTML}
      </div>

      <!-- Weekly Insights -->
      <div class="ins-section">
        <div class="ins-section-head">
          <div class="ins-section-title">💡 Auto Insights</div>
          <div class="ins-section-sub">Dihasilkan otomatis dari rule-based engine · tidak pakai AI</div>
        </div>
        <div class="insight-grid">${insightsListHTML}</div>
      </div>

      <!-- Key Highlights -->
      <div class="ins-section">
        <div class="ins-section-head">
          <div class="ins-section-title">⭐ Key Highlights</div>
          <div class="ins-section-sub">Konten paling menonjol sepanjang waktu</div>
        </div>
        ${hlHTML}
      </div>

      <!-- Best Day Ranking -->
      <div class="ins-two-col">
        <div class="ins-section">
          <div class="ins-section-head">
            <div class="ins-section-title">📅 Ranking Hari Terbaik</div>
            <div class="ins-section-sub">Berdasarkan avg ER per hari posting</div>
          </div>
          ${dayTableHTML}
        </div>

        <!-- Best Posting Time -->
        <div class="ins-section">
          <div class="ins-section-head">
            <div class="ins-section-title">⏰ Best Posting Time</div>
            <div class="ins-section-sub">${hasPostedAt?'Berdasarkan field posted_at · avg ER per jam':'Butuh data posted_at di Content editor'}</div>
          </div>
          ${timeHTML}
        </div>
      </div>

      <!-- Consistency Tracker -->
      <div class="ins-section">
        <div class="ins-section-head">
          <div class="ins-section-title">🔥 Consistency Tracker</div>
          <div class="ins-section-sub">Rekap konsistensi posting kamu</div>
        </div>
        ${streakHTML}
        ${dayRanking.length?`<div style="margin-top:12px;padding:10px 14px;background:rgba(0,232,122,.06);border:1px solid rgba(0,232,122,.15);border-radius:10px;font-size:12px;color:var(--t2);">
          📅 Hari terbaik untuk posting: <strong style="color:var(--accent);">${dayRanking[0].day}</strong> · Avg ER <span style="font-family:var(--mono);color:var(--accent);">${erStr(dayRanking[0].avgER)}</span>
        </div>`:''}
      </div>

    </div>`;
}

/* ══ RENDER: ANALYTICS ══ */
function renderAnalytics(){
  const body=document.getElementById('analytics-body');if(!body)return;
  // Revisi 6: actionable empty state
  const _allPostsCheck=getAllPosts();
  if(_allPostsCheck.length < 5 && analyticsTab==='trends'){
    const need=5-_allPostsCheck.length;
    body.innerHTML=emptyStateHTML('analytics',`Tambah <strong style="color:var(--accent);">${need} post lagi</strong> untuk membuka Analytics.`);
    return;
  }
  if(ovAcc!=='all'&&!accounts.includes(ovAcc))ovAcc='all';
  const yrs=[...new Set(months.map(m=>m.year))].sort();
  const yOpts=yrs.map(y=>`<option value="${y}" ${y===ovYear?'selected':''}>${y}</option>`).join('');
  const mOpts=MN.map((mn,i)=>`<option value="${i}" ${i===ovMonth?'selected':''}>${mn}</option>`).join('');
  const ovAccOpts=`<option value="all" ${ovAcc==='all'?'selected':''}>Semua akun</option>${accounts.map(a=>`<option value="${esc(a)}" ${ovAcc===a?'selected':''}>${a}</option>`).join('')}`;

  const tabsHTML=`<div class="analytics-tabs">
    ${[
      {id:'trends',label:'📈 Tren'},
      {id:'top',label:'🏆 Top Content'},
      {id:'pillar',label:'🎨 Pillar'},
      {id:'comparison',label:'⚖️ Komparasi'},
      {id:'multiacc',label:'👥 Semua Akun'},
    ].map(t=>`<button class="analytics-tab-btn ${analyticsTab===t.id?'act':''}" onclick="analyticsTab='${t.id}';destroyCharts();renderAnalytics()">${t.label}</button>`).join('')}
  </div>`;

  const filterHTML=`<div class="analytics-filter">
    <span class="af-label">Filter:</span>
    <div class="seg">
      <button class="seg-btn ${ovFilter==='all'?'act':''}" onclick="ovFilter='all';destroyCharts();renderAnalytics()">Semua</button>
      <button class="seg-btn ${ovFilter==='month'?'act':''}" onclick="ovFilter='month';destroyCharts();renderAnalytics()">Per Bulan</button>
      <button class="seg-btn ${ovFilter==='range'?'act':''}" onclick="ovFilter='range';destroyCharts();renderAnalytics()">Rentang</button>
    </div>
    ${ovFilter==='month'?`
      <select class="af-sel" onchange="ovYear=+this.value;destroyCharts();renderAnalytics()">${yOpts}</select>
      <select class="af-sel" onchange="ovMonth=+this.value;destroyCharts();renderAnalytics()">${mOpts}</select>`:''}
    ${ovFilter==='range'?`
      <input type="date" class="date-inp" value="${ovDateFrom}" onchange="ovDateFrom=this.value;destroyCharts();renderAnalytics()">
      <span class="af-label">s/d</span>
      <input type="date" class="date-inp" value="${ovDateTo}" onchange="ovDateTo=this.value;destroyCharts();renderAnalytics()">`:''}
    ${analyticsTab==='trends'?`
      <span class="af-label" style="margin-left:8px;">Akun:</span>
      <select class="af-sel" onchange="ovAcc=this.value;scheduleSave(100);destroyCharts();renderAnalytics()">${ovAccOpts}</select>`:''}
    <span class="af-label" style="margin-left:8px;" title="Engagement Rate dihitung dengan membagi total engagement dengan basis yang dipilih">ER ÷:</span>
    <div class="seg">
      <button class="seg-btn ${sel.erMode==='impression'?'act':''}" onclick="setER('impression')" title="ER ÷ Impresi — angka lebih kecil, lebih akurat">Impresi</button>
      <button class="seg-btn ${sel.erMode==='reach'?'act':''}" onclick="setER('reach')" title="ER ÷ Reach — akun unik yang lihat konten">Reach</button>
      <button class="seg-btn ${sel.erMode==='followers'?'act':''}" onclick="setER('followers')" title="ER ÷ Followers — standar benchmark industri (Later, Sprout Social)">Followers ★</button>
    </div>
    <span style="font-size:10px;color:var(--t3);margin-left:6px;">${sel.erMode==='followers'?'(standar industri)':sel.erMode==='reach'?'(reach-based)':'(impression-based)'}</span>
  </div>`;

  let tabContent='';

  if(analyticsTab==='trends'){
    const ttS=getFilteredStats('tt',ovYear,ovMonth);
    const igS=getFilteredStats('ig',ovYear,ovMonth);
    const cd=buildChartData();
    const overviewAccounts=accounts.filter(a=>ovAcc==='all'||a===ovAcc);
    // Pre-compute max total impression per account to avoid spread in template literal
    const _maxAccImpression=(()=>{
      let max=0;
      overviewAccounts.forEach(acc=>{
        let t=0;
        allPlatforms().forEach(p=>{months.forEach(mo=>{
          if(ovFilter==='month'&&(mo.year!==ovYear||mo.month!==ovMonth))return;
          mo.weeks.forEach(w=>{getPltPosts(aD(w,acc),p.id).forEach(post=>{
            if(ovFilter==='range'){if(!post.date)return;if(ovDateFrom&&post.date<ovDateFrom)return;if(ovDateTo&&post.date>ovDateTo)return;}
            t+=post.impression||0;
          });});
        });});
        if(t>max)max=t;
      });
      return Math.max(max,1);
    })();
    const topTT=overviewAccounts.map(a=>{
      let v=0;months.forEach(m=>{
        if(ovFilter==='month'&&(m.year!==ovYear||m.month!==ovMonth))return;
        m.weeks.forEach(w=>{getPltPosts(aD(w,a),'tt').forEach(p=>{
          if(ovFilter==='range'){if(!p.date)return;if(ovDateFrom&&p.date<ovDateFrom)return;if(ovDateTo&&p.date>ovDateTo)return;}
          v+=p.impression||0;
        });});
      });return{n:a,v};
    }).sort((a,b)=>b.v-a.v).slice(0,6);
    const topIG=overviewAccounts.map(a=>{
      let v=0;months.forEach(m=>{
        if(ovFilter==='month'&&(m.year!==ovYear||m.month!==ovMonth))return;
        m.weeks.forEach(w=>{getPltPosts(aD(w,a),'ig').forEach(p=>{
          if(ovFilter==='range'){if(!p.date)return;if(ovDateFrom&&p.date<ovDateFrom)return;if(ovDateTo&&p.date>ovDateTo)return;}
          v+=p.impression||0;
        });});
      });return{n:a,v};
    }).sort((a,b)=>b.v-a.v).slice(0,6);

    normalizeOverviewMetrics();
    const hiddenMetrics=OVERVIEW_METRIC_DEFS.filter(m=>!overviewMetrics.includes(m.key));
    const metricTools=`<div class="overview-card-tools">
      <span class="af-label">Overview cards:</span>
      <select class="af-sel" onchange="addOverviewMetric(this.value);this.value=''">
        <option value="">+ Tambah metric</option>
        ${hiddenMetrics.map(m=>`<option value="${m.key}">${m.label}</option>`).join('')}
      </select>
    </div>`;
    const statsSection=(s,label,pltId)=>{
      const c=pltColorRaw(pltId);
      return `<div style="margin-bottom:16px;">
        <div style="font-size:12px;font-weight:600;color:${c};margin-bottom:10px;display:flex;align-items:center;gap:6px;">
          <span>${pltInfo(pltId).emoji}</span>${label}
        </div>
        <div class="kpi-grid" style="margin-bottom:0;">
          ${overviewMetrics.map(k=>{
            const m=overviewMetricDef(k);if(!m)return'';
            const val=m.key==='posts'?m.value(s):fmt(m.value(s));
            return `<div class="kpi-card is-manage">
              ${overviewMetrics.length>1?`<button class="kpi-remove" onclick="event.stopPropagation();removeOverviewMetric('${m.key}')" title="Hapus metric">×</button>`:''}
              <div class="kpi-label">${m.label}</div>
              <div class="kpi-value" data-copyable="${val}" style="${m.color?`color:${m.color}`:''}">${val}</div>
              ${m.sub?`<div class="kpi-sub">${m.sub}</div>`:''}
            </div>`;
          }).join('')}
        </div>
      </div>`;
    };

    tabContent=`
      <div class="analytics-cols" style="margin-bottom:0;">
        <div class="chart-card">
          <div class="chart-title">Tren Impressi per Bulan</div>
          <div class="chart-legend">
            <span class="leg"><span class="leg-d" style="background:var(--tt)"></span>TikTok</span>
            <span class="leg"><span class="leg-d" style="background:var(--ig)"></span>Instagram</span>
          </div>
          <div style="position:relative;height:170px;"><canvas id="ch-imp"></canvas></div>
        </div>
        <div class="chart-card">
          <div class="chart-title">Tren Engagement per Bulan</div>
          <div class="chart-legend">
            <span class="leg"><span class="leg-d" style="background:var(--tt)"></span>TikTok</span>
            <span class="leg"><span class="leg-d" style="background:var(--ig)"></span>Instagram</span>
          </div>
          <div style="position:relative;height:170px;"><canvas id="ch-eng"></canvas></div>
        </div>
        <div class="chart-card">
          <div class="chart-title">ER Rate Rata-rata (%)</div>
          <div style="position:relative;height:150px;"><canvas id="ch-er"></canvas></div>
        </div>
        <div>
          ${accounts.length >= 3 ? `
          <div class="section-hdr"><div class="section-ttl">Top Akun — Total Impressi</div></div>
          <div class="leaderboard">
            ${overviewAccounts.map(a=>{
              let total=0;
              allPlatforms().forEach(p=>{
                months.forEach(m=>{
                  if(ovFilter==='month'&&(m.year!==ovYear||m.month!==ovMonth))return;
                  m.weeks.forEach(w=>{getPltPosts(aD(w,a),p.id).forEach(post=>{
                    if(ovFilter==='range'){if(!post.date)return;if(ovDateFrom&&post.date<ovDateFrom)return;if(ovDateTo&&post.date>ovDateTo)return;}
                    total+=post.impression||0;
                  });});
                });
              });
              return{n:a,v:total};
            }).sort((a,b)=>b.v-a.v).slice(0,6).map((a,i)=>`<div class="lb-row">
              <div class="lb-rank">${i+1}</div><div class="lb-name">${a.n}</div>
              <div class="lb-bar"><div class="lb-bar-fill" style="width:${a.v?Math.round(a.v/_maxAccImpression*100):0}%;background:var(--accent);"></div></div>
              <div class="lb-val">${fmt(a.v)}</div>
            </div>`).join('') || '<div style="padding:12px;color:var(--t3);font-size:12px;">Tidak ada data.</div>'}
          </div>` : ''}
        </div>
      </div>
      <div class="section-hdr" style="margin-top:20px;">
        <div class="section-ttl">📊 Benchmark Industri</div>
        <button class="bm-toggle ${benchmarkMode?'act':''}" onclick="benchmarkMode=!benchmarkMode;destroyCharts();renderAnalytics();">${benchmarkMode?'Sembunyikan':'Tampilkan Perbandingan'}</button>
      </div>
      ${benchmarkMode?`
        <div class="benchmark-banner">
          <span style="font-size:16px;">🏭</span>
          <div>Perbandingan vs <strong>rata-rata kreator konten umum</strong>. Benchmark bersifat statis — cocok untuk orientasi, bukan patokan absolut.</div>
        </div>
        ${benchmarkHTML(getAllPosts())}
      `:'<div style="font-size:12px;color:var(--t3);padding:4px 0;">Klik "Tampilkan Perbandingan" untuk melihat performa kamu vs rata-rata industri kreator.</div>'}
      <div style="margin-top:28px;padding-top:24px;border-top:1px solid var(--b);">
        <div class="section-hdr"><div class="section-ttl">💡 Insight Otomatis</div><span style="font-size:10px;color:var(--t3);font-style:italic;">Rule-based · dari data kamu</span></div>
        ${(()=>{const _ins=generateInsights(getAllPosts());return _ins.length?`<div class="insight-grid">${_ins.map((_i,_idx)=>insightCardHTML(_i,_idx)).join('')}</div>`:`<div class="dash-insight-empty" style="padding:20px;"><div style="font-size:12px;color:var(--t3);">Insight muncul otomatis setelah kamu punya 5+ post dengan data lengkap.</div></div>`;})()}
      </div>`;
    body.innerHTML=`<div class="view-header" style="margin-bottom:20px;"><div class="view-header-left"><div class="view-title">Analisis Mendalam</div><div class="view-subtitle">Grafik tren pertumbuhan, performa engagement rate, dan persebaran audiens organik</div></div><div class="view-header-right"><div style="display:flex;align-items:center;gap:6px;"><span style="font-size:11px;color:var(--t3);font-weight:600;">Rentang Waktu:</span><select class="af-sel" style="padding:6px 12px;border-radius:9px;font-size:12px;font-weight:700;" onchange="ovFilter=this.value==='all'?'all':'month';destroyCharts();renderAnalytics()"><option value="month" ${ovFilter==='month'?'selected':''}>30 Hari Terakhir</option><option value="all" ${ovFilter==='all'?'selected':''}>Semua Waktu</option></select></div></div></div>${tabsHTML}${filterHTML}${tabContent}`;

    // Draw charts
    const ttC=pltColorRaw('tt'),igC=pltColorRaw('ig');
    const ds={tension:.38,pointRadius:3,pointHoverRadius:6,borderWidth:2};
    // Build datasets for custom platforms
    const customPltDatasets_imp=[], customPltDatasets_eng=[], customPltDatasets_er=[];
    platforms.forEach(p=>{
      const c=pltColorRaw(p.id);
      customPltDatasets_imp.push({label:p.emoji+' '+p.name,data:cd.map(c2=>c2['plt_'+p.id+'_I']||0),borderColor:c,backgroundColor:c+'20',...ds});
      customPltDatasets_eng.push({label:p.emoji+' '+p.name,data:cd.map(c2=>c2['plt_'+p.id+'_Eng']||0),backgroundColor:c+'66',borderColor:c,borderWidth:1,borderRadius:4});
      customPltDatasets_er.push({label:p.emoji+' '+p.name,data:cd.map(c2=>+(c2['plt_'+p.id+'_ER']||0).toFixed(2)),backgroundColor:c+'66',borderColor:c,borderWidth:1,borderRadius:4});
    });
    setTimeout(()=>{
      charts.imp=makeChart('ch-imp','line',cd.map(c=>c.lbl),[
        {label:'TikTok',data:cd.map(c=>c.ttI),borderColor:ttC,backgroundColor:ttC+'20',...ds},
        {label:'Instagram',data:cd.map(c=>c.igI),borderColor:igC,backgroundColor:igC+'20',...ds},
        ...customPltDatasets_imp,
      ]);
      charts.eng=makeChart('ch-eng','bar',cd.map(c=>c.lbl),[
        {label:'TikTok',data:cd.map(c=>c.ttEng),backgroundColor:ttC+'66',borderColor:ttC,borderWidth:1,borderRadius:4},
        {label:'Instagram',data:cd.map(c=>c.igEng),backgroundColor:igC+'66',borderColor:igC,borderWidth:1,borderRadius:4},
        ...customPltDatasets_eng,
      ]);
      charts.er=makeChart('ch-er','bar',cd.map(c=>c.lbl),[
        {label:'TikTok',data:cd.map(c=>+c.ttER.toFixed(2)),backgroundColor:ttC+'66',borderColor:ttC,borderWidth:1,borderRadius:4},
        {label:'Instagram',data:cd.map(c=>+c.igER.toFixed(2)),backgroundColor:igC+'66',borderColor:igC,borderWidth:1,borderRadius:4},
        ...customPltDatasets_er,
      ],{plugins:{tooltip:{callbacks:{label:c=>`${c.dataset.label}: ${c.raw}%`}}},scales:{y:{ticks:{callback:v=>v+'%'}}}});
    },50);

  } else if(analyticsTab==='top'){
    const filteredPostsRaw=getAllPosts((p,m)=>{
      if(ovFilter==='month')return m.year===ovYear&&m.month===ovMonth;
      if(ovFilter==='range'){if(!p.date)return false;if(ovDateFrom&&p.date<ovDateFrom)return false;if(ovDateTo&&p.date>ovDateTo)return false;}
      return true;
    });
    const sortedPostsForTop=sortedTopPosts(filteredPostsRaw,analyticsTopSortBy,30);
    const sortDropdown=`<select class="af-sel" onchange="analyticsTopSortBy=this.value;scheduleSave(100);destroyCharts();renderAnalytics()">
      ${Object.entries(TOP_SORT_LABELS).map(([v,l])=>`<option value="${v}" ${analyticsTopSortBy===v?'selected':''}>${l}</option>`).join('')}
    </select>`;
    const sortLabel=TOP_SORT_LABELS[analyticsTopSortBy]||'Engagement';
    tabContent=`
      <div style="display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:14px;flex-wrap:wrap;">
        <div style="font-size:13px;color:var(--t2);">${filteredPostsRaw.length} konten · diurutkan berdasarkan ${sortLabel}</div>
        <div style="display:flex;align-items:center;gap:8px;"><span class="af-label">Urutkan:</span>${sortDropdown}</div>
      </div>
      <div class="top-post-list">${topPostsHTML(sortedPostsForTop,analyticsTopSortBy)}</div>`;
    body.innerHTML=`<div class="view-header" style="margin-bottom:20px;"><div class="view-header-left"><div class="view-title">Analisis Mendalam</div><div class="view-subtitle">Grafik tren pertumbuhan, performa engagement rate, dan persebaran audiens organik</div></div><div class="view-header-right"><div style="display:flex;align-items:center;gap:6px;"><span style="font-size:11px;color:var(--t3);font-weight:600;">Rentang Waktu:</span><select class="af-sel" style="padding:6px 12px;border-radius:9px;font-size:12px;font-weight:700;" onchange="ovFilter=this.value==='all'?'all':'month';destroyCharts();renderAnalytics()"><option value="month" ${ovFilter==='month'?'selected':''}>30 Hari Terakhir</option><option value="all" ${ovFilter==='all'?'selected':''}>Semua Waktu</option></select></div></div></div>${tabsHTML}${filterHTML}${tabContent}`;
    return;
    const filteredPosts=getAllPosts((p,m)=>{
      if(ovFilter==='month')return m.year===ovYear&&m.month===ovMonth;
      if(ovFilter==='range'){if(!p.date)return false;if(ovDateFrom&&p.date<ovDateFrom)return false;if(ovDateTo&&p.date>ovDateTo)return false;}
      return true;
    }).sort((a,b)=>totalEng(b)-totalEng(a));

    tabContent=`
      <div style="font-size:13px;color:var(--t2);margin-bottom:14px;">${filteredPosts.length} konten · diurutkan berdasarkan engagement tertinggi</div>
      <div class="top-post-list">
        ${filteredPosts.slice(0,30).map((p,i)=>{
          const eng=totalEng(p);
          const pi=pltInfo(p._plt);
          const pil=pillarOf(p.pillar||'other');
          const er=p._flw?calcER(p,sel.erMode,p._flw):0;
          return `<div class="top-post-item">
            <div class="rank-badge rank-${i<3?(i+1):'n'}">${i+1}</div>
            <div class="top-post-info">
              <div class="top-post-platform">
                ${pi.emoji} ${pi.name} · ${p._account}
                <span class="pillar-chip" style="background:${pil.bg};color:${pil.color};">
                  <span class="pillar-dot" style="background:${pil.color};"></span>${pil.label}
                </span>
              </div>
              <div class="top-post-meta">${p.date||'—'} · ${p._week} · ${p._month}
                ${p.link?`· <a href="${p.link}" target="_blank" style="color:var(--blue);">↗ Link</a>`:''}
              </div>
            </div>
            <div class="top-post-metrics">
              <span class="metric-pill mp-eng">❤ ${fmtFull(eng)}</span>
              <span class="metric-pill mp-reach">👁 ${fmtFull(p.reach||0)}</span>
              <span class="metric-pill" style="background:var(--blue-bg);color:var(--blue);">📊 ${fmtFull(p.impression||0)}</span>
              ${er?`<span class="metric-pill mp-er">${erStr(er)}</span>`:''}
            </div>
          </div>`;
        }).join('')||'<div style="padding:24px;text-align:center;color:var(--t3);">Tidak ada konten dalam rentang ini.</div>'}
      </div>`;
    body.innerHTML=`<div class="view-header" style="margin-bottom:20px;"><div class="view-header-left"><div class="view-title">Analisis Mendalam</div><div class="view-subtitle">Grafik tren pertumbuhan, performa engagement rate, dan persebaran audiens organik</div></div><div class="view-header-right"><div style="display:flex;align-items:center;gap:6px;"><span style="font-size:11px;color:var(--t3);font-weight:600;">Rentang Waktu:</span><select class="af-sel" style="padding:6px 12px;border-radius:9px;font-size:12px;font-weight:700;" onchange="ovFilter=this.value==='all'?'all':'month';destroyCharts();renderAnalytics()"><option value="month" ${ovFilter==='month'?'selected':''}>30 Hari Terakhir</option><option value="all" ${ovFilter==='all'?'selected':''}>Semua Waktu</option></select></div></div></div>${tabsHTML}${filterHTML}${tabContent}`;

  } else if(analyticsTab==='pillar'){
    const filteredPosts=getAllPosts((p,m)=>{
      if(ovFilter==='month')return m.year===ovYear&&m.month===ovMonth;
      if(ovFilter==='range'){if(!p.date)return false;if(ovDateFrom&&p.date<ovDateFrom)return false;if(ovDateTo&&p.date>ovDateTo)return false;}
      return true;
    });
    const pillarMap={};
    pillars.forEach(p=>{pillarMap[p.id]={count:0,eng:0,reach:0,impression:0,posts:[]};});
    filteredPosts.forEach(p=>{
      const pid=p.pillar||pillars[pillars.length-1]?.id||'other';
      if(!pillarMap[pid])pillarMap[pid]={count:0,eng:0,reach:0,impression:0,posts:[]};
      pillarMap[pid].count++;
      pillarMap[pid].eng+=totalEng(p);
      pillarMap[pid].reach+=p.reach||0;
      pillarMap[pid].impression+=p.impression||0;
      pillarMap[pid].posts.push(p);
    });
    const maxEng=Math.max(...pillars.map(p=>pillarMap[p.id]?.eng||0),1);

    tabContent=`
      <div style="display:flex;align-items:center;justify-content:flex-end;margin-bottom:12px;">
        <button class="pillar-manage-btn" onclick="openModal('m-pillar')">🎨 Kelola Pilar</button>
      </div>
      ${!pillars.some(p=>(pillarMap[p.id]?.count||0)>0)?`
      <div style="padding:40px 20px;text-align:center;">
        <div style="font-size:36px;opacity:.2;margin-bottom:12px;">🎨</div>
        <div style="font-size:14px;font-weight:700;color:var(--t);margin-bottom:6px;">Kamu belum assign pilar ke konten</div>
        <div style="font-size:12px;color:var(--t3);margin-bottom:16px;">Pilar membantu kamu tahu jenis konten mana yang perform terbaik.</div>
        <button onclick="openQCModal()" style="padding:8px 20px;border-radius:8px;border:none;background:var(--accent);color:#000;font-size:12px;font-weight:800;cursor:pointer;">+ Tambah Konten dengan Pilar</button>
      </div>`:
      `<div class="pillar-grid" style="grid-template-columns:repeat(auto-fill,minmax(240px,1fr));">
        ${pillars.map(p=>{
          const s=pillarMap[p.id]||{count:0,eng:0,reach:0,impression:0};
          const avgEng=s.count?Math.round(s.eng/s.count):0;
          const barW=Math.round((s.eng/maxEng)*100);
          return `<div class="pillar-card" style="border-left:3px solid ${p.color};">
            <div class="pillar-card-top">
              <span class="pillar-card-emoji">${p.emoji}</span>
              <span class="pillar-card-name">${p.label}</span>
              <span class="pillar-card-count">${s.count} post</span>
            </div>
            <div class="pillar-stat-row">
              <div class="pillar-stat"><div class="pillar-stat-lbl">Total Eng</div><div class="pillar-stat-val">${fmt(s.eng)}</div></div>
              <div class="pillar-stat"><div class="pillar-stat-lbl">Avg Eng</div><div class="pillar-stat-val">${fmt(avgEng)}</div></div>
              <div class="pillar-stat"><div class="pillar-stat-lbl">Total Reach</div><div class="pillar-stat-val">${fmt(s.reach)}</div></div>
            </div>
            <div class="pillar-bar-wrap" style="height:6px;"><div class="pillar-bar-fill" style="width:${barW}%;background:${p.color};"></div></div>
            <div style="font-size:10px;color:var(--t3);margin-top:5px;">${barW}% dari total engagement</div>
          </div>`;
        }).join('')}
      </div>`}
      <div class="chart-card">
        <div class="chart-title">Engagement per Pillar</div>
        <div style="position:relative;height:200px;"><canvas id="ch-pillar"></canvas></div>
      </div>
      ${pillarAdvisorHTML(filteredPosts)}
      <div class="section-hdr" style="margin-top:20px;"><div class="section-ttl">🎬 Analisis Format Konten</div></div>
      ${formatAnalyticsHTML(filteredPosts)}`;
    body.innerHTML=`<div class="view-header" style="margin-bottom:20px;"><div class="view-header-left"><div class="view-title">Analisis Mendalam</div><div class="view-subtitle">Grafik tren pertumbuhan, performa engagement rate, dan persebaran audiens organik</div></div><div class="view-header-right"><div style="display:flex;align-items:center;gap:6px;"><span style="font-size:11px;color:var(--t3);font-weight:600;">Rentang Waktu:</span><select class="af-sel" style="padding:6px 12px;border-radius:9px;font-size:12px;font-weight:700;" onchange="ovFilter=this.value==='all'?'all':'month';destroyCharts();renderAnalytics()"><option value="month" ${ovFilter==='month'?'selected':''}>30 Hari Terakhir</option><option value="all" ${ovFilter==='all'?'selected':''}>Semua Waktu</option></select></div></div></div>${tabsHTML}${filterHTML}${tabContent}`;
    setTimeout(()=>{
      charts.pillar=makeChart('ch-pillar','bar',
        pillars.map(p=>p.emoji+' '+p.label),
        [{label:'Total Engagement',data:pillars.map(p=>pillarMap[p.id]?.eng||0),
          backgroundColor:pillars.map(p=>p.color+'BB'),borderColor:pillars.map(p=>p.color),borderWidth:1,borderRadius:4}],
        {plugins:{legend:{display:false},tooltip:{callbacks:{label:c=>`Engagement: ${fmtFull(c.raw)}`}}}});
    },50);

  } else if(analyticsTab==='comparison'){
    // Comparison mode
    const pltBtns=allPlatforms().map(p=>`<button class="seg-btn ${kompPlt===p.id?(p.id==='tt'?'tt-act':p.id==='ig'?'ig-act':'act'):''}" onclick="kompPlt='${p.id}';destroyCharts();analyticsTab='comparison';renderAnalytics()">${p.emoji} ${p.name}</button>`).join('');
    const monthOpts=(v)=>months.map(m=>{const lbl=MN[m.month].slice(0,3)+' '+m.year;const val=m.year+'-'+m.month;return`<option value="${val}" ${v===val?'selected':''}>${lbl}</option>`;}).join('');
    const weekOpts=(v)=>{let opts='<option value="">— Pilih —</option>';months.forEach(m=>{m.weeks.forEach((w,wi)=>{const lbl=(w.label||'Minggu '+(wi+1))+' ('+MN[m.month].slice(0,3)+' '+m.year+')';opts+=`<option value="${w.id}" ${v===w.id?'selected':''}>${lbl}</option>`;});});return opts;};
    const accOpts=(v)=>accounts.map(a=>`<option value="${esc(a)}" ${v===a?'selected':''}>${a}</option>`).join('');

    function getKompStats(filterObj){
      const posts=[];let totalPV=0;let latestFlwMap={};
      months.forEach(m=>{
        if(filterObj.type==='bulan'){const[yS,mS]=filterObj.monthKey.split('-');if(m.year!==+yS||m.month!==+mS)return;}
        m.weeks.forEach(w=>{
          if(filterObj.type==='minggu'&&w.id!==filterObj.wid)return;
          const accs=filterObj.type==='akun'?[filterObj.acc]:accounts;
          accs.forEach(a=>{
            const d=aD(w,a);
            const wPosts=getPltPosts(d,kompPlt);const flw=getPltFlw(d,kompPlt);const pv=getPltPV(d,kompPlt);
            if(flw>0)latestFlwMap[a]=flw;totalPV+=pv;
            wPosts.forEach(p=>{
              if(filterObj.type==='akun'){
                if(filterObj.dateFrom&&(!p.date||p.date<filterObj.dateFrom))return;
                if(filterObj.dateTo&&(!p.date||p.date>filterObj.dateTo))return;
              }
              posts.push(p);
            });
          });
        });
      });
      const totalFlw=Object.values(latestFlwMap).reduce((s,v)=>s+v,0);
      const avgER=posts.length?(posts.reduce((s,p)=>s+calcER(p,sel.erMode,totalFlw/Math.max(Object.keys(latestFlwMap).length,1)),0)/posts.length):0;
      return{posts:posts.length,impression:sumP(posts,'impression'),reach:sumP(posts,'reach'),
        like:sumP(posts,'like'),comment:sumP(posts,'comment'),share:sumP(posts,'share'),
        save:sumP(posts,'save'),repost:sumP(posts,'repost'),followersGained:sumP(posts,'followersGained'),
        engagement:posts.reduce((s,p)=>s+totalEng(p),0),totalPV,totalFlw,avgER};
    }

    const KMETRICS=[
      {k:'posts',l:'Total Post'},{k:'impression',l:'Impressi'},{k:'reach',l:'Reach'},
      {k:'engagement',l:'Total Engagement'},{k:'like',l:'Like'},{k:'comment',l:'Comment'},
      {k:'share',l:'Share'},{k:'save',l:'Save'},{k:'followersGained',l:'Flw Gained'},
      {k:'totalPV',l:'Profile Visit'},{k:'totalFlw',l:'Followers'},{k:'avgER',l:'ER Rate',isPct:true},
    ];

    const buildTable=(sA,sB,lblA,lblB)=>{
      const rows=KMETRICS.map(m=>{
        const va=sA[m.k]||0,vb=sB[m.k]||0;
        const delta=vb-va;const pct=va?((delta/va)*100):null;
        const cls=delta>0?'delta-pos':delta<0?'delta-neg':'delta-neu';
        const dSign=delta>=0?'+':'';
        const dispA=m.isPct?va.toFixed(2)+'%':fmt(va);
        const dispB=m.isPct?vb.toFixed(2)+'%':fmt(vb);
        const dispPct=pct!==null?dSign+pct.toFixed(1)+'%':'—';
        const maxV=Math.max(va,vb,1);
        return `<tr>
          <td class="metric-lbl">${m.l}</td>
          <td>${dispA}</td><td>${dispB}</td>
          <td class="${cls}">${dispPct}</td>
          <td><div style="display:flex;flex-direction:column;gap:2px;width:80px;">
            <div style="height:5px;background:var(--b);border-radius:2px;overflow:hidden;"><div style="height:100%;width:${Math.round(va/maxV*100)}%;background:${pltColorRaw(kompPlt)};border-radius:2px;"></div></div>
            <div style="height:5px;background:var(--b);border-radius:2px;overflow:hidden;"><div style="height:100%;width:${Math.round(vb/maxV*100)}%;background:${pltColorRaw(kompPlt)}88;border-radius:2px;"></div></div>
          </div></td>
        </tr>`;
      }).join('');
      return `<div style="overflow-x:auto;"><table class="komp-table">
        <thead><tr><th>Metrik</th><th style="color:${pltColorRaw(kompPlt)}">${lblA}</th><th style="opacity:.7;color:${pltColorRaw(kompPlt)}">${lblB}</th><th>% Perubahan</th><th>Visual</th></tr></thead>
        <tbody>${rows}</tbody>
      </table></div>`;
    };

    let filterRow='',tableHTML='';
    if(!kompA.monthKey&&months.length)kompA.monthKey=months[0].year+'-'+months[0].month;
    if(!kompB.monthKey&&months.length)kompB.monthKey=months[Math.min(1,months.length-1)].year+'-'+months[Math.min(1,months.length-1)].month;

    if(kompMode==='bulan'){
      filterRow=`<div class="komp-filter-row">
        <span class="af-label">Bulan A:</span>
        <select class="af-sel" onchange="kompA.monthKey=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()">${monthOpts(kompA.monthKey)}</select>
        <span class="af-label">vs</span>
        <select class="af-sel" onchange="kompB.monthKey=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()">${monthOpts(kompB.monthKey)}</select>
      </div>`;
      if(kompA.monthKey&&kompB.monthKey){
        const[ay,am]=kompA.monthKey.split('-').map(Number);
        const[by,bm]=kompB.monthKey.split('-').map(Number);
        tableHTML=buildTable(getKompStats({type:'bulan',monthKey:kompA.monthKey}),getKompStats({type:'bulan',monthKey:kompB.monthKey}),
          MN[am].slice(0,3)+' '+ay,MN[bm].slice(0,3)+' '+by);
      }
    } else if(kompMode==='minggu'){
      filterRow=`<div class="komp-filter-row">
        <span class="af-label">Minggu A:</span>
        <select class="af-sel" style="max-width:200px;" onchange="kompA.wid=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()">${weekOpts(kompA.wid)}</select>
        <span class="af-label">vs</span>
        <select class="af-sel" style="max-width:200px;" onchange="kompB.wid=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()">${weekOpts(kompB.wid)}</select>
      </div>`;
      if(kompA.wid&&kompB.wid){
        const wa=months.flatMap(m=>m.weeks).find(w=>w.id===kompA.wid);
        const wb=months.flatMap(m=>m.weeks).find(w=>w.id===kompB.wid);
        tableHTML=buildTable(getKompStats({type:'minggu',wid:kompA.wid}),getKompStats({type:'minggu',wid:kompB.wid}),wa?.label||'A',wb?.label||'B');
      }
    } else {
      filterRow=`<div class="komp-filter-row">
        <span class="af-label">Akun A:</span>
        <select class="af-sel" onchange="kompA.acc=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()"><option value="">— Pilih —</option>${accOpts(kompA.acc)}</select>
        <span class="af-label">vs</span>
        <select class="af-sel" onchange="kompB.acc=this.value;analyticsTab='comparison';destroyCharts();renderAnalytics()"><option value="">— Pilih —</option>${accOpts(kompB.acc)}</select>
        <span class="af-label" style="margin-left:8px;">Dari:</span>
        <input type="date" class="date-inp" value="${kompDateFrom}" onchange="kompDateFrom=this.value;analyticsTab='comparison';renderAnalytics()">
        <span class="af-label">—</span>
        <input type="date" class="date-inp" value="${kompDateTo}" onchange="kompDateTo=this.value;analyticsTab='comparison';renderAnalytics()">
      </div>`;
      if(kompA.acc&&kompB.acc)tableHTML=buildTable(getKompStats({type:'akun',acc:kompA.acc,dateFrom:kompDateFrom,dateTo:kompDateTo}),getKompStats({type:'akun',acc:kompB.acc,dateFrom:kompDateFrom,dateTo:kompDateTo}),kompA.acc,kompB.acc);
    }

    tabContent=`
      <div style="display:flex;align-items:center;gap:8px;margin-bottom:12px;flex-wrap:wrap;">
        <span class="af-label">Platform:</span>
        <div class="seg">${pltBtns}</div>
        <div class="seg" style="margin-left:12px;">
          <button class="seg-btn ${kompMode==='bulan'?'act':''}" onclick="kompMode='bulan';analyticsTab='comparison';renderAnalytics()">📅 Bulan</button>
          <button class="seg-btn ${kompMode==='minggu'?'act':''}" onclick="kompMode='minggu';analyticsTab='comparison';renderAnalytics()">📆 Minggu</button>
          <button class="seg-btn ${kompMode==='akun'?'act':''}" onclick="kompMode='akun';analyticsTab='comparison';renderAnalytics()">👥 Akun</button>
        </div>
      </div>
      ${filterRow}
      ${tableHTML||`<div class="placeholder" style="height:auto;padding:40px;"><div class="ph-ico">⚖️</div><div class="ph-title">Pilih dua opsi untuk dibandingkan</div></div>`}`;
    body.innerHTML=`<div class="view-header" style="margin-bottom:20px;"><div class="view-header-left"><div class="view-title">Analisis Mendalam</div><div class="view-subtitle">Grafik tren pertumbuhan, performa engagement rate, dan persebaran audiens organik</div></div></div>${tabsHTML}${filterHTML}<div class="tab-content">${tabContent}</div>`;
  } else if(analyticsTab==='multiacc'){
    // ── Multi-Account Overview ──
    const now=new Date();
    const curY=ovFilter==='month'?ovYear:now.getFullYear();
    const curM=ovFilter==='month'?ovMonth:now.getMonth();

    // Build stats for every account across all platforms
    const accStats=accounts.map(acc=>{
      const platData={};
      let totalPosts=0,totalReach=0,totalEng=0,totalImp=0,totalFlw=0,totalFlwGained=0,totalSave=0,totalShare=0,erSum=0,erCount=0;
      allPlatforms().forEach(plt=>{
        let posts=[],latestFlw=0;
        months.forEach(mo=>{
          if(ovFilter==='month'&&(mo.year!==curY||mo.month!==curM))return;
          mo.weeks.forEach(wk=>{
            const d=aD(wk,acc);
            const pp=getPltPosts(d,plt.id);
            const flw=getPltFlw(d,plt.id);
            if(flw>0)latestFlw=flw;
            pp.forEach(p=>posts.push({...p,_plt:plt.id,_flw:flw}));
          });
        });
        if(posts.length>0){
          const pReach=sumP(posts,'reach');
          const pEng=posts.reduce((s,p)=>s+totalEng(p),0);
          const pER=posts.length?posts.reduce((s,p)=>s+calcER(p,sel.erMode,latestFlw),0)/posts.length:0;
          platData[plt.id]={posts:posts.length,reach:pReach,eng:pEng,er:pER,flw:latestFlw};
          totalPosts+=posts.length;
          totalReach+=pReach;
          totalEng+=pEng;
          totalImp+=sumP(posts,'impression');
          totalFlwGained+=sumP(posts,'followersGained');
          totalSave+=sumP(posts,'save');
          totalShare+=sumP(posts,'share');
          if(latestFlw>0)totalFlw+=latestFlw;
          if(pER>0){erSum+=pER*posts.length;erCount+=posts.length;}
        }
      });
      const avgER=erCount>0?erSum/erCount:0;
      const avgReach=totalPosts>0?totalReach/totalPosts:0;
      const avgEng=totalPosts>0?totalEng/totalPosts:0;
      return{acc,totalPosts,totalReach,totalEng,totalImp,totalFlw,totalFlwGained,totalSave,totalShare,avgER,avgReach,avgEng,platData};
    }).filter(s=>s.totalPosts>0||s.totalFlw>0);

    if(!accStats.length){
      tabContent=`<div class="placeholder" style="padding:60px 20px;text-align:center;">
        <div class="ph-ico">📈</div>
        <div class="ph-title">Belum ada data analytics</div>
        <div class="ph-sub">Input minimal 3 post untuk melihat chart, top content, dan insight performa.</div>
        <div style="margin-top:16px;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;">
          <button onclick="openQCModal()" style="padding:9px 20px;border-radius:9px;border:none;background:var(--accent);color:#000;font-size:13px;font-weight:800;cursor:pointer;">+ Input Post Baru</button>
          <button onclick="switchView('content')" style="padding:9px 16px;border-radius:9px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:12px;font-weight:700;cursor:pointer;">Lihat Input Data →</button>
        </div>
      </div>`;
    } else {
      // Sort by metric
      const multiSort=window._multiAccSort||'avgER';
      const sorted=[...accStats].sort((a,b)=>b[multiSort]-a[multiSort]);
      const maxVals={};
      ['totalPosts','totalReach','totalEng','avgER','avgReach','avgEng','totalFlwGained'].forEach(k=>{
        maxVals[k]=Math.max(...accStats.map(s=>s[k]||0),1);
      });

      const sortBtns=['avgER','totalReach','totalEng','totalPosts','avgEng'].map(k=>{
        const labels={avgER:'ER Rate',totalReach:'Reach',totalEng:'Engagement',totalPosts:'Post',avgEng:'Eng/Post'};
        const act=multiSort===k;
        return`<button class="seg-btn ${act?'act':''}" onclick="window._multiAccSort='${k}';analyticsTab='multiacc';renderAnalytics()">${labels[k]}</button>`;
      }).join('');

      // Ranking table
      const tableRows=sorted.map((s,i)=>{
        const initials=s.acc.replace(/[^A-Za-z0-9]/g,'').slice(0,2).toUpperCase()||'??';
        const barW=maxVals[multiSort]>0?Math.round((s[multiSort]/maxVals[multiSort])*100):0;
        const platBadges=allPlatforms().filter(p=>s.platData[p.id]).map(p=>`<span title="${p.name}: ${s.platData[p.id].posts} post">${p.emoji}</span>`).join(' ');
        return`<tr style="border-bottom:1px solid var(--b);cursor:pointer;" onclick="ovAcc='${s.acc}';analyticsTab='trends';destroyCharts();renderAnalytics()">
          <td style="padding:10px 12px;font-size:13px;font-weight:800;color:var(--t3);font-family:var(--mono);">#${i+1}</td>
          <td style="padding:10px 12px;">
            <div style="display:flex;align-items:center;gap:10px;">
              <div style="width:32px;height:32px;border-radius:10px;background:var(--accent-dim);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:800;color:var(--accent);flex-shrink:0;">${initials}</div>
              <div>
                <div style="font-size:13px;font-weight:700;color:var(--t);">${s.acc}</div>
                <div style="font-size:10px;color:var(--t3);">${platBadges}</div>
              </div>
            </div>
          </td>
          <td style="padding:10px 12px;text-align:right;font-family:var(--mono);font-size:13px;font-weight:700;color:var(--t);">${erStr(s.avgER)}</td>
          <td style="padding:10px 12px;text-align:right;font-family:var(--mono);font-size:12px;color:var(--t2);">${fmt(s.totalReach)}</td>
          <td style="padding:10px 12px;text-align:right;font-family:var(--mono);font-size:12px;color:var(--t2);">${fmt(s.totalEng)}</td>
          <td style="padding:10px 12px;text-align:right;font-family:var(--mono);font-size:12px;color:var(--t2);">${fmt(s.avgEng)}</td>
          <td style="padding:10px 12px;text-align:right;font-family:var(--mono);font-size:12px;color:var(--t2);">${s.totalPosts}</td>
          <td style="padding:10px 16px;min-width:100px;">
            <div style="display:flex;align-items:center;gap:6px;">
              <div style="flex:1;height:5px;background:var(--bg-hover);border-radius:4px;overflow:hidden;">
                <div style="height:100%;width:${barW}%;background:var(--accent);border-radius:4px;"></div>
              </div>
            </div>
          </td>
        </tr>`;
      }).join('');

      // Top/bottom callout cards
      const top1=sorted[0];
      const bot1=sorted[sorted.length-1];
      const callouts=sorted.length>=2?`
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:20px;">
          <div style="background:linear-gradient(135deg,rgba(14,159,143,.1),rgba(14,159,143,.04));border:1px solid rgba(14,159,143,.2);border-radius:12px;padding:14px 16px;">
            <div style="font-size:10px;font-weight:800;color:var(--accent);text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px;">🏆 Akun Terkuat</div>
            <div style="font-size:15px;font-weight:800;color:var(--t);margin-bottom:4px;">${top1.acc}</div>
            <div style="font-size:11px;color:var(--t3);">ER ${erStr(top1.avgER)} · Reach ${fmt(top1.totalReach)} · ${top1.totalPosts} post</div>
          </div>
          <div style="background:rgba(245,158,11,.06);border:1px solid rgba(245,158,11,.2);border-radius:12px;padding:14px 16px;">
            <div style="font-size:10px;font-weight:800;color:var(--amber);text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px;">⚡ Perlu Perhatian</div>
            <div style="font-size:15px;font-weight:800;color:var(--t);margin-bottom:4px;">${bot1.acc}</div>
            <div style="font-size:11px;color:var(--t3);">ER ${erStr(bot1.avgER)} · Reach ${fmt(bot1.totalReach)} · ${bot1.totalPosts} post</div>
          </div>
        </div>`:'';

      tabContent=`
        <div style="display:flex;align-items:center;gap:8px;margin-bottom:16px;flex-wrap:wrap;">
          <span class="af-label">Urutkan by:</span>
          <div class="seg">${sortBtns}</div>
          <span style="font-size:11px;color:var(--t4);margin-left:8px;">Klik baris untuk lihat tren akun</span>
        </div>
        ${callouts}
        <div style="overflow-x:auto;border-radius:12px;border:1px solid var(--b);">
          <table style="width:100%;border-collapse:collapse;font-size:13px;">
            <thead><tr style="background:var(--surf2);">
              <th style="padding:9px 12px;text-align:left;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">#</th>
              <th style="padding:9px 12px;text-align:left;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Akun</th>
              <th style="padding:9px 12px;text-align:right;font-size:10px;font-weight:800;color:var(--accent);text-transform:uppercase;letter-spacing:.06em;">Avg ER</th>
              <th style="padding:9px 12px;text-align:right;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Total Reach</th>
              <th style="padding:9px 12px;text-align:right;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Total Eng</th>
              <th style="padding:9px 12px;text-align:right;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Avg Eng/Post</th>
              <th style="padding:9px 12px;text-align:right;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Post</th>
              <th style="padding:9px 16px;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Bar</th>
            </tr></thead>
            <tbody>${tableRows}</tbody>
          </table>
        </div>
        <div style="margin-top:10px;font-size:11px;color:var(--t4);">
          * Filter periode di atas berlaku. ${ovFilter==='month'?`Menampilkan data ${MN[curM]} ${curY}.`:'Menampilkan semua data.'}
        </div>`;
    }
  }
  body.innerHTML=`<div class="section-hdr"><div class="section-ttl">Analytics & Insights</div></div>${tabsHTML}${filterHTML}<div class="tab-content">${tabContent}</div>`;
}

/* ══ RENDER: GOALS ══ */
function renderGoals(){
  const body=document.getElementById('goals-body');if(!body)return;
  const now=new Date();
  const year=now.getFullYear(),month=now.getMonth();
  const monthName=MN[month]+' '+year;
  const goals=customGoals;
  const dayOfMonth=now.getDate();
  const daysInMonth=new Date(year,month+1,0).getDate();
  const daysLeft=daysInMonth-dayOfMonth;

  const emptyState=goals.length===0?emptyStateHTML('goals'):'';

  // ── Pillar Performance Card ──
  const allPosts=getAllPosts();
  const pillarMap={};
  allPosts.forEach(p=>{
    const pid=p.pillar||'other';
    if(!pillarMap[pid])pillarMap[pid]={posts:[],eng:0,reach:0};
    pillarMap[pid].posts.push(p);
    pillarMap[pid].eng+=totalEng(p);
    pillarMap[pid].reach+=(p.reach||0);
  });
  const pillarEntries=Object.entries(pillarMap)
    .map(([id,d])=>{
      const pil=pillarOf(id);
      const avgER=d.posts.length?d.posts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/d.posts.length:0;
      return{id,label:pil.label,emoji:pil.emoji,color:pil.color,count:d.posts.length,avgER,totalReach:d.reach};
    }).sort((a,b)=>b.avgER-a.avgER);

  const pillarCardHTML=pillarEntries.length>=2?`
    <div style="background:var(--surf);border:1px solid var(--b);border-radius:var(--radius-lg);padding:18px 20px;margin-bottom:20px;">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;">
        <div>
          <div style="font-size:13px;font-weight:800;color:var(--t);">🎯 Performa per Pilar Konten</div>
          <div style="font-size:11px;color:var(--t3);margin-top:2px;">Berdasarkan semua data · ER tertinggi ke terendah</div>
        </div>
        <span style="font-size:10px;font-weight:800;padding:3px 9px;border-radius:20px;background:rgba(168,85,247,.12);color:#A855F7;">DIFFERENTIATOR</span>
      </div>
      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:8px;">
        ${pillarEntries.slice(0,6).map((p,i)=>{
          const maxER=pillarEntries[0].avgER||1;
          const barW=Math.round((p.avgER/maxER)*100);
          const isTop=i===0;
          return '<div style="background:'+( isTop?'linear-gradient(135deg,rgba(14,159,143,.12),rgba(14,159,143,.04))':'var(--surf2)')+';border:1px solid '+(isTop?'rgba(14,159,143,.25)':'var(--b)')+';border-radius:10px;padding:12px 14px;position:relative;">'
            +(isTop?'<div style="position:absolute;top:8px;right:8px;font-size:11px;font-weight:800;padding:1px 6px;border-radius:20px;background:rgba(14,159,143,.15);color:var(--accent);">TOP</div>':'')
            +'<div style="font-size:18px;margin-bottom:4px;">'+p.emoji+'</div>'
            +'<div style="font-size:12px;font-weight:800;color:var(--t);margin-bottom:6px;">'+p.label+'</div>'
            +'<div style="font-size:10px;color:var(--t3);margin-bottom:4px;">'+p.count+' post · ER '+erStr(p.avgER)+'</div>'
            +'<div style="height:4px;background:var(--bg-hover);border-radius:4px;overflow:hidden;">'
            +'<div style="height:100%;width:'+barW+'%;background:'+(p.color||'var(--accent)')+';border-radius:4px;"></div>'
            +'</div></div>';
        }).join('')}
      </div>
      ${pillarEntries.length>=2?'<div style="margin-top:12px;padding:10px 14px;background:var(--bg-hover);border-radius:9px;font-size:12px;color:var(--t2);line-height:1.6;">💡 <strong style=\"color:var(--t);\">&quot;'+pillarEntries[0].label+'&quot;</strong> adalah pilar terkuat kamu dengan ER '+erStr(pillarEntries[0].avgER)+'.'+(pillarEntries[pillarEntries.length-1].avgER>0&&pillarEntries[0].avgER>pillarEntries[pillarEntries.length-1].avgER*1.3?' Pertimbangkan kurangi porsi <strong>&quot;'+pillarEntries[pillarEntries.length-1].label+'&quot;</strong> (ER '+erStr(pillarEntries[pillarEntries.length-1].avgER)+').':'')+'</div>':''}
    </div>`:'';

  const goalRows=goals.map(g=>{
    const curr=getGoalCurrentValue(g);
    const target=g.target||0;
    const pct=target?Math.min(Math.round((curr/target)*100),100):0;
    const pltObj=g.platform==='all'?{name:'Semua Platform',emoji:'🌐'}:allPlatforms().find(p=>p.id===g.platform)||{name:g.platform,emoji:'📌'};
    const metricLabel=METRIC_LABELS[g.metric]||g.metric;
    // Status
    let statusBadge,statusColor;
    if(pct>=100){statusBadge='✓ Tercapai!';statusColor='var(--green)';}
    else if(pct>=70){statusBadge='↗ On Track';statusColor='var(--blue)';}
    else if(pct>=35){statusBadge='⚡ Perlu Akselerasi';statusColor='var(--amber)';}
    else{statusBadge='⚠️ Tertinggal';statusColor='var(--red)';}
    const barGrad=pct>=80?'linear-gradient(90deg,#1db954,#0ea84a)':pct>=50?'linear-gradient(90deg,#f59e0b,#d97706)':'linear-gradient(90deg,#3b82f6,#2563eb)';
    // Weekly mini bars
    const weeklyData=[];
    const mPlts=g.platform==='all'?allPlatforms().map(p=>p.id):[g.platform];
    months.filter(m=>m.year===year&&m.month===month).forEach(m=>{
      m.weeks.forEach(wk=>{
        let val=0;
        accounts.forEach(a=>{
          const d=aD(wk,a);
          mPlts.forEach(pid=>{
            getPltPosts(d,pid).forEach(p=>{
              if(g.metric==='followers')val+=(p.followersGained||0);
              else if(g.metric==='likes')val+=(p.like||0);
              else if(g.metric==='comments')val+=(p.comment||0);
              else if(g.metric==='shares')val+=(p.share||0);
              else if(g.metric==='reach')val+=(p.reach||0);
              else if(g.metric==='impression')val+=(p.impression||0);
              else if(g.metric==='posts')val+=1;
              else if(g.metric==='saves')val+=(p.save||0);
              else if(g.metric==='engagement')val+=totalEng(p);
            });
          });
        });
        weeklyData.push({label:wk.label||'W',val});
      });
    });
    const maxWVal=Math.max(...weeklyData.map(w=>w.val),1);
    const weekBarsHTML=weeklyData.length>0?`
      <div style="margin:10px 0 4px;">
        <div style="font-size:11px;font-weight:700;color:var(--t4);text-transform:uppercase;letter-spacing:.08em;margin-bottom:5px;">Progres per Minggu</div>
        <div style="display:flex;align-items:flex-end;gap:3px;height:28px;">
          ${weeklyData.map((w,i)=>{const h=Math.max(3,Math.round((w.val/maxWVal)*28));const isLast=i===weeklyData.length-1;return '<div style="flex:1;display:flex;flex-direction:column;align-items:center;" title="'+w.label+': '+fmt(w.val)+'"><div style="width:100%;height:'+h+'px;border-radius:3px 3px 0 0;background:'+(isLast?'var(--accent)':'rgba(14,159,143,0.3)')+'"></div></div>';}).join('')}
        </div>
        <div style="display:flex;gap:3px;margin-top:2px;">${weeklyData.map((w)=>'<div style="flex:1;font-size:8px;color:var(--t4);text-align:center;">'+w.label.replace('Minggu ','W')+'</div>').join('')}</div>
      </div>`:'';
    // Gap analysis
    const needed=Math.max(0,target-curr);
    let gapHTML='';
    if(target>0&&pct<100&&daysLeft>0){
      const dailyNeeded=Math.ceil(needed/daysLeft);
      const weeklyNeeded=Math.ceil(needed/Math.max(1,Math.ceil(daysLeft/7)));
      gapHTML=`<div style="display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin-top:10px;">
        <div style="background:var(--bg-hover);border-radius:8px;padding:8px 10px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px;">Sisa</div><div style="font-family:var(--mono);font-size:14px;font-weight:800;color:var(--t);">${fmt(needed)}</div></div>
        <div style="background:var(--bg-hover);border-radius:8px;padding:8px 10px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px;">Per hari</div><div style="font-family:var(--mono);font-size:14px;font-weight:800;color:${pct<30?'var(--red)':pct<60?'var(--amber)':'var(--green)'};">${fmt(dailyNeeded)}</div></div>
        <div style="background:var(--bg-hover);border-radius:8px;padding:8px 10px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px;">Per minggu</div><div style="font-family:var(--mono);font-size:14px;font-weight:800;color:var(--t);">${fmt(weeklyNeeded)}</div></div>
      </div>`;
    }else if(pct>=100){gapHTML=`<div style="background:rgba(29,185,84,.08);border:1px solid rgba(29,185,84,.2);border-radius:8px;padding:8px 12px;margin-top:8px;font-size:12px;color:var(--green);">🎉 Goal tercapai! ${fmt(curr)} dari target ${fmt(target)}.</div>`;}

    return `<div class="goal-card" style="margin-bottom:12px;">
      <div class="goal-card-top">
        <div class="goal-card-ico">${g.emoji||'🎯'}</div>
        <div style="flex:1;min-width:0;">
          <div class="goal-card-name">${g.label}</div>
          <div style="font-size:11px;color:var(--t3);margin-top:2px;">${pltObj.emoji} ${pltObj.name} · ${metricLabel}</div>
        </div>
        <span style="font-size:10px;font-weight:800;padding:3px 9px;border-radius:20px;background:${pct>=100?'rgba(29,185,84,.12)':pct>=70?'rgba(59,130,246,.1)':pct>=35?'rgba(245,158,11,.1)':'rgba(255,77,77,.1)'};color:${statusColor};">${statusBadge}</span>
        <button class="goal-card-edit" onclick="openGoalModal('${g.id}')" style="margin-left:6px;">✏ Edit</button>
        <button onclick="deleteGoal('${g.id}')" style="padding:4px 8px;border-radius:7px;border:none;background:var(--red-bg);color:var(--red);font-size:11px;font-weight:700;cursor:pointer;margin-left:4px;">✕</button>
      </div>
      <div style="margin:12px 0 6px;">
        <div style="display:flex;justify-content:space-between;align-items:baseline;margin-bottom:6px;">
          <div style="display:flex;align-items:baseline;gap:6px;"><span style="font-family:var(--mono);font-size:20px;font-weight:900;color:var(--t);">${fmt(curr)}</span><span style="font-size:11px;color:var(--t3);">/ ${fmt(target)}</span></div>
          <span style="font-family:var(--mono);font-size:18px;font-weight:900;color:${statusColor};">${pct}%</span>
        </div>
        <div style="height:8px;background:var(--bg-hover);border-radius:8px;overflow:hidden;">
          <div style="height:100%;width:${pct}%;background:${barGrad};border-radius:8px;box-shadow:0 0 8px rgba(14,159,143,.2);"></div>
        </div>
      </div>
      ${weekBarsHTML}
      ${gapHTML}
    </div>`;
  }).join('');

  const completedGoals=goals.filter(g=>{const c=getGoalCurrentValue(g);const t=g.target||0;return t>0&&c>=t;});
  const summaryHTML=goals.length>0?`
    <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:20px;">
      <div style="background:var(--surf);border:1px solid var(--b);border-radius:12px;padding:12px 14px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:4px;">Goal Aktif</div><div style="font-family:var(--mono);font-size:22px;font-weight:900;color:var(--t);">${goals.length}</div></div>
      <div style="background:linear-gradient(135deg,rgba(29,185,84,.1),rgba(29,185,84,.04));border:1px solid rgba(29,185,84,.2);border-radius:12px;padding:12px 14px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--green);text-transform:uppercase;letter-spacing:.08em;margin-bottom:4px;">Tercapai</div><div style="font-family:var(--mono);font-size:22px;font-weight:900;color:var(--green);">${completedGoals.length}</div></div>
      <div style="background:var(--surf);border:1px solid var(--b);border-radius:12px;padding:12px 14px;text-align:center;"><div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:4px;">Sisa Hari</div><div style="font-family:var(--mono);font-size:22px;font-weight:900;color:${daysLeft<=5?'var(--red)':daysLeft<=10?'var(--amber)':'var(--t)'};">${daysLeft}</div></div>
    </div>`:'';

  body.innerHTML=`
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-title">Target & Pilar</div>
        <div class="view-subtitle">${monthName} · Pantau progres goal dan performa pilar kontenmu</div>
      </div>
      <div class="view-header-right">
        <button class="view-header-btn" onclick="openGoalModal()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
          Tambah Goal
        </button>
      </div>
    </div>
    ${pillarCardHTML}
    ${summaryHTML}
    ${emptyState}
    <div>${goalRows}</div>
    ${goals.length > 0 ? buildGoalHistoryHTML(goals) : ''}
  `;
}

/* ── Goals Historical Trend: 3 bulan terakhir ── */
function buildGoalHistoryHTML(goals) {
  const now = new Date();
  // Build last 3 months (not including current)
  const pastMonths = [];
  for (let i = 1; i <= 3; i++) {
    let m = now.getMonth() - i;
    let y = now.getFullYear();
    if (m < 0) { m += 12; y -= 1; }
    // Only include months that have data
    if (months.some(mo => mo.year === y && mo.month === m)) {
      pastMonths.push({ year: y, month: m });
    }
  }
  if (!pastMonths.length) return '';

  const rows = pastMonths.map(pm => {
    const monthLabel = MN[pm.month] + ' ' + pm.year;
    const achieved = goals.filter(g => {
      const val = getGoalValueForMonth(g, pm.year, pm.month);
      return (g.target || 0) > 0 && val >= g.target;
    });
    const total = goals.length;
    const pct = total ? Math.round((achieved.length / total) * 100) : 0;
    const color = pct === 100 ? 'var(--green)' : pct >= 50 ? 'var(--amber)' : 'var(--red)';
    const emoji = pct === 100 ? '🏆' : pct >= 50 ? '📈' : '📉';

    // Per-goal achievement detail for this month
    const goalDetails = goals.map(g => {
      const val = getGoalValueForMonth(g, pm.year, pm.month);
      const t = g.target || 0;
      const p = t ? Math.min(Math.round((val / t) * 100), 100) : 0;
      const ok = t > 0 && val >= t;
      return `<div style="display:flex;align-items:center;gap:8px;padding:6px 0;border-bottom:1px solid rgba(255,255,255,.05);">
        <span style="font-size:14px;">${g.emoji || '🎯'}</span>
        <span style="flex:1;font-size:12px;color:var(--t2);overflow:hidden;white-space:nowrap;text-overflow:ellipsis;">${g.label}</span>
        <span style="font-size:11px;font-family:var(--mono);color:${ok?'var(--green)':'var(--t3)'};">${fmt(val)} / ${fmt(t)}</span>
        <span style="font-size:10px;font-weight:800;padding:2px 7px;border-radius:10px;min-width:42px;text-align:center;background:${ok?'rgba(39,209,127,.12)':'rgba(255,255,255,.06)'};color:${ok?'var(--green)':'var(--t4)'};">${p}%</span>
      </div>`;
    }).join('');

    return `<div style="background:var(--surf);border:1px solid var(--b);border-radius:12px;padding:14px 16px;transition:background .15s;" onmouseover="this.style.background='var(--bg-hover)'" onmouseout="this.style.background='var(--surf)'">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;">
        <div style="display:flex;align-items:center;gap:8px;">
          <span style="font-size:20px;">${emoji}</span>
          <div>
            <div style="font-size:13px;font-weight:800;color:var(--t);">${monthLabel}</div>
            <div style="font-size:11px;color:var(--t3);">${achieved.length} dari ${total} goal tercapai</div>
          </div>
        </div>
        <span style="font-family:var(--mono);font-size:18px;font-weight:900;color:${color};">${pct}%</span>
      </div>
      <div style="height:5px;background:var(--bg-hover);border-radius:5px;overflow:hidden;margin-bottom:10px;">
        <div style="height:100%;width:${pct}%;background:${color};border-radius:5px;transition:width .4s;"></div>
      </div>
      ${goalDetails}
    </div>`;
  }).join('');

  return `<div style="margin-top:28px;">
    <div style="font-size:11px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.1em;margin-bottom:12px;">📊 Riwayat Pencapaian 3 Bulan Terakhir</div>
    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(260px,1fr));gap:10px;">${rows}</div>
  </div>`;
}
/* ══ SPRINT 2: REPORTS VIEW ══ */

// ── Get aggregated stats for a specific period and platform ──
// ══ GLOBAL COMPARISON ENGINE (dipakai di Report & Analytics) ══
function getCompStats(filterObj){
  const {type,monthKey,wid,acc,dateFrom,dateTo,pltId}=filterObj;
  const posts=[];
  let latestFlwMap={};
  const targetPlts=pltId&&pltId!=='all'?[pltId]:allPlatforms().map(p=>p.id);
  months.forEach(m=>{
    const mk=m.year+'-'+String(m.month+1).padStart(2,'0');
    m.weeks.forEach(w=>{
      const inMonth=type==='bulan'&&mk===monthKey;
      const inWeek=type==='minggu'&&w.id===wid;
      const inAcc=type==='akun';
      if(!inMonth&&!inWeek&&!inAcc)return;
      (acc?[acc]:accounts).forEach(a=>{
        const d=aD(w,a);
        targetPlts.forEach(pid=>{
          const wPosts=getPltPosts(d,pid);
          const flw=getPltFlw(d,pid);
          if(flw>0)latestFlwMap[a+'_'+pid]=flw;
          wPosts.forEach(p=>{
            if(inAcc&&dateFrom&&dateTo&&p.date){
              if(p.date<dateFrom||p.date>dateTo)return;
            }
            posts.push({...p,_plt:pid,_account:a,_flw:flw});
          });
        });
      });
    });
  });
  const engagement=posts.reduce((s,p)=>s+totalEng(p),0);
  let totalER=0,erCount=0;
  posts.forEach(p=>{
    const er=calcER(p,sel.erMode,p._flw||0);
    if(er>0){totalER+=er;erCount++;}
  });
  return{
    posts:posts.length,
    impression:sumP(posts,'impression'),
    reach:sumP(posts,'reach'),
    engagement,
    like:sumP(posts,'like'),
    comment:sumP(posts,'comment'),
    share:sumP(posts,'share'),
    save:sumP(posts,'save'),
    repost:sumP(posts,'repost'),
    followersGained:sumP(posts,'followersGained'),
    avgER:erCount?totalER/erCount:0,
    _posts:posts
  };
}

function buildCompTable(sA,sB,labelA,labelB){
  const rows=[
    {label:'Posts',a:sA.posts,b:sB.posts,fmt:fmtFull,icon:'📝'},
    {label:'Impressi',a:sA.impression,b:sB.impression,fmt:fmt,icon:'📺'},
    {label:'Reach',a:sA.reach,b:sB.reach,fmt:fmt,icon:'👁'},
    {label:'Engagement',a:sA.engagement,b:sB.engagement,fmt:fmt,icon:'❤️'},
    {label:'Likes',a:sA.like,b:sB.like,fmt:fmt,icon:'👍'},
    {label:'Comments',a:sA.comment,b:sB.comment,fmt:fmt,icon:'💬'},
    {label:'Shares',a:sA.share,b:sB.share,fmt:fmt,icon:'🔁'},
    {label:'Saves',a:sA.save,b:sB.save,fmt:fmt,icon:'🔖'},
    {label:'Flw Gained',a:sA.followersGained,b:sB.followersGained,fmt:fmt,icon:'📈'},
    {label:'Avg ER',a:sA.avgER,b:sB.avgER,fmt:erStr,icon:'📊'},
  ];
  const rowsHTML=rows.map(r=>{
    const d=r.b>0?((r.a-r.b)/r.b)*100:null;
    const dStr=d===null?'—':((d>=0?'+':'')+d.toFixed(1)+'%');
    const dCls=d===null?'':'comp-delta-'+(d>0?'pos':d<0?'neg':'neu');
    const winner=r.a>r.b?'A':r.a<r.b?'B':'';
    return`<tr class="comp-row">
      <td class="comp-metric">${r.icon} ${r.label}</td>
      <td class="comp-val ${winner==='A'?'comp-val-win':''}" style="font-family:var(--mono);">${r.fmt(r.a)}</td>
      <td class="comp-val ${winner==='B'?'comp-val-win':''}" style="font-family:var(--mono);">${r.fmt(r.b)}</td>
      <td class="comp-delta ${dCls}">${dStr}</td>
    </tr>`;
  }).join('');
  return`<div class="comp-wrap">
    <table class="comp-table">
      <thead><tr>
        <th class="comp-th">Metrik</th>
        <th class="comp-th">${labelA}</th>
        <th class="comp-th">${labelB}</th>
        <th class="comp-th">Delta A vs B</th>
      </tr></thead>
      <tbody>${rowsHTML}</tbody>
    </table>
  </div>`;
}

function getReportStats(pltId, year, month, periodType) {
  const posts = [];
  let totalPV = 0;
  let latestFlwMap = {};
  const plts = pltId === 'all' ? allPlatforms().map(p => p.id) : [pltId];
  (months || []).forEach(m => {
    if (!m) return;
    if (periodType === 'month' && (m.year !== year || m.month !== month)) return;
    (m.weeks || []).forEach(w => {
      if (!w) return;
      (accounts || []).forEach(a => {
        const d = aD(w, a);
        if (!d) return;
        plts.forEach(pid => {
          const wPosts = getPltPosts(d, pid) || [];
          const flw = getPltFlw(d, pid) || 0;
          const pv = getPltPV(d, pid) || 0;
          if (flw > 0) latestFlwMap[a + '_' + pid] = flw;
          totalPV += pv;
          wPosts.forEach(p => {
            if (p) posts.push({ ...p, _plt: pid, _account: a, _flw: flw });
          });
        });
      });
    });
  });
  const totalFlw = Object.values(latestFlwMap).reduce((s, v) => s + v, 0);
  const engagement = posts.reduce((s, p) => s + totalEng(p), 0);
  let totalER = 0, erCount = 0;
  posts.forEach(p => {
    const er = calcER(p, sel.erMode, p._flw || 0);
    if (er > 0) { totalER += er; erCount++; }
  });
  return {
    posts: posts.length,
    impression: sumP(posts, 'impression'),
    reach: sumP(posts, 'reach'),
    engagement,
    like: sumP(posts, 'like'),
    comment: sumP(posts, 'comment'),
    share: sumP(posts, 'share'),
    save: sumP(posts, 'save'),
    repost: sumP(posts, 'repost'),
    followersGained: sumP(posts, 'followersGained'),
    totalPV, totalFlw,
    avgER: erCount ? totalER / erCount : 0,
    _posts: posts
  };
}

// ── Month-over-month delta ──
function getMoMDelta(currVal, prevVal) {
  if (!prevVal || prevVal === 0) return null;
  return ((currVal - prevVal) / prevVal) * 100;
}

function deltaHTML(curr, prev) {
  const d = getMoMDelta(curr, prev);
  if (d === null) return '';
  const sign = d > 0 ? '+' : '';
  const cls = d > 0 ? 'rpt-delta-pos' : d < 0 ? 'rpt-delta-neg' : 'rpt-delta-neu';
  const arrow = d > 0 ? '↑' : d < 0 ? '↓' : '→';
  return `<span class="rpt-kpi-delta ${cls}">${arrow} ${sign}${d.toFixed(1)}% vs bln lalu</span>`;
}

// ── Previous month helper ──
function prevMonthYear(year, month) {
  if (month === 0) return { year: year - 1, month: 11 };
  return { year, month: month - 1 };
}

// ── Lazy script loader ──
function loadScript(src) {
  return new Promise((resolve, reject) => {
    if (document.querySelector(`script[src="${src}"]`)) { resolve(); return; }
    const s = document.createElement('script');
    s.src = src; s.onload = resolve; s.onerror = reject;
    document.head.appendChild(s);
  });
}

async function loadExportLibs() {
  await loadScript('https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js');
  await loadScript('https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js');
}

// ── Export PDF ──
async function exportPDF() {
  const btn = document.getElementById('rpt-pdf-btn');
  const spinner = document.getElementById('rpt-pdf-spin');
  if (btn) { btn.disabled = true; if (spinner) spinner.style.display = 'block'; }
  try {
    await loadExportLibs();
    const el = document.getElementById('report-body');
    const canvas = await html2canvas(el, {
      scale: 2, useCORS: true, backgroundColor: null,
      logging: false
    });
    const { jsPDF } = window.jspdf;
    const pdf = new jsPDF('p', 'mm', 'a4');
    const imgW = 210;
    const imgH = (canvas.height * imgW) / canvas.width;
    const imgData = canvas.toDataURL('image/png');
    let y = 0;
    const pageH = 297;
    while (y < imgH) {
      if (y > 0) pdf.addPage();
      pdf.addImage(imgData, 'PNG', 0, -y, imgW, imgH);
      y += pageH;
    }
    const pLabel = reportPeriod === 'month' ? `${MN[reportMonth]}_${reportYear}` : 'semua';
    pdf.save(`creatorlytics_laporan_${pLabel}.pdf`);
  } catch (e) {
    _qcToast('❌ Gagal export PDF. Coba lagi.');
    console.error(e);
  } finally {
    if (btn) { btn.disabled = false; if (spinner) spinner.style.display = 'none'; }
  }
}

// ── Export Image (PNG) ──
async function exportImage() {
  const btn = document.getElementById('rpt-img-btn');
  const spinner = document.getElementById('rpt-img-spin');
  if (btn) { btn.disabled = true; if (spinner) spinner.style.display = 'block'; }
  try {
    await loadExportLibs();
    const el = document.getElementById('report-body');
    const canvas = await html2canvas(el, {
      scale: 2, useCORS: true, backgroundColor: null, logging: false
    });
    const link = document.createElement('a');
    const pLabel = reportPeriod === 'month' ? `${MN[reportMonth]}_${reportYear}` : 'semua';
    link.download = `creatorlytics_laporan_${pLabel}.png`;
    link.href = canvas.toDataURL('image/png');
    link.click();
  } catch (e) {
    _qcToast('❌ Gagal export gambar. Coba lagi.');
    console.error(e);
  } finally {
    if (btn) { btn.disabled = false; if (spinner) spinner.style.display = 'none'; }
  }
}

/* ══ RENDER: REPORT ══ */
function renderReport() {
  loadRUD(); // load user data for current period
  const wrap = document.getElementById('report-view-inner');
  if (!wrap) return;

  // ── Build filter options ──
  const yrs = [...new Set(months.map(m => m.year))].sort();
  const yearOpts = yrs.map(y => `<option value="${y}" ${y === reportYear ? 'selected' : ''}>${y}</option>`).join('');
  const monthOpts = MN.map((mn, i) => `<option value="${i}" ${i === reportMonth ? 'selected' : ''}>${mn}</option>`).join('');
  const pltOpts = [
    `<option value="all" ${reportPlt === 'all' ? 'selected' : ''}>Semua Platform</option>`,
    ...allPlatforms().map(p => `<option value="${p.id}" ${reportPlt === p.id ? 'selected' : ''}>${p.emoji} ${p.name}</option>`)
  ].join('');

  // ── Period display label ──
  const periodLabel = reportPeriod === 'month' ? `${MN[reportMonth]} ${reportYear}` : 'Semua Waktu';
  const pltLabel = reportPlt === 'all' ? 'Semua Platform' : pltInfo(reportPlt).emoji + ' ' + pltInfo(reportPlt).name;

  // ── Compute stats ──
  const curr = getReportStats(reportPlt, reportYear, reportMonth, reportPeriod);
  const prev = (() => {
    if (reportPeriod !== 'month') return null;
    const { year: py, month: pm } = prevMonthYear(reportYear, reportMonth);
    return getReportStats(reportPlt, py, pm, 'month');
  })();

  const allReportPosts = curr._posts;
  const hasData = allReportPosts.length > 0;

  // ── KPI Definitions (4 core only — Sprint simplification) ──
  const kpiDefs = [
    { key: 'reach',           label: 'Total Reach',       value: curr.reach,           prevVal: prev?.reach,           accent: '#4D79FF',        fmt: fmt },
    { key: 'engagement',      label: 'Total Engagement',  value: curr.engagement,      prevVal: prev?.engagement,      accent: 'var(--accent)',        fmt: fmt },
    { key: 'avgER',           label: 'Avg ER Rate',       value: curr.avgER,           prevVal: prev?.avgER,           accent: 'var(--purple)',  fmt: v => erStr(v), mono: true },
    { key: 'followersGained', label: 'Followers Gained',  value: curr.followersGained, prevVal: prev?.followersGained, accent: 'var(--amber)',   fmt: fmt },
  ];

  // ── Content Performance data ──
  const totalSave    = sumP(allReportPosts,'save');
  const totalShare   = sumP(allReportPosts,'share');
  const totalReachCP = sumP(allReportPosts,'reach');
  const avgSaveRate  = totalReachCP>0?((totalSave/totalReachCP)*100):0;
  const avgShareRate = totalReachCP>0?((totalShare/totalReachCP)*100):0;
  // Best pillar by avg ER
  const pillarERMap={};
  allReportPosts.forEach(p=>{
    const pid=p.pillar||'other';
    if(!pillarERMap[pid]){pillarERMap[pid]={sum:0,count:0};}
    const er=calcER(p,sel.erMode,p._flw||0);
    if(er>0){pillarERMap[pid].sum+=er;pillarERMap[pid].count++;}
  });
  const bestPillar=pillars.map(p=>({
    ...p,avgER:pillarERMap[p.id]?.count?(pillarERMap[p.id].sum/pillarERMap[p.id].count):0,
    count:pillarERMap[p.id]?.count||0
  })).filter(p=>p.count>0).sort((a,b)=>b.avgER-a.avgER)[0]||null;

  const pltCards = allPlatforms().map(p => {
    const s = getReportStats(p.id, reportYear, reportMonth, reportPeriod);
    if (s.posts === 0 && reportPlt !== 'all' && reportPlt !== p.id) return '';
    if (s.posts === 0) return '';
    const lvl = erLevel(s.avgER);
    return `<div class="rpt-plt-card">
      <div class="rpt-plt-card-top">
        <span class="rpt-plt-icon">${p.emoji}</span>
        <span class="rpt-plt-name">${p.name}</span>
        <span class="rpt-plt-post-count">${s.posts} post</span>
      </div>
      <div class="rpt-plt-stats">
        <div class="rpt-plt-stat-row">
          <span class="rpt-plt-stat-lbl">Impressi</span>
          <span class="rpt-plt-stat-val">${fmt(s.impression)}</span>
        </div>
        <div class="rpt-plt-stat-row">
          <span class="rpt-plt-stat-lbl">Reach</span>
          <span class="rpt-plt-stat-val">${fmt(s.reach)}</span>
        </div>
        <div class="rpt-plt-stat-row">
          <span class="rpt-plt-stat-lbl">Engagement</span>
          <span class="rpt-plt-stat-val" style="color:var(--green);">${fmt(s.engagement)}</span>
        </div>
        <div class="rpt-plt-stat-row">
          <span class="rpt-plt-stat-lbl">Flw Gained</span>
          <span class="rpt-plt-stat-val">${fmt(s.followersGained)}</span>
        </div>
        <div class="rpt-plt-stat-row">
          <span class="rpt-plt-stat-lbl">Profile Visit</span>
          <span class="rpt-plt-stat-val">${fmt(s.totalPV)}</span>
        </div>
      </div>
      <div class="rpt-plt-er-badge" style="background:${lvl.bg};color:${lvl.color};">
        ${lvl.emoji} ER ${erStr(s.avgER)} · ${lvl.label}
      </div>
    </div>`;
  }).filter(Boolean).join('');

  // ── Key Highlights ──
  const highlights = computeKeyHighlights(allReportPosts);
  const highlightCards = highlights.slice(0, 4).map(h => {
    const p = h.post;
    const postName = p?.name || p?.link?.replace(/^https?:\/\//, '').slice(0, 32) || '—';
    return `<div class="rpt-highlight-card">
      <div class="rpt-hl-badge"><span>${h.icon}</span>${h.label}</div>
      <div class="rpt-hl-value">${h.metric}</div>
      <div class="rpt-hl-sub">${postName}</div>
    </div>`;
  }).join('') || '';

  // ── Top 3 & Bottom 2 content (Slide 4) ──
  const topContent = [...allReportPosts]
    .filter(p => (p.reach||0)+(p.impression||0) > 0)
    .sort((a,b)=>totalEng(b)-totalEng(a)).slice(0,3);
  const worstContent = [...allReportPosts]
    .filter(p => (p.reach||0)+(p.impression||0) > 0)
    .sort((a,b)=>totalEng(a)-totalEng(b)).slice(0,2);

  function contentEvalCard(p, rank, isTop){
    const pi = pltInfo(p._plt||'tt');
    const er = calcER(p,sel.erMode,p._flw||0);
    const lvl = erLevel(er);
    const sg = scoreGrade(computeContentScore(p,allReportPosts));
    const name = p.name || p.link?.slice(0,40) || '(Tanpa judul)';
    const borderColor = isTop ? 'var(--accent)' : 'var(--red)';
    return `<div class="rpt-content-card ${isTop?'rpt-cc-top':'rpt-cc-low'}">
      <div class="rpt-cc-rank" style="background:${isTop?'rgba(0,232,122,.12)':'rgba(255,85,102,.12)'};color:${isTop?'var(--accent)':'var(--red)'};">${isTop?'#'+rank:'↓'}</div>
      <div class="rpt-cc-body">
        <div class="rpt-cc-name">${name}</div>
        <div class="rpt-cc-meta">
          <span style="color:${pi.color||'var(--t3)'};">${pi.emoji} ${pi.name}</span>
          <span style="color:var(--t3);">${p.date||'—'}</span>
          ${er>0?`<span style="color:${lvl.color};font-family:var(--mono);font-size:11px;">${erStr(er)} ER · ${lvl.label}</span>`:''}
          <span style="background:${sg.bg};color:${sg.color};padding:1px 6px;border-radius:4px;font-size:10px;font-weight:700;">${sg.grade}</span>
        </div>
        <div class="rpt-cc-stats">
          <span>❤️ ${fmt(totalEng(p))}</span>
          <span>👁 ${fmt(p.reach||0)}</span>
          <span>💾 ${fmt(p.save||0)}</span>
          <span>🔁 ${fmt(p.share||0)}</span>
        </div>
        ${!isTop?`<div class="rpt-cc-note">💡 Konten ini performa di bawah rata-rata — evaluasi format, waktu posting, atau relevansi topik.</div>`:''}
      </div>
    </div>`;
  }

  // ── Metrics vs Target table (Slide 3) ──
  const rud = reportUserData;
  function targetRow(label, actual, target, prevVal, fmtFn, isER){
    const pctOfTarget = target>0 ? Math.min(Math.round((actual/target)*100),999) : null;
    const prevDelta = prevVal!=null && prevVal>0 ? ((actual-prevVal)/prevVal*100) : null;
    const tColor = pctOfTarget==null?'var(--t3)':pctOfTarget>=100?'var(--green)':pctOfTarget>=70?'var(--amber)':'var(--red)';
    return `<tr class="rpt-tgt-row">
      <td class="rpt-tgt-label">${label}</td>
      <td class="rpt-tgt-actual" style="font-family:var(--mono);">${fmtFn(actual)}</td>
      <td class="rpt-tgt-target" style="font-family:var(--mono);">${target>0?fmtFn(target):'—'}</td>
      <td class="rpt-tgt-pct" style="color:${tColor};font-family:var(--mono);font-weight:700;">${pctOfTarget!=null?pctOfTarget+'%':'—'}</td>
      <td class="rpt-tgt-prev">${prevDelta!=null?`<span style="color:${prevDelta>=0?'var(--green)':'var(--red)'};">${prevDelta>=0?'▲':'▼'} ${Math.abs(prevDelta).toFixed(0)}%</span>`:'—'}</td>
    </tr>`;
  }
  const metricsTableHTML = `
    <div style="overflow-x:auto;border-radius:12px;border:1px solid rgba(255,255,255,.07);">
      <table style="width:100%;border-collapse:collapse;font-size:13px;">
        <thead>
          <tr style="background:rgba(255,255,255,.04);">
            <th class="rpt-tgt-th">Metrik</th>
            <th class="rpt-tgt-th">Aktual</th>
            <th class="rpt-tgt-th">Target</th>
            <th class="rpt-tgt-th">Capaian</th>
            <th class="rpt-tgt-th">vs Bln Lalu</th>
          </tr>
        </thead>
        <tbody>
          ${targetRow('📝 Jumlah Konten', curr.posts, rud.targetPosts, prev?.posts, fmtFull, false)}
          ${targetRow('👁 Reach', curr.reach, rud.targetReach, prev?.reach, fmt, false)}
          ${targetRow('❤️ Engagement', curr.engagement, rud.targetEngagement, prev?.engagement, fmt, false)}
          ${targetRow('📈 Followers Gained', curr.followersGained, rud.targetFollowers, prev?.followersGained, fmt, false)}
          ${curr.avgER>0?targetRow('📊 Avg ER Rate', curr.avgER, 0, prev?.avgER, erStr, true):''}
        </tbody>
      </table>
    </div>
    <div class="rpt-tgt-edit-row">
      <span style="font-size:10px;color:var(--t3);">Set target:</span>
      ${[
        ['Posts','targetPosts',fmtFull],
        ['Reach','targetReach',fmt],
        ['Engagement','targetEngagement',fmt],
        ['Followers','targetFollowers',fmt],
      ].map(([lbl,field])=>`
        <div style="display:flex;flex-direction:column;gap:3px;flex:1;min-width:80px;">
          <span style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--t3);">${lbl}</span>
          <input type="number" min="0" class="rpt-tgt-inp" value="${rud[field]||''}"
            placeholder="0"
            oninput="updateRUD('${field}',+this.value||0);renderReport()">
        </div>`).join('')}
    </div>`;

  // ── KPI cards HTML ──
  const kpiCardsHTML = kpiDefs.map(k => `
    <div class="rpt-kpi-card" style="--rpt-accent:${k.accent};">
      <div class="rpt-kpi-label">${k.label}</div>
      <div class="rpt-kpi-value">${k.fmt(k.value)}</div>
      ${prev ? deltaHTML(k.value, k.prevVal) : ''}
    </div>`).join('');

  // ── Executive Summary HTML ──
  const execSummaryHTML = (() => {
    if (!hasData) return '';
    const avgER_ = curr.avgER;
    const erLvl_ = erLevel(avgER_);
    let trendLine = '';
    if (prev && prev.posts > 0) {
      const reachDelta = ((curr.reach - prev.reach) / Math.max(prev.reach, 1)) * 100;
      const engDelta = ((curr.engagement - prev.engagement) / Math.max(prev.engagement, 1)) * 100;
      const trend = reachDelta >= 5 ? 'meningkat signifikan' : reachDelta <= -5 ? 'menurun' : 'relatif stabil';
      trendLine = `Performa bulan ini ${trend} dibandingkan bulan lalu — reach ${reachDelta >= 0 ? '+' : ''}${reachDelta.toFixed(0)}%, engagement ${engDelta >= 0 ? '+' : ''}${engDelta.toFixed(0)}%.`;
    }
    const pillarLine = bestPillar ? `Pilar ${bestPillar.emoji} <strong>${bestPillar.label}</strong> menjadi konten terkuat dengan rata-rata ER ${erStr(bestPillar.avgER)}.` : '';
    return `<div class="rpt-exec-summary">
      <div class="rpt-exec-badge">📋 Executive Summary</div>
      <p class="rpt-exec-text">
        Periode <strong>${periodLabel}</strong> menghasilkan <strong>${fmtFull(curr.posts)} konten</strong> dengan total reach <strong>${fmt(curr.reach)}</strong> dan engagement <strong>${fmt(curr.engagement)}</strong>.
        Rata-rata ER berada di <strong style="color:${erLvl_.color};">${erStr(avgER_)}</strong> — level <strong>${erLvl_.label}</strong>.
        ${trendLine ? '<br><br>' + trendLine : ''}
        ${pillarLine ? '<br><br>' + pillarLine : ''}
      </p>
    </div>`;
  })();

  // ── Recommendations HTML ──
  const recsHTML = (() => {
    if (!hasData) return '';
    const recs = [];
    const avgER_ = curr.avgER;
    if (avgER_ < 2) {
      recs.push({ ico:'📉', text:'ER rata-rata masih di bawah 2%. Coba eksperimen dengan hook yang lebih kuat di 3 detik pertama dan perbanyak call-to-action.' });
    } else if (avgER_ >= 4) {
      recs.push({ ico:'🔥', text:'ER sangat baik! Pertahankan pola konten ini dan pertimbangkan untuk meningkatkan frekuensi posting.' });
    }
    if (bestPillar) {
      recs.push({ ico:'🎯', text:'Konten <strong>' + bestPillar.emoji + ' ' + bestPillar.label + '</strong> perform terbaik (ER ' + erStr(bestPillar.avgER) + '). Naikkan porsi ke 40–50% dari total posting bulan depan.' });
    }
    const weeksInPeriod = reportPeriod === 'month' ? 4 : Math.max(months.length * 4, 1);
    const postsPerWeek = curr.posts / weeksInPeriod;
    if (postsPerWeek < 3) {
      recs.push({ ico:'📅', text:'Rata-rata posting hanya ' + postsPerWeek.toFixed(1) + ' per minggu. Target minimal 4–5 post per minggu untuk menjaga konsistensi algoritma.' });
    }
    if (totalReachCP > 0) {
      if (avgSaveRate < 1) recs.push({ ico:'💾', text:'Save rate masih rendah (<1%). Tambahkan value atau tips yang bisa disimpan — tutorial, checklist, atau infografis.' });
      if (avgShareRate < 0.5) recs.push({ ico:'🔁', text:'Share rate perlu ditingkatkan. Konten yang relatable atau punya angle segar cenderung lebih banyak dibagikan.' });
    }
    const shown = recs.slice(0, 4);
    if (!shown.length) return '';
    return '<div class="rpt-section"><div class="rpt-section-title">💡 Rekomendasi Aksi</div><div class="rpt-recs-list">' +
      shown.map(r => '<div class="rpt-rec-item"><span class="rpt-rec-ico">' + r.ico + '</span><div class="rpt-rec-text">' + r.text + '</div></div>').join('') +
      '</div></div>';
  })();

  // ── Assemble ──
  wrap.innerHTML = `
    <!-- Page Header -->
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-title">Pusat Laporan</div>
        <div class="view-subtitle">Ekspor laporan kinerja sosial media komprehensif untuk dikirimkan ke brand partner atau agensi</div>
      </div>
    </div>

    <!-- Report Form Card (Gemini-style) -->
    <div class="chart-card" style="margin-bottom:24px;padding:24px 28px;">
      <div style="font-size:15px;font-weight:800;color:var(--t);margin-bottom:4px;">Buat Laporan Baru</div>
      <div style="font-size:12px;color:var(--t3);margin-bottom:20px;">Pilih format, parameter metrik, dan rentang waktu pembuatan laporan analitik.</div>
      <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-bottom:20px;">
        <div>
          <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t3);margin-bottom:6px;">RENTANG WAKTU LAPORAN</div>
          <div style="position:relative;">
            <select class="af-sel" style="width:100%;padding:9px 12px;border-radius:9px;font-size:13px;" onchange="reportPeriod=this.value==='all'?'all':'month';if(this.value!=='all'){const[m,y]=this.value.split('-');reportMonth=+m;reportYear=+y;}renderReport()">
              <option value="month-${reportMonth}-${reportYear}" ${reportPeriod==='month'?'selected':''}>1 Bulan Terakhir (${MN[reportMonth]} ${reportYear})</option>
              <option value="all" ${reportPeriod==='all'?'selected':''}>Semua Waktu</option>
            </select>
          </div>
        </div>
        <div>
          <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t3);margin-bottom:6px;">METRIK YANG DISERTAKAN</div>
          <select class="af-sel" style="width:100%;padding:9px 12px;border-radius:9px;font-size:13px;" onchange="reportPlt=this.value;renderReport()">
            <option value="all">Semua Metrik (Reach, ER, Posts, Detail Konten)</option>
            ${allPlatforms().map(p=>`<option value="${p.id}" ${reportPlt===p.id?'selected':''}>Hanya ${p.emoji} ${p.name}</option>`).join('')}
          </select>
        </div>
        <div>
          <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.12em;color:var(--t3);margin-bottom:6px;">FORMAT FILE LAPORAN</div>
          <select class="af-sel" style="width:100%;padding:9px 12px;border-radius:9px;font-size:13px;">
            <option>PDF Dokumen Premium (.pdf)</option>
            <option>PNG Gambar (.png)</option>
          </select>
        </div>
      </div>
      <div style="display:flex;gap:8px;flex-wrap:wrap;">
        <button class="view-header-btn" id="rpt-pdf-btn" onclick="showPDFPreview()" style="padding:10px 22px;">
          <div class="rpt-export-spinner" id="rpt-pdf-spin"></div>
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v7M4 6l3 3 3-3M2 10v1a1 1 0 001 1h8a1 1 0 001-1v-1" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Preview &amp; Unduh PDF
        </button>
        <button class="rpt-export-btn rpt-export-btn-sec" onclick="openModal('m-rpt-cfg')" title="Konfigurasi laporan (opsional)">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 9.5a2 2 0 1 0 0-4 2 2 0 0 0 0 4z" stroke="currentColor" stroke-width="1.3"/><path d="M11.7 7a4.9 4.9 0 0 0-.1-.9l1.4-1.1-1.3-2.2-1.7.6a5 5 0 0 0-1.5-.9L8.2 1h-2.4L5.5 2.5c-.6.3-1.1.6-1.5.9L2.3 2.8 1 5l1.4 1.1a5 5 0 0 0 0 1.8L1 9l1.3 2.2 1.7-.6c.4.3.9.6 1.5.9L5.8 13h2.4l.3-1.5c.6-.3 1.1-.6 1.5-.9l1.7.6L13 9l-1.4-1.1c.1-.3.1-.6.1-.9z" stroke="currentColor" stroke-width="1.3"/></svg>
          Konfigurasi
        </button>
        <button class="rpt-export-btn rpt-export-btn-sec" id="rpt-img-btn" onclick="exportImage()">
          <div class="rpt-export-spinner" id="rpt-img-spin" style="border-top-color:var(--t2);"></div>
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><rect x="1.5" y="1.5" width="11" height="11" rx="1.5" stroke="currentColor" stroke-width="1.3"/><circle cx="5" cy="5" r="1" fill="currentColor"/><path d="M1.5 9l3-3 2.5 2.5L9.5 6l3 3.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Export PNG
        </button>
      </div>
    </div>

    <!-- Filter Bar (hidden, keep for compat) -->
    <div class="rpt-filter-bar" style="display:none;">
      <select class="af-sel" onchange="reportPlt=this.value;renderReport()">${pltOpts}</select>
    </div>

    <!-- Download History (Gemini-style) -->
    <div style="margin-bottom:24px;">
      <div style="font-size:15px;font-weight:800;color:var(--t);margin-bottom:14px;">Riwayat Unduhan Laporan</div>
      <div id="rpt-dl-history">
        <div style="background:var(--surf);border:1px solid var(--b);border-radius:12px;padding:16px 20px;display:flex;align-items:center;gap:14px;margin-bottom:8px;transition:background .15s;" onmouseover="this.style.background='var(--bg-hover)'" onmouseout="this.style.background='var(--surf)'">
          <div style="width:34px;height:34px;border-radius:9px;background:rgba(239,68,68,.12);display:flex;align-items:center;justify-content:center;flex-shrink:0;">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="2.5" y="1.5" width="11" height="13" rx="1.5" stroke="#EF4444" stroke-width="1.4"/><path d="M5 5.5h6M5 8h6M5 10.5h3.5" stroke="#EF4444" stroke-width="1.3" stroke-linecap="round"/></svg>
          </div>
          <div style="flex:1;">
            <div style="font-size:13px;font-weight:700;color:var(--t);">Laporan_Bulanan_${MN[reportMonth]}_Creatorlytics.pdf</div>
            <div style="font-size:11px;color:var(--t3);margin-top:2px;">Dibuat pada: ${new Date().toLocaleDateString('id-ID',{day:'numeric',month:'short',year:'numeric'})} • Ukuran: 2.4 MB</div>
          </div>
          <button onclick="showPDFPreview()" style="padding:6px 14px;border-radius:8px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:12px;font-weight:700;cursor:pointer;display:flex;align-items:center;gap:5px;">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M6.5 2v7M4 6l2.5 3 2.5-3M1.5 10v.5a1 1 0 001 1h8a1 1 0 001-1V10" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
        </div>
        <div style="background:var(--surf);border:1px solid var(--b);border-radius:12px;padding:16px 20px;display:flex;align-items:center;gap:14px;transition:background .15s;" onmouseover="this.style.background='var(--bg-hover)'" onmouseout="this.style.background='var(--surf)'">
          <div style="width:34px;height:34px;border-radius:9px;background:rgba(34,197,94,.12);display:flex;align-items:center;justify-content:center;flex-shrink:0;">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="1.5" y="1.5" width="13" height="13" rx="1.5" stroke="#22C55E" stroke-width="1.4"/><path d="M4 8h8M4 5h8M4 11h5" stroke="#22C55E" stroke-width="1.3" stroke-linecap="round"/></svg>
          </div>
          <div style="flex:1;">
            <div style="font-size:13px;font-weight:700;color:var(--t);">Raw_Data_Video_TikTok_Q1_2026.csv</div>
            <div style="font-size:11px;color:var(--t3);margin-top:2px;">Dibuat pada: 30 Apr 2026 • Ukuran: 182 KB</div>
          </div>
          <button onclick="exportImage()" style="padding:6px 14px;border-radius:8px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:12px;font-weight:700;cursor:pointer;display:flex;align-items:center;gap:5px;">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M6.5 2v7M4 6l2.5 3 2.5-3M1.5 10v.5a1 1 0 001 1h8a1 1 0 001-1V10" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
        </div>
      </div>
    </div>

    <!-- Report Body (exported) -->
    <div id="report-body">

      <!-- Report Header -->
      <div class="rpt-header">
        <div class="rpt-brand">
          <div class="rpt-brand-mark">📊</div>
          <div>
            <div class="rpt-brand-name">${rud.brandName||'Creatorlytics'}</div>
            <div class="rpt-brand-sub">Laporan Performa Konten</div>
          </div>
        </div>
        <div style="text-align:right;">
          <div class="rpt-period-badge">${periodLabel} · ${pltLabel}</div>
          <div style="font-size:10px;color:var(--t3);margin-top:6px;font-weight:600;">
            ${new Date().toLocaleDateString('id-ID',{day:'numeric',month:'long',year:'numeric'})}
          </div>
        </div>
      </div>

      ${!hasData ? `
        <div class="rpt-empty">
          <div class="rpt-empty-ico">📭</div>
          <div class="rpt-empty-title">Tidak ada data di periode ini</div>
          <div class="rpt-empty-sub">Pilih periode lain atau tambahkan konten terlebih dahulu.</div>
        </div>
      ` : `

      <!-- SLIDE 1 -->
      <div class="rpt-slide">
        <div class="rpt-slide-num">Slide 1</div>
        <div class="rpt-section-title">🏷️ Identitas & Konteks Kampanye</div>
        <div class="rpt-slide-desc">Selaraskan ekspektasi atasan dengan konteks kampanye bulan ini.</div>
        <div class="rpt-identity-grid">
          <div class="rpt-identity-field">
            <label class="rpt-field-label">Nama Brand / Akun</label>
            <input class="rpt-field-inp" type="text" placeholder="Nama Brand" value="${rud.brandName||''}"
              oninput="updateRUD('brandName',this.value)">
          </div>
          <div class="rpt-identity-field" style="grid-column:1/-1;">
            <label class="rpt-field-label">Fokus Utama Periode Ini <span style="color:var(--t3);font-weight:400;font-size:10px;">— tema atau tujuan campaign bulan ini</span></label>
            <textarea class="rpt-field-ta" rows="3" placeholder="Contoh: Bulan ini fokus pada peningkatan awareness melalui campaign Ramadan dengan target reach 200K..."
              oninput="updateRUD('brandFocus',this.value)">${rud.brandFocus||''}</textarea>
          </div>
        </div>
        ${rud.brandFocus ? `<div class="rpt-slide-preview"><strong>📌 Fokus:</strong> ${rud.brandFocus}</div>` : ''}
      </div>

      <!-- SLIDE 2 -->
      <div class="rpt-slide">
        <div class="rpt-slide-num">Slide 2</div>
        <div class="rpt-section-title">📋 Eksekutif Summary</div>
        <div class="rpt-slide-desc">Performa keseluruhan, kendala yang dihadapi, dan strategi yang diterapkan.</div>
        ${execSummaryHTML}
        <div class="rpt-exec-input-grid">
          <div class="rpt-exec-field">
            <label class="rpt-field-label">⚠️ Kendala Operasional</label>
            <textarea class="rpt-field-ta" rows="3" placeholder="Contoh: Kuantitas konten tidak mencapai target karena load kerja tim desain sedang penuh..."
              oninput="updateRUD('kendala',this.value)">${rud.kendala||''}</textarea>
            ${rud.kendala ? `<div class="rpt-slide-preview" style="border-color:var(--amber);background:rgba(255,179,71,.06);">${rud.kendala}</div>` : ''}
          </div>
          <div class="rpt-exec-field">
            <label class="rpt-field-label">💡 Strategi & Inovasi Baru</label>
            <textarea class="rpt-field-ta" rows="3" placeholder="Contoh: Mulai pakai Notion untuk memangkas waktu approval desain..."
              oninput="updateRUD('solusi',this.value)">${rud.solusi||''}</textarea>
            ${rud.solusi ? `<div class="rpt-slide-preview" style="border-color:var(--accent);background:rgba(0,232,122,.06);">${rud.solusi}</div>` : ''}
          </div>
        </div>
      </div>

      <!-- SLIDE 3 -->
      <div class="rpt-slide">
        <div class="rpt-slide-num">Slide 3</div>
        <div class="rpt-section-title">📊 Laporan Metrik</div>
        <div class="rpt-slide-desc">Pencapaian aktual vs target, dibandingkan dengan bulan lalu.</div>
        ${metricsTableHTML}
        <div style="margin-top:16px;">
          <div class="rpt-cc-section-title">📈 KPI Overview</div>
          <div class="rpt-kpi-grid" style="margin-top:10px;">${kpiCardsHTML}</div>
        </div>
        ${pltCards ? `<div style="margin-top:16px;"><div class="rpt-cc-section-title">📱 Platform Performance</div><div class="rpt-plt-grid" style="margin-top:10px;">${pltCards}</div></div>` : ''}
      </div>

      <!-- SLIDE 4 — PILAR BREAKDOWN (DIFFERENTIATOR) -->
      <div class="rpt-slide" style="page-break-inside:avoid;">
        <div class="rpt-slide-num">Slide 4</div>
        <div style="display:flex;align-items:center;gap:10px;margin-bottom:4px;">
          <div class="rpt-section-title" style="margin:0;">🎯 Analisis Pilar Konten</div>
          <span style="font-size:11px;font-weight:800;padding:2px 8px;border-radius:20px;background:rgba(168,85,247,.15);color:#A855F7;letter-spacing:.06em;">AGENCY INSIGHT</span>
        </div>
        <div class="rpt-slide-desc">Breakdown performa per pilar konten — mana yang harus ditambah, mana yang dievaluasi.</div>
        ${(() => {
          const pilMap = {};
          allReportPosts.forEach(p => {
            const pid = p.pillar || 'other';
            if (!pilMap[pid]) pilMap[pid] = { posts: [], eng: 0, reach: 0, like: 0, save: 0, share: 0 };
            pilMap[pid].posts.push(p);
            pilMap[pid].eng   += totalEng(p);
            pilMap[pid].reach += (p.reach || 0);
            pilMap[pid].like  += (p.like  || 0);
            pilMap[pid].save  += (p.save  || 0);
            pilMap[pid].share += (p.share || 0);
          });
          const entries = Object.entries(pilMap).map(([id, d]) => {
            const pil = pillarOf(id);
            const avgER = d.posts.length ? d.posts.reduce((s,p)=>s+calcER(p,sel.erMode,p._flw||0),0)/d.posts.length : 0;
            const avgEng = d.posts.length ? d.eng / d.posts.length : 0;
            const saveRate = d.reach > 0 ? (d.save / d.reach * 100) : 0;
            const shareRate = d.reach > 0 ? (d.share / d.reach * 100) : 0;
            return { id, label: pil.label, emoji: pil.emoji, color: pil.color || 'var(--accent)', count: d.posts.length, avgER, avgEng, totalReach: d.reach, totalEng: d.eng, saveRate, shareRate };
          }).sort((a,b) => b.avgER - a.avgER);
          if (!entries.length) return '<div style="color:var(--t3);font-size:12px;padding:20px;text-align:center;">Belum ada data pilar. Pastikan setiap post sudah diberi tag pilar konten.</div>';
          const maxER = entries[0].avgER || 1;
          const rows = entries.map((e, i) => {
            const barW = maxER > 0 ? Math.round((e.avgER / maxER) * 100) : 0;
            const isTop = i === 0;
            const rec = isTop ? '✅ Prioritas utama — perbanyak konten ini' : e.avgER > maxER * 0.7 ? '⚡ Performa kuat — pertahankan frekuensi' : e.avgER > maxER * 0.4 ? '🔍 Perlu optimasi hook & format' : '⚠️ Evaluasi ulang relevansi topik';
            return `<tr style="border-bottom:1px solid var(--b);">
              <td style="padding:10px 12px;font-size:13px;font-weight:700;color:var(--t);">
                <span style="font-size:16px;margin-right:6px;">${e.emoji}</span>${e.label}
                ${isTop ? '<span style="font-size:11px;font-weight:800;padding:1px 6px;border-radius:10px;background:rgba(14,159,143,.15);color:var(--accent);margin-left:6px;">TOP</span>' : ''}
              </td>
              <td style="padding:10px 12px;text-align:center;font-family:var(--mono);font-size:12px;color:var(--t2);">${e.count}</td>
              <td style="padding:10px 16px;min-width:120px;">
                <div style="display:flex;align-items:center;gap:8px;">
                  <div style="flex:1;height:6px;background:rgba(255,255,255,.07);border-radius:6px;overflow:hidden;">
                    <div style="height:100%;width:${barW}%;background:${e.color};border-radius:6px;"></div>
                  </div>
                  <span style="font-family:var(--mono);font-size:12px;font-weight:800;color:var(--t);min-width:42px;">${erStr(e.avgER)}</span>
                </div>
              </td>
              <td style="padding:10px 12px;text-align:center;font-family:var(--mono);font-size:12px;color:var(--t2);">${fmt(Math.round(e.avgEng))}</td>
              <td style="padding:10px 12px;text-align:center;font-family:var(--mono);font-size:11px;color:var(--t3);">${e.saveRate.toFixed(1)}%</td>
              <td style="padding:10px 12px;font-size:11px;color:${isTop?'var(--accent)':e.avgER>maxER*0.7?'var(--green)':e.avgER>maxER*0.4?'var(--amber)':'var(--red)'};">${rec}</td>
            </tr>`;
          }).join('');
          const topEntry = entries[0];
          const bottomEntry = entries[entries.length - 1];
          const summaryBanner = entries.length >= 2 ? `
            <div style="margin-top:16px;padding:14px 16px;background:var(--accent-dim);border:1px solid rgba(14,159,143,.2);border-radius:10px;">
              <div style="font-size:11px;font-weight:800;color:var(--accent);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px;">💡 Rekomendasi Strategis</div>
              <div style="font-size:12px;color:var(--t2);line-height:1.7;">
                Pilar <strong style="color:var(--t);">"${topEntry.label}"</strong> adalah konten terkuat dengan avg ER <strong style="color:var(--accent);">${erStr(topEntry.avgER)}</strong> — rekomendasikan klien untuk meningkatkan frekuensi minimal 50% pada pilar ini.
                ${bottomEntry !== topEntry && bottomEntry.avgER < topEntry.avgER * 0.5 ? ` Sebaliknya, pilar <strong>"${bottomEntry.label}"</strong> (ER ${erStr(bottomEntry.avgER)}) perlu direview: apakah formatnya sesuai, atau topiknya kurang relevan dengan audiens saat ini.` : ''}
              </div>
            </div>` : '';
          return `
            <div style="overflow-x:auto;border-radius:10px;border:1px solid var(--b);margin-bottom:0;">
              <table style="width:100%;border-collapse:collapse;font-size:13px;">
                <thead>
                  <tr style="background:var(--surf2);">
                    <th style="padding:9px 12px;text-align:left;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Pilar</th>
                    <th style="padding:9px 12px;text-align:center;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Post</th>
                    <th style="padding:9px 16px;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Avg ER</th>
                    <th style="padding:9px 12px;text-align:center;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Avg Eng</th>
                    <th style="padding:9px 12px;text-align:center;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Save Rate</th>
                    <th style="padding:9px 12px;font-size:10px;font-weight:800;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;">Rekomendasi</th>
                  </tr>
                </thead>
                <tbody>${rows}</tbody>
              </table>
            </div>
            ${summaryBanner}`;
        })()}
      </div>

      <!-- SLIDE 5 — EVALUASI KONTEN (was slide 4) -->
      <div class="rpt-slide">
        <div class="rpt-slide-num">Slide 5</div>
        <div class="rpt-section-title">🎬 Evaluasi Kinerja Konten</div>
        <div class="rpt-slide-desc">Konten terbaik dan terendah — insight untuk efisiensi effort tim ke depannya.</div>
        ${topContent.length ? `
          <div class="rpt-cc-section-title">🏆 Top Performing Content</div>
          <div class="rpt-content-eval-list">${topContent.map((p,i)=>contentEvalCard(p,i+1,true)).join('')}</div>` : ''}
        ${worstContent.length ? `
          <div class="rpt-cc-section-title" style="margin-top:16px;">📉 Low Performance Content</div>
          <div class="rpt-content-eval-list">${worstContent.map((p,i)=>contentEvalCard(p,i+1,false)).join('')}</div>` : ''}
        ${highlightCards ? `
          <div style="margin-top:16px;">
            <div class="rpt-cc-section-title">⭐ Key Highlights</div>
            <div class="rpt-highlights-grid" style="margin-top:10px;">${highlightCards}</div>
          </div>` : ''}
      </div>

      <!-- SLIDE 6 — ACTION PLAN (was slide 5) -->
      <div class="rpt-slide">
        <div class="rpt-slide-num">Slide 6</div>
        <div class="rpt-section-title">🚀 Action Plan</div>
        <div class="rpt-slide-desc">Target dan rencana taktis untuk periode selanjutnya.</div>
        <div class="rpt-exec-input-grid">
          <div class="rpt-exec-field" style="grid-column:1/-1;">
            <label class="rpt-field-label">🎯 Target Bulan Depan</label>
            <textarea class="rpt-field-ta" rows="2" placeholder="Contoh: Meningkatkan followers 500 akun baru, reach 150K melalui campaign giveaway..."
              oninput="updateRUD('nextTarget',this.value)">${rud.nextTarget||''}</textarea>
            ${rud.nextTarget ? `<div class="rpt-slide-preview">${rud.nextTarget}</div>` : ''}
          </div>
          <div class="rpt-exec-field" style="grid-column:1/-1;">
            <label class="rpt-field-label">📋 Rencana Taktis</label>
            <textarea class="rpt-field-ta" rows="3" placeholder="Contoh: Giveaway minggu ke-2, kolaborasi 2 kreator niche edukasi, posting 5x per minggu..."
              oninput="updateRUD('rencanaTaktis',this.value)">${rud.rencanaTaktis||''}</textarea>
            ${rud.rencanaTaktis ? `<div class="rpt-slide-preview">${rud.rencanaTaktis}</div>` : ''}
          </div>
        </div>
        ${recsHTML}
        <div style="margin-top:16px;">
          <div class="rpt-cc-section-title">📊 Benchmark vs Niche: ${getNicheBenchmark().label}</div>
          <div class="rpt-benchmark-grid" style="margin-top:10px;">
            ${getBenchmarkComparison(allReportPosts).map(r=>{
              const dCls=r.delta>0.1?'pos':r.delta<-0.1?'neg':'neu';
              const dStr=r.delta>0.1?'▲ +'+(r.unit==='%'?r.delta.toFixed(2)+'%':fmt(r.delta)):r.delta<-0.1?'▼ '+(r.unit==='%'?Math.abs(r.delta).toFixed(2)+'%':fmt(Math.abs(r.delta))):'≈ Setara';
              return`<div class="rpt-bm-card"><div class="rpt-bm-label">${r.label}</div><div class="rpt-bm-yours" style="color:${r.delta>0.1?'var(--green)':r.delta<-0.1?'var(--red)':'var(--t)'}">${r.yours}</div><div class="rpt-bm-industry">Benchmark: ${r.industry}</div><div class="rpt-bm-delta ${dCls}">${dStr}</div></div>`;
            }).join('')||'<div style="color:var(--t3);font-size:12px;padding:12px;">Belum cukup data.</div>'}
          </div>
        </div>
      </div>

      <div style="margin-top:28px;padding-top:16px;border-top:1px solid var(--b);display:flex;align-items:center;justify-content:space-between;">
        <div style="font-size:10px;color:var(--t3);font-weight:600;">Creatorlytics · Social Analytics Platform</div>
        <div style="font-size:10px;color:var(--t3);font-family:var(--mono);">${periodLabel} · ${fmtFull(curr.posts)} posts · ${fmt(curr.reach)} reach</div>
      </div>

      `}
    </div>`;
}


/* ══ SPRINT 7: ONBOARDING ══ */
const OB_KEY = 'clytics:onboarded:v1';
let _obPlatforms = new Set(['tt','ig']); // default selected

function obTogglePlt(id, btn) {
  if (_obPlatforms.has(id)) {
    _obPlatforms.delete(id);
    btn.classList.remove('sel');
  } else {
    _obPlatforms.add(id);
    btn.classList.add('sel');
  }
}

function obGoTo(step) {
  [1,2,3].forEach(n => {
    document.getElementById('ob-s'+n)?.classList.toggle('act', n===step);
    const dot = document.getElementById('ob-d'+n);
    if (dot) dot.classList.toggle('act', n<=step);
  });
}

function obNext(fromStep) {
  if (fromStep === 1) {
    // Step 1: simpan nama brand/akun
    const name = document.getElementById('ob-acc-name')?.value.trim();
    if (name && !accounts.includes(name)) {
      accounts.unshift(name);
      sel.acc = name;
      // Update sidebar user name
      const unEl = document.getElementById('nav-user-name');
      if (unEl) unEl.textContent = name;
      const avEl = document.getElementById('nav-user-avatar');
      if (avEl) avEl.textContent = name.slice(0,2).toUpperCase();
      const wsEl = document.getElementById('ws-active-name');
      if (wsEl) wsEl.textContent = name;
    }
    obGoTo(2);
  } else if (fromStep === 2) {
    // Step 2: simpan platform pilihan
    if (_obPlatforms.has('yt') && !platforms.find(p=>p.id==='yt')) {
      platforms.push({id:'yt',name:'YouTube',emoji:'▶️'});
    }
    if (_obPlatforms.has('other_plt')) {
      const customName = (document.getElementById('ob-other-name')?.value.trim()) || 'Lainnya';
      const finalId = 'plt_' + customName.toLowerCase().replace(/\s+/g,'_').replace(/[^a-z0-9_]/g,'').slice(0,12) || 'other_plt';
      if (!platforms.find(p=>p.id===finalId)) {
        platforms.push({id:finalId, name:customName, emoji:'📌'});
      }
    }
    obGoTo(3);
  }
}

function obSkip() {
  try { localStorage.setItem(OB_KEY,'1'); } catch(e) {}
  document.getElementById('onboarding-ov')?.classList.add('hidden');
  renderNavPlatforms();
}

function obFinish() {
  try { localStorage.setItem(OB_KEY,'1'); } catch(e) {}
  document.getElementById('onboarding-ov')?.classList.add('hidden');
  scheduleSave(200);
  renderNavPlatforms();
  renderDashboard();
}

function checkShowOnboarding() {
  try {
    const done = localStorage.getItem(OB_KEY);
    if (!done && !months.length) {
      document.getElementById('onboarding-ov')?.classList.remove('hidden');
    }
  } catch(e) {}
}

/* ══ SPRINT 7: IMPROVED openQCModal ══ */
function openQCModal() {
  const plts = allPlatforms();
  const row = document.getElementById('qc-plt-row');
  if (row) {
    row.innerHTML = plts.map(p =>
      `<button class="qc-plt-btn${_qcPlt===p.id?' act':''}" onclick="qcSetPlt('${p.id}',this)">${p.emoji} ${p.name}</button>`
    ).join('');
  }
  const accSel = document.getElementById('qc-acc');
  if (accSel) {
    accSel.innerHTML = accounts.map(a =>
      `<option value="${a}"${sel.acc===a?' selected':''}>${a}</option>`
    ).join('');
  }
  const dateInp = document.getElementById('qc-date');
  if (dateInp) dateInp.value = new Date().toISOString().split('T')[0];

  // Populate pillar dropdown
  const pillarSel = document.getElementById('qc-pillar');
  if (pillarSel) {
    pillarSel.innerHTML = pillars.map(p =>
      `<option value="${p.id}">${p.emoji} ${p.label}</option>`
    ).join('');
  }

  // Clear number fields
  ['qc-name','qc-link','qc-like','qc-comment','qc-share','qc-save','qc-reach','qc-impression'].forEach(id => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  const fmtSel = document.getElementById('qc-format');
  if (fmtSel) fmtSel.value = '';

  openModal('m-qc');
  setTimeout(() => {
    // Focus first unfilled number field on mobile, name field on desktop
    const target = window.innerWidth <= 768
      ? document.getElementById('qc-like')
      : document.getElementById('qc-name');
    target?.focus();
  }, 80);
}

/* ══ SPRINT 7: Updated _qcBuildPost (now reads pillar + format) ══ */
function _qcBuildPost(saveFull) {
  const acc  = document.getElementById('qc-acc')?.value || accounts[0] || '';
  const date = document.getElementById('qc-date')?.value || new Date().toISOString().split('T')[0];
  const name = document.getElementById('qc-name')?.value.trim() || '';
  if (!acc) return null;

  // ── EDIT MODE: update existing post in-place ──
  if (window._qcEditId) {
    const editPlt = window._qcEditPlt || _qcPlt;
    // Find the post across all months/weeks
    let found = null;
    for (const mo of months) {
      for (const wk of mo.weeks) {
        const d = aD(wk, acc);
        const key = editPlt + 'Posts';
        const posts = d[key] || [];
        const p = posts.find(x => x.id === window._qcEditId);
        if (p) {
          p.name    = name;
          p.date    = date;
          p.link    = (document.getElementById('qc-link')?.value||'').trim() || p.link || '';
          p.pillar  = document.getElementById('qc-pillar')?.value || p.pillar;
          p.format  = document.getElementById('qc-format')?.value || p.format || '';
          if (saveFull) {
            const num = (id) => Math.max(0, parseInt(document.getElementById(id)?.value)||0);
            p.like       = num('qc-like');
            p.comment    = num('qc-comment');
            p.share      = num('qc-share');
            p.save       = num('qc-save');
            p.reach      = num('qc-reach');
            p.impression = num('qc-impression');
          }
          found = { mo, wk, acc, plt: editPlt };
          break;
        }
      }
      if (found) break;
    }
    window._qcEditId  = null;
    window._qcEditPlt = null;
    return found;
  }

  // ── CREATE MODE: build and insert new post ──
  const d = new Date(date + 'T12:00:00');
  const y = d.getFullYear(); const m = d.getMonth();
  let mo = months.find(x => x.year===y && x.month===m);
  if (!mo) {
    mo = {id:uid(), year:y, month:m, weeks:[]};
    months.push(mo);
    months.sort((a,b) => a.year!==b.year ? a.year-b.year : a.month-b.month);
    exp[mo.id] = true;
  }
  // Auto-assign ke minggu berdasarkan tanggal (day 1-7=W1, 8-14=W2, 15-21=W3, 22+=W4)
  const _day = d.getDate();
  const _wkNum = _day<=7?1:_day<=14?2:_day<=21?3:4;
  const _wkLbl = 'Minggu '+_wkNum;
  if (!mo.weeks.length) {
    const wk = {id:uid(), label:'Minggu 1', data:{}};
    if (!wk.data[acc]) wk.data[acc] = emptyAcc();
    mo.weeks.push(wk);
  }
  // Cari atau buat minggu yang tepat
  let wk = mo.weeks.find(w=>w.label===_wkLbl);
  if (!wk) {
    wk = {id:uid(), label:_wkLbl, data:{}};
    mo.weeks.push(wk);
    mo.weeks.sort((a,b)=>parseInt(a.label.split(' ')[1]||1)-parseInt(b.label.split(' ')[1]||1));
  }
  if (!wk.data[acc]) wk.data[acc] = emptyAcc();
  const d2 = wk.data[acc];
  const key = _qcPlt + 'Posts';
  if (!d2[key]) d2[key] = [];
  const p = emptyPost();
  p.date   = date;
  p.name   = name;
  p.link   = (document.getElementById('qc-link')?.value||'').trim();
  p.pillar = document.getElementById('qc-pillar')?.value || pillars[0]?.id || 'other';
  p.format = document.getElementById('qc-format')?.value || '';
  if (saveFull) {
    const num = (id) => Math.max(0, parseInt(document.getElementById(id)?.value)||0);
    p.like       = num('qc-like');
    p.comment    = num('qc-comment');
    p.share      = num('qc-share');
    p.save       = num('qc-save');
    p.reach      = num('qc-reach');
    p.impression = num('qc-impression');
  }
  d2[key].push(p);
  return {mo, wk, acc, plt: _qcPlt, _savedPost: p};
}

/* ══ SPRINT 7: SEED INSIGHTS (for users with <5 posts) ══ */
function getSeedInsights(postCount) {
  // Even with 1–4 posts, give actionable insights based on actual data
  const allPosts = getAllPosts();
  const insights = [];

  if(allPosts.length > 0) {
    // Rule: find highest performing post so far
    const byEng = [...allPosts].sort((a,b) => totalEng(b)-totalEng(a));
    const topPost = byEng[0];
    if(topPost && totalEng(topPost) > 0) {
      const pil = pillarOf(topPost.pillar||'other');
      insights.push({type:'highlight',icon:'🏆',priority:0,
        title:`Post terbaik kamu: "${(topPost.name||'Post pertama').slice(0,30)}"`,
        message:`Engagement ${fmtFull(totalEng(topPost))} · Pilar: ${pil.emoji} ${pil.label}${topPost.format?' · Format: '+topPost.format.toUpperCase():''}`,
        action:`Buat lebih banyak konten dengan formula yang sama: pilar "${pil.label}"${topPost.format?' + format '+topPost.format.toUpperCase():''}. Lihat apa yang membuat post ini perform.`});
    }
    // Rule: ER check
    const postsWithFlw = allPosts.filter(p => (p.reach||0) > 0 || (p.impression||0) > 0);
    if(postsWithFlw.length > 0) {
      const avgER = postsWithFlw.reduce((s,p) => s+calcER(p,sel.erMode,p._flw||0),0)/postsWithFlw.length;
      const lvl = erLevel(avgER);
      insights.push({type:'highlight',icon:lvl.emoji||'📊',priority:1,
        title:`ER awalmu: ${erStr(avgER)} — ${lvl.label}`,
        message:`Dari ${postsWithFlw.length} post yang punya data reach/impressi. ${avgER >= 3 ? 'Start yang bagus!' : 'Masih bisa ditingkatkan.'}`,
        action: avgER >= 3
          ? 'Pertahankan konsistensi! Tambah data dari lebih banyak post untuk insight yang lebih akurat.'
          : 'Fokus ke save dan share — keduanya punya bobot besar di algoritma dan meningkatkan ER.'});
    }
    // Rule: pillar usage
    const withPillar = allPosts.filter(p => p.pillar && p.pillar !== 'other');
    if(withPillar.length === 0 && allPosts.length >= 2) {
      insights.push({type:'pillar',icon:'🎯',priority:2,
        title:'Belum ada pilar konten yang ditag',
        message:'Pilar konten adalah fitur unggulan Creatorlytics untuk analisis mendalam. Saat ini '+allPosts.length+' post belum punya pilar.',
        action:'Buka Input Data → klik kolom Pilar di setiap baris. Pilih antara Edukasi, Hiburan, Promo, dll. Ini data kritis untuk rekomendasi konten.'});
    }
  }

  // Default seeds
  const seeds = [
    {type:'highlight',icon:'👋',priority:3,
     title:'Selamat datang di Creatorlytics!',
     message:'Dashboard ini makin cerdas seiring data yang masuk. Mulai dengan input '+Math.max(3, 5-allPosts.length)+' post lagi.',
     action:'Gunakan tombol "+ Post" di topbar untuk input cepat tanpa navigasi. Atau import via CSV untuk data bulk.'},
    {type:'highlight',icon:'📅',priority:4,
     title:'Tip: Input data per post, bukan per minggu',
     message:'Setiap post punya tanggal sendiri — tidak perlu menunggu akhir minggu. Input langsung setelah posting.',
     action:'Klik "+ Post" di topbar → isi tanggal, nama konten, dan metrik utama (reach, like, comment). Selesai dalam 30 detik.'},
    {type:'platform',icon:'📊',priority:5,
     title:'Tip: ER lebih jujur dari reach',
     message:'Reach bisa naik karena keberuntungan algoritma. ER (engagement rate) adalah ukuran kualitas konten yang sesungguhnya.',
     action:'Target ER 3–5% untuk kreator umum. Di bawah 2% = evaluasi hook dan call-to-action di caption.'},
  ];

  const result = [...insights, ...seeds.slice(0, Math.max(0, 4-insights.length))];
  return result.sort((a,b)=>a.priority-b.priority).slice(0,4);
}






/* ══ REVISI 1: Timeline View ══ */

// Flatten all posts from storage, sorted by date desc
function getAllPostsFlat(filters={}) {
  const { plt, acc, pillar, periodYear, periodMonth } = filters;
  const posts = [];
  (months || []).forEach(mo => {
    if (!mo) return;
    if (periodYear !== undefined && mo.year !== periodYear) return;
    if (periodMonth !== undefined && mo.month !== periodMonth) return;
    (mo.weeks || []).forEach(wk => {
      if (!wk) return;
      (accounts || [])
        .filter(a => !acc || a === acc)
        .forEach(a => {
          allPlatforms()
            .filter(p => !plt || p.id === plt)
            .forEach(p => {
              const wkData = aD(wk, a);
              if (!wkData) return;
              (getPltPosts(wkData, p.id) || [])
                .filter(post => post && (!pillar || post.pillar === pillar))
                .forEach(post => {
                  posts.push({
                    ...post,
                    _plt: p.id,
                    _pltName: p.name,
                    _pltEmoji: p.emoji,
                    _acc: a,
                    _mo: mo,
                    _wk: wk,
                  });
                });
            });
        });
    });
  });
  return posts.sort((a, b) => (b.date || '').localeCompare(a.date || ''));
}

// Delete post by ID across all storage
function deletePostById(postId) {
  months.forEach(mo => {
    mo.weeks.forEach(wk => {
      accounts.forEach(a => {
        allPlatforms().forEach(p => {
          const key = p.id + 'Posts';
          const d = aD(wk, a);
          const idx = (d[key] || []).findIndex(x => x.id === postId);
          if (idx !== -1) d[key].splice(idx, 1);
        });
      });
    });
  });
  scheduleSave(300);
}

/* ── Undo helpers: stash a post before deletion so it can be restored ── */
function _stashPost(postId) {
  let found = null;
  months.forEach(mo => {
    mo.weeks.forEach(wk => {
      accounts.forEach(a => {
        allPlatforms().forEach(plt => {
          const key = plt.id + 'Posts';
          const d = aD(wk, a);
          const idx = (d[key] || []).findIndex(x => x.id === postId);
          if (idx !== -1 && !found) {
            found = { post: {...d[key][idx]}, moId: mo.id, wkId: wk.id, acc: a, pltKey: key };
          }
        });
      });
    });
  });
  return found;
}
function _restorePost(stash) {
  if (!stash) return;
  const mo = months.find(m => m.id === stash.moId);
  if (!mo) return;
  const wk = mo.weeks.find(w => w.id === stash.wkId);
  if (!wk) return;
  const d = aD(wk, stash.acc);
  if (!d[stash.pltKey]) d[stash.pltKey] = [];
  // Only restore if not already there
  if (!d[stash.pltKey].find(p => p.id === stash.post.id)) {
    d[stash.pltKey].push(stash.post);
    d[stash.pltKey].sort((a,b) => (b.date||'').localeCompare(a.date||''));
  }
  scheduleSave(300);
}

let _timelineFilters = { plt: '', acc: '', pillar: '', period: 'all', _search: '' };
let _timelinePage = 1;
const TIMELINE_PAGE_SIZE = 20;

function renderContentTimeline() {
  const wrap = document.getElementById('v-content');
  if (!wrap) return;

  // Build flat filtered list
  const f = _timelineFilters;
  let filterArgs = { plt: f.plt || undefined, acc: f.acc || undefined, pillar: f.pillar || undefined };
  if (f.period === 'month') {
    filterArgs.periodYear = new Date().getFullYear();
    filterArgs.periodMonth = new Date().getMonth();
  } else if (f.period === '3months') {
    // handled by post-filter below
  }
  let posts = getAllPostsFlat(filterArgs);
  if (f.period === '3months') {
    const cutoff = new Date();
    cutoff.setMonth(cutoff.getMonth() - 3);
    const cutStr = cutoff.toISOString().split('T')[0];
    posts = posts.filter(p => (p.date || '') >= cutStr);
  }

  if (f._search) {
    const q = f._search.toLowerCase();
    posts = posts.filter(p => (p.name||'').toLowerCase().includes(q) || (p.desc||'').toLowerCase().includes(q));
  }

  // Group by YYYY-MM for headers
  const grouped = {};
  const noDate = [];
  posts.forEach(p => {
    if (!p.date) { noDate.push(p); return; }
    const [y, m] = p.date.split('-');
    const key = `${y}-${m}`;
    if (!grouped[key]) grouped[key] = [];
    grouped[key].push(p);
  });

  const groupKeys = Object.keys(grouped).sort((a, b) => b.localeCompare(a));
  if (noDate.length) groupKeys.push('__nodate');
  if (noDate.length) grouped['__nodate'] = noDate;

  // Filter options
  const pltOpts = ['<option value="">Semua Platform</option>',
    ...allPlatforms().map(p => `<option value="${p.id}" ${f.plt===p.id?'selected':''}>${p.emoji} ${p.name}</option>`)
  ].join('');
  const accOpts = ['<option value="">Semua Akun</option>',
    ...accounts.map(a => `<option value="${esc(a)}" ${f.acc===a?'selected':''}>${a}</option>`)
  ].join('');
  const pillarOpts = ['<option value="">Semua Pilar</option>',
    ...pillars.map(p => `<option value="${p.id}" ${f.pillar===p.id?'selected':''}>${p.emoji} ${p.label}</option>`)
  ].join('');
  const periodOpts = [
    `<option value="all" ${f.period==='all'?'selected':''}>Semua Waktu</option>`,
    `<option value="month" ${f.period==='month'?'selected':''}>Bulan Ini</option>`,
    `<option value="3months" ${f.period==='3months'?'selected':''}>3 Bulan Terakhir</option>`,
  ].join('');

  // Platform chip filter
  const allPlts = [{id:'',label:'Semua'},...allPlatforms().map(p=>({id:p.id,label:p.name+' Only',emoji:p.emoji}))];
  const chipFilter = `
    <div class="view-header" style="margin-bottom:20px;">
      <div class="view-header-left">
        <div class="view-title">Manajemen Konten</div>
        <div class="view-subtitle">Kelola, filter, dan pantau performa seluruh video &amp; postingan secara real-time</div>
      </div>
      <div class="view-header-right">
        <button class="view-header-btn" onclick="openQCModal()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
          Tambah Konten Baru
        </button>
      </div>
    </div>
    <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:20px;">
      <div style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;flex:1;">
        <span style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-right:2px;">Filter Platform:</span>
        ${allPlts.map(p=>`<button class="content-chip ${(!_timelineFilters.plt&&!p.id)||_timelineFilters.plt===p.id?'act':''}" onclick="_timelineFilters.plt='${p.id}';_timelinePage=1;renderContentTimeline()">${p.emoji?p.emoji+' ':''}${p.label}</button>`).join('')}
      </div>
      <div class="content-search-wrap">
        <svg class="content-search-ico" width="13" height="13" viewBox="0 0 13 13" fill="none"><circle cx="5.5" cy="5.5" r="4" stroke="currentColor" stroke-width="1.3"/><path d="M9 9l2.5 2.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
        <input class="content-search-input" type="text" placeholder="Cari judul konten..." 
          value="${_timelineFilters._search||''}"
          oninput="this._t&&clearTimeout(this._t);this._t=setTimeout(()=>{_timelineFilters._search=this.value;_timelinePage=1;renderContentTimeline();},300)">
      </div>
    </div>`;

  const filterBar = `<div class="timeline-filter-bar" style="display:none;">
    <select onchange="_timelineFilters.plt=this.value;_timelinePage=1;renderContentTimeline()">${pltOpts}</select>
    <select onchange="_timelineFilters.acc=this.value;_timelinePage=1;renderContentTimeline()">${accOpts}</select>
    <select onchange="_timelineFilters.pillar=this.value;_timelinePage=1;renderContentTimeline()">${pillarOpts}</select>
    <select onchange="_timelineFilters.period=this.value;_timelinePage=1;renderContentTimeline()">${periodOpts}</select>
    <button class="timeline-add-btn" onclick="openQCModal()">＋ Tambah Konten</button>
  </div>`;

  // Build card for each post
  function postCard(p) {
    const er = calcER(p, sel.erMode, p._flw || 0);
    const lvl = erLevel(er);
    const score = computeContentScore(p, getAllPosts());
    const pil = pillarOf(p.pillar || '');
    const pi = pltInfo(p._plt);
    const nm = p.name || p.link?.replace(/^https?:\/\//,'').slice(0,50) || '(Tanpa judul)';
    const erPill = er > 0 ? `<span class="timeline-metric-pill" style="background:${lvl.bg};color:${lvl.color};">${erStr(er)}</span>` : '';
    const fmtLabel = p.format ? (FORMAT_TYPES.find(f=>f.id===p.format)?.emoji||'') + ' ' : '';
    let statusBadge = '';
    if (score >= 80 || er >= 5) {
      statusBadge = `<span class="content-status-badge status-viral">VIRAL TERDETEKSI</span>`;
    } else if (score >= 60 || er >= 3) {
      statusBadge = `<span class="content-status-badge status-normal">NORMAL PERFORMA</span>`;
    } else if ((p.reach||0) > 0 || (p.impression||0) > 0) {
      statusBadge = `<span class="content-status-badge status-attention">BUTUH PERHATIAN</span>`;
    }
    return `<div class="timeline-card" id="tc-${p.id}">
      <div class="timeline-card-main">
        <div class="timeline-card-body" style="width:100%;">
          <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:8px;margin-bottom:8px;">
            <div>${statusBadge||''}</div>
            <div style="display:flex;align-items:center;gap:5px;flex-shrink:0;">
              <span style="font-size:16px;">${pi.emoji}</span>
              <span style="font-size:11px;font-weight:700;color:var(--t3);">${pi.name.replace('TikTok','TikTok').replace('Instagram','Reels')}</span>
            </div>
          </div>
          <div class="timeline-card-name" style="font-size:15px;font-weight:800;letter-spacing:-.02em;margin-bottom:10px;">${nm}</div>
          ${p.desc?`<div style="font-size:12px;color:var(--t2);margin-bottom:10px;line-height:1.5;overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;">${p.desc}</div>`:''}
          <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:10px;">
            <div style="padding:8px 10px;background:var(--surf2);border-radius:8px;text-align:center;">
              <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.08em;color:var(--t3);margin-bottom:3px;">VIEWS</div>
              <div style="font-size:14px;font-weight:800;font-family:var(--mono);color:var(--t);">${fmt(p.reach||p.impression||0)}</div>
            </div>
            <div style="padding:8px 10px;background:var(--surf2);border-radius:8px;text-align:center;">
              <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.08em;color:var(--t3);margin-bottom:3px;">LIKES</div>
              <div style="font-size:14px;font-weight:800;font-family:var(--mono);color:var(--t);">${fmt(p.like||0)}</div>
            </div>
            <div style="padding:8px 10px;background:var(--surf2);border-radius:8px;text-align:center;">
              <div style="font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.08em;color:var(--t3);margin-bottom:3px;">ENGAGEMENT</div>
              <div style="font-size:14px;font-weight:800;font-family:var(--mono);color:${er>0?lvl.color:'var(--t)'};">${er>0?erStr(er):'—'}</div>
            </div>
          </div>
          <div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;">
            <span style="font-size:11px;color:var(--t3);">${p._acc}</span>
            <span style="font-size:11px;color:var(--t4);">·</span>
            <span style="font-size:11px;color:var(--t3);">Diposting: ${p.date||'—'}</span>
            ${pil.id!=='other'?`<span style="font-size:11px;color:var(--t4);">·</span><span style="font-size:11px;color:${pil.color};font-weight:700;">${pil.emoji} ${pil.label}</span>`:''}
          </div>
        </div>
      </div>
      <div class="timeline-card-actions">
        <button class="timeline-btn-edit" onclick="editFromTimeline('${p.id}','${p._plt}','${p._acc}','${p._mo.id}','${p._wk.id}')">✏ Edit</button>
        <button class="timeline-btn-del" onclick="deleteFromTimeline('${p.id}')">🗑 Hapus</button>
        ${p.link?`<a href="${p.link}" target="_blank" style="margin-left:auto;font-size:11px;font-weight:800;color:var(--accent);text-decoration:none;display:flex;align-items:center;gap:3px;">Detail Konten →</a>`:''}
      </div>
    </div>`;
  }

  // Paginate
  const totalFlat = posts.length;
  const pagedPosts = posts.slice(0, _timelinePage * TIMELINE_PAGE_SIZE);

  // Rebuild grouped from paged posts
  const pagedGrouped = {};
  const pagedNoDate = [];
  pagedPosts.forEach(p => {
    if (!p.date) { pagedNoDate.push(p); return; }
    const [y, m] = p.date.split('-');
    const key = `${y}-${m}`;
    if (!pagedGrouped[key]) pagedGrouped[key] = [];
    pagedGrouped[key].push(p);
  });
  const pagedGroupKeys = Object.keys(pagedGrouped).sort((a, b) => b.localeCompare(a));

  const MN_ID = ['Jan','Feb','Mar','Apr','Mei','Jun','Jul','Agu','Sep','Okt','Nov','Des'];
  function groupLabel(key) {
    if (key === '__nodate') return 'Tanpa Tanggal';
    const [y, m] = key.split('-');
    return `${MN_ID[parseInt(m,10)-1]} ${y}`;
  }

  let cardsHTML = '';
  pagedGroupKeys.forEach(key => {
    cardsHTML += `<div class="timeline-group-label">${groupLabel(key)} <span style="font-weight:400;opacity:.6;">(${grouped[key]?.length || pagedGrouped[key].length} post)</span></div>`;
    cardsHTML += pagedGrouped[key].map(postCard).join('');
  });
  if (pagedNoDate.length) {
    cardsHTML += `<div class="timeline-group-label">Tanpa Tanggal (${pagedNoDate.length} post)</div>`;
    cardsHTML += pagedNoDate.map(postCard).join('');
  }

  const hasMore = pagedPosts.length < totalFlat;
  const emptyState = totalFlat === 0 ? `<div class="timeline-empty">
    <div style="font-size:48px;opacity:.15;margin-bottom:16px;">📋</div>
    <div style="font-size:15px;font-weight:700;color:var(--t);margin-bottom:8px;">
      ${Object.values(_timelineFilters).some(v=>v&&v!=='all') ? 'Tidak ada post yang cocok dengan filter.' : 'Belum ada konten tercatat.'}
    </div>
    <div style="font-size:12px;color:var(--t3);margin-bottom:20px;">
      ${Object.values(_timelineFilters).some(v=>v&&v!=='all') ?
        'Coba reset filter untuk melihat semua post.' :
        'Catat post pertama kamu — cukup 30 detik.'}
    </div>
    ${Object.values(_timelineFilters).some(v=>v&&v!=='all') ?
      `<button onclick="_timelineFilters={plt:'',acc:'',pillar:'',period:'all',_search:''};_timelinePage=1;renderContentTimeline()" style="padding:9px 22px;border-radius:9px;border:1px solid var(--b2);background:none;color:var(--t2);font-size:12px;font-weight:700;cursor:pointer;">↺ Reset Filter</button>` :
      `<button onclick="openQCModal()" style="padding:10px 24px;border-radius:10px;border:none;background:var(--accent);color:#000;font-size:13px;font-weight:800;cursor:pointer;">+ Catat Post Pertama</button>`}
  </div>` : '';

  wrap.innerHTML = `
    ${chipFilter}
    ${filterBar}
    <div class="timeline-wrap">
      ${emptyState}
      ${cardsHTML}
      ${hasMore ? `<div style="padding:12px 16px;">
        <button class="timeline-load-more" onclick="_timelinePage++;renderContentTimeline()">
          Tampilkan lebih banyak (${totalFlat - pagedPosts.length} tersisa)
        </button>
      </div>` : ''}
    </div>`;
}

function editFromTimeline(pid, plt, acc, mid, wid) {
  // Set context before opening
  sel.mid = mid; sel.wid = wid; sel.acc = acc; sel.plt = plt;
  window._qcEditId = pid;
  window._qcEditPlt = plt;
  openQCModal();
  setTimeout(() => {
    window._qcEditId = pid;
    window._qcEditPlt = plt;
    const wk = getW(mid, wid);
    if (!wk) return;
    const d = aD(wk, acc);
    const p = (d[plt + 'Posts'] || []).find(x => x.id === pid);
    if (!p) return;
    const sv = (id, val) => { const el = document.getElementById(id); if (el) el.value = val ?? ''; };
    sv('qc-name', p.name); sv('qc-date', p.date); sv('qc-link', p.link||'');
    sv('qc-like', p.like || 0); sv('qc-comment', p.comment || 0);
    sv('qc-share', p.share || 0); sv('qc-save', p.save || 0);
    sv('qc-reach', p.reach || 0); sv('qc-impression', p.impression || 0);
    const ps = document.getElementById('qc-pillar'); if (ps && p.pillar) ps.value = p.pillar;
    const fs = document.getElementById('qc-format'); if (fs && p.format) fs.value = p.format;
    // Highlight correct platform button
    document.querySelectorAll('#qc-plt-row .qc-plt-btn').forEach(b => b.classList.remove('act'));
    document.querySelector(`#qc-plt-row .qc-plt-btn[onclick*="${plt}"]`)?.classList.add('act');
    // Update modal title and save button
    const titleEl = document.querySelector('#m-qc .modal-title');
    if (titleEl) titleEl.textContent = '✏️ Edit Konten';
    const saveBtn = document.getElementById('qc-save-btn');
    if (saveBtn) saveBtn.textContent = '💾 Update Post';
  }, 80);
}

function deleteFromTimeline(pid) {
  showConfirm('Hapus Post Ini?', 'Data engagement dan metrik post ini akan hilang permanen.', () => {
    // Soft-delete: stash the post first for potential undo
    const stashed = _stashPost(pid);
    deletePostById(pid);
    renderContentTimeline();
    if (view === 'dash') renderDashboard();
    _qcToastUndo('🗑 Post dihapus.', () => {
      if (stashed) { _restorePost(stashed); renderContentTimeline(); if(view==='dash')renderDashboard(); }
    });
  }, true);
}


/* ══ REVISI 2: First Insight Modal ══ */
const SEED_INSIGHTS_FI = {
  'tt_edukasi':    'Konten edukasi di TikTok perform terbaik Selasa–Kamis, jam 18:00–21:00. Hook 3 detik pertama menentukan segalanya.',
  'tt_hiburan':    'Konten hiburan di TikTok spike di akhir pekan — Jumat malam sampai Minggu. Sound trending bisa 2x boost reach.',
  'tt_lifestyle':  'Lifestyle content di TikTok perform baik di pagi hari (6–9) dan malam (19–22). Format video pendek < 30 detik biasanya menang.',
  'tt_bisnis':     'Konten bisnis di TikTok paling engaging kalau pakai storytelling — cerita gagal/berhasil lebih menarik dari tips generik.',
  'ig_edukasi':    'Carousel edukatif di Instagram rata-rata punya save rate 2x lebih tinggi dari single image. Slide 1 harus jadi hook.',
  'ig_lifestyle':  'Konten lifestyle di Instagram lebih banyak di-save dibanding di-share. Fokus ke visual berkualitas tinggi.',
  'ig_hiburan':    'Reels hiburan di Instagram perform terbaik Jumat–Minggu. Caption pendek + CTA di akhir meningkatkan komentar.',
  'ig_bisnis':     'Infografis bisnis di Instagram punya save rate tinggi. Pastikan font besar dan bisa dibaca tanpa zoom.',
  'default':       'Konsistensi posting lebih penting dari viral sesekali. Target minimal 3 post per minggu untuk menjaga momentum algoritma.',
};

function getSeedInsightFI(plt, pillar) {
  const key = `${plt}_${pillar?.id || ''}`;
  return SEED_INSIGHTS_FI[key] || SEED_INSIGHTS_FI['default'];
}

function showFirstInsightModal(post) {
  const pil = pillarOf(post.pillar || '');
  const tip = getSeedInsightFI(post._plt || 'tt', pil);
  const need = Math.max(0, 5 - getAllPosts().length);
  const tipEl = document.getElementById('fi-tip-text');
  const needEl = document.getElementById('fi-need-count');
  if (tipEl) tipEl.textContent = tip;
  if (needEl) needEl.textContent = need;
  document.getElementById('fi-modal')?.classList.remove('hidden');
}

function closeFiModal() {
  document.getElementById('fi-modal')?.classList.add('hidden');
}

/* ══ REVISI 5: Retention Reminder Banner ══ */
function checkRetentionReminder() {
  const allPosts = getAllPosts();
  if (!allPosts.length) return;
  try {
    const lastInput = localStorage.getItem('cl-last-input');
    if (!lastInput) return;
    const daysSince = Math.floor((new Date() - new Date(lastInput)) / (1000*60*60*24));
    if (daysSince >= 7) showRetentionBanner(daysSince);
  } catch(e) {}
}

function showRetentionBanner(days) {
  // Avoid duplicate
  if (document.getElementById('retention-banner')) return;
  const banner = document.createElement('div');
  banner.id = 'retention-banner';
  banner.className = 'retention-banner';
  banner.innerHTML = `
    <span style="font-size:20px;">📅</span>
    <div class="retention-banner-text">
      <strong>Sudah ${days} hari sejak input terakhir.</strong><br>
      Data belum terupdate — insight mungkin tidak akurat.
    </div>
    <button class="retention-banner-btn" onclick="openQCModal();dismissRetentionBanner()">+ Input Sekarang</button>
    <button class="retention-banner-x" onclick="dismissRetentionBanner()" title="Tutup">×</button>
  `;
  const dashBody = document.getElementById('dash-body');
  if (dashBody) dashBody.prepend(banner);
}

function dismissRetentionBanner() {
  document.getElementById('retention-banner')?.remove();
  try { localStorage.setItem('cl-last-input', new Date().toISOString()); } catch(e) {}
}

/* ══ REVISI 7: Smart Save Toast ══ */
function smartSaveToast(post, res) {
  // Build location string: "Mei 2026 · Minggu 2"
  let location = '';
  if (res && res.mo && res.wk) {
    const wkNum = res.wk.label ? res.wk.label.replace(/^Minggu\s*/i,'W') : 'W?';
    location = ` → ${MN[res.mo.month]} ${res.mo.year} · ${wkNum}`;
  }
  if (!post) { _qcToast('✅ Post disimpan' + location + '!'); return; }
  const er = calcER(post, sel.erMode, post._flw || 0);
  const lvl = erLevel(er);
  if (er > 0) {
    const score = computeContentScore(post, getAllPosts());
    const sg = scoreGrade(score);
    _qcToast(`✅ Disimpan${location} · ER ${erStr(er)} · Grade ${sg.grade}`);
  } else {
    _qcToast(`✅ Disimpan${location}. Tambah angka engagement untuk insight.`);
  }
}


/* ══ WEEKLY INTELLIGENCE SUMMARY (dashboard hero narrative) ══ */
function buildWeeklyNarrative(allPosts, currStats, prevStats, insights) {
  if (!allPosts.length) return null;
  const now = new Date();
  const monthName = ['Jan','Feb','Mar','Apr','Mei','Jun','Jul','Agu','Sep','Okt','Nov','Des'][now.getMonth()];

  // Pick the most impactful insight for the headline
  const topInsight = insights.find(i => i.priority <= 1) || insights[0];

  // Month-over-month narrative
  let trendLine = '';
  if (prevStats && prevStats.reach > 0) {
    const delta = ((currStats.reach - prevStats.reach) / prevStats.reach) * 100;
    if (delta >= 10) trendLine = `Bulan ${monthName} tumbuh <strong>${delta.toFixed(0)}%</strong> dari bulan lalu.`;
    else if (delta >= 2) trendLine = `Bulan ${monthName} sedikit naik dari bulan lalu (+${delta.toFixed(0)}%).`;
    else if (delta <= -10) trendLine = `Bulan ${monthName} turun ${Math.abs(delta).toFixed(0)}% dari bulan lalu — saatnya evaluasi.`;
    else trendLine = `Performa bulan ${monthName} relatif stabil dibanding bulan lalu.`;
  }

  const headline = topInsight ? topInsight.message : trendLine;
  return headline || null;
}


/* ════════════════════════════════════════════════════
   CONTENT PLANNER — IDE & BRIEF
   ════════════════════════════════════════════════════ */

function genId(prefix=''){return prefix+(Date.now().toString(36)+Math.random().toString(36).slice(2,6)).toUpperCase();}

// ── Open/Close idea modal ──
function openIdeaModal(id=null){
  const isEdit=!!id;
  document.getElementById('m-idea-title').textContent=isEdit?'Edit Konten':'Content Planner Baru';
  document.getElementById('m-idea-id').value=id||'';
  ideaTab('info');

  // Populate platform select
  const pltSel=document.getElementById('idea-inp-plt');
  pltSel.innerHTML='<option value="">Semua Platform</option>'+allPlatforms().map(p=>`<option value="${p.id}">${p.emoji} ${p.name}</option>`).join('');

  // Populate pillar select
  const pilSel=document.getElementById('idea-inp-pillar');
  pilSel.innerHTML='<option value="">Pilih pilar...</option>'+pillars.map(p=>`<option value="${p.id}">${p.emoji} ${p.label}</option>`).join('');

  if(isEdit){
    const idea=contentIdeas.find(i=>i.id===id);
    if(!idea)return;
    document.getElementById('idea-inp-title').value=idea.title||'';
    document.getElementById('idea-inp-desc').value=idea.desc||'';
    document.getElementById('idea-inp-plt').value=idea.platform||'';
    document.getElementById('idea-inp-format').value=idea.format||'';
    document.getElementById('idea-inp-pillar').value=idea.pillar||'';
    document.getElementById('idea-inp-priority').value=idea.priority||'med';
    document.getElementById('idea-inp-status').value=idea.status||'idea';
    document.getElementById('idea-inp-date').value=idea.targetDate||'';
    document.getElementById('idea-inp-tags').value=(idea.tags||[]).join(', ');
    const b=idea.brief||{};
    _ideaRefLinks=(idea.refLinks||[]).slice();renderIdeaRefLinks();
    document.getElementById('idea-inp-hook').value=b.hook||'';
    document.getElementById('idea-inp-script').value=b.script||'';
    document.getElementById('idea-inp-cta').value=b.cta||'';
    document.getElementById('idea-inp-ref').value=b.ref||'';
    document.getElementById('idea-inp-notes').value=b.notes||'';
  } else {
    ['title','desc'].forEach(f=>{const el=document.getElementById('idea-inp-'+f);if(el)el.value='';});
    _ideaRefLinks=[];renderIdeaRefLinks();
    ['hook','script','cta','ref','notes'].forEach(f=>{const el=document.getElementById('idea-inp-'+f);if(el)el.value='';});
    const _prio=document.getElementById('idea-inp-priority');if(_prio)_prio.value='med';
    const _stat=document.getElementById('idea-inp-status');if(_stat)_stat.value='idea';
    const _date=document.getElementById('idea-inp-date');if(_date)_date.value='';
    const _plt=document.getElementById('idea-inp-plt');if(_plt)_plt.value='';
    const _pil=document.getElementById('idea-inp-pillar');if(_pil)_pil.value='';
    const _fmt=document.getElementById('idea-inp-format');if(_fmt)_fmt.value='';
  }
  openModal('m-idea');
}

function ideaTab(tab){
  document.getElementById('itab-info-panel').style.display=tab==='info'?'':'none';
  document.getElementById('itab-brief-panel').style.display=tab==='brief'?'':'none';
  ['info','brief'].forEach(t=>{
    const btn=document.getElementById('itab-'+t);
    btn.style.background=t===tab?'var(--surf)':'none';
    btn.style.color=t===tab?'var(--t)':'var(--t3)';
  });
}

// ── Idea Ref Links ──
let _ideaRefLinks=[];

function renderIdeaRefLinks(){
  const container=document.getElementById('idea-reflinks-list');
  if(!container)return;
  if(!_ideaRefLinks.length){container.innerHTML='';return;}
  container.innerHTML=_ideaRefLinks.map((url,i)=>{
    let label=url;
    try{const u=new URL(url);label=u.hostname.replace('www.','')+u.pathname.slice(0,30)+(u.pathname.length>30?'...':'');}catch(e){}
    const platform=getPlatformFromUrl(url);
    return `<div style="display:flex;align-items:center;gap:8px;padding:8px 12px;background:var(--surf2);border:1px solid var(--b);border-radius:9px;">
      <span style="font-size:14px;">${platform.emoji}</span>
      <a href="${url}" target="_blank" onclick="event.stopPropagation()" style="flex:1;font-size:11px;font-weight:600;color:var(--accent);text-overflow:ellipsis;overflow:hidden;white-space:nowrap;min-width:0;" title="${url}">${label}</a>
      <button onclick="_ideaRefLinks.splice(${i},1);renderIdeaRefLinks()" style="width:20px;height:20px;border-radius:5px;border:none;background:none;color:var(--t4);font-size:12px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;" onmouseover="this.style.color='var(--red)'" onmouseout="this.style.color='var(--t4)'">✕</button>
    </div>`;
  }).join('');
}

function getPlatformFromUrl(url){
  const u=url.toLowerCase();
  if(u.includes('tiktok.com'))return{emoji:'📱',name:'TikTok'};
  if(u.includes('instagram.com'))return{emoji:'📸',name:'Instagram'};
  if(u.includes('youtube.com')||u.includes('youtu.be'))return{emoji:'▶️',name:'YouTube'};
  if(u.includes('twitter.com')||u.includes('x.com'))return{emoji:'🐦',name:'X/Twitter'};
  if(u.includes('facebook.com')||u.includes('fb.com'))return{emoji:'👤',name:'Facebook'};
  if(u.includes('linkedin.com'))return{emoji:'💼',name:'LinkedIn'};
  return{emoji:'🔗',name:'Link'};
}

function renderIdeaRefLinkChips(links, marginTop){
  if(!links||!links.length) return '';
  const chips = links.map(function(url){
    var label = '🔗 Link';
    try {
      var u = new URL(url);
      var host = u.hostname.replace('www.','');
      if(host.includes('tiktok')) label = '📱 TikTok';
      else if(host.includes('instagram')) label = '📸 IG';
      else if(host.includes('youtube') || url.includes('youtu.be')) label = '▶️ YT';
      else if(host.includes('twitter') || host.includes('x.com')) label = '🐦 X';
      else if(host.includes('facebook') || host.includes('fb.com')) label = '👤 FB';
      else if(host.includes('linkedin')) label = '💼 LI';
      else label = '🔗 ' + host.split('.')[0];
    } catch(e) {}
    return '<a href="' + url + '" target="_blank" onclick="event.stopPropagation()" style="font-size:11px;font-weight:700;padding:2px 8px;border-radius:20px;background:var(--accent-dim);color:var(--accent);text-decoration:none;white-space:nowrap;" title="' + url + '">' + label + '</a>';
  });
  return '<div style="display:flex;flex-wrap:wrap;gap:4px;margin-top:' + (marginTop||'6px') + ';">' + chips.join('') + '</div>';
}

function addIdeaRefLink(){
  const inp=document.getElementById('idea-inp-reflink-new');
  const val=(inp.value||'').trim();
  if(!val)return;
  let url=val;
  if(!/^https?:\/\//i.test(url))url='https://'+url;
  if(_ideaRefLinks.includes(url)){inp.value='';return;}
  _ideaRefLinks.push(url);
  inp.value='';
  renderIdeaRefLinks();
}

// Allow pressing Enter in the URL input to add
document.addEventListener('DOMContentLoaded',()=>{
  const inp=document.getElementById('idea-inp-reflink-new');
  if(inp)inp.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();addIdeaRefLink();}});
});

function saveIdea(){
  const title=document.getElementById('idea-inp-title').value.trim();
  if(!title){_qcToast('⚠️ Judul konten wajib diisi!');return;}
  const id=document.getElementById('m-idea-id').value||genId('IDEA-');
  const tagsRaw=document.getElementById('idea-inp-tags').value;
  const tags=tagsRaw?tagsRaw.split(',').map(t=>t.trim()).filter(Boolean):[];
  const idea={
    id,title,
    desc:document.getElementById('idea-inp-desc').value.trim(),
    platform:document.getElementById('idea-inp-plt').value,
    format:document.getElementById('idea-inp-format').value,
    pillar:document.getElementById('idea-inp-pillar').value,
    priority:document.getElementById('idea-inp-priority').value,
    status:(()=>{
      const briefPanel=document.getElementById('itab-brief-panel');
      const briefActive=briefPanel&&briefPanel.style.display!=='none';
      const briefHasData=!!(document.getElementById('idea-inp-hook')?.value||document.getElementById('idea-inp-script')?.value);
      return (briefActive&&briefHasData)?'brief':(document.getElementById('idea-inp-status')?.value||'idea');
    })(),
    targetDate:document.getElementById('idea-inp-date').value,
    tags,
    createdAt:new Date().toISOString(),
    refLinks:_ideaRefLinks.slice(),
    brief:{
      hook:document.getElementById('idea-inp-hook').value.trim(),
      script:document.getElementById('idea-inp-script').value.trim(),
      cta:document.getElementById('idea-inp-cta').value.trim(),
      ref:document.getElementById('idea-inp-ref').value.trim(),
      notes:document.getElementById('idea-inp-notes').value.trim(),
    }
  };
  const idx=contentIdeas.findIndex(i=>i.id===id);
  if(idx>=0)contentIdeas[idx]=idea;
  else contentIdeas.unshift(idea);
  scheduleSave(300);
  closeModal('m-idea');
  updatePlannerBadge();
  if(view==='planner')renderPlanner();
}

function deleteIdea(id){
  const _idea=contentIdeas.find(i=>i.id===id);
  showConfirm('Hapus Ide Konten?',`"${_idea?_idea.title:'Ide ini'}" akan dihapus permanen.`,()=>{
    contentIdeas=contentIdeas.filter(i=>i.id!==id);
    scheduleSave(300);
    updatePlannerBadge();
    renderPlanner();
  },true);
}

function ideaToCalendar(){
  const title=document.getElementById('idea-inp-title').value.trim();
  if(!title){_qcToast('⚠️ Isi judul dulu!');return;}
  saveIdea();
  const id=document.getElementById('m-idea-id').value;
  closeModal('m-idea');
  openCalEventModal(null, {
    title,
    platform:document.getElementById('idea-inp-plt').value,
    format:document.getElementById('idea-inp-format').value,
    pillar:document.getElementById('idea-inp-pillar').value,
    ideaId:id,
  });
}

function updatePlannerBadge(){
  const badge=document.getElementById('nav-planner-badge');
  if(!badge)return;
  const n=contentIdeas.length;
  badge.textContent=n>0?n:'';
}

// ── Render Planner ──
function renderPlanner(){
  const body=document.getElementById('planner-body');
  if(!body)return;
  updatePlannerBadge();

  const statusGroups=[
    {key:'idea',label:'💡 Ide',cls:'status-idea-badge'},
    {key:'brief',label:'📋 Content Brief',cls:'status-brief-badge'},
  ];

  function priorityLabel(p){return p==='high'?'🔴 Tinggi':p==='med'?'🟡 Sedang':'🟢 Rendah';}
  function priorityCls(p){return p==='high'?'priority-high':p==='med'?'priority-med':'priority-low';}
  function briefComplete(idea){const b=idea.brief||{};return !!(b.hook&&b.script);}
  function pltLabel(id){const p=allPlatforms().find(pl=>pl.id===id);return p?p.emoji+' '+p.name:'';}
  function pilLabel(id){const p=pillars.find(pl=>pl.id===id);return p?p.emoji+' '+p.label:'';}
  const fmtIcons={video:'🎬',reels:'📱',carousel:'🎠',photo:'📸',story:'⭕',live:'🔴'};

  // Filter
  let ideas=[...contentIdeas];
  if(ideaFilter.platform)ideas=ideas.filter(i=>i.platform===ideaFilter.platform);
  if(ideaFilter.pillar)ideas=ideas.filter(i=>i.pillar===ideaFilter.pillar);
  if(ideaFilter.status)ideas=ideas.filter(i=>i.status===ideaFilter.status);
  if(ideaFilter.priority)ideas=ideas.filter(i=>i.priority===ideaFilter.priority);

  const filterBar=`
    <div class="planner-filter-bar">
      <div class="planner-seg">
        <button class="planner-seg-btn ${plannerView==='board'?'act':''}" onclick="plannerView='board';renderPlanner()">⊞ Board</button>
        <button class="planner-seg-btn ${plannerView==='list'?'act':''}" onclick="plannerView='list';renderPlanner()">☰ List</button>
      </div>
      <select class="qc-inp" style="padding:5px 10px;font-size:12px;" onchange="ideaFilter.platform=this.value;renderPlanner()">
        <option value="">Semua Platform</option>${allPlatforms().map(p=>`<option value="${p.id}" ${ideaFilter.platform===p.id?'selected':''}>${p.emoji} ${p.name}</option>`).join('')}
      </select>
      <select class="qc-inp" style="padding:5px 10px;font-size:12px;" onchange="ideaFilter.pillar=this.value;renderPlanner()">
        <option value="">Semua Pilar</option>${pillars.map(p=>`<option value="${p.id}" ${ideaFilter.pillar===p.id?'selected':''}>${p.emoji} ${p.label}</option>`).join('')}
      </select>
      <select class="qc-inp" style="padding:5px 10px;font-size:12px;" onchange="ideaFilter.priority=this.value;renderPlanner()">
        <option value="">Semua Prioritas</option><option value="high" ${ideaFilter.priority==='high'?'selected':''}>🔴 Tinggi</option><option value="med" ${ideaFilter.priority==='med'?'selected':''}>🟡 Sedang</option><option value="low" ${ideaFilter.priority==='low'?'selected':''}>🟢 Rendah</option>
      </select>
      ${Object.values(ideaFilter).some(Boolean)?`<button onclick="ideaFilter={platform:'',pillar:'',status:'',priority:''};renderPlanner()" style="padding:5px 12px;border-radius:8px;border:1px solid var(--b2);background:none;color:var(--t3);font-size:11px;cursor:pointer;font-family:var(--font);">↺ Reset</button>`:''}
    </div>`;

  let contentHTML='';
  if(plannerView==='board'){
    const cols=statusGroups.map(sg=>{
      const colIdeas=ideas.filter(i=>i.status===sg.key);
      const cards=colIdeas.length?colIdeas.map(idea=>{
        const hasBrief=briefComplete(idea);
        return `<div class="idea-card" onclick="openIdeaModal('${idea.id}')">
          <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:6px;margin-bottom:6px;">
            <div class="idea-card-title">${idea.title}</div>
            <span class="idea-card-tag ${priorityCls(idea.priority)}" style="flex-shrink:0;">${idea.priority==='high'?'🔴':idea.priority==='med'?'🟡':'🟢'}</span>
          </div>
          <div class="idea-card-meta">
            ${idea.platform?`<span class="idea-card-tag" style="background:var(--surf2);color:var(--t3);">${pltLabel(idea.platform)}</span>`:''}
            ${idea.format?`<span class="idea-card-tag" style="background:var(--surf2);color:var(--t3);">${fmtIcons[idea.format]||''} ${idea.format}</span>`:''}
            ${idea.pillar?`<span class="idea-card-tag" style="background:var(--surf2);color:var(--t3);">${pilLabel(idea.pillar)}</span>`:''}
          </div>
          ${idea.desc?`<div style="font-size:11px;color:var(--t3);margin-bottom:6px;overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;">${idea.desc}</div>`:''}
          ${idea.targetDate?`<div style="font-size:10px;color:var(--t3);">📅 ${idea.targetDate}</div>`:''}
          ${renderIdeaRefLinkChips(idea.refLinks,"6px")}
          <div class="idea-card-actions" onclick="event.stopPropagation()">
            <button class="idea-card-btn primary" onclick="openCalEventModal(null,{title:'${idea.title.replace(/'/g,"\'")}',platform:'${idea.platform||''}',format:'${idea.format||''}',pillar:'${idea.pillar||''}',ideaId:'${idea.id}'})">📅 Jadwal</button>
            ${!hasBrief?`<button class="idea-card-btn" onclick="openIdeaModal('${idea.id}');setTimeout(()=>ideaTab('brief'),100)">📋 Brief</button>`:`<span style="font-size:11px;color:var(--green);font-weight:700;padding:4px 8px;">✓ Brief</span>`}
            <button class="idea-card-btn" style="margin-left:auto;color:var(--red);border-color:rgba(239,68,68,.2);" onclick="deleteIdea('${idea.id}')">🗑</button>
          </div>
        </div>`;
      }).join(''):`<div style="text-align:center;padding:20px;color:var(--t4);font-size:12px;">Kosong</div>`;
      return `<div class="kanban-col">
        <div class="kanban-col-header">
          <span class="kanban-col-title">${sg.label}</span>
          <span class="kanban-col-count">${colIdeas.length}</span>
        </div>
        ${cards}
        <button onclick="openIdeaModal()" style="width:100%;margin-top:4px;padding:7px;border-radius:9px;border:1px dashed rgba(255,255,255,.1);background:none;color:var(--t4);font-size:12px;cursor:pointer;transition:all .12s;font-family:var(--font);" onmouseover="this.style.borderColor='rgba(99,102,241,.4)';this.style.color='#818CF8'" onmouseout="this.style.borderColor='rgba(255,255,255,.1)';this.style.color='var(--t4)'">+ Tambah Konten</button>
      </div>`;
    }).join('');
    contentHTML=`<div class="planner-kanban">${cols}</div>`;
  } else {
    // List view
    const rows=ideas.length?ideas.map(idea=>{
      const hasBrief=briefComplete(idea);
      const sg=statusGroups.find(s=>s.key===idea.status)||statusGroups[0];
      return `<div class="idea-list-item" onclick="openIdeaModal('${idea.id}')">
        <div style="width:36px;height:36px;border-radius:10px;background:rgba(99,102,241,.12);display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0;">${fmtIcons[idea.format]||'💡'}</div>
        <div style="flex:1;min-width:0;">
          <div style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin-bottom:3px;">
            <span style="font-size:13px;font-weight:700;color:var(--t);">${idea.title}</span>
            <span class="idea-card-tag ${sg.cls}" style="font-size:11px;">${sg.label}</span>
            <span class="idea-card-tag ${priorityCls(idea.priority)}" style="font-size:11px;">${priorityLabel(idea.priority)}</span>
            ${hasBrief?`<span style="font-size:11px;color:var(--green);font-weight:700;">✓ Brief</span>`:''}
          </div>
          <div style="display:flex;align-items:center;gap:8px;font-size:11px;color:var(--t3);">
            ${idea.platform?`<span>${pltLabel(idea.platform)}</span>`:''}
            ${idea.pillar?`<span>${pilLabel(idea.pillar)}</span>`:''}
            ${idea.targetDate?`<span>📅 ${idea.targetDate}</span>`:''}
            ${(idea.tags||[]).length?`<span>🏷 ${idea.tags.slice(0,3).join(', ')}</span>`:''}
          </div>
          ${renderIdeaRefLinkChips(idea.refLinks,"5px")}
        </div>
        <div style="display:flex;gap:4px;flex-shrink:0;" onclick="event.stopPropagation()">
          <button class="idea-card-btn primary" onclick="openCalEventModal(null,{title:'${idea.title.replace(/'/g,"\'")}',platform:'${idea.platform||''}',format:'${idea.format||''}',pillar:'${idea.pillar||''}',ideaId:'${idea.id}'})">📅</button>
          <button class="idea-card-btn" onclick="deleteIdea('${idea.id}')" style="color:var(--red);border-color:rgba(239,68,68,.2);">🗑</button>
        </div>
      </div>`;
    }).join(''):`<div style="text-align:center;padding:40px;color:var(--t3);">
      <div style="font-size:48px;margin-bottom:12px;opacity:.2;">💡</div>
      <div style="font-size:14px;font-weight:700;">Belum ada konten di planner</div>
      <div style="font-size:12px;margin-top:4px;">Klik "+ Tambah Konten" untuk mulai</div>
    </div>`;
    contentHTML=`<div>${rows}</div>`;
  }

  // Stats strip
  const total=contentIdeas.length;
  const byStatus=statusGroups.map(s=>({...s,n:contentIdeas.filter(i=>i.status===s.key).length}));
  const statsHTML=`<div style="display:flex;gap:12px;flex-wrap:wrap;margin-bottom:16px;">
    ${byStatus.map(s=>`<div style="background:var(--surf);border:1px solid var(--b);border-radius:10px;padding:10px 16px;text-align:center;">
      <div style="font-size:18px;font-weight:900;font-family:var(--mono);color:var(--t);">${s.n}</div>
      <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-top:2px;">${s.label}</div>
    </div>`).join('')}
  </div>`;

  body.innerHTML=`
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-title">Content Planner</div>
        <div class="view-subtitle">Simpan ide, link referensi, dan buat content brief untuk konten kamu</div>
      </div>
      <div class="view-header-right">
        <button class="view-header-btn" onclick="openIdeaModal()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
          + Tambah Konten
        </button>
      </div>
    </div>
    ${statsHTML}
    ${filterBar}
    ${contentHTML}
  `;
}

/* ════════════════════════════════════════════════════
   CONTENT CALENDAR
   ════════════════════════════════════════════════════ */

function openCalEventModal(id=null, prefill={}){
  document.getElementById('m-calevent-title').textContent=id?'Edit Jadwal':'Jadwal Konten Baru';
  document.getElementById('m-calevent-id').value=id||'';
  document.getElementById('m-calevent-ideaid').value=prefill.ideaId||'';

  // Populate selects
  const pltSel=document.getElementById('calevent-inp-plt');
  pltSel.innerHTML='<option value="">Pilih platform...</option>'+allPlatforms().map(p=>`<option value="${p.id}">${p.emoji} ${p.name}</option>`).join('');
  const accSel=document.getElementById('calevent-inp-acc');
  accSel.innerHTML='<option value="">Pilih akun...</option>'+accounts.map(a=>`<option value="${a}">${a}</option>`).join('');
  const pilSel=document.getElementById('calevent-inp-pillar');
  pilSel.innerHTML='<option value="">Pilih pilar...</option>'+pillars.map(p=>`<option value="${p.id}">${p.emoji} ${p.label}</option>`).join('');

  if(id){
    const ev=calendarPosts.find(e=>e.id===id);
    if(!ev)return;
    document.getElementById('calevent-inp-title').value=ev.title||'';
    document.getElementById('calevent-inp-plt').value=ev.platform||'';
    document.getElementById('calevent-inp-acc').value=ev.account||'';
    document.getElementById('calevent-inp-date').value=ev.scheduledDate||'';
    document.getElementById('calevent-inp-time').value=ev.scheduledTime||'19:00';
    document.getElementById('calevent-inp-format').value=ev.format||'video';
    document.getElementById('calevent-inp-status').value=ev.status||'idea';
    document.getElementById('calevent-inp-pillar').value=ev.pillar||'';
    document.getElementById('calevent-inp-notes').value=ev.notes||'';
    document.getElementById('calevent-inp-brief').value=ev.brief||'';
    document.getElementById('calevent-inp-hook').value=ev.hook||'';
  } else {
    document.getElementById('calevent-inp-title').value=prefill.title||'';
    document.getElementById('calevent-inp-plt').value=prefill.platform||'';
    document.getElementById('calevent-inp-format').value=prefill.format||'video';
    document.getElementById('calevent-inp-pillar').value=prefill.pillar||'';
    document.getElementById('calevent-inp-acc').value='';
    document.getElementById('calevent-inp-date').value=prefill.date||'';
    document.getElementById('calevent-inp-time').value='19:00';
    document.getElementById('calevent-inp-status').value='idea';
    document.getElementById('calevent-inp-notes').value='';
    document.getElementById('calevent-inp-brief').value='';
    document.getElementById('calevent-inp-hook').value='';
  }
  openModal('m-calevent');
}

function saveCalEvent(){
  const title=document.getElementById('calevent-inp-title').value.trim();
  if(!title){_qcToast('⚠️ Judul wajib diisi!');return;}
  const id=document.getElementById('m-calevent-id').value||genId('CAL-');
  const ev={
    id,title,
    platform:document.getElementById('calevent-inp-plt').value,
    account:document.getElementById('calevent-inp-acc').value,
    scheduledDate:document.getElementById('calevent-inp-date').value,
    scheduledTime:document.getElementById('calevent-inp-time').value,
    format:document.getElementById('calevent-inp-format').value,
    status:document.getElementById('calevent-inp-status').value,
    pillar:document.getElementById('calevent-inp-pillar').value,
    notes:document.getElementById('calevent-inp-notes').value.trim(),
    brief:document.getElementById('calevent-inp-brief').value.trim(),
    hook:document.getElementById('calevent-inp-hook').value.trim(),
    ideaId:document.getElementById('m-calevent-ideaid').value,
    createdAt:new Date().toISOString(),
  };
  const idx=calendarPosts.findIndex(e=>e.id===id);
  if(idx>=0)calendarPosts[idx]=ev;
  else calendarPosts.push(ev);
  scheduleSave(300);
  closeModal('m-calevent');
  if(view==='calendar')renderCalendar();
}

function deleteCalEvent(id){
  const _ev=calendarPosts.find(e=>e.id===id);
  showConfirm('Hapus Jadwal?',`"${_ev?_ev.title:'Jadwal ini'}" akan dihapus dari kalender.`,()=>{
    calendarPosts=calendarPosts.filter(e=>e.id!==id);
    scheduleSave(300);
    renderCalendar();
  },true);
}

function updateCalEventStatus(id, status){
  const ev=calendarPosts.find(e=>e.id===id);
  if(!ev)return;
  ev.status=status;
  scheduleSave(200);
  renderCalendar();
}

// ── Render Calendar ──
function renderCalendar(){
  const body=document.getElementById('calendar-body');
  if(!body)return;

  const now=calendarDate;
  const y=now.getFullYear(),m=now.getMonth();
  const firstDay=new Date(y,m,1).getDay(); // 0=Sun
  const daysInMonth=new Date(y,m+1,0).getDate();
  const today=new Date();
  const MN_ID=['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'];
  const DAY_NAMES=['Min','Sen','Sel','Rab','Kam','Jum','Sab'];
  const STATUS_ORDER={idea:0,in_progress:1};
  const STATUS_LABELS={idea:'💡 Ide',in_progress:'📋 Content Brief'};
  const fmtIcons={video:'🎬',reels:'📱',carousel:'🎠',photo:'📸',story:'⭕',live:'🔴'};

  // Stats
  const monthStr=`${y}-${String(m+1).padStart(2,'0')}`;
  const monthEvents=calendarPosts.filter(e=>(e.scheduledDate||'').startsWith(monthStr));
  const byStatus={idea:0,in_progress:0};
  monthEvents.forEach(e=>{if(byStatus[e.status]!==undefined)byStatus[e.status]++;});

  const statsHTML=`<div style="display:flex;gap:10px;flex-wrap:wrap;margin-bottom:20px;">
    ${Object.entries(STATUS_LABELS).map(([k,l])=>`<div style="display:flex;align-items:center;gap:6px;background:var(--surf);border:1px solid var(--b);border-radius:10px;padding:8px 14px;">
      <div style="font-size:16px;font-weight:900;font-family:var(--mono);color:var(--t);">${byStatus[k]}</div>
      <div style="font-size:10px;color:var(--t3);font-weight:700;">${l}</div>
    </div>`).join('')}
  </div>`;

  // Build calendar grid
  const dayHeaders=DAY_NAMES.map(d=>`<div class="cal-header-day">${d}</div>`).join('');
  let cells='';
  // Empty cells for first week
  for(let i=0;i<firstDay;i++){
    cells+=`<div class="cal-cell other-month"></div>`;
  }
  // Day cells
  for(let d=1;d<=daysInMonth;d++){
    const dateStr=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
    const isToday=today.getFullYear()===y&&today.getMonth()===m&&today.getDate()===d;
    const dayEvents=calendarPosts.filter(e=>e.scheduledDate===dateStr).sort((a,b)=>(STATUS_ORDER[a.status]||0)-(STATUS_ORDER[b.status]||0));
    const plt_info=(id)=>{const p=allPlatforms().find(pl=>pl.id===id);return p?p.emoji:'';};
    const evHTML=dayEvents.slice(0,3).map(ev=>{
      const pil=pillarOf(ev.pillar||'other');
      const pilDot=ev.pillar?`<span style="display:inline-block;width:6px;height:6px;border-radius:50%;background:${pil.color||'var(--accent)'};flex-shrink:0;margin-right:3px;"></span>`:'';
      return`<div class="cal-event cal-event-${ev.status}" onclick="event.stopPropagation();openCalEventModal('${ev.id}')" title="${ev.title}${ev.brief?'\n📋 '+ev.brief.slice(0,60):''}${ev.hook?'\n🪝 '+ev.hook.slice(0,60):''}">${pilDot}${plt_info(ev.platform)} ${ev.title}</div>`;
    }).join('');
    const moreHTML=dayEvents.length>3?`<div style="font-size:11px;color:var(--t3);padding:1px 4px;">+${dayEvents.length-3} lagi</div>`:'';
    cells+=`<div class="cal-cell${isToday?' today':''}" onclick="openCalEventModal(null,{date:'${dateStr}'})">
      <div class="cal-cell-date">${d}</div>
      ${evHTML}${moreHTML}
      ${dayEvents.length===0?`<div class="cal-add-btn">+</div>`:''}
    </div>`;
  }
  // Fill remaining
  const totalCells=firstDay+daysInMonth;
  const remaining=(7-totalCells%7)%7;
  for(let i=0;i<remaining;i++)cells+=`<div class="cal-cell other-month"></div>`;

  // Upcoming posts list
  const upcomingDate=new Date();upcomingDate.setHours(0,0,0,0);
  const upcoming=calendarPosts
    .filter(e=>e.scheduledDate&&new Date(e.scheduledDate)>=upcomingDate&&e.status!=='posted')
    .sort((a,b)=>a.scheduledDate.localeCompare(b.scheduledDate))
    .slice(0,8);

  const upcomingHTML=upcoming.length?`
    <div style="margin-top:28px;">
      <div style="font-size:14px;font-weight:800;color:var(--t);margin-bottom:12px;">📌 Jadwal Mendatang</div>
      ${upcoming.map(ev=>{
        const pltInfo=allPlatforms().find(p=>p.id===ev.platform);
        const d=new Date(ev.scheduledDate);
        const dateLabel=`${d.getDate()} ${MN_ID[d.getMonth()]} ${d.getFullYear()}${ev.scheduledTime?' · '+ev.scheduledTime:''}`;
        return `<div style="display:flex;align-items:flex-start;gap:12px;padding:12px 16px;background:var(--surf);border:1px solid var(--b);border-radius:12px;margin-bottom:8px;cursor:pointer;" onclick="openCalEventModal('${ev.id}')">
          <div style="text-align:center;min-width:40px;">
            <div style="font-size:18px;font-weight:900;font-family:var(--mono);color:var(--t);">${d.getDate()}</div>
            <div style="font-size:11px;color:var(--t3);font-weight:700;">${MN_ID[d.getMonth()].slice(0,3).toUpperCase()}</div>
          </div>
          <div style="flex:1;min-width:0;">
            <div style="font-size:13px;font-weight:700;color:var(--t);margin-bottom:3px;">${ev.title}</div>
            <div style="display:flex;align-items:center;gap:6px;font-size:11px;color:var(--t3);flex-wrap:wrap;">
              ${pltInfo?`<span>${pltInfo.emoji} ${pltInfo.name}</span>`:''}
              ${ev.account?`<span>· ${ev.account}</span>`:''}
              ${ev.scheduledTime?`<span>· ⏰ ${ev.scheduledTime}</span>`:''}
              ${ev.pillar?(()=>{const pil=pillarOf(ev.pillar);return`<span style="display:inline-flex;align-items:center;gap:3px;padding:1px 7px;border-radius:20px;background:${pil.color||'var(--accent)'}22;color:${pil.color||'var(--accent)'};font-size:10px;font-weight:700;">${pil.emoji} ${pil.label}</span>`;})():''}
            </div>
            ${ev.brief?`<div style="font-size:11px;color:var(--t3);margin-top:5px;padding:6px 8px;background:var(--surf2);border-radius:7px;border-left:2px solid var(--b2);line-height:1.5;">📋 ${ev.brief.slice(0,120)}${ev.brief.length>120?'…':''}</div>`:''}
            ${ev.hook?`<div style="font-size:11px;color:var(--t3);margin-top:4px;padding:6px 8px;background:rgba(99,102,241,.06);border-radius:7px;border-left:2px solid rgba(99,102,241,.3);line-height:1.5;">🪝 ${ev.hook.slice(0,100)}${ev.hook.length>100?'…':''}</div>`:''}
          </div>
          <div style="display:flex;gap:4px;align-items:center;">
            <select onclick="event.stopPropagation()" onchange="updateCalEventStatus('${ev.id}',this.value)" class="calevent-status-sel" style="padding:4px 8px;border-radius:7px;border:1px solid var(--b2);background:var(--surf2);color:var(--t);font-size:11px;font-family:var(--font);" title="Update status">
              ${Object.entries(STATUS_LABELS).map(([k,l])=>`<option value="${k}" ${ev.status===k?'selected':''}>${l}</option>`).join('')}
            </select>
          </div>
        </div>`;
      }).join('')}
    </div>`:'';

  body.innerHTML=`
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-title">Kalender Konten</div>
        <div class="view-subtitle">Jadwalkan postingan, pantau progres dari ide sampai terposting</div>
      </div>
      <div class="view-header-right" style="flex-wrap:wrap;gap:8px;">
        <button onclick="calendarDate=new Date(calendarDate.getFullYear(),calendarDate.getMonth()-1,1);renderCalendar()" style="padding:7px 12px;border-radius:9px;border:1px solid var(--b);background:none;color:var(--t);cursor:pointer;font-size:14px;">←</button>
        <div style="font-size:14px;font-weight:800;color:var(--t);min-width:140px;text-align:center;">${MN_ID[m]} ${y}</div>
        <button onclick="calendarDate=new Date(calendarDate.getFullYear(),calendarDate.getMonth()+1,1);renderCalendar()" style="padding:7px 12px;border-radius:9px;border:1px solid var(--b);background:none;color:var(--t);cursor:pointer;font-size:14px;">→</button>
        <div class="seg" style="margin-left:4px;">
          <button class="seg-btn ${!window._calKanban?'act':''}" onclick="window._calKanban=false;renderCalendar()" title="Tampilan Kalender">📅</button>
          <button class="seg-btn ${window._calKanban?'act':''}" onclick="window._calKanban=true;renderCalendar()" title="Tampilan Kanban">🗂</button>
        </div>
        <button class="view-header-btn" onclick="openCalEventModal()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
          Jadwal Baru
        </button>
      </div>
    </div>
    ${statsHTML}
    ${window._calKanban ? (()=>{
      const colColors={idea:'rgba(99,102,241,.1)',in_progress:'rgba(245,158,11,.09)',scheduled:'rgba(139,92,246,.09)',posted:'rgba(34,197,94,.07)'};
      const colBorders={idea:'rgba(99,102,241,.22)',in_progress:'rgba(245,158,11,.22)',scheduled:'rgba(139,92,246,.22)',posted:'rgba(34,197,94,.18)'};
      const cols=Object.entries(STATUS_LABELS).map(([status,label])=>{
        const evs=monthEvents.filter(e=>e.status===status).sort((a,b)=>(a.scheduledDate||'').localeCompare(b.scheduledDate||''));
        const cards=evs.map(ev=>{
          const plt=allPlatforms().find(p=>p.id===ev.platform);
          const pil=pillarOf(ev.pillar||'other');
          const d=ev.scheduledDate?new Date(ev.scheduledDate+' 12:00:00'):null;
          const dateStr=d?d.getDate()+' '+MN_ID[d.getMonth()].slice(0,3):'';
          return`<div class="kanban-card" onclick="openCalEventModal('${ev.id}')">
            <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:6px;margin-bottom:6px;">
              <div style="font-size:12px;font-weight:700;color:var(--t);line-height:1.4;flex:1;">${ev.title}</div>
              <div style="display:flex;align-items:center;gap:4px;flex-shrink:0;">
                ${plt?`<span style="font-size:13px;">${plt.emoji}</span>`:''}
                <div style="width:8px;height:8px;border-radius:50%;background:${pil.color||'var(--b2)'};flex-shrink:0;" title="${pil.label}"></div>
              </div>
            </div>
            ${ev.hook?`<div style="font-size:10px;color:var(--t4);background:var(--bg-hover);border-radius:5px;padding:4px 6px;margin-bottom:5px;line-height:1.4;">🪝 ${ev.hook.slice(0,60)}${ev.hook.length>60?'…':''}</div>`:''}
            <div style="display:flex;align-items:center;justify-content:space-between;margin-top:4px;">
              <div style="font-size:10px;color:var(--t4);">${dateStr}${ev.account?' · '+ev.account:''}</div>
              <select onclick="event.stopPropagation()" onchange="updateCalEventStatus('${ev.id}',this.value)" style="font-size:10px;padding:2px 5px;border-radius:5px;border:1px solid var(--b);background:var(--surf2);color:var(--t3);cursor:pointer;font-family:var(--font);">
                ${Object.entries(STATUS_LABELS).map(([k,l])=>`<option value="${k}" ${ev.status===k?'selected':''}>${l}</option>`).join('')}
              </select>
            </div>
          </div>`;
        }).join('');
        return`<div style="flex:1;min-width:220px;max-width:320px;">
          <div style="background:${colColors[status]};border:1px solid ${colBorders[status]};border-radius:10px;padding:10px 12px;margin-bottom:10px;display:flex;align-items:center;justify-content:space-between;">
            <span style="font-size:12px;font-weight:800;color:var(--t);">${label}</span>
            <span style="font-size:11px;font-weight:700;padding:2px 8px;border-radius:20px;background:rgba(255,255,255,.07);color:var(--t3);">${evs.length}</span>
          </div>
          <div style="display:flex;flex-direction:column;gap:8px;min-height:80px;">
            ${cards||`<div onclick="openCalEventModal(null,{date:'${new Date().toISOString().split('T')[0]}'})" style="border:1.5px dashed var(--b2);border-radius:9px;padding:16px;text-align:center;font-size:11px;color:var(--t4);cursor:pointer;transition:border-color .15s;" onmouseover="this.style.borderColor='var(--accent)'" onmouseout="this.style.borderColor='var(--b2)'">+ Tambah</div>`}
          </div>
        </div>`;
      }).join('');
      return`<div style="display:flex;gap:12px;overflow-x:auto;padding-bottom:12px;">${cols}</div>`;
    })() : `<div class="cal-grid">${dayHeaders}${cells}</div>${upcomingHTML}`}
  `;
}

/* ══ INIT ══ */
(async function(){
  initTheme();
  await load();
  // Migration: force-save immediately if any legacy accounts were stripped during load
  // so stale localStorage is overwritten with the clean state
  const _MIGRATE_KEY = KEY + '-legacy-clean-v1';
  try {
    const alreadyMigrated = localStorage.getItem(_MIGRATE_KEY);
    if (!alreadyMigrated) {
      localStorage.setItem(_MIGRATE_KEY, '1');
      scheduleSave(200); // persist clean data (legacy accounts already stripped in load())
    }
  } catch(e) {}
  months.forEach(m=>{exp[m.id]=true;});
  renderNavPlatforms();
  updatePlannerBadge();
  switchView('dash');
  // Init content panels (always rendered but hidden until needed)
  if(view==='content')renderContentTimeline();
  // Sprint 6: init notification permission state & check reminders
  if ('Notification' in window) notifPermission = Notification.permission;
  setTimeout(()=>checkReminders(false), 5000);
  // Sprint 7: show onboarding for new users
  checkShowOnboarding();
  // Revisi 5: check retention reminder
  setTimeout(checkRetentionReminder, 1500);

  // ── Global Keyboard Shortcuts ──
  document.addEventListener('keydown', e => {
    // Ignore when typing in input/textarea/select or modal is open
    const tag = document.activeElement?.tagName;
    const inInput = tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT'
      || document.activeElement?.isContentEditable;

    // ESC — close topmost open modal or settings
    if (e.key === 'Escape') {
      if (!document.getElementById('m-settings')?.classList.contains('hidden')) {
        closeSettings(); return;
      }
      // Close any visible modal-ov
      const openModals = [...document.querySelectorAll('.modal-ov:not(.hidden)')];
      if (openModals.length) {
        const last = openModals[openModals.length - 1];
        if (last.id !== 'm-settings') closeModal(last.id);
        return;
      }
    }

    if (inInput) return; // don't intercept typing below this

    // N — new post (Quick Add)
    if (e.key === 'n' || e.key === 'N') {
      e.preventDefault();
      openQCModal();
      return;
    }

    // / — focus content search (switches to content view if needed)
    if (e.key === '/') {
      e.preventDefault();
      if (view !== 'content') switchView('content');
      // Small delay so timeline renders first
      setTimeout(() => {
        const inp = document.querySelector('.content-search-input');
        if (inp) { inp.focus(); inp.select(); }
      }, 100);
      return;
    }

    // G then D/A/C/G/R — go to view (vim-style nav)
    if (e.key === 'g' && !e._gPressed) {
      e._gPressed = true;
      const handler = e2 => {
        document.removeEventListener('keydown', handler);
        const map = { d:'dash', a:'analytics', c:'content', g:'goals', r:'report', p:'planner', k:'calendar', m:'competitor' };
        if (map[e2.key]) switchView(map[e2.key]);
      };
      document.addEventListener('keydown', handler, { once: true });
      setTimeout(() => document.removeEventListener('keydown', handler), 1500);
      return;
    }

    // ? — show shortcuts cheatsheet
    if (e.key === '?') {
      e.preventDefault();
      _showShortcutsToast();
      return;
    }
  });

  // ── Global click-to-copy handler ──
  document.addEventListener('click', e => {
    const el = e.target.closest('[data-copyable]');
    if (!el) return;
    const val = el.getAttribute('data-copyable');
    if (val) {
      e.stopPropagation();
      navigator.clipboard.writeText(val).then(() => {
        showToast('📋 Disalin!', 'info', 1500);
      }).catch(() => {});
    }
  });

  // ── Auto-backup reminder ──
  setTimeout(checkAutoBackupReminder, 3000);
})();

/* ══════════════════════════════════════════════
   REVISI AGENCY — CSV IMPORT, COMPETITOR, REPORT CONFIG
   ══════════════════════════════════════════════ */

/* ── CSV Import ── */
let _csvParsed = [];
let _csvTab = 'custom';

function openCSVImportModal(){
  if(!sel.wid||!sel.acc){
    _qcToast('⚠️ Pilih minggu & akun dulu sebelum import.');
    return;
  }
  _csvParsed=[];
  document.getElementById('csv-preview').style.display='none';
  document.getElementById('csv-import-btn').style.display='none';
  document.getElementById('csv-file-input').value='';
  openModal('m-csv');
}

function setCsvTab(tab){
  _csvTab=tab;
  ['custom','tiktok'].forEach(t=>{
    const btn=document.getElementById('csv-tab-'+t);
    const content=document.getElementById('csv-tab-content-'+t);
    if(btn)btn.classList.toggle('act',t===tab);
    if(content)content.style.display=t===tab?'block':'none';
  });
}

function downloadCSVTemplate(){
  const headers='tanggal,nama_konten,pilar,format,impression,reach,like,comment,share,save,repost,followers_gained,link';
  const ex='2024-05-15,Review Product X,edukasi,video,12500,8200,980,145,67,230,12,45,https://tiktok.com/@akun/video/123';
  const blob=new Blob([headers+'\n'+ex],{type:'text/csv'});
  const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='creatorlytics_template.csv';a.click();
}

function exportAllPostsCSV(){
  const allPosts=getAllPosts();
  if(!allPosts.length){_qcToast('⚠️ Belum ada post untuk di-export.');return;}
  const headers=['tanggal','platform','akun','nama_konten','pilar','format','impression','reach','like','comment','share','save','repost','followers_gained','er','link'];
  const esc=v=>{
    if(v===null||v===undefined)return '';
    const s=String(v);
    return s.includes(',')||s.includes('"')||s.includes('\n')?'"'+s.replace(/"/g,'""')+'"':s;
  };
  const rows=[headers.join(',')];
  // Get platform name for each post
  getAllPosts((p,m,w,a)=>{
    const pltObj=allPlatforms().find(pl=>pl.id===p._plt)||{name:p._plt||'',emoji:''};
    const pilObj=pillars.find(pi=>pi.id===p.pillar)||{label:p.pillar||''};
    const er=calcER(p,sel.erMode,p._flw||0);
    rows.push([
      esc(p.date||''),
      esc(pltObj.name),
      esc(a||p._acc||''),
      esc(p.caption||p.title||''),
      esc(pilObj.label),
      esc(p.format||''),
      esc(p.impression||0),
      esc(p.reach||0),
      esc(p.like||0),
      esc(p.comment||0),
      esc(p.share||0),
      esc(p.save||0),
      esc(p.repost||0),
      esc(p.followersGained||0),
      esc(er?er.toFixed(2):''),
      esc(p.link||''),
    ].join(','));
    return false; // don't modify post
  });
  const now=new Date();
  const fname=`creatorlytics_export_${now.getFullYear()}${String(now.getMonth()+1).padStart(2,'0')}${String(now.getDate()).padStart(2,'0')}.csv`;
  const blob=new Blob([rows.join('\n')],{type:'text/csv;charset=utf-8;'});
  const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=fname;a.click();
  _qcToast(`✅ ${allPosts.length} post di-export ke ${fname}`);
}

function handleCSVDrop(e){
  e.preventDefault();
  document.getElementById('csv-drop').classList.remove('drag-over');
  const file=e.dataTransfer?.files?.[0];
  if(file)handleCSVFile(file);
}

function handleCSVFile(file){
  if(!file||!file.name.endsWith('.csv')){_qcToast('⚠️ File harus format .csv');return;}
  const reader=new FileReader();
  reader.onload=function(e){
    const text=e.target.result;
    try{
      const rows=parseCSVText(text);
      if(rows.length<2){_qcToast('⚠️ File CSV kosong atau format salah.');return;}
      const parsed=_csvTab==='tiktok'?parseTikTokCSV(rows):parseCustomCSV(rows);
      _csvParsed=parsed;
      showCSVPreview(parsed, rows[0]);
    }catch(err){
      _qcToast('❌ Gagal baca CSV: '+err.message);
    }
  };
  reader.readAsText(file,'UTF-8');
}

function parseCSVText(text){
  const lines=text.trim().split(/\r?\n/);
  return lines.map(line=>{
    const result=[];let cur='';let inQ=false;
    for(let i=0;i<line.length;i++){
      if(line[i]==='"'){inQ=!inQ;}
      else if(line[i]===','&&!inQ){result.push(cur.trim());cur='';}
      else{cur+=line[i];}
    }
    result.push(cur.trim());
    return result;
  });
}

function parseCustomCSV(rows){
  const h=rows[0].map(c=>c.toLowerCase().trim().replace(/\s+/g,'_'));
  const get=(row,name)=>{
    const i=h.indexOf(name);
    return i>=0?(row[i]||'').trim():'';
  };
  const result=[];
  for(let i=1;i<rows.length;i++){
    const row=rows[i];
    if(!row||row.every(c=>!c.trim()))continue;
    const p=emptyPost();
    p.date=get(row,'tanggal')||'';
    p.name=get(row,'nama_konten');
    const pillarName=get(row,'pilar').toLowerCase();
    const matchedPillar=pillars.find(pl=>pl.label.toLowerCase()===pillarName||pl.id===pillarName);
    if(matchedPillar)p.pillar=matchedPillar.id;
    const fmtMatch=FORMAT_TYPES.find(f=>f.id===get(row,'format').toLowerCase()||f.label.toLowerCase()===get(row,'format').toLowerCase());
    if(fmtMatch)p.format=fmtMatch.id;
    p.impression=parseInt(get(row,'impression'))||0;
    p.reach=parseInt(get(row,'reach'))||0;
    p.like=parseInt(get(row,'like'))||0;
    p.comment=parseInt(get(row,'comment'))||0;
    p.share=parseInt(get(row,'share'))||0;
    p.save=parseInt(get(row,'save'))||0;
    p.repost=parseInt(get(row,'repost'))||0;
    p.followersGained=parseInt(get(row,'followers_gained'))||0;
    p.link=get(row,'link');
    result.push(p);
  }
  return result;
}

function parseTikTokCSV(rows){
  // TikTok Creator Studio export format: Video title, Date, Video views, Likes, Comments, Shares, Profile visits
  const h=rows[0].map(c=>c.toLowerCase().trim());
  const colIdx=(names)=>{for(const n of names){const i=h.findIndex(c=>c.includes(n));if(i>=0)return i;}return -1;};
  const iTitle=colIdx(['video title','title']);
  const iDate=colIdx(['date','time']);
  const iViews=colIdx(['video views','views','impressions']);
  const iLikes=colIdx(['likes','like']);
  const iCmts=colIdx(['comments','comment']);
  const iShares=colIdx(['shares','share']);
  const iProf=colIdx(['profile visits','profile views']);
  const iReach=colIdx(['reach','audience reached']);
  const result=[];
  for(let i=1;i<rows.length;i++){
    const row=rows[i];
    if(!row||row.every(c=>!c.trim()))continue;
    const p=emptyPost();
    p.name=iTitle>=0?(row[iTitle]||''):'';
    p.date=iDate>=0?(row[iDate]||'').split(' ')[0]:'';
    p.impression=iViews>=0?parseInt((row[iViews]||'').replace(/,/g,''))||0:0;
    p.reach=iReach>=0?parseInt((row[iReach]||'').replace(/,/g,''))||0:0;
    p.like=iLikes>=0?parseInt((row[iLikes]||'').replace(/,/g,''))||0:0;
    p.comment=iCmts>=0?parseInt((row[iCmts]||'').replace(/,/g,''))||0:0;
    p.share=iShares>=0?parseInt((row[iShares]||'').replace(/,/g,''))||0:0;
    result.push(p);
  }
  return result;
}

function showCSVPreview(parsed, headerRow){
  const preview=document.getElementById('csv-preview');
  const table=document.getElementById('csv-preview-table');
  const status=document.getElementById('csv-status');
  if(!preview||!table)return;
  const displayFields=['date','name','impression','reach','like','comment','share'];
  const labels={date:'Tanggal',name:'Nama Konten',impression:'Impr',reach:'Reach',like:'Like',comment:'Comment',share:'Share'};
  const thead='<tr>'+displayFields.map(f=>`<th>${labels[f]||f}</th>`).join('')+'</tr>';
  const tbody=parsed.slice(0,5).map(p=>'<tr>'+displayFields.map(f=>`<td>${p[f]||'—'}</td>`).join('')+'</tr>').join('');
  table.innerHTML='<thead>'+thead+'</thead><tbody>'+tbody+'</tbody>';
  status.innerHTML=`<span style="color:var(--green);font-weight:700;">✓ ${parsed.length} baris terdeteksi</span> — siap diimport ke <strong>${sel.acc}</strong> · <strong>${allPlatforms().find(p=>p.id===sel.plt)?.name||sel.plt}</strong>`;
  preview.style.display='block';
  document.getElementById('csv-import-btn').style.display='block';
}

function executeCSVImport(){
  if(!_csvParsed.length){_qcToast('⚠️ Tidak ada data untuk diimport.');return;}
  if(!sel.wid||!sel.acc){_qcToast('⚠️ Pilih minggu & akun dulu.');return;}
  const wk=getW(sel.mid,sel.wid);
  if(!wk){_qcToast('⚠️ Minggu tidak ditemukan.');return;}
  const d=aD(wk,sel.acc);
  const plt=sel.plt;
  if(!d[plt+'Posts'])d[plt+'Posts']=[];
  let added=0;
  _csvParsed.forEach(p=>{
    d[plt+'Posts'].push({...p,id:uid()});
    added++;
  });
  scheduleSave(200);
  closeModal('m-csv');
  renderEditorBody();
  if(view==='dash')renderDashboard();
  _qcToast(`✅ ${added} post berhasil diimport!`);
  // Auto-suggest backup (F3)
  setTimeout(() => {
    if (added >= 5) {
      showToast(`💾 ${added} post baru diimport. Backup data sekarang?`, 'warn', 6000);
      setTimeout(() => {
        if (confirm(`📦 ${added} post baru diimport. Backup data sekarang?`)) {
          exportJSONBackup();
        }
      }, 1500);
    }
  }, 2000);
}

/* ── Report Config ── */
let _reportMetricsEnabled=new Set(['reach','engagement','avgER','followersGained','impression','posts']);

function toggleReportMetric(key){
  const el=document.getElementById('rptm-'+key);
  if(!el)return;
  if(_reportMetricsEnabled.has(key)){
    if(_reportMetricsEnabled.size<=2){_qcToast('⚠️ Minimal 2 metrik harus dipilih.');return;}
    _reportMetricsEnabled.delete(key);
    el.classList.remove('selected');
  }else{
    _reportMetricsEnabled.add(key);
    el.classList.add('selected');
  }
}

function openReportConfigModal(){
  // Pre-fill brand name if available
  const inp=document.getElementById('rpt-cfg-brand');
  if(inp&&reportUserData.brandName)inp.value=reportUserData.brandName;
  openModal('m-rpt-cfg');
}

/* ── Competitor Tracker ── */
let competitors=[];
try{
  // Load competitors from main key (loaded via loadData above).
  // Legacy fallback: if main data didn't have competitors yet, read old separate key.
  if(!competitors.length){
    const d=localStorage.getItem('creatorlytics-competitors');
    if(d) competitors=JSON.parse(d);
  }
}catch(e){}

// saveCompetitors: just piggybacks on the main persist cycle — no separate key needed
function saveCompetitors(){ scheduleSave(300); }

let _editingCompId=null;

function openAddCompetitorModal(id){
  _editingCompId=id||null;
  const title=document.getElementById('comp-modal-title');
  if(title)title.textContent=id?'✏️ Edit Kompetitor':'➕ Tambah Kompetitor';
  const comp=id?competitors.find(c=>c.id===id):null;
  document.getElementById('mi-comp-name').value=comp?.name||'';
  document.getElementById('mi-comp-platform').value=comp?.platform||'tt';
  document.getElementById('mi-comp-flw').value=comp?.followers||'';
  document.getElementById('mi-comp-note').value=comp?.note||'';
  document.getElementById('mi-comp-reach').value=comp?.avgReach||'';
  document.getElementById('mi-comp-er').value=comp?.avgER||'';
  document.getElementById('mi-comp-posts').value=comp?.postsPerMonth||'';
  document.getElementById('mi-comp-eng').value=comp?.avgEngagement||'';
  openModal('m-competitor');
}

function saveCompetitor(){
  const name=document.getElementById('mi-comp-name').value.trim();
  if(!name){_qcToast('⚠️ Nama akun kompetitor wajib diisi.');return;}
  const data={
    id:_editingCompId||uid(),
    name,
    platform:document.getElementById('mi-comp-platform').value,
    followers:parseInt(document.getElementById('mi-comp-flw').value)||0,
    note:document.getElementById('mi-comp-note').value.trim(),
    avgReach:parseInt(document.getElementById('mi-comp-reach').value)||0,
    avgER:parseFloat(document.getElementById('mi-comp-er').value)||0,
    postsPerMonth:parseInt(document.getElementById('mi-comp-posts').value)||0,
    avgEngagement:parseInt(document.getElementById('mi-comp-eng').value)||0,
    updatedAt:new Date().toISOString()
  };
  if(_editingCompId){
    const idx=competitors.findIndex(c=>c.id===_editingCompId);
    if(idx>=0)competitors[idx]=data;
  }else{
    competitors.push(data);
  }
  saveCompetitors();
  closeModal('m-competitor');
  renderCompetitor();
  _qcToast(_editingCompId?'✅ Kompetitor diperbarui':'✅ Kompetitor ditambahkan');
}

function deleteCompetitor(id){
  const _c=competitors.find(c=>c.id===id);
  showConfirm('Hapus Kompetitor?',`Akun "${_c?_c.name:'ini'}" akan dihapus dari tracker.`,()=>{
    competitors=competitors.filter(c=>c.id!==id);
    saveCompetitors();
    renderCompetitor();
    _qcToast('🗑 Kompetitor dihapus');
  },true);
}

function renderCompetitor(){
  const body=document.getElementById('competitor-body');
  if(!body)return;

  // Get my own stats for comparison
  const myStats=getReportStats('all',new Date().getFullYear(),new Date().getMonth(),'month');
  const myPosts=myStats.posts;
  const myER=myStats.avgER||0;
  const myReach=myPosts>0?(myStats.reach/myPosts):0;
  const myFlw=myStats.totalFlw||0;
  const myEng=myPosts>0?(myStats.engagement/myPosts):0;

  const compRows=competitors.map(c=>{
    const plt=allPlatforms().find(p=>p.id===c.platform)||{emoji:'📌',name:c.platform};
    const erDiff=myER-c.avgER;
    const reachDiff=myReach-c.avgReach;
    const flwDiff=myFlw-c.followers;
    function diffChip(diff,fmt_fn,isFlw){
      if(!diff&&diff!==0)return '';
      const pos=isFlw?diff>0:diff>0;
      const cls=pos?'color:var(--green)':'color:var(--red)';
      const sign=diff>0?'+':'';
      return `<span style="font-size:10px;font-weight:700;${cls}">${sign}${fmt_fn(diff)}</span>`;
    }
    return `<div class="comp-card">
      <div class="comp-card-header">
        <div style="width:38px;height:38px;border-radius:10px;background:var(--red-bg);display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0;">${plt.emoji}</div>
        <div style="flex:1;min-width:0;">
          <div style="font-size:14px;font-weight:800;color:var(--t);">${c.name}</div>
          <div style="font-size:11px;color:var(--t3);">${plt.emoji} ${plt.name}${c.note?` · ${c.note}`:''}</div>
        </div>
        <span class="comp-vs-badge">VS</span>
        <button onclick="openAddCompetitorModal('${c.id}')" style="padding:4px 10px;border-radius:7px;border:1px solid var(--b);background:none;color:var(--t3);font-size:11px;font-weight:700;cursor:pointer;margin-left:4px;">✏ Edit</button>
        <button onclick="deleteCompetitor('${c.id}')" style="padding:4px 8px;border-radius:7px;border:none;background:var(--red-bg);color:var(--red);font-size:11px;cursor:pointer;margin-left:4px;">✕</button>
      </div>
      <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:8px;">
        ${[
          {label:'Followers',mine:fmt(myFlw),theirs:fmt(c.followers),diff:diffChip(flwDiff,fmt,true)},
          {label:'Avg ER %',mine:erStr(myER),theirs:erStr(c.avgER),diff:diffChip(erDiff,v=>Math.abs(v).toFixed(2)+'%',false)},
          {label:'Avg Reach/Post',mine:fmt(myReach),theirs:fmt(c.avgReach),diff:diffChip(reachDiff,fmt,false)},
          {label:'Post/Bulan',mine:String(myPosts),theirs:String(c.postsPerMonth||0),diff:''},
          {label:'Avg Eng/Post',mine:fmt(myEng),theirs:fmt(c.avgEngagement||0),diff:diffChip(myEng-(c.avgEngagement||0),fmt,false)},
        ].map(m=>`<div style="background:var(--surf2);border-radius:9px;padding:10px 12px;">
          <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:6px;">${m.label}</div>
          <div style="display:flex;align-items:baseline;gap:6px;flex-wrap:wrap;">
            <span style="font-family:var(--mono);font-size:14px;font-weight:800;color:var(--t);">${m.mine}</span>
            <span style="font-size:10px;color:var(--t4);">vs</span>
            <span style="font-family:var(--mono);font-size:13px;font-weight:700;color:var(--t3);">${m.theirs}</span>
            ${m.diff}
          </div>
        </div>`).join('')}
      </div>
      ${(()=>{
        if(!c.updatedAt) return '<div style="font-size:10px;color:var(--amber);margin-top:10px;">⚠️ Belum pernah diupdate — klik Edit untuk isi data terbaru</div>';
        const daysSince=Math.floor((Date.now()-new Date(c.updatedAt).getTime())/(1000*60*60*24));
        const dateStr=new Date(c.updatedAt).toLocaleDateString('id-ID');
        if(daysSince>14) return '<div style="font-size:10px;font-weight:700;color:var(--red);margin-top:10px;display:flex;align-items:center;gap:5px;"><span style=\'background:rgba(255,107,107,.15);padding:2px 7px;border-radius:5px;\'>⚠️ Data sudah '+daysSince+' hari lalu ('+dateStr+') — segera update!</span> <button onclick=\"openAddCompetitorModal(\'' +c.id+ '\')" style=\"padding:2px 8px;border-radius:5px;border:1px solid var(--red);background:none;color:var(--red);font-size:10px;font-weight:700;cursor:pointer;\">Update</button></div>';
        if(daysSince>7) return '<div style="font-size:10px;color:var(--amber);margin-top:10px;">🕐 Data '+daysSince+' hari lalu ('+dateStr+') — pertimbangkan update minggu ini</div>';
        return '<div style="font-size:10px;color:var(--t4);margin-top:10px;">✓ Diperbarui: '+dateStr+' ('+daysSince+' hari lalu)</div>';
      })()}
    </div>`;
  }).join('');

  const emptyState=competitors.length===0?`
    <div style="margin-bottom:24px;">${emptyStateHTML('competitor')}</div>
    <div style="display:flex;flex-direction:column;gap:6px;margin-bottom:24px;text-align:left;max-width:400px;margin-left:auto;margin-right:auto;">
      <div style="font-size:12px;color:var(--t3);padding:8px 12px;background:var(--surf2);border-radius:8px;">💡 Cari kompetitor di TikTok/IG → catat followers & avg reach per post</div>
      <div style="font-size:12px;color:var(--t3);padding:8px 12px;background:var(--surf2);border-radius:8px;">📅 Update data minimal sekali seminggu agar perbandingan akurat</div>
    </div>`:'';

  // My snapshot card
  const myCard=`<div style="background:linear-gradient(135deg,rgba(14,159,143,.15),rgba(14,159,143,.05));border:1px solid rgba(14,159,143,.2);border-radius:14px;padding:16px 18px;margin-bottom:16px;">
    <div style="font-size:10px;font-weight:800;color:var(--accent);text-transform:uppercase;letter-spacing:.1em;margin-bottom:10px;">📊 Performa Saya — Bulan Ini</div>
    <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(110px,1fr));gap:8px;">
      ${[['Followers',fmt(myFlw)],['Total Reach',fmt(myStats.reach)],['Avg ER',erStr(myER)],['Total Post',String(myPosts)],['Avg Eng/Post',fmt(myEng)]].map(([l,v])=>`
        <div style="background:rgba(255,255,255,.04);border-radius:8px;padding:8px 10px;">
          <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:3px;">${l}</div>
          <div style="font-family:var(--mono);font-size:14px;font-weight:800;color:var(--t);">${v}</div>
        </div>`).join('')}
    </div>
    <div style="font-size:10px;color:var(--t4);margin-top:8px;">* Diambil dari data bulan ${MN[new Date().getMonth()]} — semua platform</div>
  </div>`;

  body.innerHTML=`
    <div class="view-header">
      <div class="view-header-left">
        <div class="view-title">Kompetitor</div>
        <div class="view-subtitle">Track akun kompetitor secara manual dan bandingkan performa side-by-side</div>
      </div>
      <div class="view-header-right">
        <button class="view-header-btn" onclick="openAddCompetitorModal()">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M7 2v10M2 7h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
          Tambah Kompetitor
        </button>
      </div>
    </div>
    ${myCard}
    ${emptyState}
    ${compRows}
  `;
}

/* ── Override exportPDF to open config modal first ── */
const _origExportPDF = exportPDF;
window._exportPDFDirect = async function(){await _origExportPDF();};
// Replace the existing PDF button handler via report config modal
function openPDFExport(){
  // Pre-fill brand from reportUserData
  const inp=document.getElementById('rpt-cfg-brand');
  if(inp)inp.value=reportUserData.brandName||'';
  openModal('m-rpt-cfg');
}

/* ══ QUICK ADD POST ══ */
function openQuickAdd(){
  if(accounts.length===0){_qcToast('⚠️ Tambah akun dulu di sidebar sebelum input post.');return;}
  const accSel=document.getElementById('qa-acc');
  const pltSel=document.getElementById('qa-plt');
  const pillarSel=document.getElementById('qa-pillar');
  if(accSel)accSel.innerHTML=accounts.map(a=>`<option value="${a}" ${a===sel.acc?'selected':''}>${a}</option>`).join('');
  if(pltSel)pltSel.innerHTML=allPlatforms().map(p=>`<option value="${p.id}" ${p.id===sel.plt?'selected':''}>${p.emoji||''} ${p.name}</option>`).join('');
  if(pillarSel)pillarSel.innerHTML=pillars.map(p=>`<option value="${p.id}">${p.emoji} ${p.label}</option>`).join('');
  const dateInp=document.getElementById('qa-date');
  if(dateInp)dateInp.value=new Date().toISOString().split('T')[0];
  ['reach','imp','like','comment','share','save','repost','flw'].forEach(k=>{const el=document.getElementById('qa-'+k);if(el)el.value='';});
  const nameInp=document.getElementById('qa-name');
  if(nameInp)nameInp.value='';
  openModal('m-quickadd');
  setTimeout(()=>document.getElementById('qa-name')?.focus(),200);
}

function executeQuickAdd(){
  const dateVal=(document.getElementById('qa-date')?.value||'').trim();
  const accVal=document.getElementById('qa-acc')?.value;
  const pltVal=document.getElementById('qa-plt')?.value;
  if(!dateVal){_qcToast('⚠️ Tanggal wajib diisi.');return;}
  if(!accVal){_qcToast('⚠️ Pilih akun.');return;}
  if(!pltVal){_qcToast('⚠️ Pilih platform.');return;}
  const d=new Date(dateVal+'T12:00:00');
  const y=d.getFullYear(),m=d.getMonth();
  let month=months.find(mo=>mo.year===y&&mo.month===m);
  if(!month){month=emptyMonth(y,m);months.push(month);months.sort((a,b)=>a.year!==b.year?a.year-b.year:a.month-b.month);}
  const dayOfMonth=d.getDate();
  const weekIndex=Math.min(Math.floor((dayOfMonth-1)/7),3);
  while(month.weeks.length<=weekIndex)month.weeks.push(emptyWeek(month.weeks.length+1));
  const week=month.weeks[weekIndex];
  if(!week.data[accVal])week.data[accVal]=emptyAcc();
  const accData=week.data[accVal];
  const key=pltVal+'Posts';
  if(!accData[key])accData[key]=[];
  const post=emptyPost();
  post.date=dateVal;
  post.name=(document.getElementById('qa-name')?.value||'').trim();
  post.pillar=document.getElementById('qa-pillar')?.value||pillars[0]?.id||'other';
  post.format=document.getElementById('qa-format')?.value||'';
  post.reach=parseInt(document.getElementById('qa-reach')?.value)||0;
  post.impression=parseInt(document.getElementById('qa-imp')?.value)||0;
  post.like=parseInt(document.getElementById('qa-like')?.value)||0;
  post.comment=parseInt(document.getElementById('qa-comment')?.value)||0;
  post.share=parseInt(document.getElementById('qa-share')?.value)||0;
  post.save=parseInt(document.getElementById('qa-save')?.value)||0;
  post.repost=parseInt(document.getElementById('qa-repost')?.value)||0;
  post.followersGained=parseInt(document.getElementById('qa-flw')?.value)||0;
  accData[key].push(post);
  sel.mid=month.id;sel.wid=week.id;sel.acc=accVal;sel.plt=pltVal;
  scheduleSave(300);
  closeModal('m-quickadd');
  if(view==='dash')renderDashboard();
  else if(view==='analytics'){destroyCharts();renderAnalytics();}
  else if(view==='content')renderContentTimeline();
  else if(view==='goals')renderGoals();
  const eng=post.like+post.comment+post.share+post.save+post.repost;
  _qcToast(`✅ Post disimpan${post.name?' · "'+post.name.slice(0,25)+'"':''} → ${MN[m]} Minggu ${weekIndex+1}${eng>0?' · Eng: '+fmt(eng):''}`);
}
</script>
<!-- ── CSV IMPORT MODAL ── -->
<div class="modal-ov hidden" id="m-csv" onclick="if(event.target===this)closeModal('m-csv')">
  <div class="modal" style="max-width:640px;">
    <div class="modal-title">📥 Import Data via CSV</div>
    <div style="display:flex;gap:4px;margin-bottom:16px;background:var(--surf2);border-radius:9px;padding:4px;">
      <button class="csv-tab act" id="csv-tab-custom" onclick="setCsvTab('custom')">Template Creatorlytics</button>
      <button class="csv-tab" id="csv-tab-tiktok" onclick="setCsvTab('tiktok')">TikTok Creator Studio</button>
    </div>
    <div id="csv-tab-content-custom">
      <div style="background:var(--accent-dim);border:1px solid rgba(14,159,143,.2);border-radius:9px;padding:10px 14px;margin-bottom:14px;font-size:12px;color:var(--t2);line-height:1.6;">
        <strong style="color:var(--accent);">Template Custom Creatorlytics</strong> — download dulu, isi datanya, lalu upload kembali. Kolom yang tersedia: <code style="font-size:11px;background:rgba(255,255,255,.08);padding:1px 5px;border-radius:4px;">tanggal, nama_konten, pilar, format, impression, reach, like, comment, share, save, repost, followers_gained, link</code>
      </div>
      <button onclick="downloadCSVTemplate()" style="width:100%;padding:10px;border-radius:9px;border:1.5px dashed var(--b2);background:none;color:var(--accent);font-size:13px;font-weight:700;cursor:pointer;margin-bottom:8px;transition:all .15s;" onmouseover="this.style.background='var(--accent-dim)'" onmouseout="this.style.background='none'">
        ⬇️ Download Template CSV
      </button>
      <button onclick="exportAllPostsCSV()" style="width:100%;padding:10px;border-radius:9px;border:1.5px solid rgba(39,209,127,.25);background:rgba(39,209,127,.06);color:var(--green);font-size:13px;font-weight:700;cursor:pointer;margin-bottom:14px;transition:all .15s;" onmouseover="this.style.background='rgba(39,209,127,.12)'" onmouseout="this.style.background='rgba(39,209,127,.06)'">
        📤 Export Semua Post ke CSV
      </button>
    </div>
    <div id="csv-tab-content-tiktok" style="display:none;">
      <div style="background:var(--tt-bg);border:1px solid rgba(61,199,183,.2);border-radius:9px;padding:10px 14px;margin-bottom:14px;font-size:12px;color:var(--t2);line-height:1.6;">
        <strong style="color:var(--tt);">TikTok Creator Studio Export</strong> — buka TikTok Creator Studio → Analytics → Export Data, pilih format CSV. Creatorlytics akan otomatis mapping kolom <code style="font-size:11px;background:rgba(255,255,255,.08);padding:1px 5px;border-radius:4px;">Video views, Likes, Comments, Shares, Profile visits</code> ke field yang sesuai.
      </div>
    </div>
    <!-- Drop zone -->
    <div class="csv-drop-area" id="csv-drop" onclick="document.getElementById('csv-file-input').click()" ondragover="event.preventDefault();this.classList.add('drag-over')" ondragleave="this.classList.remove('drag-over')" ondrop="handleCSVDrop(event)">
      <div class="csv-drop-icon">📂</div>
      <div style="font-size:13px;font-weight:700;color:var(--t);margin-bottom:4px;">Klik atau drag & drop file CSV</div>
      <div style="font-size:11px;color:var(--t3);">Format: .csv — maks. 5.000 baris</div>
    </div>
    <input type="file" id="csv-file-input" accept=".csv" style="display:none" onchange="handleCSVFile(this.files[0])">
    <div id="csv-preview" style="display:none;margin-top:14px;">
      <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px;">Preview (5 baris pertama)</div>
      <div style="max-height:180px;overflow:auto;border-radius:8px;border:1px solid var(--b);">
        <table class="csv-preview-table" id="csv-preview-table"></table>
      </div>
      <div id="csv-status" style="margin-top:8px;font-size:12px;color:var(--t2);"></div>
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-csv')">Batal</button>
      <button class="btn-ok" id="csv-import-btn" style="display:none;" onclick="executeCSVImport()">Import Data</button>
    </div>
  </div>
</div>

<!-- ── REPORT CONFIG MODAL ── -->
<div class="modal-ov hidden" id="m-rpt-cfg" onclick="if(event.target===this)closeModal('m-rpt-cfg')">
  <div class="modal" style="max-width:480px;">
    <div class="modal-title">⚙️ Konfigurasi Laporan</div>
    <div style="font-size:12px;color:var(--t3);margin-bottom:14px;">Pilih metrik yang ingin ditampilkan dalam laporan PDF. Klik untuk toggle.</div>
    <div id="rpt-cfg-metrics">
      <div class="rpt-cfg-metric selected" id="rptm-reach" onclick="toggleReportMetric('reach')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">👁</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Reach</span>
      </div>
      <div class="rpt-cfg-metric selected" id="rptm-engagement" onclick="toggleReportMetric('engagement')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">❤️</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Engagement</span>
      </div>
      <div class="rpt-cfg-metric selected" id="rptm-avgER" onclick="toggleReportMetric('avgER')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">📊</span><span style="font-size:12px;font-weight:600;color:var(--t);">Avg ER Rate</span>
      </div>
      <div class="rpt-cfg-metric selected" id="rptm-posts" onclick="toggleReportMetric('posts')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">📝</span><span style="font-size:12px;font-weight:600;color:var(--t);">Jumlah Post</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-followersGained" onclick="toggleReportMetric('followersGained')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">📈</span><span style="font-size:12px;font-weight:600;color:var(--t);">Followers Gained</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-impression" onclick="toggleReportMetric('impression')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">📺</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Impressi</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-like" onclick="toggleReportMetric('like')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">👍</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Like</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-comment" onclick="toggleReportMetric('comment')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">💬</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Comment</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-share" onclick="toggleReportMetric('share')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">🔁</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Share</span>
      </div>
      <div class="rpt-cfg-metric" id="rptm-save" onclick="toggleReportMetric('save')">
        <div class="rpt-cfg-check"><svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M1.5 5l2.5 2.5 4.5-4.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
        <span style="font-size:16px;">🔖</span><span style="font-size:12px;font-weight:600;color:var(--t);">Total Save</span>
      </div>
    </div>
    <div class="modal-fld" style="margin-top:16px;">
      <label>Nama Brand / Klien (opsional)</label>
      <input class="modal-inp" id="rpt-cfg-brand" type="text" placeholder="Masukkan nama brand untuk laporan..." maxlength="60">
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-rpt-cfg')">Batal</button>
      <button class="btn-ok" onclick="const b=document.getElementById('rpt-cfg-brand');if(b&&b.value.trim())updateRUD('brandName',b.value.trim());closeModal('m-rpt-cfg');exportPDF()">Export PDF</button>
    </div>
  </div>
</div>

<!-- ── ADD COMPETITOR MODAL ── -->
<div class="modal-ov hidden" id="m-competitor" onclick="if(event.target===this)closeModal('m-competitor')">
  <div class="modal" style="max-width:460px;">
    <div class="modal-title" id="comp-modal-title">➕ Tambah Kompetitor</div>
    <div class="modal-fld">
      <label>Nama Akun Kompetitor</label>
      <input class="modal-inp" id="mi-comp-name" type="text" placeholder="@namaakun" maxlength="60">
    </div>
    <div class="modal-fld">
      <label>Platform</label>
      <select class="modal-inp" id="mi-comp-platform">
        <option value="tt">🎵 TikTok</option>
        <option value="ig">📸 Instagram</option>
      </select>
    </div>
    <div class="modal-fld">
      <label>Followers saat ini</label>
      <input class="modal-inp" id="mi-comp-flw" type="number" min="0" placeholder="0">
    </div>
    <div class="modal-fld">
      <label>Catatan (opsional)</label>
      <input class="modal-inp" id="mi-comp-note" type="text" placeholder="Industri, niche, dll." maxlength="100">
    </div>
    <div id="comp-manual-data" style="margin-top:4px;">
      <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px;">Data Performa (opsional)</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
        <div class="modal-fld" style="margin:0;"><label>Avg Reach/Post</label><input class="modal-inp" id="mi-comp-reach" type="number" min="0" placeholder="0"></div>
        <div class="modal-fld" style="margin:0;"><label>Avg ER %</label><input class="modal-inp" id="mi-comp-er" type="number" min="0" step="0.01" placeholder="0.00"></div>
        <div class="modal-fld" style="margin:0;"><label>Post/Bulan</label><input class="modal-inp" id="mi-comp-posts" type="number" min="0" placeholder="0"></div>
        <div class="modal-fld" style="margin:0;"><label>Avg Engagement</label><input class="modal-inp" id="mi-comp-eng" type="number" min="0" placeholder="0"></div>
      </div>
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-competitor')">Batal</button>
      <button class="btn-ok" onclick="saveCompetitor()">Simpan</button>
    </div>
  </div>
</div>

<!-- ── QUICK ADD POST MODAL ── -->
<div class="modal-ov hidden" id="m-quickadd" onclick="if(event.target===this)closeModal('m-quickadd')">
  <div class="modal" style="max-width:520px;">
    <div class="modal-title">⚡ Quick Add Post</div>
    <div style="font-size:12px;color:var(--t3);margin-bottom:16px;">Input post langsung — tidak perlu navigasi bulan/minggu. Creatorlytics akan otomatis menempatkan di minggu yang sesuai.</div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;">
      <div class="modal-fld" style="margin:0;">
        <label>Tanggal Post *</label>
        <input class="modal-inp" id="qa-date" type="date">
      </div>
      <div class="modal-fld" style="margin:0;">
        <label>Akun *</label>
        <select class="modal-inp" id="qa-acc"></select>
      </div>
      <div class="modal-fld" style="margin:0;">
        <label>Platform *</label>
        <select class="modal-inp" id="qa-plt"></select>
      </div>
      <div class="modal-fld" style="margin:0;">
        <label>Format</label>
        <select class="modal-inp" id="qa-format">
          <option value="">— Format —</option>
          <option value="video">🎬 Video</option>
          <option value="reel">⚡ Reels/Short</option>
          <option value="carousel">🎠 Carousel</option>
          <option value="photo">🖼️ Foto</option>
          <option value="live">🔴 Live</option>
          <option value="story">⭕ Story</option>
        </select>
      </div>
    </div>
    <div class="modal-fld" style="margin-top:10px;">
      <label>Nama Konten</label>
      <input class="modal-inp" id="qa-name" type="text" placeholder="Judul atau deskripsi singkat konten..." maxlength="120">
    </div>
    <div class="modal-fld">
      <label>Pilar Konten</label>
      <select class="modal-inp" id="qa-pillar"></select>
    </div>
    <div style="background:var(--surf2);border-radius:10px;padding:12px 14px;margin-bottom:10px;">
      <div style="font-size:10px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:10px;">Metrik Utama</div>
      <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;">
        <div class="modal-fld" style="margin:0;"><label>Reach</label><input class="modal-inp" id="qa-reach" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Impressi</label><input class="modal-inp" id="qa-imp" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Like</label><input class="modal-inp" id="qa-like" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Comment</label><input class="modal-inp" id="qa-comment" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Share</label><input class="modal-inp" id="qa-share" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Save</label><input class="modal-inp" id="qa-save" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Repost</label><input class="modal-inp" id="qa-repost" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
        <div class="modal-fld" style="margin:0;"><label>Flw Gained</label><input class="modal-inp" id="qa-flw" type="number" min="0" placeholder="0" style="padding:7px 10px;"></div>
      </div>
    </div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-quickadd')">Batal</button>
      <button class="btn-ok" onclick="executeQuickAdd()">
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none" style="margin-right:4px;"><path d="M6.5 1.5v10M1.5 6.5h10" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
        Simpan Post
      </button>
    </div>
  </div>
</div>


<!-- ── CUSTOM CONFIRM MODAL (menggantikan native confirm()) ── -->
<div class="modal-ov hidden" id="m-confirm">
  <div class="modal" style="max-width:380px;">
    <div class="modal-title" id="confirm-modal-title" style="color:var(--red);">⚠️ Konfirmasi</div>
    <div id="confirm-modal-body" style="font-size:13px;color:var(--t2);line-height:1.65;margin-bottom:4px;"></div>
    <div style="font-size:11px;color:var(--t3);margin-top:10px;">Tindakan ini tidak dapat dibatalkan.</div>
    <div class="modal-acts">
      <button class="btn-c" onclick="closeModal('m-confirm')">Batal</button>
      <button id="confirm-modal-ok" class="btn-danger">Ya, Hapus</button>
    </div>
  </div>
</div>

<script>
/* ── showConfirm: pengganti native confirm() yang bisa di-style ── */
function showConfirm(title, message, onConfirm, isDanger){
  const ovEl  = document.getElementById('m-confirm');
  const ttlEl = document.getElementById('confirm-modal-title');
  const bodyEl= document.getElementById('confirm-modal-body');
  const okBtn = document.getElementById('confirm-modal-ok');
  if(!ovEl||!ttlEl||!bodyEl||!okBtn) return;
  ttlEl.textContent = (isDanger ? '⚠️ ' : '❓ ') + title;
  ttlEl.style.color = isDanger ? 'var(--red)' : 'var(--t)';
  bodyEl.textContent = message;
  okBtn.textContent  = isDanger ? 'Ya, Hapus' : 'Konfirmasi';
  okBtn.className    = isDanger ? 'btn-danger' : 'btn-ok';
  okBtn.onclick = () => { closeModal('m-confirm'); onConfirm(); };
  ovEl.classList.remove('hidden');
}
</script>


<!-- ── PDF Preview Modal ── -->
<div class="modal-ov hidden" id="m-pdf-preview" onclick="if(event.target===this)closeModal('m-pdf-preview')">
  <div class="modal" style="max-width:480px;">
    <div class="modal-title">📄 Preview Laporan PDF</div>
    <div id="pdf-preview-body" style="font-size:13px;color:var(--t2);line-height:1.7;"></div>
    <div class="modal-acts" style="margin-top:16px;">
      <button class="btn-c" onclick="closeModal('m-pdf-preview')">Batal</button>
      <button class="rpt-export-btn rpt-export-btn-sec" onclick="closeModal('m-pdf-preview');openModal('m-rpt-cfg')" style="margin-right:4px;">
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none"><path d="M6.5 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4z" stroke="currentColor" stroke-width="1.2"/><path d="M11 6.5a4.5 4.5 0 0 0-.1-.8l1.3-1-1.2-2-1.6.5a4.5 4.5 0 0 0-1.4-.8L7.7 1H5.3l-.3 1.4c-.5.2-1 .5-1.4.8l-1.6-.5-1.2 2 1.3 1a4.5 4.5 0 0 0 0 1.6l-1.3 1 1.2 2 1.6-.5c.4.3.9.6 1.4.8L5.3 12h2.4l.3-1.4c.5-.2 1-.5 1.4-.8l1.6.5 1.2-2-1.3-1c0-.3.1-.5.1-.8z" stroke="currentColor" stroke-width="1.2"/></svg>
        Konfigurasi Dulu
      </button>
      <button class="btn-ok" onclick="closeModal('m-pdf-preview');exportPDF()">
        <svg width="13" height="13" viewBox="0 0 13 13" fill="none" style="margin-right:4px;"><path d="M6.5 1v8M3.5 6.5l3 3 3-3M1.5 11h10" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Download PDF
      </button>
    </div>
  </div>
</div>

<script>
function showPDFPreview(){
  const period = typeof reportPeriod!=='undefined'&&reportPeriod==='month'
    ? (MN[reportMonth]||'')+' '+(reportYear||'')
    : 'Semua Waktu';
  const pltLbl = typeof reportPlt!=='undefined'&&reportPlt==='all'
    ? 'Semua Platform'
    : (allPlatforms().find(p=>p.id===reportPlt)?.emoji||'')
      +' '+(allPlatforms().find(p=>p.id===reportPlt)?.name||reportPlt||'');
  const allP = typeof getReportStats==='function'
    ? getReportStats(reportPlt,reportYear,reportMonth,reportPeriod)
    : {posts:0,reach:0,engagement:0,avgER:0};
  const body = document.getElementById('pdf-preview-body');
  if(body){
    body.innerHTML = `
      <div style="background:var(--surf2);border:1px solid var(--b);border-radius:10px;padding:14px 16px;margin-bottom:12px;">
        <div style="font-size:11px;font-weight:700;color:var(--t3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px;">Laporan yang akan di-export</div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
          <div><div style="font-size:10px;color:var(--t3);">Periode</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${period}</div></div>
          <div><div style="font-size:10px;color:var(--t3);">Platform</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${pltLbl}</div></div>
          <div><div style="font-size:10px;color:var(--t3);">Total Post</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${allP.posts||0} post</div></div>
          <div><div style="font-size:10px;color:var(--t3);">Total Reach</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${typeof fmt==='function'?fmt(allP.reach||0):(allP.reach||0)}</div></div>
          <div><div style="font-size:10px;color:var(--t3);">Avg ER</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${typeof erStr==='function'?erStr(allP.avgER||0):(allP.avgER||0)+'%'}</div></div>
          <div><div style="font-size:10px;color:var(--t3);">Engagement</div><div style="font-size:13px;font-weight:700;color:var(--t);">\${typeof fmt==='function'?fmt(allP.engagement||0):(allP.engagement||0)}</div></div>
        </div>
      </div>
      <div style="font-size:11px;color:var(--t3);padding:0 2px;">
        Format: PDF A4 · Resolusi tinggi · Nama file: <code style="font-size:10px;background:var(--surf2);padding:1px 5px;border-radius:4px;">creatorlytics_laporan_\${period.replace(/ /g,'_').toLowerCase()}.pdf</code>
      </div>`;
  }
  document.getElementById('m-pdf-preview')?.classList.remove('hidden');
}

/* ════════════════════════════════════════
   SETTINGS PANEL
   ════════════════════════════════════════ */
function openSettings() {
  const el = document.getElementById('m-settings');
  if (!el) return;
  el.classList.remove('hidden');

  // Populate fields
  document.getElementById('st-email').value         = userProfile.email || '';
  document.getElementById('st-displayname').value   = userProfile.displayName || '';
  document.getElementById('st-ermode').value         = sel.erMode || 'impression';

  // Theme toggle
  const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
  const tt = document.getElementById('st-theme-toggle');
  if (tt) { tt.checked = isDark; }
  const tl = document.getElementById('st-theme-label');
  if (tl) tl.textContent = isDark ? 'Dark mode aktif' : 'Light mode aktif';

  // Niche select — build from NICHE_BENCHMARKS
  const ns = document.getElementById('st-niche');
  if (ns) {
    ns.innerHTML = Object.entries(NICHE_BENCHMARKS)
      .map(([id, n]) => `<option value="${id}" ${userNiche===id?'selected':''}>${n.label}</option>`)
      .join('');
    ns.onchange = () => {
      userNiche = ns.value;
      destroyCharts();
      if (view === 'analytics') renderAnalytics();
    };
  }

  // Notif toggle
  const nt = document.getElementById('st-notif-toggle');
  if (nt) nt.checked = userProfile.reminderEnabled !== false;
  _updateNotifStatus();

  // Storage info
  _updateStorageInfo();
}

function closeSettings() {
  document.getElementById('m-settings')?.classList.add('hidden');
}

function saveSettings() {
  userProfile.email         = (document.getElementById('st-email')?.value || '').trim();
  userProfile.displayName   = (document.getElementById('st-displayname')?.value || '').trim();
  sel.erMode                = document.getElementById('st-ermode')?.value || sel.erMode;
  userNiche                 = document.getElementById('st-niche')?.value || userNiche;
  scheduleSave(200);
  updateWorkspaceName();
  // Refresh current view if needed
  if (view === 'dash')      renderDashboard();
  if (view === 'analytics') { destroyCharts(); renderAnalytics(); }
  closeSettings();
  _qcToast('✅ Pengaturan disimpan!');
}

function toggleThemeFromSettings(isDark) {
  const next = isDark ? 'dark' : 'light';
  document.documentElement.setAttribute('data-theme', next);
  try { localStorage.setItem('sm-theme', next); } catch(e) {}
  const lbl = document.getElementById('st-theme-label');
  if (lbl) lbl.textContent = isDark ? 'Dark mode aktif' : 'Light mode aktif';
  destroyCharts();
  if (view === 'analytics') renderAnalytics();
  if (view === 'dash') renderDashboard();
}

function toggleReminderSetting(enabled) {
  userProfile.reminderEnabled = enabled;
  if (enabled && 'Notification' in window && Notification.permission === 'default') {
    Notification.requestPermission().then(p => {
      notifPermission = p;
      _updateNotifStatus();
    });
  } else {
    _updateNotifStatus();
  }
}

function _updateNotifStatus() {
  const el = document.getElementById('st-notif-status');
  if (!el) return;
  const enabled = userProfile.reminderEnabled !== false;
  if (!('Notification' in window)) {
    el.textContent = '⚠️ Browser tidak mendukung notifikasi.'; return;
  }
  const perm = Notification.permission;
  if (!enabled)                el.textContent = '🔕 Reminder dinonaktifkan.';
  else if (perm === 'granted') el.textContent = '✅ Notifikasi aktif — kamu akan diingatkan jika tidak posting 3 hari berturut-turut.';
  else if (perm === 'denied')  el.textContent = '❌ Notifikasi diblokir browser. Ubah di pengaturan browser.';
  else                         el.textContent = '⏳ Izin belum diberikan. Aktifkan toggle untuk request izin.';
}

function _updateStorageInfo() {
  const el = document.getElementById('st-storage-info');
  if (!el) return;
  try {
    const raw = localStorage.getItem('smanalytics:v7') || '';
    const kb = (raw.length * 2 / 1024).toFixed(1);
    const allP = getAllPosts();
    el.textContent = `~${kb} KB digunakan · ${allP.length} post · ${accounts.length} akun · ${competitors.length} kompetitor`;
  } catch(e) { el.textContent = 'Tidak dapat membaca info storage.'; }
}

/* ── Export / Import JSON Backup ── */
function exportJSONBackup() {
  try {
    const raw = localStorage.getItem('smanalytics:v7');
    if (!raw) { _qcToast('⚠️ Tidak ada data untuk di-export.'); return; }
    const now = new Date();
    const fname = `creatorlytics_backup_${now.getFullYear()}${String(now.getMonth()+1).padStart(2,'0')}${String(now.getDate()).padStart(2,'0')}.json`;
    const blob = new Blob([raw], { type: 'application/json' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = fname;
    a.click();
    _qcToast(`✅ Backup disimpan: ${fname}`);
  } catch(e) { _qcToast('❌ Gagal export: ' + e.message); }
}

function importJSONBackup(input) {
  const file = input?.files?.[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = e => {
    showConfirm(
      'Import Backup?',
      'Semua data saat ini akan digantikan dengan data dari file backup. Lanjutkan?',
      () => {
        try {
          const parsed = JSON.parse(e.target.result);
          // Validate basic structure
          if (!parsed.months && !parsed.accounts) throw new Error('File tidak valid');
          localStorage.setItem('smanalytics:v7', JSON.stringify(parsed));
          _qcToast('✅ Import berhasil! Memuat ulang...');
          setTimeout(() => location.reload(), 1200);
        } catch(err) {
          _qcToast('❌ Import gagal: ' + err.message);
        }
      },
      true
    );
  };
  reader.readAsText(file);
  input.value = ''; // reset so same file can be re-selected
}

/* ── Reset All Data ── */
function confirmResetAllData() {
  showConfirm(
    '⚠️ Reset Semua Data?',
    'Ini akan menghapus seluruh post, akun, goals, pilar, kompetitor, dan pengaturan secara permanen. Tidak bisa dibatalkan.',
    () => {
      try {
        localStorage.removeItem('smanalytics:v7');
        localStorage.removeItem('sm-theme');
        localStorage.removeItem('smanalytics:v7-meta');
        _qcToast('🗑 Semua data dihapus. Memuat ulang...');
        setTimeout(() => location.reload(), 1200);
      } catch(e) {
        _qcToast('❌ Gagal reset: ' + e.message);
      }
    },
    true
  );
}

</script>

<!-- ── SETTINGS PANEL ── -->
<div class="modal-ov hidden" id="m-settings" onclick="if(event.target===this)closeSettings()">
  <div class="settings-panel" onclick="event.stopPropagation()">
    <div class="settings-header">
      <div style="display:flex;align-items:center;gap:10px;">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M9 11.5a2.5 2.5 0 1 0 0-5 2.5 2.5 0 0 0 0 5z" stroke="var(--accent)" stroke-width="1.5"/><path d="M14.8 9a5.9 5.9 0 0 0-.1-1l1.7-1.3-1.6-2.8-2 .7a6 6 0 0 0-1.8-1.1L10.5 2h-3L7 3.5A6 6 0 0 0 5.2 4.6l-2-.7L1.6 6.7 3.3 8A6 6 0 0 0 3.3 10l-1.7 1.3 1.6 2.8 2-.7c.5.4 1.1.8 1.8 1.1l.5 1.5h3l.5-1.5c.7-.3 1.3-.7 1.8-1.1l2 .7 1.6-2.8L14.8 10c0-.3.1-.6.1-1z" stroke="var(--accent)" stroke-width="1.5"/></svg>
        <div class="settings-header-title">Pengaturan</div>
      </div>
      <button class="settings-close" onclick="closeSettings()">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M2 2l10 10M12 2L2 12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
      </button>
    </div>

    <div class="settings-body" id="settings-body">

      <!-- PROFIL & WORKSPACE -->
      <div class="settings-section">
        <div class="settings-section-label">👤 Profil &amp; Workspace</div>
        <div class="settings-row" style="flex-direction:column;align-items:stretch;gap:0;">
          <div class="settings-row-label">Email Google</div>
          <div class="settings-row-sub">Digunakan sebagai identitas workspace. Tidak ada autentikasi.</div>
          <input class="settings-inp" id="st-email" type="email" placeholder="contoh@gmail.com" oninput="userProfile.email=this.value">
        </div>
        <div class="settings-row" style="flex-direction:column;align-items:stretch;gap:0;">
          <div class="settings-row-label">Nama Display</div>
          <div class="settings-row-sub">Ditampilkan di sidebar dan laporan PDF.</div>
          <input class="settings-inp" id="st-displayname" type="text" placeholder="Contoh: Kak Budi" maxlength="40" oninput="userProfile.displayName=this.value">
        </div>
        <div class="settings-row" style="flex-direction:column;align-items:stretch;gap:0;">
          <div class="settings-row-label">Niche Konten</div>
          <div class="settings-row-sub">Dipakai sebagai tolok ukur benchmark di Analytics.</div>
          <select class="settings-select" id="st-niche" onchange="userNiche=this.value;destroyCharts();if(view==='analytics')renderAnalytics();"></select>
        </div>
      </div>

      <div class="settings-divider"></div>

      <!-- PREFERENSI -->
      <div class="settings-section">
        <div class="settings-section-label">⚙️ Preferensi</div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Tampilan</div>
            <div class="settings-row-sub" id="st-theme-label">Dark mode aktif</div>
          </div>
          <label class="settings-toggle">
            <input type="checkbox" id="st-theme-toggle" onchange="toggleThemeFromSettings(this.checked)">
            <div class="settings-toggle-track"></div>
          </label>
        </div>
        <div class="settings-row" style="flex-direction:column;align-items:stretch;gap:0;">
          <div class="settings-row-label">Cara Hitung ER</div>
          <div class="settings-row-sub">Dasar kalkulasi Engagement Rate di seluruh app.</div>
          <select class="settings-select" id="st-ermode" onchange="sel.erMode=this.value;if(view==='analytics')renderAnalytics();if(view==='dash')renderDashboard();">
            <option value="impression">Berbasis Impressi</option>
            <option value="reach">Berbasis Reach</option>
            <option value="followers">Berbasis Followers</option>
          </select>
        </div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Bahasa</div>
            <div class="settings-row-sub">Saat ini hanya Bahasa Indonesia.</div>
          </div>
          <span style="font-size:12px;font-weight:700;color:var(--t3);background:var(--bg-hover);padding:4px 10px;border-radius:8px;">🇮🇩 ID</span>
        </div>
      </div>

      <div class="settings-divider"></div>

      <!-- NOTIFIKASI -->
      <div class="settings-section">
        <div class="settings-section-label">🔔 Notifikasi</div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Reminder Posting</div>
            <div class="settings-row-sub">Notifikasi jika tidak ada post dalam 3 hari.</div>
          </div>
          <label class="settings-toggle">
            <input type="checkbox" id="st-notif-toggle" onchange="toggleReminderSetting(this.checked)">
            <div class="settings-toggle-track"></div>
          </label>
        </div>
        <div id="st-notif-status" style="font-size:11px;padding:8px 12px;border-radius:8px;margin-top:4px;background:var(--bg-hover);color:var(--t3);"></div>
      </div>

      <div class="settings-divider"></div>

      <!-- DATA & PRIVASI -->
      <div class="settings-section">
        <div class="settings-section-label">🗄️ Data &amp; Privasi</div>
        <div class="settings-btn-row" style="margin-bottom:8px;">
          <button class="settings-action-btn" onclick="exportJSONBackup()">⬇️ Export JSON Backup</button>
          <button class="settings-action-btn" onclick="document.getElementById('st-json-import').click()">⬆️ Import JSON Backup</button>
          <input type="file" id="st-json-import" accept=".json" style="display:none" onchange="importJSONBackup(this)">
        </div>
        <div class="settings-btn-row">
          <button class="settings-action-btn" onclick="exportAllPostsCSV()">📤 Export Semua Post (CSV)</button>
        </div>
        <div style="margin-top:12px;">
          <button class="settings-action-btn danger" style="width:100%;" onclick="confirmResetAllData()">🗑️ Reset Semua Data</button>
          <div style="font-size:10px;color:var(--t4);margin-top:6px;line-height:1.5;">Reset akan menghapus seluruh post, akun, goals, dan kompetitor. Tidak bisa dibatalkan.</div>
        </div>
      </div>

      <div class="settings-divider"></div>

      <!-- TENTANG -->
      <div class="settings-section">
        <div class="settings-section-label">ℹ️ Tentang</div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Creatorlytics</div>
            <div class="settings-row-sub">Analytics dashboard untuk kreator konten Indonesia.</div>
          </div>
          <span style="font-size:11px;font-weight:800;color:var(--accent);background:var(--accent-dim);padding:4px 10px;border-radius:8px;">v7.0</span>
        </div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Penyimpanan</div>
            <div class="settings-row-sub" id="st-storage-info">Menghitung...</div>
          </div>
        </div>
        <div class="settings-row">
          <div class="settings-row-left">
            <div class="settings-row-label">Mode</div>
            <div class="settings-row-sub">Local-only · Auth belum aktif · Data di perangkat ini</div>
          </div>
          <span style="font-size:11px;font-weight:700;color:var(--amber);background:rgba(251,191,36,.1);padding:4px 10px;border-radius:8px;">Dev</span>
        </div>
      </div>

    </div><!-- /settings-body -->

    <div class="settings-save-row">
      <button class="btn-ok" style="width:100%;" onclick="saveSettings()">Simpan Pengaturan</button>
    </div>
  </div>
</div>

</body>
</html>
